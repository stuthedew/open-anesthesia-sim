---
id: PL-ZPDM
title: vcs.tags, vcs.changed_items and vcs.default_base answer with a bare collection or string, so a git that does not answer is indistinguishable from an empty repository and the three cannot decline at all
status: untriaged
feature: git-silence-channel
added: 2026-09-19
---

**Problem.** vcs.tags, vcs.changed_items and vcs.default_base answer with a bare collection or string, so a git that does not answer is indistinguishable from an empty repository and the three cannot decline at all
