## v0.2.7 - 2026-08-30

### defect

- PL-0999 docket verify defaults to a local main that a fresh checkout leaves stale — `8c72f1b`
- PL-36SC The release offer and 'what next' ranking fire in topic-specific discussions, where they read as noise — `3078bdb`
- PL-5GFK The gate-progress rule injects simulator work into every process-work session, which PL-36SC did not reach — `948ca35`
- PL-5Q71 An action the owner must take outside the repository is handed back as intent rather than as its steps — `e1ccbd4`
- PL-9PMV Two readings of the gate's re-entry rule disagree, and a real finding now sits between them — `47b1077`
- PL-FY68 A session pushes its branch before the discussion has settled, leaving stale remote branches — `3078bdb`
- PL-HZC2 The closing action block annotates order instead of carrying it, so a numbered list can disagree with itself — `e1ccbd4`
- PL-ZRFC '`make docket` is a phony target with no recipe, so it silently succeeds without validating the store' — `62ba91a`

### docs

- PL-R5GN Adopt the nine instruction-writing rules, scoped for an agent that can read and edit files itself — `487d7d5`

### safety

- PL-629Z Decide whether the cardiac-output slider should reach zero — `a0b3ab1`
- PL-74TX Re-decide the two-decimal readout against the widened splitting-error measurement — `f252aa4`
- PL-SLHS Bound the splitting error across setting changes, not one held operating point — `a0ac750`
- PL-VP7N Refuse a simulation step outside the operator split's applicability domain — `82813bf`

### session-cost

- PL-F58L The session digest surfaces the queue but never the roadmap, so timeline placement has no alarm clock — `c6956b1`
