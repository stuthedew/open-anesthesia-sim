## v0.4.7 - 2026-09-07

### defect

- PL-GVXP Separate the six chart traces by more than colour, and meet contrast minima — #422
- PL-KX9N bin/docket record writes pr numbers on a shallow clone that docket check declines to verify on the same clone, so it silently records wrong provenance — #419

### infra

- PL-QPJZ Add `bin/docket trend` so the workflow-to-product balance over time is a command rather than a session's derivation — #424

### planning

- PL-5VR1 "Cut the v0.4.6 release: provenance read at the source" — #411
- PL-LYX2 Triage the thirty-one captures open on 2026-09-06 — #416

### refactor

- PL-BD94 venous_blood_volume_l names a mixing volume but reads as the physiologic venous blood volume — #423

### safety

- PL-Y5WR The 30-day scenario cap is enforced nowhere as an explicit halt, and dropping PL-011 removes the only item that required it — #421
- PL-ZVS7 docs/MODEL.md's control-resolution tolerance table is a measured safety claim with no regression test; re-measuring it 2026-09-06 reproduced it, but nothing would have caught a drift — #420

### science

- PL-3YZW venous_blood_volume_l is 1.0 but the Workbook table it cites has no such value - its Blood row reads 5.00 L — #418
- PL-8ZJQ Davis and Mapleson 1981 gives a published, quantified blood-pool structure, which is the source PL-3YZW's 1.0 L venous pool has never had — #423
