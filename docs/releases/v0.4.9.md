## v0.4.9 - 2026-09-07

### defect

- PL-BKDP "A v0.4.8 tag exists on the commit that closed PL-0GTC and PL-SR8F, but no release was cut there: pyproject.toml still reads 0.4.7, there is no docs/releases/v0.4.8.md, and doc_check fails on the missing version-table row" — #440
- PL-20CQ "docket check runs every open item's verify: command, so a verify: that invokes docket check recurses without bound"
- PL-4WQS The docket summary and session digest label a count 'open' that excludes untriaged items, understating the queue by exactly the number beside it — #445
- PL-F5GN The oracle's independence check walks only ImportFrom, so a plain import bypasses it
- PL-GZP6 Two of docs/MODEL.md's required tests are not implemented — #444
- PL-X94L CITATION_RE's quoted section name cannot span a source line, so every citation whose section title wraps is invisible to check_citations in the documents it already reads
- PL-0ZGK main's quality run has failed on its last three merges and nothing surfaces it: the whole-store verify replay runs only on push to main, so a green pull request turns main red after it lands — #438
- PL-X2XX doc_check's citation check reads neither docs/items/*.md nor source docstrings, so nothing holds the queue's or the code's citations to the docs they name

### docs

- PL-8B1K Two citations into docs/WORKING_NOTES.md name sections that no longer exist

### infra

- PL-1JDD Make the source tier machine-readable so doc_check can decide it — #446

### planning

- PL-2B7B Triage the seven captures open on the evening of 2026-09-07 — #439

### refactor

- PL-X204 require_valid_agent_accounting() reads nothing from self, so it will judge a result from another accounting period — #442

### science

- PL-FJGY docs/MODEL.md says tier 2 may never be the authority for a stored value, and venous_pool_volume_l adopts a tier-2 source — #447

### test

- PL-LKCN Pin PL-010's no-rebuild property with an allocation test on the render tick — #449
