## v0.3.9 - 2026-09-04

### defect

- PL-JHJ3 The landing split compares blob identity, so a squash that merged content reads as unlanded and two sessions writing one record line read as landed — #317
- PL-3D2M A commit pushed after its pull request merged lands nowhere, and nothing outside the item store notices — #313

### infra

- PL-D4MZ Nothing reserves work at the moment it is recommended, so two sessions handed the same closing recommendation both start it — #318

### perf

- PL-D9WD Build an M4 aggregate cache on a fixed dyadic grid — #311

### safety

- PL-VM40 Derive simulated time from a step count and never catch up to the wall clock — #316

### ux

- PL-CG7J Add per-trace show/hide controls to the chart, as Gas Man has — #315

### also inside this tag's span

`git describe --contains` resolves these commits to v0.3.9, but this cut did
not stamp them, so each is described in the release named beside it.
`ROADMAP.md` § "Tags" is why the two records differ.

- PL-CC23 - #319 - described in v0.4.0
