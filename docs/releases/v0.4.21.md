## v0.4.21 - 2026-09-13

### defect

- PL-F4JS bin/docket triage omits the **Decision needed.** requirement from the rules it prints, so triaging an item to needs-decision fails check after the edit — #542
- PL-HKF4 doc_check's tag advisory prints `<merge commit>`, which a shell reads as redirection, so pasting it fails with "no such file or directory: merge" instead of tagging — #542
- PL-K2C8 The docket skill's stranded-branch recovery deletes only the local remote-tracking ref, which the next non-prune fetch restores while the branch still exists on the remote — #542
- PL-KD98 bin/docket wave reports 'implement' for a milestone whose Required scope is complete, because the beat reads only the frozen gate list and never the scope's item ids — #544
- PL-NB4D '`docket verify` reports "1 commit(s)" when the work it just checked is entirely uncommitted' — #542
- PL-YNCW docket new --touches before the title swallows it, because nargs='*' is greedy and the error names the title instead — #542
- PL-NBCS docket next reads an exclusion written inside a Required scope bullet as membership, so PL-B9PY is ranked in scope for v0.4.0 when ROADMAP.md sends it to Gate 1 — #547
- PL-BZCM docket status shows no plan placement, so the feature survey a session leads with cannot say which work the current step includes — #544
- PL-GVNS docket.toml's workflow_paths lists docs/worker.md but not docs/maintainer.md, so an apparatus item whose record lands in the maintainer doc ranks in the product lane — #544
- PL-VFVW docket feature draws a dropped item with the same empty checkbox as an open one, so counting the boxes disagrees with the 'N left' figure printed beside them — #542
- PL-VP40 docket verify reads per-commit patches, so a line a branch added and then removed still reads as added — #542

### docs

- PL-BLHV Record the intended-use statement and the IEC 62304 safety classification in docs/MODEL.md — #547
- PL-FDBK docs/MODEL.md has no hazard table, so every mitigation is argued forward and none is checked backward — #547
- PL-8GJ6 The agent files record what Anesthesiology 69:A615 contains as unknown, and the authors' own reference list now confirms it is the 1988 abstract of the 1989 human-tissues paper — #539

### infra

- PL-YPWT PL-4QCJ names PL-Z3V5 as a prerequisite in prose without declaring the edge in blocked-by, so docket next offers it as startable work whose first step is another item — #546
- PL-ZGK2 Recover the nine items stranded on five abandoned branches, and leave the four on a live session's branch — #540
- PL-H1JD An outstanding needs_action is lost when its session is archived, so a request nobody acted on drops out of the closing-block read — #545

### planning

- PL-D89D "Cut v0.4.20: the release where four stored values' sources were read back against the publications" — #540

### science

- PL-B9K7 Yasuda, Targ and Eger 1989's tissue-solubility full text is the one document that could confirm or refute the Workbook's attribution of the twelve coefficients, and neither PubMed Central nor the reference corpus holds it — #539
- PL-FN5F Decide whether to adopt Yasuda, Targ and Eger 1989 as the authority for the nine tissue:gas coefficients, and whether to store its figures rather than Gas Man's rounding of them — #539
- PL-LS3H docs/MODEL.md models the breathing circuit as a non-absorbing volume, and a held measurement says its components absorb substantially and unequally by agent — #539

### also inside this tag's span

`git describe --contains` resolves these commits to v0.4.21, but this cut did
not stamp them, so each is described in the release named beside it.
`ROADMAP.md` § "Tags" is why the two records differ.

- PL-JYR4 - #548 - described in v0.4.22
