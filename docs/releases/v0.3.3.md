## v0.3.3 - 2026-09-03

### defect

- PL-N5WZ The merge-time pr: write cannot push to main, because required status checks can never report on a GITHUB_TOKEN push - write it locally instead, riding the session's next commit — #265
- PL-WTQR Write a closed item's pr: at merge time from the pull_request payload, so the backfill advisory and its dedicated commit stop existing — #261

### infra

- PL-79N5 pr_title_check.py is the one tools/ script CI never runs at the declared floor — #259

### refactor

- PL-WB0X 'Split simulation_view.py: formatting and chart series are not the view''s job' — #263

### test

- PL-22Z3 Gate coverage at 100% on core/, so today's saturation cannot silently regress — #264
