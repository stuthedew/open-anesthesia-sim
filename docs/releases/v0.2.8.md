## v0.2.8 - 2026-09-01

### defect

- PL-0RS6 docket next will still lead with out-of-scope work after PL-1TPM, because marking does not reorder — #92
- PL-1CYR Nothing re-checks the branch against main once a session is already underway — #125
- PL-1TPM docket next ranks work the current milestone excludes, with no sign that it does — #92
- PL-3CBS docket has no way to notice that an open item's work already landed on main — #128
- PL-5KR2 A session handed an item by name never reads the in-flight answer, because docket next is the only surface that applies it — #152
- PL-5QKT A verify command whose -k selects no test exits 5, so it reads as discriminating forever — #146
- PL-768D The tools/ import guard reads the running interpreter's stdlib, not the floor's — #166
- PL-794W A closing-block line putting a decision to the project owner does not disclose that its answer will produce a commit, so it can be ordered after a line that merges the branch the commit belongs in — #161
- PL-8HJ2 make release stops mid-way on the ROADMAP table it does not write, so every release ends in a red test — #100
- PL-921W The formatter target applies to tools/, which must run under bare python3, and nothing guards it the way subprojects/docket/ is guarded — #141
- PL-99Y4 A closure with no pr: turns main red one merge later, because CI's shallow clone cannot recover the number
- PL-BKQW The resident-growth check nets growth against shrinkage, so resident text trimmed to pay for an addition is invisible in the total
- PL-CPSY A squash-merged branch whose ref survives reports its items in flight forever — #121
- PL-D2GW The session digest offers a release whose version names a milestone whose gate is still open — #134
- PL-DL1X docket release stamps every item file before it bumps the version, so a failure in the bump leaves the store recording a release that did not happen — #156
- PL-F5HB The project runs a Python 3.14 release candidate, not 3.14 final — #96
- PL-FBCC The session-start hook prints fabricated ahead/behind counts when no merge base resolves — #109
- PL-HDY6 The scope reader counts any id a milestone section names, so docket next marks an id the section names to exclude as in scope — #131
- PL-J295 The release-train check reads a tag missing from a shallow clone as a release that was never tagged — #111
- PL-J5NN Nothing evaluates warn_unused_ignores on the test trees, so a new inert type: ignore is invisible again the moment PL-CMCB's audit is taken
- PL-KWC1 docket flight reads ids from branch names only, so every harness-named branch is invisible — #113
- PL-MGNC A readable merge-base does not make the in-flight commit walk complete, so a shallow clone reports merged items as in flight — #122
- PL-P5S0 Closing an item needs a pull request number that does not exist until the pull request does, so the closure is always a second commit a fast merge can strand — #140
- PL-Q2BJ The session-start digest still leads with work the current step excludes — #144
- PL-Q9NJ Run the branch re-check at the moment a session starts editing, not only when it asks — #127
- PL-QDH7 tools/ promises standard-library-only imports and nothing guards it, the way the parse floor now is — #163
- PL-QS72 Every closure needs a second commit for its pr:, because the number does not exist when CLAUDE.md requires the closure to be committed - derive it from the squash-merge subject instead — #151
- PL-S1P1 The in-flight ids reach docket next without the refs that went unread — #123
- PL-STNV Retire the review-verification harness and capture its last live finding — #94
- PL-TH9V Inline math in docs/MODEL.md renders as literal parentheses on GitHub — #136
- PL-WFJ9 render.py annotates three parameters `object` and pays for it with 12 `type: ignore[attr-defined]` — #104
- PL-XCYB A provenance check must refuse to answer in a shallow checkout, not answer wrongly — #90
- PL-YSXF A ref named as unread loses the item id its own branch name carries, which needs no history to read — #124
- PL-3576 docket check's offered-advisory reads a flight answer that may be partial and says nothing about it
- PL-4CW7 The web session container ships uv 0.8.17, below the project's required-version floor — #105
- PL-H8MQ ROADMAP.md states the v0.2.8 entry count in four places and nothing holds them to the list — #142
- PL-HXYY Gate membership is recorded by milestone: on some open entries and not others — #158
- PL-JT6L The resident growth advisory says "grew 1 lines" because it formats the count directly instead of using _plural
- PL-MZH2 "PL-D9H6's verify: command is quoted so the shell cannot run it, and exits 127 every time" — #138

### docs

- PL-DGM4 The instruction-writing rules and the docket skill prescribe opposite openings for a 'what next' reply — #110
- PL-DVPZ The recommended -k verify shape asserts nothing until the work lands, so 19 of 34 open items carry a command that proves nothing today — #153
- PL-M5FK ROADMAP.md's tag statements go stale on every release and no check reads them — #100
- PL-N7R9 The pull request waits to be asked for, after the owner has already agreed to the work — #111
- PL-QDN7 A freeze closes new scope, not the completeness of a fix, and the finishing item is paired — #88
- PL-Z8SV Pushing no longer waits for the owner - only the pull request does — #89

### infra

- PL-020 Widen the type-check gate past `src`, and ship a `py.typed` marker — #102
- PL-1Q3S A merged PR leaves a stale tracking ref, so the stop hook demands a push that would recreate a dead branch — #132
- PL-5YK8 The `verify:` advisory nags about 39 grandfathered items, so it can never reach zero — #101
- PL-J3ZK Tag releases so a commit can be mapped to the version it shipped in — #82
- PL-JWXF Scope the selects-no-test advisory to what next is about to offer, as the unspecified-command advisory already is — #148
- PL-K6QR Record what this project is at the top of CLAUDE.md, so the workflow-versus-simulator trade-off has its reason resident
- PL-PF8H A never-pushed branch's seeded origin/ ref pins the stop hook's comparison to the session-start main tip, so merely fetching main reports unpushed commits — #162
- PL-S4M2 Switch main to squash-merge, so each item lands as one commit — #106
- PL-W5LG Hold CI config to the same path checks as the documentation — #106
- PL-ZQ9C Record an item's pull request, so provenance survives squash-merge — #90
- PL-019F The 'what should we work on next' rule answers at feature altitude, one level below the roadmap step that should decide it — #110
- PL-69J3 Clear the inert `noqa` directives and record why the deliberate suppressions exist — #105
- PL-CMCB The ten type: ignore directives all sit in tests/, outside the mypy gate, so warn_unused_ignores never evaluates them — #168
- PL-D9H6 quality.yml runs the full suite twice on every push to a branch with an open pull request — #138
- PL-NSN9 A milestone that gates itself reports 'implement' when its gate clears, not 'release' — #164
- PL-RWZV The brief check tests for a literal marker, so an elaborated heading fails and an empty one passes
- PL-ZN0N Enable ruff RUF100 so inert noqa directives fail the build — #105

### other

- PL-0TRS Type-check docket's own source: mypy runs on src only, so subprojects/docket/src is unchecked by make check and CI — #102

### planning

- PL-51T3 Scope v0.2.8 - the workflow works - and freeze its gate — #86
- PL-6GS0 Decide whether the coupled step should be an exact matrix exponential rather than an operator split — #94

### session-cost

- PL-20ZR The 'workflow before features' ordering is re-explained every session because nothing records it — #81
- PL-64LS Detect items stranded on an unmerged branch — #108
- PL-7838 Record the compounding-friction re-entry rule's own first miss, so the pattern is recognizable — #91
- PL-H7XN CLAUDE.md keeps every rule resident whether or not a session needs it, which reduces adherence to the ones it does — #116
- PL-HVX9 "The scaffolding standard says 'staying small', which reads as a size cap rather than a bar on bloat" — #143
