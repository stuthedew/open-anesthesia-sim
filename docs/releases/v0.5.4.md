## v0.5.4 - 2026-09-22

### defect

- PL-DK8Y The session-start digest prints 'The plan's numbering is behind the project' for every Wave.stale statement, which is false for the section-that-no-timeline-row-bears one — #894
- PL-SPN6 Three compartments now raise the same 'partial_pressure_fraction must be between 0 and 1', so a refused step no longer says which one refused — #889
- PL-T441 The git reads behind a printed count resolve the store from config.items_dir rather than the tracked directory, so docket check --items pointed at a store elsewhere silently finds no closures — #899
- PL-WF3X cmd_check scopes the verify replay with config.items_dir rather than the store --items actually resolved to, so a queue outside docs/items has its diff taken against a directory it does not live in — #899
- PL-WG73 COMPARING_FORK_LOCK_TEXT tells a learner to Reset the case, which labels no control - and the Reset they are likeliest to press discards the branch and leaves the lock standing — #902
- PL-YZJD falsifies: can only ever fold for an item whose declaration reached the base before the working session started, so an untriaged item triaged and worked in one session structurally cannot use it - which is most of why it stands at 0 of 1,324 — #890
- PL-4L49 tools/contrast_check.py measures only declared pairs, so a control that declares no colour reads as covered when nothing measures it — #897
- PL-73P0 default_base falls back to the literal string main when no candidate ref resolves, so every comparison in vcs.py can be taken against a guessed base with nothing in the answer saying so — #896
- PL-KRZW Decide what the interface does under a dark system appearance, now that every colour it declares is a light-theme value — #898
- PL-ZPDM vcs.tags and vcs.changed_items answer with a bare frozenset, so a git that does not answer is indistinguishable from a repository with no tags and a branch that changed nothing — #892
- PL-P4XB test_verify.py's _commission helper commits unconditionally, so calling it with fields identical to the base store's copy dies inside subprocess with CalledProcessError rather than saying nothing changed — #893
- PL-Z891 doc_check's DECLINED_HEADING_RE matches only '### Declined to Gate', so a disposition the roadmap records under '### Sequenced past vX.Y.Z ...' or '### Deferred to vX.Y.Z ...' is not read and check_gate_dispositions reports its open debt items as owed — #891

### docs

- PL-036 Extend `doc_check.py` to the statements it currently cannot decide — #900
- PL-P669 Eleven tagged release spans cover a closing pull request their own notes never name, and nothing points a reader from the tag to where that work is described — #901

### housekeeping

- PL-CY4F Cut v0.5.3 from the eleven items finished since v0.5.2: the four gates that were reporting a result they had not established, with the simulator's stillness readable off one tree hash — #888

### infra

- PL-NJ9M bin/docket delegable cannot tell a brief that determines the fix from one that leaves the choice open, so a hand-off meant to need no reading still does — #895
