## v0.2.6 - 2026-08-30

### defect

- PL-674D '`docket release` bumps pyproject.toml but leaves uv.lock stale, breaking make check' — `e9fc842`
- PL-G049 A `verify:` command that has never been run is not a specification, and nothing currently requires running one — `4f4c8f4`
- PL-K79K The session-start digest tells every session to run `bin/docket triage`, which does not exist — `54bdb5b`
- PL-N2N1 docket release does not update ROADMAP.md's version table or baseline heading, which has now drifted twice — `ebefcc2`

### infra

- PL-9CNQ A `docket gate` command, so the frozen debt list is computed rather than transcribed — `0eaa17f`
- PL-6G8C A docket wave command, so the current beat of the rolling-wave cadence is computed rather than recalled — `bff11d0`
- PL-RZ9Q Make ROADMAP's timeline table machine-readable and enforce the row shape in doc_check — `11fc560`

### safety

- PL-042 Bound the splitting error across the settings envelope, not one point — `1180158`

### session-cost

- PL-037 Check the branch is current against `main` before starting an item — `d89df5e`
- PL-FVM5 `docket verify` re-runs the project's whole check once per item, so a six-item batch takes two minutes — `c25c91b`
