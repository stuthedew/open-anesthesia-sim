## v0.5.7 - 2026-09-23

### defect

- PL-9RFP verify.changed_paths discards _run's exit status and _run returns combined stdout+stderr, so any base that does not resolve makes git's three-line fatal message come back as three changed paths and the commission audit reports them as findings while missing the real ones — #933
- PL-B60Q ROADMAP.md's rule that a closed Declined-to-Gate entry carries the release that took it is stated only inside v0.5.0's own subsection, so the release mode never reads it and v0.6.0's first closed deferral will go unmarked exactly as PL-0VFF's three did — #937
- PL-KH3Q main is red on a65b440c: test_a_changed_file_list_git_does_not_answer_declines_the_replay, added by #933 (PL-9RFP), asserts git's 'index file corrupt' but check now prints #930's (PL-NGBM) '--no-git asks git nothing' sentence, because #933's CI ran on a base without #930 — #939
- PL-8YXJ A status or blocker change rewrites the front matter and leaves the brief's prose narrating the old state, and nothing reports it: the prose-dependency advisory skips closed prerequisites and nothing reads a brief's claims about its own status — #935
- PL-J6HP Gate facts - which entries a milestone clears itself, which are deferred, which closed entries carry the release that took them, how many a heading holds - live in ROADMAP.md prose that plan.py, roadmap.py and tools/doc_check.py each read on their own, so every new phrasing or fact arrives as a new item — #937
- PL-WNCT A session that ends without a pull request strands its captures on its branch and nothing at session end names them: ten recovery items were filed 09-19 to 09-21, against nine in 09-05 to 09-15 — #941
- PL-19T3 changed_items answers confidently that nothing changed when run outside a git repository, because git diff exits 1 there and _run_git reads exit 1 as an answer, where git tag --list exits 128 and declines — #934
- PL-1PBV Three tools/ scripts still collapse a failed git call to empty output, the convention PL-9RFP removed from verify: pr_title_check passes a head it cannot read as closing no item, generator_check attributes no item when its log fails, and branch_id_check's walk keeps the route PL-73P0 patched only for an unresolved base — #938
- PL-NGBM --no-git does not stop the git reads behind a printed count, so docket digest --no-git shells out to git despite the flag saying branch detection is off — #930

### docs

- PL-3T2Q _tracked returns the prefix '.' where --items names the repository root itself, which its docstring calls the empty-prefix case — #930
- PL-RWJD The convention that keeps an answered decision section readable - leave the question, write the dated answer underneath - is written down nowhere a triaging session reads, so the practice holding delegable's open-brief count at zero is an accident — #935
- PL-X4RX PL-SYG4's brief says it is left at needs-decision while its front matter reads ready, and nothing checks a brief's prose against its own status field — #935

### housekeeping

- PL-38HD Cut v0.5.6 from the twelve items finished since v0.5.5: the in-flight guards stop guessing who holds an item, and verify's assertion check reads parsed statements rather than lines, with nothing in the simulator moving — #931
- PL-SRBR Delete claude/recurrence-signal-feature-3hnynt once #794 is confirmed to hold the change in 52c6d698, the commit tools/left_behind_check.py reports as left behind by #793 — #941

### infra

- PL-R808 Build the exact left-behind check as a tools/ script comparing a branch tip against refs/pull/<n>/head, declining where those refs are not fetched — #932
- PL-XYQW Collapse the nine identical docket record advisories that fire on every healthy run — #940

### perf

- PL-M6FY cli.cmd_flight builds its own GitRunner instead of the invocation's, so flight loses the cat-file memo every other command shares — #930

### also inside this tag's span

`git describe --contains` resolves these commits to v0.5.7, but this cut did
not stamp them, so each is described in the release named beside it.
`ROADMAP.md` § "Tags" is why the two records differ.

- PL-BBT8 - #944 - described in v0.5.8
