## v0.5.10 - 2026-09-24

### defect

- PL-QFWF A live generator head blocked on its own build items leaves docket next's generator tier, and nothing ranks the blockers: PL-MB2W's first build item PL-3FYK dropped to an ordinary P2 among 149 the moment the design round blocked the head on it — #982
- PL-0TD9 bin/docket claim and yield write the claim, and start mode is rewritten around them — #987
- PL-3FYK Record who holds an item as a Claim trailer bound to the holder's own branch, read by one claims.holdings reader under a 7-day lease — #983
- PL-5MYR Generator identification reaches one level up and never clusters the heads: triage asks each item whether a head explains it, so one record read by several readers gets a head per reader, and both such generators found so far - PL-WD5Z's gate prose after three heads, and the claim family PL-MB2W names after four - surfaced only in a sweep the owner asked for — #985
- PL-NST2 claims.holdings reads status dispositions, the landed prefix, the item resource field and cut holds, and adapts to FlightReport — #988

### housekeeping

- PL-R498 Cut v0.5.9 from the 27 items finished since v0.5.8: gate dispositions move onto the item, main requires an up-to-date branch against merge skew, an item filed more than 14 days ago is re-confirmed before it is worked, and docs/MODEL.md is corrected on how the chart's percent axis is scaled, with nothing in src/ moving — #980

### other

- PL-SW2K Branches started between PL-3FYK's merge and PL-0TD9's write start mode's old empty start commit onto a tree already carrying claims.py, so claims.holdings reads them as claiming nothing; re-pointing CUTOVER_MARKER at a file PL-0TD9 creates would close the window — #987

### session-cost

- PL-038 Verify what a mid-session `CLAUDE.md` edit actually invalidates — #984
