## v0.3.7 - 2026-09-04

### defect

- PL-Q197 Running the app degrades to unresponsive after about a minute: sliders move but stop updating values — #297
- PL-0D4X A session prompted for one lane runs bare `docket next` and takes the other lane's item — #295
- PL-WW08 The stop hook picks its comparison point from a ref that merely resolves locally, so a merged branch's stale tracking ref makes it demand a push that would recreate a dead branch — #300

### docs

- PL-JX2T Decide what to do with origin/Review_articles: one unattributed commit ahead of main that no guard can see — #298
- PL-THVN PL-011's history sizing assumes six flat floats per sample, which PL-W3DD's per-substance mapping invalidates inside the same milestone — #292

### feature

- PL-DR1Z Record the control-input timeline and mark it on the chart — #301

### infra

- PL-5WFS docket next cannot see a brief's stated prerequisite, so it offers PL-F52R ahead of the PL-WB0X extraction that gates it — #292
- PL-CP74 Unfiled housekeeping work carries no item id, so every in-flight guard is structurally blind to it — #296
