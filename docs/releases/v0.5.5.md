## v0.5.5 - 2026-09-22

### defect

- PL-59WB chart_time_base.fit_to_run takes a parameter called run_length_s but is passed a case instant, which is the duration_s-was-not-a-duration defect one module over — #908
- PL-5B88 verify's suppression check misses @pytest.mark.skip and @pytest.mark.skipif, the two commonest ways a pytest test is disabled: @skip needs its @ against the name and pytest.skip needs its halves adjacent, so the check reports none while a disabled test sits in the diff — #910
- PL-YKSD check_tags verifies a release tag exists and never that it points at that release's own commit, so a tag pushed before the release merged reads as tagged to every check and lets bin/docket release cut the next version on a false premise — #909
- PL-FPY2 A wrapped mark row's continuation can be a whole clause naming a run, so it reads as a row of its own and binds to the wrong mark — #905
- PL-Q8RQ Four open items' verify commands rest on a bare 'pytest -k SUBSTRING', which any unrelated test name can satisfy: PL-S5YM turned main red exactly this way, and whether a command can discriminate at all is decidable enough for docket check to refuse it — #907
- PL-SZJ2 wave reports nothing for a released milestone row whose own frozen list still has open entries, which is PL-LN3T's case in the other subsection: a gate-only milestone released with its list open drops out of the report entirely — #906

### housekeeping

- PL-14QR Triage the 20 untriaged captures standing in the queue on 2026-09-22, leaving PL-54V0 to the branch already editing it — #904
- PL-4KSZ Cut v0.5.4 from the sixteen items finished since v0.5.3: the first release since the MVP that changes what a learner sees - a window wholly in its own colours, and refusals that name what refused and the control that ends them - with nothing computational moving — #903
- PL-Y4YG Groom the ten oldest open items for relevance on 2026-09-22: drop what the tree has overtaken, bring the rest up to date — #914

### infra

- PL-Q89J docket next --oldest: hand out owed work longest-waiting first, so low-priority items that newer work keeps outranking stop starving — #913

### planning

- PL-6Q9L Pause new workflow mechanisms and re-specifications of the generator rule while any open item carries generator: live, so the live generators are worked before more apparatus is added (project owner, 2026-09-22, ratified) — #916
- PL-G40Z Fix-now test 2 turns a no-behaviour repair outside the finder's touches into an item: eleven items filed 09-20 to 09-22 are repairs citation-drift.md says need none, because that licence loads only after the decision to file — #916
- PL-KVDK Scrutinize every new workflow-lane item for an unresolved generator or a cause the code does not recognize: a triage step, and a first pass over the 161 items filed 2026-09-20 to 09-22 — #915

### refactor

- PL-CZTR ResumePoint.elapsed_s is the fork instant on the case's axis and should be named fork_instant_s, now that the definition's own instants are instant_s — #911

### safety

- PL-1K9G Which grid column a trace answers a hover at is decided in two dimensions, so a purely vertical 2 px move can change the instant a value is labelled with — #912

### also inside this tag's span

`git describe --contains` resolves these commits to v0.5.5, but this cut did
not stamp them, so each is described in the release named beside it.
`ROADMAP.md` § "Tags" is why the two records differ.

- PL-0HPV - #920 - described in v0.5.6
- PL-WFFX - #921 - described in v0.5.6
