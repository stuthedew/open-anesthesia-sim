## v0.4.6 - 2026-09-06

### defect

- PL-GJDW contrast_check's requirement descriptions cite simulation_view line numbers, and all eight are wrong — #400
- PL-JBZK docket.toml's workflow_paths names three tools tests by path, so the other seven and every new one fall on the product side of the lane boundary
- PL-N2X4 Hold tools/contrast_check.py's line citations to the file, or drop them — #400
- PL-S5LB _number_closing walks git log without rename detection, so an item whose file was renamed after it closed recovers the renaming commit's pull request instead of its own — #401
- PL-GNN1 The contrast checker cannot express a requirement met by either of two channels

### infra

- PL-VRMK 27 of the gate's 112 open entries declare docs/MODEL.md, so a quarter of Gate 1 can only be worked one item at a time — #402

### planning

- PL-VR27 "Cut the v0.4.5 release: going public, finished" — #397

### safety

- PL-GYH2 Bound or document the two gas volumes, which no supported range covers
- PL-GJYL docs/MODEL.md claimed the exact propagator solves the governing equations at any positive gas volumes, which measurement contradicts below 1e-9 L

### science

- PL-4GN8 The mass-balance release gate's absolute tolerance tracks whichever dial its test happens to run at
- PL-XTMB The Gas Man Workbook citation still rests on a bare vendor URL with no edition, section or DOI
- PL-6Q8N The reference adult's eleven physiologic parameters have no primary source at all — #399
