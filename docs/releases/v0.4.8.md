## v0.4.8 - 2026-09-07

### defect

- PL-1VFK Recover PL-6BDX and PL-6YYR, stranded on claude/gate-items-8yep0k after its pull request merged without the two captures pushed behind it — #437
- PL-3MJH CLAUDE.md's capture rule exempts a finding fixed in the same session but gives no session a way to enter that exemption, so every trivial fix becomes a queue item — #436
- PL-CL8J Close PL-0GTC and PL-SR8F, whose work landed under other ids and left main's whole-store verify replay red for four consecutive merges — #432
- PL-D188 A brief written into an untriaged item is appended below docket new's template rather than replacing it, so the dead stub survives to triage — #428
- PL-JL2M docket new seeds a template brief that a later-written brief appends to rather than replaces, leaving empty required headings that block triage — #428

### docs

- PL-JX0Z Check whether trace-against-trace 3:1 is stricter than SC 1.4.11 requires, since it is the premise PL-GVXP's palette search rests on — #433
- PL-0GTC docs/MODEL.md's step-atomicity section still calls the class RespiratorySystem, renamed four releases ago — #376

### planning

- PL-GBBZ Clear the four undeclared prose prerequisites the new advisory names — #434
- PL-H1GH "Cut the v0.4.7 release: the supported run length, the tolerance gate, and traces you can tell apart" — #425
- PL-Q2PX Triage the five captures open on 2026-09-07 — #429

### refactor

- PL-T691 The run is its control-input timeline: hold keyframes at every event and answer any window in closed form — #430

### safety

- PL-HLD5 ROADMAP v0.5.0 branch comparison assigns line style to the run, which leaves the compartment on colour alone — #427
- PL-SR8F docs/MODEL.md calls the case-opening displacement 'two orders milder at every rate' but the figures in the same sentence are 0.8 to 1.3 orders apart, so a reader sizing the ordinary manoeuvre against the binding one is told the wrong ratio — #420
- PL-P1Z3 State the canonical evaluation rule that carries determinism once the step is no longer fixed, and gate it — #435

### science

- PL-L2F2 The fixed-volume alveolus blocks nitrous oxide, not just omits it — #426

### test

- PL-B1WW The reference gate's oracle step is not converged during a transient, so 86-99.8 percent of what it reports is the oracle's own RK4 truncation rather than the shipped step's error — #431
- PL-D3XX v0.4.7's venous pool change moved every measured residual in tests/reference/test_coupled_dynamics.py and none of the file's documented measurement tables were re-derived — #431
