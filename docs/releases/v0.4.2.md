## v0.4.2 - 2026-09-05

### defect

- PL-DNYL rules_paths_check verifies that a paths: entry is anchored but not that it points anywhere, so a typo'd prefix is a rule that silently never fires — #363
- PL-VSJZ Recover the seven items stranded on abandoned branches, including two the score-architecture drops created — #360
- PL-X1S4 Pushing a closing commit before retitling the pull request races pr-title, so a green PR shows a red run that means nothing — #358

### docs

- PL-4H01 Route out 563 resident characters whose carriers already exist: the lane sentence, the finish-a-feature bullet, and the duplicated quality-suite line — #362
- PL-NJTZ Resident rules have no retirement test while checks do, so the resident set can only grow — #362

### infra

- PL-D551 The floor job bills a whole minute for eight seconds; its steps could run in the checks job ahead of the uv install and keep the bare-interpreter contract — #361
- PL-W9DW No spending limit and no usage alert, so the first sign of Actions overage is the invoice — #361

### planning

- PL-J7Y7 Cut the v0.4.1 release: the apparatus patch — #359

### science

- PL-X9HM README's status section understates the solver disagreement as 1.2e-2 percentage points where MODEL.md gives 2.3e-2 for the same domain
