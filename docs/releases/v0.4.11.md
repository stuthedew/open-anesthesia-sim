## v0.4.11 - 2026-09-08

### defect

- PL-20PT bin/docket verify's item-front-matter guard compares git show <base>:<bare filename> because Item.path holds no directory, so the lookup always fails, the miss is swallowed as 'a new item file' and the check reports PASS on a branch that marked its own item done
- PL-MKFG PL-B9PY sits at needs-decision though v0.5.0's scoping on 2026-09-06 answered the rendering decision its own brief named as the promotion condition, so docket next cannot rank it and PL-8PSW stays blocked behind it — #478
- PL-36R4 "Forty-two presence-qualifying items carry neither a gate placement nor a recorded deferral, which is the one disposition ROADMAP's presence rule forbids" — #476
- PL-S2L4 bin/docket delegable offers items that docs/worker.md forbids a worker to touch — #471

### docs

- PL-3Y96 The docket skill routes every undecided item to needs-decision, so a session triaging one that waits on an unscoped milestone will not reach for the blocked-by entry PL-W8XP added — #477
- PL-7Y27 docs/references/ holds no copy of the three vaporizer sources PL-5K5C wrote into docs/MODEL.md, so the numbers taken from their full texts cannot be re-checked — #469
- PL-18ND PL-YDKJ's block on PL-2FM6 rests on the point-movement rate being its main input, which the PL-YSZN measurement contradicts for the cost that now dominates

### infra

- PL-33WM main is red: PL-HX5C was merged in #477 with no gate disposition, because #476 added the presence check on a base #477's CI never saw — #480
- PL-W8XP An item blocked on a milestone being scoped cannot say so: blocked-by only names items — #478

### other

- PL-QC38 PL-61WW reported as still reproducing after the fix merged: establish whether it is a stale build or a second element — #468

### perf

- PL-KP7H Every LineChartDataPoint carries a default tooltip holding a full TextStyle, which is half the per-point diff cost: dropping it cut a saturated frame from 52 to 27 ms — #475
- PL-R2YM One slider on_change costs 59 ms because it triggers a whole-page update: a drag emitting 30 events a second would need 1.8 s of event-loop time per second — #475
- PL-YDKJ Decide whether the chart should keep patching one control per plotted point

### planning

- PL-0YQX "Place the interface pass on the plan: a designed theme, type scale and layout have no roadmap item and no timeline row, and WORKING_NOTES shelved the thread pending the owner asking again" — #474
- PL-VLZT Cut v0.4.10 - the release where the apparatus stopped being implicit — #467
- PL-YXXG PL-YDKJ sits in the debt gate's 'cleared before v0.5.0 begins' group, but PL-2FM6's brief says decide it after PL-2FM6 lands and PL-2FM6 is v0.5.0's own scope — #473

### refactor

- PL-2CS8 "Nothing carries roadmap item 24's stated prerequisite: the display constants are scattered across theme.py, simulation_view.py and duplicated core/app defaults, with no item to consolidate them"

### science

- PL-YKSM Lowe and Ernst's cardiac output is allometric (0.2 x M^0.75 = 4.84 L/min at 70 kg), not the stored fixed 5.0, and weight_kg is still read by no equation — #470

### ux

- PL-6580 Strip the concentration chart's explanatory prose down to legend and labels — #479
- PL-B89V The interface's reader is named in no file a session loads, so every session writing app/ writes for a lay reader by default — #472
- PL-YKF8 The interface shows only the released version, so an owner verifying a fix by eye cannot tell which build drew the screen — #468
