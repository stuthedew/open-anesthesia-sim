## v0.4.4 - 2026-09-06

### defect

- PL-2HTF MODEL.md's numerical requirements contradict the operator split it implements — #376
- PL-Y5BV The pulmonary uptake rate MODEL.md specifies is never computed — #376

### docs

- PL-88GQ State every displayed decimal count as a presentation decision the owner can revise — #378
- PL-HDDJ pr-title.yml's comments still cite quality.yml's floor job, which PL-D551 folded into checks — #377

### infra

- PL-KPP1 Removing a workflow job orphans any branch-protection required check named after it, and nothing in the repository can see that list — #377
- PL-WC72 CI re-runs in full on every main merge while a pull request waits for approval, discarding a green result each time — #373

### planning

- PL-Y0W6 Cut the v0.4.3 release: the CI-cost and public-readiness batch — #374

### refactor

- PL-3TLK core/ and MODEL.md call the middle gas-phase state F_C (circuit), but the domain's symbol is F_I (inspired) and the F_A/F_I curve is the field's canonical teaching graph — #376
- PL-LKRP apply_blood_uptake computes its result twice and accepts bool where parameters.py rejects it — #376

### science

- PL-7PLY formatting.py's displayed-precision comment stops at the 1.2e-2 envelope corner and omits the 2.3e-2 worst trajectory, the row that actually tests its 'second decimal is the uncertain digit' claim — #378
- PL-ZHTT docs/MODEL.md says an exact solver leaves the fixed 0.1 s step unaffected, and that is false — the step's determinism justification does not survive the matrix exponential — #376
- PL-X9KD Re-derive Displayed precision and the supported step bound, and retire the splitting-error constants, after the exact step lands — #378
- PL-GS5X Replace the operator split with the exact matrix exponential, so the code that computes the answer is the governing equations — #376
