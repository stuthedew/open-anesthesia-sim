## v0.4.15 - 2026-09-13

### defect

- PL-3ZZT PL-8M8H would move a reading PL-XLQ5 and PL-Y31G document as wrong onto docket branch's restart path, inverting a noisy error into one that destroys a branch with an open pull request — #500
- PL-6BDX "Two shipped items are reported in flight in every session's digest, because their branch refs outlived their merges: PL-GVXP (v0.4.7) and PL-S5LB (v0.4.6)" — #504
- PL-8PZ1 docs/MODEL.md's release gate is still headed 'Version v0.1.0 is complete only when', but entries are being added to it for v0.3.0 — #507
- PL-CW14 The top-band advisory tells a session to demote work docket check itself pins to P1 — #506
- PL-JSRH A closed item's closed: and milestone: are records too, and nothing stops a branch rewriting either — #504
- PL-NGLV PL-4YY1 is classed defect+docs but its subject is two unsourced clinical constants in core/circuit.py, so docket check's safety pin never seats it at P1 where every other safety item sits — #500
- PL-YDL6 An item whose work landed in one pull request but whose status done was written in a later one recovers the later number, which carries the closure but none of the work — #504
- PL-8MJ3 bin/docket triage's 'already edited on a branch' mark reads the merge base, so it keeps firing after that branch's edit has merged - it told one pass to skip four of its five items, every one a false positive — #504
- PL-JBRC docket stranded still calls a branch merged when one of its commits is only docket record output, which converges byte-for-byte with the base — #504
- PL-XLQ5 The orphaned report counts a branch's superseded intermediate blob as work the squash left behind — #504
- PL-Y31G bin/docket stranded classes a branch left on pre-rewrite history as one whose pull request merged, because it compares file content and a rewrite leaves content unchanged — #504
- PL-3833 An item file's name can drift from its title and nothing checks it, so the store carries a stale slug until some unrelated rewrite happens to fix it — #506
- PL-KJ63 doc_check reads any double-quoted phrase in a doc as a section citation, so quoting a measured figure hard-fails the check — #506
- PL-R6D8 git log --source does not attribute a shared commit to the ref named first, and _unmerged_commits' docstring says it does — #504
- PL-W1LN The in-flight walk guard cannot catch a false positive whose walk ends against a commit the base reaches by another path — #505
- PL-WSDY "ROADMAP.md's current-baseline section hardcodes 'Gate 1 remains open at 92 of its 121 entries', which the gate section's own convention forbids and which is now wrong twice over" — #507
- PL-WTQ1 doc_check's math-rendering check reads a regex in an item's verify: command as LaTeX and hard-fails — #506
- PL-Z0G0 doc_check candidates attributes a hit to the alphabetically first matching term, so a line matched through render.py prints as (render) — #506

### docs

- PL-21GS "Readout 4's zeros were measured on a blind instrument: stranded could not read the refs a shallow clone truncated" — #225
- PL-27S8 "Require a threshold analysis before any tightening proposal: name what the suppressed side would have to be worth, and count it" — #499
- PL-L7JB docs/MODEL.md names the mass-balance tolerances MASS_BALANCE_ABSOLUTE_TOLERANCE and MASS_BALANCE_RELATIVE_TOLERANCE, but core/ calls them AGENT_ACCOUNTING_ABSOLUTE_TOLERANCE_L and AGENT_ACCOUNTING_RELATIVE_TOLERANCE, so a reader grepping the documented name finds nothing — #507
- PL-MS54 MODEL.md quotes mass-balance tolerance names that do not exist in the code — #507

### infra

- PL-0QQP Triage pass over the 18 untriaged captures left behind by the v0.4.14 range — #503
- PL-CSHL Pre-registered test: close the 15 open vcs.py items as one root-cause round and count what they spawn against the cluster's own baseline of 1.05 — #504
- PL-VV4D Decide whether the left-behind check should compare against refs/pull/<n>/head, which is exact but makes the check GitHub-specific — #499

### perf

- PL-PMT7 Memoize branches_in_flight so cmd_next and cmd_digest stop computing _flight twice per invocation — #504

### planning

- PL-3Z9X "Cut v0.4.14: the five ways bin/docket verify rejected correct work, and the checks that named the wrong cause" — #498
- PL-8VL1 "Record the project owner's decision that v0.5.1 reserves for planned-milestone item 34's tiled layout: panels as independent widgets in nested splitters, with the handles inert until item 34" — #502

### safety

- PL-DZFJ ROADMAP.md's gate text and PL-YLKR's brief say every drawn chart point is an M4 representative of a bucket of recorded samples, which PL-2FM6 deleted in v0.4.12 - the chart evaluates the score at the plotted instants — #507
- PL-MMWX ROADMAP.md's planned-milestone item 30 does not say that cardiac-output scaling is one of its obligations, or that weight scaling is only safe as a coupled package — #507

### science

- PL-7KDC docs/MODEL.md's 'Where this project stands' paragraph still says all eleven reference-patient parameters are the Gas Man default patient and 26 of 29 rows are tier 3, though PL-8ZJQ moved venous_pool_volume_l to a tier-2 source — #507
- PL-Q5NS docs/MODEL.md's Known limitations does not list lung tissue and pulmonary blood as an omitted store, which is worth about 20 percent of every agent's fast pool — #507
- PL-S3Q0 Two paraphrases of the tier-2 absolute survived PL-FJGY's sweep, in docs/MODEL.md's Kharasch note and reference_adult.json's Frayn and Karpe note — #507
