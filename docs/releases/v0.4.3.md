## v0.4.3 - 2026-09-06

### defect

- PL-L17Q tools/contrast_check.py runs in the CI floor job but reads 3.14 source, so one PEP 695 generic in app/ turns it red — #369

### docs

- PL-CCLL quality.yml's concurrency comment rests on the repository's visibility, which flipped twice in a day
- PL-SSQX quality.yml's concurrency comment says the repository is public and standard runners are free, and the repository is private — #369
- PL-WB5K Delete README.md until a deliberate rewrite replaces it — #366
- PL-XYRN Open the go-public gate: the owner chose to publish now and run the human-facing pass afterwards — #370
- PL-1DN9 A pull request body loses an angle-bracket placeholder to HTML stripping, so a session ships a command that reads origin/ with no branch — #369

### infra

- PL-55JM drift.yml runs pytest twice serially, and the comment explaining why it stays bare is about --cov rather than about -n auto — #369
- PL-NT6G A pull request cannot converge while main moves, because auto-merge updates the branch only once
- PL-J3BB make check cannot run tools/pr_title_check.py, so a branch that closes an item after its PR opened goes red in CI with nothing locally to catch it — #369

### perf

- PL-SDHR The verify replay is 87s of the checks job's 152s, and nothing in the workflow records what it now costs — #369

### planning

- PL-7HDY 'Cut the v0.4.2 release: the apparatus patch that completes docket-store' — #365

### session-cost

- PL-VJ7W test_verify.py spends 24s of the suite's 84s serial cost sleeping, and sets the parallel floor — #369
