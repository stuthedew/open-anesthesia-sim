## v0.2.10 - 2026-09-02

### bug

- PL-T940 A verify: command that exceeds LANDED_TIMEOUT returns 1 and reads as one that correctly fails — #195

### defect

- PL-30P6 ACCENT is used as text and fails WCAG AA in both places it appears — #190
- PL-B0YN docket next ranks by smallest effort among items in underway features, not by which feature is nearest completion, so its rationale line and CLAUDE.md both describe a ranking the code does not implement — #186
- PL-X0RG MUTED fails WCAG AA on the labels naming every clinical readout — #187
- PL-G1MF docket next says 'Finishes <feature>' for any item in an underway feature, and contradicts itself in the same sentence with the count still open — #186

### infra

- PL-5TN8 Two core-guard-coverage items will each add a ~50 s full-suite --cov command to every make check when triaged — #195

### perf

- PL-LXR3 bin/docket check runs every open item's verify command serially on every make check, costing 50 s and growing with the queue — #193

### safety

- PL-8M05 Keep the concentration readouts on one baseline at narrow window widths — #196
- PL-NV9W Label the alveolar readout end-tidal-equivalent, as MODEL.md requires — #194
- PL-0MLQ Refuse a setting outside the documented supported input range — #191

### ux

- PL-HK75 Decide the application's display name, which still reads "Working Title"
