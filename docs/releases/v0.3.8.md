## v0.3.8 - 2026-09-04

### docs

- PL-0XMD Primary literature is unreachable by direct HTTP from a remote session, and the finding is currently buried in a dropped item — #305
- PL-5DN3 The literature-route rule lands in docs/worker.md, which only delegated workers read, while the model-spec-accuracy items that need it are not delegable — #307

### infra

- PL-YHD3 Two sessions that discover they are on the same work have no rule for which one yields — #310

### perf

- PL-0VM7 snapshot() copies the entire run history on every frame — #308
- PL-MJ7B select_envelope_indices materializes one values list per trace per frame, and removing it is a constant-factor win the item that asked for it read as an asymptotic one — #309

### science

- PL-ZRSP Plot the F_A/F_I ratio the uptake literature plots — #306
