## v0.4.25 - 2026-09-14

### defect

- PL-3LZB RunDefinition's lower bound is the literal 0.0 and _segment_index_at wraps, so any definition opening after zero would answer from its last segment instead of refusing — #572
- PL-5328 PL-RD3B's brief names RunHistory, HistoryWindow and SimulationHistorySample, which PL-2FM6 deleted, and the test file its Done when rests on no longer exists — #566
- PL-6T4L The digest offers v0.5.0 while that milestone has 8 of 22 Required scope entries open, because release_offer's reserved-version guard compares the bump against plan.step rather than the beat's milestone — #571

### docs

- PL-H27D The completeness pass over PL-ZMRT found the accumulator claim false in three places and the retired frame language in five more, all outside what that item's own sweep reached — #574
- PL-YVM1 Twenty-nine open items, src/anesthesia_sim/app/theme.py and docs/MODEL.md still name the Qt port v0.5.1 across 51 references, a version ROADMAP.md no longer contains, and doc_check cannot see it because none of them is a section citation
- PL-T7PY Four item files still gloss the Qt port as v0.5.1 in their deferral blockquote while the citation beneath it reads ROADMAP.md section v0.4.25, so the header and its own citation disagree: PL-7J96, PL-F0L8, PL-NC2P, PL-027

### feature

- PL-J2TD Open a live run at a canonical keyframe state with its clock re-based, which is what a fork resumes into — #568
- PL-TFX5 Fork a run at any control-input event, flat rather than as a tree — #570

### planning

- PL-1V3X "Cut v0.4.24: the release where core/ settled what its dimensionless numbers are called" — #565
- PL-RKWB Decide whether the Qt port ships ahead of v0.5.0's display half: PL-8PSW is the only scheduled item the port rewrites, and on Flet it is built where no session can look at it — #573

### refactor

- PL-B9PY Decompose SimulationView so two runs can be rendered at once — #567
- PL-ZMRT Decide whether a branch's run definition should open at the fork instant, leaving the app one simulated-time frame instead of two — #572
