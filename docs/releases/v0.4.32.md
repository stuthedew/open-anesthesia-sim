## v0.4.32 - 2026-09-20

### docs

- PL-4DCG Survey the anesthesia machines in current clinical use for the variables that change a simulated result — #742

### housekeeping

- PL-T2YR Recover PL-66Z5 and PL-ZM48, stranded on origin/claude/practical-brown-wr7u6i by a completed session that opened no pull request — #744

### infra

- PL-XZD0 Nothing reconciles the jobs that report a status check on pull_request against the branch-protection required list, in either direction: PL-KPP1 covers removing one by comment alone, and nothing at all covers adding one — #740

### planning

- PL-R5VS Cut v0.4.31 from the 14 finished items since v0.4.30: the release that completes model-capability-routing and recommendation-rationale — #739
- PL-TGFY PL-Z34C is a frozen gate entry that qualified as needs-decision, has since moved to blocked, and is no longer debt by any class - so the gate holds an entry nothing can clear — #745

### safety

- PL-JVHL The hover answers for whichever run is marginally nearer, so a 2 px hand movement silently swaps which run's value is read — #743
