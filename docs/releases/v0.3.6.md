## v0.3.6 - 2026-09-04

### defect

- PL-GW37 docket check recovers the wrong pull request for an item closed as a rider on another item's PR, and advises writing that number in — #285

### infra

- PL-Y0RZ Nothing enforces that only core/parameters.py imports Pydantic — #289
- PL-ZSV6 A triage reply recommends work and drives fixes, when it should summarize what was triaged and where the queue stands — #286

### safety

- PL-F52R Draw the MAC-awake reference band on the chart — #288
- PL-DHV7 Express compartment concentrations in MAC multiples as a display unit — #283

### session-cost

- PL-2NSX The session-start digest names one Top: item for every session, so two parallel sessions still open on the same id — #287
- PL-8165 docket next answers for the whole queue only, so two simultaneous sessions rank onto the same item — #287
