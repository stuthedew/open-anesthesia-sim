## v0.4.23 - 2026-09-14

### defect

- PL-JRS3 Establish what a Qt port would cost the checks that read theme.py: contrast_check and agent_identity_check would pass silently on a tree they no longer describe — #555
- PL-3PRZ The agent-accounting guard catches an absurdly small alveolar volume at 1e-9 L but stops catching it by 1e-300 L, where the run returns a concentration of exactly zero with accounting passing — #556
- PL-7QKY The working-notes discovery instruction is circular - a session must read the whole file to learn whether its task touches one of its threads — #556
- PL-8XPQ Nothing checks that an interface string uses glyphs the Flutter client can actually draw — #555
- PL-7790 A session that starts an item by pushing only queue-file edits stakes no claim docket next can see — #558
- PL-B73C Decide whether docket flight and the digest should name unlanded refs carrying no item id at all — #558

### docs

- PL-11YF MODEL.md says the readouts sit in one row without saying above what width — #555

### perf

- PL-R460 The exact step costs 27 us against the split's 18 us per step at held settings, and 1.2 ms on every settings change — #556

### planning

- PL-MHYG "Cut v0.4.22: the release where what this project says about itself was checked against what is true" — #554
- PL-WGXJ apparatus-standard.md structurally exempts the apparatus from the expert-review standard, so the largest part of the tree has no review bar — #558
- PL-XR8K "Tag v0.4.22 on the merge commit of #554: the release is cut and only the project owner can push a tag ref from this environment" — #557

### refactor

- PL-6194 46 redundant parentheses around bare keyword-argument values across src/, from the initial build — #555

### session-cost

- PL-JQVB CLAUDE.md's four dispositions for a new rule list no carrier for a paste-able brief or an agent definition, and a skill's resident cost is invisible to measure_resident — #558

### ux

- PL-LL9Y Check the warning and alert colours against the medical alarm-colour convention — #557
