## v0.3.5 - 2026-09-04

### defect

- PL-9NKK verify.py's LANDED_TIMEOUT and landed_workers() record 48-49 candidates at 34.9 s serial and 6.9 s worst; measured 2026-09-03 it is 78 candidates at 175.5 s serial and 27.4 s worst — #274
- PL-FRGP The slow-verify advisory says the named command sets the check's floor, but the pool is now throughput-bound: removing PL-GS5X measured 28.6 s to 23.7 s, not the ~24 s the sentence implies — #276

### infra

- PL-D3M2 Nothing holds the Makefile's pytest line and quality.yml's identical, though both comments say they must stay the same — #278
- PL-MC8Z The start-an-item guard checks who is on the item but never what files it touches, so overlap is found at merge — #279

### perf

- PL-FX3N make test runs the suite serially while make check runs it at -n auto, so the target a session uses while iterating is the slow one — #280
- PL-WCZV pytest-xdist -n 4 measured 73.0 s to 24.9 s on the full suite with coverage identical at 100 percent; decide whether to take the dependency and where the flag lives — #276
- PL-8BFV bin/docket check is now the largest item in make check - 30.5 s of 59.2 s after PL-WCZV, where it was a quarter — #278
- PL-KCQ7 40 percent of the suite's wall clock (29.2 s of 70 s) tests subprojects/docket, so the workflow apparatus dominates the simulator's own gate — #277
