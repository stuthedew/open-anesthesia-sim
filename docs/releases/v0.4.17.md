## v0.4.17 - 2026-09-13

### defect

- PL-KBD0 A blocked-by edge with no status blocked is invisible to the ranking, which is the same silent wrong answer one step along — #515
- PL-1MKQ bin/docket release stamps its items one file at a time, so an interrupted cut leaves a milestone stamped on items no release notes name, and the re-run's notes silently cover only the remainder — #513
- PL-74R0 core/ still holds four compartment-level advance methods that the coupled step no longer calls — #516

### docs

- PL-4T90 The docket skill lets a session answer a needs-decision item whose answer is direction rather than fact, and PL-8GV5 was closed on a session's own reasoning as a result — #516
- PL-1KTV docket.toml counts .github as the workflow lane but apparatus-standard.md's paths do not, so 500 lines of workflow comments sit under the simulator's standard by default — #515
- PL-79YX Six exact float-equality branch guards in core/ are correct and nowhere explained — #516

### infra

- PL-8PT6 subprojects/docket/uv.lock is not gitignored, so running uv from inside the subproject leaves an untracked lockfile a git add -A would commit — #515

### planning

- PL-9FMV "Cut v0.4.16: the release where one quantity stopped being stated two ways" — #514
- PL-8GV5 Decide whether the model should represent anaesthesia's own effect on cardiac output and regional perfusion, which it currently holds fixed — #516

### refactor

- PL-WVSK Nothing distinguishes a concentration fraction from a percent at the type level, and two boundaries convert implicitly — #516

### session-cost

- PL-MHQK The contrast checker prints its known-shortfall detail on every run, where the verify advisory was narrowed to what a session is about to trip over — #515
