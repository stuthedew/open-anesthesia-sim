## v0.4.0 - 2026-09-05

### defect

- PL-8GLL WORKING_NOTES' 88 B per-sample memory figure describes a per-step history PL-D9WD replaced, and PL-011's retention arithmetic rests on it — #344
- PL-LLWN No check holds a .claude/rules/ paths: entry to the repository root, so every glob silently matches the same name at any depth — #351
- PL-QV1F The resident-instruction metric counts lines, but 21% of CLAUDE.md's text sits on 6% of its lines, so a 433-character cut inside one paragraph reported as unchanged — #348
- PL-R3KB Selecting an agent silently discards the running case — #326
- PL-WWDT The expert-review standard is path-scoped, so it does not load when the approach is being decided - the design round it matters most in — #350
- PL-ZQ35 The README freeze's path glob does not say whether it reaches subprojects/docket/README.md — #350
- PL-90Y6 The MAC-awake band draws as a line at any axis range the overpressure constraint allows — #345
- PL-B2NS doc_check candidates matches short identifiers against ordinary prose, so one function named settle produced 30 false lines — #335
- PL-JZ1D A closed item's verify: command can be silently invalidated by later work, and nothing notices — #334
- PL-X3WZ A commit that only annotates an item's brief marks it IN FLIGHT, so docket next hides an item nobody is working — #324
- PL-YBWT The off-scale notice's rationale calls 8% sevoflurane the standard induction setting, which overstates it — #321

### docs

- PL-6SBB CLAUDE.md's apparatus-only standard sits three sentences from its subject and reads as a whole-repo aphorism, so a session applied polishing it past sufficient to src/ — #339
- PL-JRPP PL-MGF9's brief misquotes CLAUDE.md's measure of success as 'still enjoyed in a year', a leftover of the pre-correction one-year horizon — #346
- PL-MHFK Drop the one-year foil from CLAUDE.md's horizon paragraph, which keeps being read as a leftover of the pre-correction horizon — #346
- PL-QTN6 Freeze README edits until the project owner decides what the README is for — #327
- PL-RCTQ Sweep MODEL.md and README for the new unit, time base and run rate — #352
- PL-RM83 Decide what README.md is for and what belongs in it before the rewrite starts — #338
- PL-MPZ0 Route Lee 2018's ten rules for documenting scientific software into this repository's standards, adopting only the rules that are not already practice here — #347
- PL-1M5B PL-W3DD's Where claims chart_downsampling.py consumes the sample accessors, but that module is generic over SampleT and imports nothing from controller.py — #343
- PL-8DDG Add CITATION.cff so the simulator is citable, and decide whether CONTRIBUTING.md is warranted yet — #338

### feature

- PL-SN2C Add the playback multiplier as steps per tick, with the rate always visible — #331

### infra

- PL-J833 Nothing stops core/ importing a wall clock, so the never-wall-clock rule is prose rather than a check — #333
- PL-W4H9 A session in auto mode cannot edit .claude/settings.json, and the cheap way past that guard is to append to an already-wired hook script, which is exactly what it must not do — #340
- PL-ZBRB An item can name a prerequisite in prose without declaring it in blocked-by, and nothing notices — #329
- PL-ZK9R The core/ import boundary confines time, datetime and random but not secrets or uuid, which break the reproducibility guarantee by the same route — #336
- PL-66FP Two sessions cut the same release independently: docket release has no in-flight guard, so a second session bumps the version and writes notes for a version already on main — #323

### planning

- PL-2TCS Triage the fourteen captures from the 2026-09-05 sessions — #347
- PL-72V3 Triage the eight untriaged captures from the 2026-09-05 sessions — #342
- PL-SN5S Triage the six captures from the 2026-09-05 sessions — #332
- PL-MPTN Triage the 32 untriaged captures from the 2026-09-04/05 sessions — #330

### refactor

- PL-W3DD Key SimulationHistorySample by substance rather than six flat compartment floats — #343

### session-cost

- PL-P3B6 The verify replay costs every make check 31s to answer a question about the queue rather than the commit — #322
- PL-JK0M Route or justify the 548 resident instruction lines every session loads before reading anything — #349
- PL-VZ8P pytest -n auto leaves about 20 percent on the table because much of the suite waits on subprocesses rather than CPU — #328

### ux

- PL-CC23 Scale the chart's vertical axis to the run, not to the vaporizer dial — #319
- PL-SSBP Add the chart time-base selector with 15, 30 and 60 minute scales plus Fit run — #337
