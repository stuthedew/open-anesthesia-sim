## v0.4.18 - 2026-09-13

### defect

- PL-9S30 Gate 1's deferral section records one of the nine entries sequenced past v0.5.0; the other eight exist only as a blocked-by field, which the gate's own snapshot rule says must be written down and explained — #521
- PL-J790 Every recommendation of a next item should state its relation to the current gate, and docket show never states one at all
- PL-SL70 bin/docket wave and gate count the seven Gate 1 entries sequenced behind v0.5.1 as open debt, so every session opens on a beat asking for a target the plan forbids — #521

### docs

- PL-WT07 docs/MODEL.md says the ventilator start binds the per-rate displacement table 'throughout', but at 300x it leads the unperfused load by only 3% (10.36 vs 10.03 pp) and 'throughout' reads as a comfortable margin
- PL-212V MODEL.md's Symbols table has no row for the tissue:gas coefficient, which is the parameter actually stored and the one core/ names — #519
- PL-39K7 Nothing in the auto-loaded surface points a session at docs/ARCHITECTURE.md's "Where new code belongs" — #522
- PL-DXQC The v0.4.0 Goal section states two problems in the present tense that are now fixed
- PL-XF89 README's opening says inhaled-anesthetic where the About field and pyproject say volatile - decide which scope word the project's one-line self-description uses
- PL-H46J Give MODEL.md's Symbols table a Code column naming the expression that denotes each symbol — #519
- PL-N6Y0 .claude/rules/apparatus-standard.md states no test bar, so apparatus tests inherit the simulator's instinct — #522

### infra

- PL-TQJ4 Triage the fourteen captures open on 2026-09-13
- PL-NB35 Give docket a bounded startup tier carrying failed approaches and why they failed
- PL-VV16 Instrument which item files sessions actually open, and whether citation edges are ever traversed

### planning

- PL-SXBQ "Cut v0.4.17: the release where core/ said what it meant" — #517

### refactor

- PL-9SH6 Give the partial-pressure-equivalent fraction one accessor name across every compartment in core/

### safety

- PL-V6M0 _apply_setting reports a SimulationExecutionError as a refused setting and lets the run continue, which is the opposite of what that type means

### ux

- PL-YTX9 Decide whether a hidden compartment trace should keep its legend entry or vanish from the legend entirely
