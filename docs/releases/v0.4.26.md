## v0.4.26 - 2026-09-17

### defect

- PL-0PJG agent_identity_check prints 'none of them rendered disabled' from an empty measurement set, so the affirmative sentence cannot be told apart from the same sentence earned — #581
- PL-BXB2 contrast_check reads only theme.py and simulation_view.py, so a colour declared in any other app/ module is measured by nothing and missed by nothing, and check_colors_live_in_the_theme inspects only the view — #581
- PL-GS3R The drawn chart's worst error moved from the control change to the steep early wash-in, and PL-4RBD's 0.32 MAC only fell to 0.26 MAC: uniform columns chord across the same width M4's buckets did — #583
- PL-V53R agent_identity_check reads only simulation_view.py and keys on _apply_agent_color_scheme by name, so moving one class out of that module silences it rather than failing — #581
- PL-32Z9 main is red: PL-D1RT's verify: command already passes on a tree carrying none of its work, and only the push-to-main --verify sweep can see it, so no pull request will go red and every merge re-reds main
- PL-3355 The 'Simulated time' and compartment readouts wrap their value onto a second line at some window widths — #588
- PL-5D2R The ask-gate in CLAUDE.md is stated once with emphasis and released four times without it, which is the contradiction shape Anthropic documents as resolved arbitrarily — #622
- PL-7XTS Nothing routes a close-out to bin/docket verify --self, so a session auditing its own branch runs the delegated mode PL-69JZ fixed and still gets a REJECT on correct work — #595
- PL-9KDK tools/import_boundary_check.py confines no UI toolkit and no numpy, so nothing stops PySide6, pyqtgraph or numpy landing in core/ during the port, and ROADMAP.md credits it with a Flet count it does not check — #581
- PL-9PD6 docs/interface-provenance.md states at line 94 that README.md records the Blender attribution and at its Attribution section that README.md deliberately does not yet carry it, which is the contradiction PL-RTG9 was refuted against — #629
- PL-B5VM "main is red on PL-S5YM's non-discriminating verify: an unrelated merge added a test whose name contains 'covered', flipping 'pytest -k covered' to passing while the item's own work is untouched" — #598
- PL-D584 PL-TH35's blocked-by names PL-8VL1, a planning item that closed 2026-09-12, so bin/docket check advises on every run that the Editor contract is ready to promote - the one thing ROADMAP.md and .claude/rules/ui-areas.md both say it must not do — #629
- PL-F933 doc_check resolves a path citation against the working tree, so a citation to a gitignored path passes locally and reddens CI — #593
- PL-J4NW docs/ARCHITECTURE.md's 'Where new code belongs' routes every new display panel to run_view.py or simulation_view.py by asking whose it is, which is a fixed two-level layout the area model refuses - and .claude/rules/where-new-code-goes.md loads it on every src/ read — #629
- PL-KND7 main has been red since #634 because PL-D1RT's Done when. was overtaken by two owner decisions, and the only check that sees it is one no local make check runs
- PL-LKFP bin/docket show marks an item IN FLIGHT after its branch's pull request squash-merged, so PL-2M4X is ready on main and byte-identical on the branch and still says 'do not start' - PL-8MJ3 fixed this for the triage mark only — #620
- PL-MXSL doc_check requires a cited directory to exist with no exemption for one .gitignore covers, so documenting a generated directory fails CI while make check passes locally the moment anything has created it — #593
- PL-N638 A session's own multi-item diagnosis has no path that sets feature:, so the group it proposed exists only in the reply - PL-XD3C, PL-0J9K and PL-MMVF were each filed with none — #632
- PL-Q4VH The compartment chart labels its percent axis at a different interval from the gridlines it rules — #588
- PL-QSJM "make check's ruff passes where CI fails after a module is deleted: ruff's cache keys a result to the linted file's mtime alone, so a first-party import that no longer resolves keeps its stale verdict" — #590
- PL-R0Q0 doc_check's safety-class gate advisory reads only the frozen list, so a safety item deferred with a written reason advises forever — #628
- PL-SMN4 "A push run on main can be cancelled by a later merge, so a commit lands with no whole-store verify at all: quality.yml's concurrency comment claims every commit on main keeps its own run" — #615
- PL-THXF The trace legend swatch is a solid bar for a trace that is dashed, so the legend's own redundant channel is words only — #588
- PL-Y6W9 tests/unit/test_docket_branch_guard.py and test_docket_digest_hook.py run the hooks under PATH=/usr/bin:/bin:/usr/local/bin, so on macOS bin/docket runs under the 3.9.6 system python3, fails on datetime.UTC, and eight tests are red on the owner's own machine while green in CI — #584
- PL-Y88W CLAUDE.md's first architecture rule, ROADMAP.md's development rules and docs/resident-instructions.md all still say 'independent of Flet', a toolkit the tree no longer imports, so the resident rule names the wrong dependency in every session — #609
- PL-FWJF The Qt port is the next substantial work but sits on a — timeline row, so bin/docket wave anchors on v0.5.0 and every port item reads as placed by no section — #580
- PL-VFD8 The reserved-version guard cannot see a milestone that has a timeline row but no section yet, so the digest will offer v0.6.0 the moment v0.5.x is behind it — #606
- PL-188T The reserved-version guard cannot see the Qt port's section once the port is numbered past the current patch, so a patch cut mid-port is offered the port's own number as free — #606
- PL-CNJ1 The Qt spike writes screenshots to a caller-supplied path with no default directory and no ignore rule, so a --screenshot run in the repo root leaves a PNG a git add -A would commit — #589
- PL-RFGY .gitignore ignores .vscode/ while .vscode/settings.json is tracked, so extensions.json and launch.json cannot be added without git add -f — #589
- PL-TG60 Stop printing six decimals of an exhaust integral good to three — #588
- PL-W8DQ The four slider active tracks use ACCENT and miss the non-text minimum — #621

### docs

- PL-D1RT docs/WORKING_NOTES.md's UI-structure thread and PL-037Y still describe the port as following v0.5.0, which the 2026-09-14 reorder makes stale — #645
- PL-GDB0 Three times in one session a rule true only of the currently implemented model or mechanism was written into ROADMAP.md or docs/MODEL.md as a permanent rule about the project, each needing its own correction commit — #608
- PL-GN8C docs/references/README.md records no reading of Lowe and Ernst 1981, and the interlibrary-loan route that reached it is the answer PL-XJ5P is looking for — #612
- PL-KQHN PL-VFD8 made v0.4.26 reserved, so ROADMAP.md's Qt-port paragraph now says both that a patch cut takes this section's number and that the guard withholds it, and the policy question was never put to the owner — #642
- PL-LH18 Every main UI element is built to become a Blender-style area-type widget, and app/ has no rule saying so — #618
- PL-P5QX ROADMAP item 34 records the GPL limit on Blender's source but not that the Blender Manual and Developer Documentation are CC-BY-SA 4.0, which is the share-alike encumbrance that actually reaches this Apache-2.0 tree, and it names the Manual rather than the Developer Docs and Human Interface Guidelines where the design rationale it wants actually lives — #623
- PL-R7XK v0.5.0's gate defers PL-MN4J in the declined subsection, but 'Debt inside the milestone's own scope' already places it in Required scope — #640
- PL-ZF2G The anticipated debt-gate carve-out never expires, so an anticipated safety item stays invisible to the gate after its hazard goes live — #644
- PL-83LS The debt gate has no disposition for an anticipated safety finding whose hazard an unscoped later milestone creates — #640
- PL-9KP5 docs/ARCHITECTURE.md still describes the sample store PL-2FM6 deleted, and routes every new-code session at history_window() — #582
- PL-FTP5 Record the Blender study protocol and the interface model's design provenance in a durable doc, so a session building the layout knows which Blender sources it may read, what it may carry across, and what the project attributes - the rule currently exists only as a paragraph inside ROADMAP item 34 — #623
- PL-WLWY docs/MODEL.md's Minimum displayed outputs assumes one fixed layout, so planned-milestone item 34's removable areas would let a learner delete the readouts that are the stated safety floor for hiding a chart trace — #605
- PL-4K9V app/formatting.py's module docstring names SimulationHistorySample, which was deleted with the sample store — #582
- PL-7G5M PL-16ZC's brief still reads 'sequenced past v0.5.0, not clearable before it begins' after PL-G59B closed and promoted it to ready, so the one entry wave says Gate 1 can clear tells the session that opens it not to build it — #597
- PL-8KPD Drop PL-0QLX as a duplicate of PL-Y6W9 once claude/pensive-haslett-0f5025 lands: both capture the hook tests red under macOS's system python3, and PL-Y6W9 is the one worked — #586
- PL-C92D PL-YSZN's Flet frame table predates PL-2FM6 and measures a tree that no longer exists in two of its three stages, so the Qt/Flet comparison rests on one row — #617
- PL-Z4WL Planned-milestone item 1 does not name PL-4DCG or PL-FG9D, the survey and design round that scope it, and ROADMAP.md twice says PL-FG9D has no planned-milestone line of its own when item 1 is that line — #604

### feature

- PL-25KS Port the dashboard to PySide6: the readout row, the four parameter controls, the agent selector, the transport, the new-case dialog and the notice banner — #588
- PL-G59B Port the concentration chart to pyqtgraph: six traces, both clinical references, both axes, the control marks and the wash-in plot — #578
- PL-L9RD Re-express app/theme.py for Qt, and decide whether the interface pass rides this port — #621
- PL-8PSW Overlay two branches on one time axis, with every curve attributable to its run and its settings — #618

### infra

- PL-3SQT Swap the dependencies for the Qt port: PySide6-Essentials, pyqtgraph and numpy in, flet and flet-charts out, and re-argue the 'Decided: no numpy' note on its new scope — #609
- PL-JQJQ A bare python3 here is the 3.11 tools floor, so any session or subagent that parses src/ with it hits a false SyntaxError in app_metadata.py and re-investigates PEP 758 from scratch; nothing in the tree records that the failure is expected — #630
- PL-MQH0 Apply feature: session-start-cost to PL-JH3T, PL-XD3C, PL-0J9K, PL-MMVF and PL-RC86 once claude/confident-gauss-r3rg8i merges, which is the backfill PL-N638 could not land without five add/add conflicts — #637
- PL-2QMK No session in the web container can visually confirm a chart change, because Flet's web renderer fetches its Flutter assets from a host the egress proxy denies — #588
- PL-NGF7 tools/contrast_check.py can see no disabled-state colour, because none of them is a constant in theme.py — #621
- PL-9TNJ "Report the I001-plus-cache interaction to astral-sh/ruff: #5449 has the same root cause open for INP001 only, and PL-QSJM established both halves" — #591
- PL-DLR3 Three local working directories at the repository root - oas-mirror, output and outputs - are neither tracked nor ignored, so every session and the owner read them as untracked in git status and a git add -A would sweep them in — #589
- PL-FBXP Record the seven pull request numbers owed by the closures that reached main on 2026-09-16 — #633
- PL-J7MM assets/branding/ has held only a .gitkeep since the bootstrap commit and nothing in the tree references it — #589
- PL-VHLZ Nothing installs libegl1, so a fresh web container or CI runner dies on the first PySide6.QtGui import; quality.yml has no apt step and the environment setup script has no such line — #590
- PL-YMKV "Recover PL-SYG4 and PL-DL4M, which exist only on the branch refs of two archived sessions: no pull request is open for either, so nothing will carry them to main" — #594

### planning

- PL-0C6W Triage the fourteen captures open on the morning of 2026-09-16 — #611
- PL-3J2P Decide whether the area layout is a shared-vertex graph or a nested-splitter tree before the Qt layout work starts, because ROADMAP item 34 already specifies aligned-border dragging, four-way corner operations and arbitrary area swap, and a QSplitter tree makes all three expensive — #623
- PL-554Q Triage the 13 untriaged items left after #636, less the six that claude/gifted-fermi-m1cksg holds under PL-MQH0 — #639
- PL-G7RD Cut v0.4.25 from the ten finished items, renumbering the Qt port's section to v0.4.26 as ROADMAP.md's own risk paragraph provides for — #576
- PL-NMTF No open item builds ROADMAP item 34's area system at all - PL-3J2P and PL-C842 decided the representation and the container, PL-FTP5 recorded the provenance, and every other interface-areas item is a prerequisite waiting on a build item that does not exist — #629
- PL-Q0J1 Triage the six captures open on 2026-09-16, second pass — #617
- PL-T86Q ROADMAP item 34 records floating panels as permanently not admitted, which is a hard never the project owner did not take - they asked for tiled first with floating kept open as a later option, and the real constraint is on silent occlusion of a required value rather than on floating — #601
- PL-XWH4 A decision the owner ratified and one they specified read identically in the record, so a later session cannot tell a design they authored from a recommendation they agreed to, and defends both at the same bar — #629
- PL-6FJ5 Triage the 45 captures open on 2026-09-14 — #579
- PL-BNYF Audit the open queue and ROADMAP against the area/workspace layout model, so items scoped before it was decided are re-briefed or dropped rather than built and undone — #625
- PL-C842 Decide whether the layout container sits behind a layout model of our own with QSplitter as an implementation detail, deferred by the project owner until the Blender deep dive shows how Blender actually separates container from view — #623
- PL-LPHT Triage the 29 untriaged items left by the 2026-09-16 area-model audit and the release-seam sweep — #626
- PL-4D1M ROADMAP item 34 records Blender's window management but not the widget catalogue it implies, the named task workspaces the owner described, or the ordering that puts the layout mechanism before any widget — #597
- PL-H620 ROADMAP items 34 and 36 call an area's contents a view or a widget, but the project owner named Blender's own vocabulary - Workspace, Area, Editor - as the concept to implement, and the manual's Areas and Workspaces pages are now available at source — #605
- PL-JFXG Nothing on the roadmap plans an opioid/hypnotic interaction display, though CLAUDE.md's safety standard names interaction surfaces outright and the owner wants an isobologram widget predicting tolerance of laryngoscopy — #597
- PL-KFJQ Record branding - name, app icon, splash screen, wordmark - as planned-milestone intent — #614
- PL-PHKP Move the v0.5.x interface pass after v0.7.0 on the timeline: item 34 and break-out introduce the area header, workspace tab strip and drag affordances that a visual pass run before them would have to be redone for — #634

### refactor

- PL-7SVX Delete spikes/ and the last Flet import once the port is complete — #638
- PL-RD3B app/controller.py holds the run's trace vocabulary and drawn window as well as the UI-to-core boundary, and they are separable — #596

### safety

- PL-2K1R The interface carries a use disclaimer but no interpretation one, so nothing tells a reader the compartment readouts and chart traces are modelled rather than measured — #588
- PL-YVHK Implement the chart hover readout on pyqtgraph to the derivation docs/MODEL.md now carries, turn hoverable on so the affordance is not silently lost, and say in README.md that it exists — #588
- PL-27H0 docs/MODEL.md still specifies a per-step recorded sample store and a display operation that selects which recorded samples a trace draws, both of which PL-2FM6 deleted — #582

### science

- PL-9GP1 Appendix C of the Gas Man Workbook carries three values the provenance notes say it does not — #602

### session-cost

- PL-H253 Measure session context length as the dominant instruction-adherence variable, and give a session a way to see its own — #622
- PL-NW76 Turn the 150,000-token session cap from a stopping rule into a starting budget — #624
- PL-PX7V Nothing runs /doctor's CLAUDE.md trim check, a deterministic carrier this project's own tooling doctrine would prefer — #631

### test

- PL-GMM7 Assert the educational disclaimer's text, not just that its line executes — #588
- PL-YCWZ Add headless rendering tests over the real Qt interface, which is what PL-2QMK has been waiting for — #588

### ux

- PL-005 Replace the full-screen startup window with a sized, centered one — #588
- PL-16ZC The two clinical references and the control marks have no show/hide control, though the chart's traces now do — #597
