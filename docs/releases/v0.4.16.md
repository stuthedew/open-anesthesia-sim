## v0.4.16 - 2026-09-13

### anticipated

- PL-KCWD APP_VERSION falls back to 'unknown' in the one line tying a displayed value to the model that produced it

### defect

- PL-B32L core/parameters.py raises OSError and JSONDecodeError outside the exception hierarchy its own docstring promises
- PL-D143 Five Gate 1 entries are deferred to the Qt port at v0.5.1 by owner decision but still count as open gate debt, so the gate that v0.5.0 waits on cannot close until after v0.5.1
- PL-V13T doc_check reads a section citation in the comma form but not the section-mark form, so 285 of the 324 citations in the tree are unchecked while the run reports they all resolve
- PL-YK2V _apply_setting catches a narrower exception class than the timer paths, so an unexpected raise escapes into Flet's dispatch
- PL-KGNF docs/MODEL.md still names sevoflurane specifically in section headings and Purpose, four releases after three agents shipped

### docs

- PL-B9VL Rename the wash-in validation module and its MODEL.md section now that both cover elimination too
- PL-VZL0 Cite MODEL.md from every core/ function implementing a governing equation, and restate the solved form the spec lacks

### planning

- PL-T6F9 "Cut v0.4.15: the release where the checks stopped over-reaching, and Gate 1 passed half" — #511

### refactor

- PL-ZX12 Rename RunScore to RunDefinition: 'score' is a metaphor a domain reader has to be taught, in the package that should read like the domain
- PL-TCW5 FLOW_FRACTION_TOLERANCE is defined twice, so the two perfusion-sum guards can drift apart silently

### ux

- PL-Q4M4 The simulated-time clock reads in seconds while the chart's own time axis reads in hours and minutes
- PL-CZFY The elapsed-time readout states seconds, so a run at the newly declared 24-hour limit reads '86400.0 s' - seven characters of tenths on a quantity a reader thinks about in hours
