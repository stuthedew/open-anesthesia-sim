## v0.4.19 - 2026-09-13

### defect

- PL-RC0M "A blocked item's verify: command is never replayed, so it can rot unnoticed and redden whichever pull request unblocks it" — #530

### docs

- PL-B667 MODEL.md's required invariants say derived fractions stay finite and nonnegative, but core/ enforces the upper bound of 1 in five places and the list never states it — #532

### infra

- PL-FZ6T Check the symbol map, the retired names and the partition-coefficient rule, so core/ cannot drift back off the domain — #532

### planning

- PL-WHG6 "Cut v0.4.18: the release where the project checked what it tells the next session" — #529

### refactor

- PL-TCD1 SimulationSnapshot still names six flat compartment floats, so the readouts cannot express a second substance now that the recorded run can — #530

### safety

- PL-1XPX Decide what the readouts show while two branches are displayed — #535

### science

- PL-RFLN Settle what causes desflurane's five-minute washout residual, which PL-73G7 narrowed to end-tidal sampling or the published datum — #533
- PL-XJ5P Citing-sources says there is always a route, but a pre-abstract subscription paper has none and docs/references can no longer hold one — #531

### ux

- PL-0Q1T The new-case dialog leads its carry-over list with circuit volume, the one entry in it a user cannot set, and it is now the interface's only mention of the parameter — #530
- PL-F9TQ PL-6580 strips only the concentration chart, but the wash-in panel's two paragraphs are the largest standing prose block on the screen and no item covers them — #530
