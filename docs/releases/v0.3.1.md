## v0.3.1 - 2026-09-03

### defect

- PL-2XTF A closed item's pull request is recovered from the commit subject, which a squash-merged UI-titled PR does not carry, so main goes red with an unrecoverable error — #227
- PL-71P4 "A verify: command with a -k selector flips from correctly failing to falsely passing when unrelated work adds a matching test name, so the already-passes set grows on its own" — #236
- PL-BNPY advance()'s rollback hangs off two except clauses rather than the unwind path, so a BaseException leaves a partial step — #246
- PL-BR4G parse_front_matter silently keeps the last of a duplicate front-matter key, so two branches writing the same field merge into a corrupt item that docket check passes — #237
- PL-CGSS The drift interpreter job reports success identically whether it tested a newer CPython or the pinned one, so its green proves nothing on its own — #245
- PL-L9JS "Eight open items carry a verify: command that passes without their work, so docket verify would accept a branch that did nothing" — #236
- PL-MVC2 "Nothing checks `classes:` and `feature:` against a declared vocabulary, so a misspelled class escapes the safety-class pin and a misspelled feature silently splits the group docket next ranks by" — #237
- PL-P0QT A merge resolution can delete a captured item with nothing recording that it did, and nothing checks for it — #228
- PL-T63T "Half the store's recorded commit: hashes resolve nowhere, because the field names the branch commit a squash discards" — #236
- PL-VYXP AgentUptakeSystem.reset() anchors the mass-balance baseline to an implicit zero it does not itself establish — #246

### docs

- PL-D1ST v0.4.0's Required scope describes items whose ids it never prints, so PL-SN2C and the playback multiplier are placed nowhere — #231
- PL-1TF4 tools/contrast_check.py's docstring says four known shortfalls where KNOWN_SHORTFALLS holds three — #249

### infra

- PL-QSWS No item or roadmap entry covers dependency and toolchain drift, which a multi-year horizon makes a certainty rather than a risk — #239
- PL-J49T "Enforce the verify: command as an error when a grandfathered item is closed, so the burn-down stops depending on anyone reading an advisory" — #249
- PL-V87X ruff format warns on every run that isort.split-on-trailing-comma conflicts with skip-magic-trailing-comma — #236
