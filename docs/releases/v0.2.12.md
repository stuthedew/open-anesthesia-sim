## v0.2.12 - 2026-09-02

### defect

- PL-004 Decide the fate of the two uncalled descriptive time constants — #210
- PL-PRHN bin/docket triage names no in-flight refs, so two sessions triaged the same two items concurrently and collided at merge — #211

### docs

- PL-007 Make the payload/public-dataclass pattern self-evident in `core/parameters.py` — #208

### infra

- PL-SK88 The in-flight answer needs a push to be true; a session-title read would close the window a push only shrinks — #211
- PL-1BPV Reference-patient and agent values restated in MODEL.md prose are outside the provenance check — #215
- PL-K2ZK The session-start digest could deepen the clone once so the in-flight read answers instead of declining — #214
