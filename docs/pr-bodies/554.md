---
pr: 554
commit: 82f00d3a80bbee5eac25635e0901398e17f8f967
merged: 2026-09-13
items: PL-MHYG
subject: PL-MHYG: cut v0.4.22, the release where what this project says about itself was checked against what is true (#554)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 82f00d3a landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Fifteen finished items since v0.4.21, on the project owner's approval of 2026-09-14. `v0.4.21` was confirmed tagged on the remote before the cut.

## Why a patch

§ "Versioning decision" chooses the number for the capability boundary a release crosses, and this crosses none. Measured rather than asserted: `src/anesthesia_sim/app/`, `tests/reference/` and `.github/` resolve to the same tree objects as at `v0.4.21` (`7586231`, `e519afd`, `5aa42db`). The whole of `src/` differs in two files and neither moves a number — a docstring on `core/alveolar.py`, and one `sources` entry plus a rewritten `provenance_gap` on `data/machines/reference_circle_system.json`. `2.5`, `4.0` and `6.0` stand where they stood.

## What the release is about

**The specification stopped saying a measurement had not been sought.** One had been — Targ, Yasuda and Eger's 9860 ml by water filling, cited and *not* adopted because the system included a latex reservoir bag standing in for the patient's lungs, and the apparatus's own share cannot be recovered from a volume obtained by filling that bag with water. What it supplies is a measured upper bound and an ordering: 6.0 L stored, 8.0 L published, 9.86 L measured. The like-for-like comparison is the one to read — apparatus plus lung is 8.5 L against 9.86, 13.8% below, where the bare 6.0 reads as 39% below (`PL-QBKQ`). The same paper strikes circuit-wall absorption as the cause of desflurane's washout residual while naming a retardation this model has no term for (`PL-XWCY`), and `AlveolarCompartment`'s restated `2.5`/`4.0` now name the patient file as authority with a test holding them equal (`PL-DJYF`).

**What a number says when nothing around it qualifies it.** The chart's hover readout is derived the way § "Displayed precision" derives the numeric readouts. The measurement that decides it: `pyqtgraph.ScatterPlotItem`'s default `tip` prints `3.52e-05` on the fat compartment at 48.3 s where the readout beside it says `<0.01%` (`PL-YLKR`, `PL-DNHM`, `PL-3M3K`).

**Nine apparatus items, one of which recommended destroying work.** `bin/docket branch` told a session whose pull request had already merged to merge the base in rather than restart — precisely the push that loses the work (`PL-8M8H`). `bin/docket concurrent` refused nearly everything because `docs/MODEL.md` dominated every answer, and now groups by path, rarest first (`PL-PGZK`). Plus `PL-028F`, `PL-GLBF`, `PL-J7C5`, `PL-NR2K`, and `PL-JYR4`, which closed by its branch merging.

## Riders

- Eight `pr` numbers the base was owed ride the cut commit, per `docket check`.
- `PL-2M5T` filed from the cut itself: the notes are generated before `docket record` runs, so those eight items carry no pull request link in their own release notes, and the tool refuses to regenerate them until the release is tagged.
- One live session (`PL-GLBF, PL-J7C5, PL-PGZK, PL-8M8H, PL-028F` gate batch) was also waiting on this cut decision. It is a separate container and `SendMessage` cannot reach it, so this branch was pushed as soon as the cut existed — `bin/docket release`'s refusal on an unmerged ref carrying a cut is what guards the collision (`PL-66FP`).

## Checks

`make check` green — 2640 tests, ruff, mypy, `docket` 0 errors, `doc_check` 0 errors including the current-baseline and release-train resolutions.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Ba7f7mDvek16RCN6vSyoxd

---
_Generated by [Claude Code](https://claude.ai/code/session_01Ba7f7mDvek16RCN6vSyoxd)_
