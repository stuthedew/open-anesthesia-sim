---
pr: 273
commit: 45433b9184d1cbcb278179a2de25729506c7dc9c
merged: 2026-09-03
items: (none)
subject: Release v0.3.4 — the statements the project was making about itself (#273)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 45433b91 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts v0.3.4, and carries two queue changes that came out of choosing what to work on next.

## The release

Five items, and no shipped Python at all: `src/` is byte-identical to v0.3.3 and `src/anesthesia_sim/data/` is unchanged, so no equation, parameter, numerical method, unit or displayed value moved. The release restores the correspondence between the simulator and what this repository says about it.

**Two corrections in `docs/MODEL.md`.**

- `PL-C1KK` — the Status section named v0.2.3 as the current released baseline, thirteen releases after v0.2.3. It now names no current version at all: `ROADMAP.md`'s version table is where the baseline lives, and a second copy of it goes stale at every release with nothing reading wrong in between.
- `PL-B875` — the numerical-method section still recorded the operator split as kept, on reasoning the 2026-09-03 re-scope reversed. The supersession is now recorded with its date and its ground, alongside the original decision on its own terms, and marked as an interim correction that `PL-X9KD` owns finishing.

**Two checks that reported a false state.**

- `PL-P23D` — the top-band advisory counted blocked items a session cannot start, which also made it reachable with nobody over-prioritizing anything, since the blocker-band rule lifts a blocker into the band of what it blocks. It now counts startable items and names the held ones separately; `docket triage` was moved onto the same number.
- `PL-R7C0` — `doc_check`'s baseline-tag advisory fired on any full clone whose tags predated the newest release, and printed a `git tag` command for a tag that already existed. Distinguishing that from a genuinely untagged release needs the network, so it is retired rather than narrowed. Nothing permanent is lost: once that version stops being the baseline it falls through to an error, and `docket release` still refuses to cut while the previous release is untagged.

**One sequencing bug.**

- `PL-9K7K` — `docket next` offered `PL-ZRSP` ahead of `PL-DR1Z`, which records the constant-inspired condition `PL-ZRSP`'s trace depends on. The instance is closed; the general case is `PL-5WFS` and stays open.

`ROADMAP.md` carries the version-table row, the moved baseline mark and the new baseline section. `docs/releases/v0.3.4.md` and the item milestone stamps are generated.

## Also on this branch

- **`PL-DHV7` (MAC multiples as a display unit) gained a scope note and a decision.** Promoting `mac_percent` from a starting-value convenience to the divisor of every displayed compartment value makes it a clinically meaningful transformation, and falsifies the clause in all three agent data files that says it is "not a governing-equation parameter". Against Mapleson's age-40 meta-analytic MAC ([Br J Anaesth 1996;76:179–85](https://doi.org/10.1093/bja/76.2.179)) the stored tier-3 values put sevoflurane 10% low and desflurane 10% high, so a sevo-versus-des comparison in MAC is skewed about 22% by source choice alone. Decided: keep the values, state the deviation, and place the tier upgrade in v0.4.1 or Gate 1 where a parameter change is admissible.
- **`PL-X3WZ` captures a guard failure this branch tripped.** `docket flight` parses commit subjects for ids and `CLAUDE.md` requires every subject to lead with one, so a commit that only records a note into an item's brief marks that item `IN FLIGHT` and `docket next` stops offering it. Measured here: `PL-DHV7` ranked first, was annotated and pushed, and moved into "Excluded, already in flight" with nobody implementing it. Self-healing on merge, so not urgent.

## Checks

`make check` green: 1210 tests, `core/` at 100% statements and branches, `docket check` 0 errors, `doc_check` 0 errors and 0 advisories.

## After the merge

The tag is not automatic:

```bash
git tag -a v0.3.4 <the squash commit on main> -m "v0.3.4"
git push origin v0.3.4
```

`bin/docket release` refuses to cut v0.4.0 while v0.3.4 is untagged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01J3T8jsizDAYp9iM5W3dK8c

---
_Generated by [Claude Code](https://claude.ai/code/session_01J3T8jsizDAYp9iM5W3dK8c)_
