---
pr: 886
commit: 1e460414ef84484be128233edd01ad7cb7b65a2d
merged: 2026-09-21
items: PL-K4BN
subject: PL-K4BN: backfill the eight pr numbers origin/main is owed (#886)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 1e460414 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`bin/docket record`, run once. Eight items are marked `done` on `origin/main` and record no `pr`, so `bin/docket check` reported each as its own advisory — **8 of the 13** `make docket` printed. After this: **5**.

## Why they accumulated, which is the part worth reading

The advisory tells you to let the write "ride the commit you are already making rather than composing one". That is right while commits are flowing, and it silently becomes wrong once nobody has one in hand. Six of the eight merged between 21:35 and 00:36, in a window where every session that could have carried them had already pushed. A session arriving afterwards reads eight advisories it is told not to act on by themselves.

That is `CLAUDE.md`'s routed-around test rather than tidiness. The same run also carried four advisories that genuinely need judgment — the stale slug on `PL-843V`, a closed thread in `docs/WORKING_NOTES.md`, and two `payoff`/recommendation gaps — and those are the ones this block was crowding out of a section sessions skim.

## Verified against the squash subjects rather than trusting the tool

| Item | `pr` | Subject on `origin/main` |
| --- | --- | --- |
| `PL-JYTJ` | 881 | cut v0.5.2, and prove core's only movement docstring-only |
| `PL-J3WK`, `PL-PBP5` | 882 | close the make check / CI gate seam in both directions |
| `PL-PHK4`, `PL-T5K1` | 883 | audit the instruction set's dated assertions, on a 90-day threshold |
| `PL-G7ST` | 884 | correct PL-M7W1's #880 section |
| `PL-44DG` | 885 | count the two resident payloads the size gauge could not see |
| `PL-2JRC` | 880 | triage the untriaged captures standing in the queue on 2026-09-21 |

Two of those subjects lead with two ids, which is the one-id-grammar rule working as intended: `PL-PBP5` and `PL-T5K1` recover to the pull request whose subject names them rather than to their own capture commits.

## The `verify:` was wrong the first time, and quietly

Worth recording because the failure mode looks like success. `! grep -L '^pr:' <files>` reads as "fail while any file lacks `pr:`" and does the opposite: `grep -L` takes its exit status from whether any *line* matched, not from whether it printed a filename. With every file missing `pr:`, nothing matches, grep exits 1, and the negation **passes** — a command that would have certified the work done before it started.

Replaced with an explicit count of the files that do carry `pr:`. Run both ways: exit 1 before `docket record`, exit 0 after. Both observed, not reasoned about.

## Scope

- `PL-K4BN` filed before the work and closed with it, classed `housekeeping` — a pass whose work *is* the queue edit, so it owes **Problem.** only, and not a debt class, so it owes v0.6.0's frozen gate no disposition.
- Nine files touched, all under `docs/items/`. No tool, check or workflow changed.
- Left open deliberately: whether the advisory's "let it ride" wording should say something different once a group passes some size. That is a real question and not a housekeeping pass's to answer.

## Verification

- `make check` — exit 0, output redirected to a file rather than piped, so the status is `make`'s and not `tail`'s (`PL-D0W8`).
- `make docket` — 0 errors, advisories 13 → 5.
- `tools/doc_check.py check` — 0 errors, 0 advisories.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015gxBzyZ9VzB3KHS29WKGVt


---
_Generated by [Claude Code](https://claude.ai/code/session_015gxBzyZ9VzB3KHS29WKGVt)_
