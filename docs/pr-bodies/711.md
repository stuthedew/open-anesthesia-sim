---
pr: 711
commit: e69d2e9018de9a048aebc14ca2c1c0faf91bf16c
merged: 2026-09-19
items: PL-09G9, PL-FZ58, PL-VHVJ
subject: PL-FZ58, PL-09G9, PL-VHVJ: close the verify: inflow rather than strip the backlog (#711)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit e69d2e90 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

## PL-FZ58 — answered, and neither direction it names is the work

The brief offered two: strip the pytest clauses from the commands naming its three test files, or make those files cheaper. Re-measured on this checkout, both fail.

**Stripping them in one pass** is the alternative `PL-6TP8` offered and the project owner declined on 2026-09-19, ratifying repair-as-started on the argument that the bill falls as the queue turns over. That argument holds on a number the case never carried: the queue closed **672 items in the fourteen dated days to 2026-09-19 — 48/day against 324 open**, so a specific 25 drain in about a week, faster than a 25-file pull request would matter. (0 of the 25 files are edited on any of this checkout's 23 non-`main` refs, so the collision objection is small today — but the drain rate is what decides it.)

**Making the three files cheaper** is worth much less than the brief assumes, and "helps every consumer including `make check`" is the clause that does not survive measurement:

| file | serial | share of the suite |
| --- | --- | --- |
| `tests/reference/test_coupled_dynamics.py` | 74.2 s | 27.2% |
| `tests/reference/test_published_wash_in_and_elimination.py` | 37.5 s | 13.8% |
| **the three files PL-FZ58 names, together** | **63.7 s** | **23.4%** |

A single reference file outweighs all three, and `make check` already runs at `-n $(cpu*2) --dist worksteal`: 77.8 s wall against 272 s serial on four cores, within 11% of the CPU floor. Deleting all three outright would buy ~16 s of a 78 s run, and the two files above them are reference validation the safety-critical standard asks for.

## PL-09G9 — what was actually left: the inflow

Repair-as-started drains a *closed* set, and nothing closed it. 82 of 180 open commands carried the shape, 23 of them written on 2026-09-14 alone, and what prescribed the replacement was one table in `.claude/skills/docket/SKILL.md`.

`docket check` now errors on a `verify:` command that runs pytest over a tree named in `collected_test_paths` while a **separate**, non-pytest clause stands beside it — and `docket set` refuses the write in the checker's own words, so the rule is met when the command is typed.

The separate clause is what makes it decidable: the author has already said which clause discriminates, so the pytest run is not it, whatever the item is about. Left alone: a lone pytest clause, one carrying `-k`/`-m`/`--cov`/`::`, and a target outside the declared trees — that last is the condition that would falsify the rule, and **0 of the 82 name such a target**.

The other 73 multi-clause commands (`python3 tools/doc_check.py check && …`) stay prose deliberately. Which clause is the prerequisite there depends on the item's work — an item whose work is *making `doc_check` pass* has that clause as its discriminator — and scripting that judgment is worse than no tool.

Grandfathered on the capture date, dated `2026-09-20` so `PL-QSGX`, captured today carrying the shape, is left to its own start rather than repaired from here. Verified against the store: the predicate fires on exactly the 82 and on nothing else.

## PL-VHVJ — a `REJECT` on correct work, found by the close-out

`PL-09G9`'s audit failed `no suppression added` on a line listing pytest's value-taking options: `SUPPRESSIONS` is matched with `s in line`, and `xfail` is a substring of `--maxfail`. Each entry now anchors on the left with `\b`, and only where it begins with a word character — two open on `#` and `@`, where `\b` asserts the opposite of what is wanted. Measured over five lines: `--maxfail` stops matching; `@pytest.mark.xfail`, `# type: ignore[misc]`, `pytest.skip` and `@skipif` all still do.

## Captured, not fixed

- **`PL-YRYR`** — ~50 s of the suite's serial cost is per-test git fixtures in `test_cli.py` and `test_verify.py` (304 tests, no test above 1.3 s), with the shape a fix would take and what must not be traded for it.
- **`PL-STC4`** — the suppression check reads prose where the assertion check beside it does not: 12 of the 13 lines it flagged on `PL-VHVJ`'s own branch were an item brief, a comment or a docstring naming the thing being fixed. `ASSERTION_BEARING_SUFFIX = ".py"` and `PL-7TYC` are the precedent one function away.

**`PL-09G9` and `PL-VHVJ` still self-audit `REJECT`, on `PL-STC4` alone** — their diffs necessarily contain the word `xfail`, because that is what they are about. `PL-FZ58` audits `ACCEPT`.

## Checks

`make check` green. Roadmap disposition recorded for `PL-09G9` under the existing `### Declined to Gate 2` heading (a second one would orphan the other 174), on its own ground: its problem postdates the 2026-09-06 freeze, so it fails the presence test outright rather than being deferred despite passing it.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01EgZsiLDbaCtdJZD2uyVQge

---
_Generated by [Claude Code](https://claude.ai/code/session_01EgZsiLDbaCtdJZD2uyVQge)_
