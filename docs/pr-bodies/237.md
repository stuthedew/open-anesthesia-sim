---
pr: 237
commit: 184456abb9dca7984223ffc41d44a2ac977e2ee9
merged: 2026-09-02
items: PL-BR4G, PL-MVC2
subject: PL-BR4G, PL-MVC2: close the two items #236 left open, and record #236 on the four it closed (#237)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 184456ab landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follow-up to #236, which merged before these two closures reached it. No new
behaviour — this is the bookkeeping #236 owed, plus two test renames.

## `PL-BR4G` and `PL-MVC2` were left at `ready` with their work landed

Both were implemented in #236 and never closed. What is interesting is how they
were caught: by the error `PL-71P4` added in that same pull request, which this
is the first finding of.

Their `verify:` commands still *failed*, because each named a test its brief had
commissioned and the tests had been written under near-synonyms:

| Item | Commissioned | Written as |
| --- | --- | --- |
| `PL-BR4G` | `test_a_repeated_front_matter_key_is_an_error` | `test_a_duplicate_front_matter_key_is_an_error` |
| `PL-MVC2` | `test_a_class_outside_the_declared_vocabulary_is_an_error` | `test_a_class_outside_the_vocabulary_is_an_error` |

**The tests were renamed to the commissioned names, not the commands rewritten
to match what was built.** That direction is the whole of `PL-L9JS`: a `verify:`
edited to fit the work proves nothing, while a test renamed to fit the
commission leaves the commission intact. Renaming was the right call here rather
than a judgment call — the briefs stated their acceptance criteria in advance,
and the names they chose are good ones.

Both items now carry their close-out reasoning: why `PL-BR4G` kept last-wins in
`parse_front_matter` rather than switching to first-wins (either choice picks a
winner; picking one *silently* was the defect), and why `PL-MVC2`'s feature half
was scoped to mechanical variants only.

## `pr: 236` recorded on the four items that pull request closed

`bin/docket check` raised its post-merge advisory for `PL-71P4`, `PL-L9JS`,
`PL-T63T` and `PL-V87X` — each closed on the branch carrying its work, so the
number could not be known until #236 existed. Writing it in puts the pointer in
the file rather than only in git history.

`PL-68XK` carries none: it was dropped rather than done, so there is no change
to point at. `PL-BR4G` and `PL-MVC2` are left empty deliberately — their closure
is in *this* pull request, and the same advisory will name this number once it
merges.

## Checks

`make check` green — ruff format, ruff lint, mypy, the full test suite,
`bin/docket check` at 0 errors, `doc_check.py` at 0 errors, contrast unchanged.
One advisory stands and is expected: three `ready` items predating the `verify:`
requirement, unrelated to this change.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018rVJTxSXWM8jdcndjU2p6s

---
_Generated by [Claude Code](https://claude.ai/code/session_018rVJTxSXWM8jdcndjU2p6s)_
