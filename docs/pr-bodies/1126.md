---
pr: 1126
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeG9f2SH649zw5rBznNuvz1iP)_

Before: `bin/docket arm` read only `HEAD`. When someone else moved the pull request's branch on the remote, it answered for a commit the pull request had moved past. After GitHub's *Update branch* it said `behind 1` of a pull request already level with `main`, and advised `update_pull_request_branch` or a merge and push, both of which misfire. After a second writer pushed a commit outside the store onto the branch, it answered `arm` for a pull request landing that path. The brief had only inferred that second case; the new test reproduces it on `main`.

After: `arm` first reads the branch's copy on the remote. Where `HEAD` lacks any of its commits, the answer is `unknown`, or `hold` where a hold already stands, and the line after the answer says how many commits and what brings them in: `git pull --ff-only` from origin's branch, or `git pull --no-rebase` where `HEAD` has commits of its own. Once they are in, it answers from `HEAD` as before.

How: `arming.arm` takes one `git ls-remote --heads` listing after its fetch, as `PL-MT3R` decided for every reader of the remote's copy, and hands it to `holdings` as well, so a tracking ref for a branch the remote deleted is neither the branch's copy nor a holder. Under `--no-fetch`, or after a failed fetch, it reads the tracking ref and names it as that. `merge-base --is-ancestor` exiting 0 or 1 is the answer; anything else, such as 128 for a tip this clone has not fetched, is said as unread and never as "HEAD holds it", which is the point `PL-C3MN` left for this item. The remedies pull from the remote rather than merging the tracking ref, so one run against a deleted branch fails loudly.

Tests: five real-git cases in `subprojects/docket/tests/test_cli.py`, all failing on `main` (`behind`, `arm`, `behind`, `arm`, `hold`). The *Update branch* merge, a second writer's commit outside the store, and a diverged branch each answer `unknown`, fetched and under `--no-fetch`, then `arm` or `hold` once pulled. A tip this clone has not fetched answers `unknown`. A branch restarted from the base after its old copy was deleted on the remote answers `arm`, rather than holding on the deleted copy's claim.

Verification: `make check` passes locally (5,073 tests), and `bin/docket verify --self PL-21KN` answers ACCEPT on the tree merged with `main`.

`bin/docket arm` holds this pull request because it changes `arming.py`, the gate itself. The only way it loosens the gate is by dropping a hold read from a branch the remote deleted, which no pull request can carry.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01DX3KuPCeXwSbfhj4mBMZCd
