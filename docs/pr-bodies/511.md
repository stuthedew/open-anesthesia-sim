---
pr: 511
commit: 56bdaa7eede3c9158e7c49705da893fe6af91dd4
merged: 2026-09-12
items: PL-T6F9
subject: PL-T6F9: cut v0.4.15 - the release where the checks stopped over-reaching, and Gate 1 passed half (#511)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 56bdaa7e landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts v0.4.15: version bump, `uv.lock`, the 33 stamped items, `docs/releases/v0.4.15.md`, and the `ROADMAP.md` prose that nothing generates.

## What the release is

Ten of the 33 entries are one defect in ten places — a check answering a question it could not decide, answering it confidently, and charging a session for the difference. Four are in `doc_check` and `docket check` (#506): the top-band advisory prescribing a demotion `docket check` itself rejects, on a band where 12 of 13 items are class-pinned; a shell regex in a `verify:` field read as LaTeX; any double-quoted phrase read as a section citation; a candidate line attributed to the alphabetically first matching term. Six are the `vcs.py` ref-lifecycle cluster closed as one root-cause round (#504). `PL-W1LN` is the judgment running the other way — a guard's blind spot reproduced, accepted and pinned rather than papered over — and `PL-3833` adds the one check that was missing, as an advisory, because its remedy needs judgment a tool does not have.

Nine more entries are the documentation stating something untrue (#507), two safety- and three science-classed, mostly in `docs/MODEL.md`.

Nothing a learner can observe moved. `src/anesthesia_sim/core/` and `app/` are byte-identical to `v0.4.14`; the whole of `src/` changes in one provenance note's wording.

## Two judgment calls worth a reviewer's eye

**The gate numbers are in the item, not in `ROADMAP.md`.** `bin/docket wave` reads 56 of 158 entries cleared at the `v0.4.14` tag and 80 of 159 at this cut, so Gate 1 crossed half inside this range. The baseline section states the crossing and points at `bin/docket wave` for the split, because the frozen-list section's own convention forbids writing a closed-count into the document — and `PL-WSDY`, shipping in this same release, closed the last release narrative that carried one. The item's brief asked for the count in the prose; the convention wins, and the measurement is recorded in the item instead.

**Two numbers in the item's brief were wrong and are corrected there.** The `P1` band fell 11 → 8, not 13 → 8 (13 was its size on 2026-09-03, carried forward unchecked); and the over-reach count is four for #506, ten for the release.

## The cut had to be run twice

An interrupted `make release` left 26 items stamped `milestone: v0.4.15` with the version unbumped and no notes file. The re-run saw only the 7 still-unstamped items and produced a perfectly ordinary-looking 7-item release — nothing in its output distinguishes that from a correct cut. Reverted and cut once, uninterrupted; the notes now name all 33, verified against the 33 stamped items. Filed as `PL-1MKQ`, with `PL-D4GS` for `PL-3833`'s new slug-drift advisory firing on 8 files no item owns.

## Checks

`make check` green: 2388 tests, 100% coverage, 0 documentation errors, `docket check` 0 errors.

After merge, the tag is the project owner's to push — a session's tag push reports `[new tag]` and then fails (`PL-N936`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01HUFiefBEPqBD68F88XFkiQ

---
_Generated by [Claude Code](https://claude.ai/code/session_01HUFiefBEPqBD68F88XFkiQ)_
