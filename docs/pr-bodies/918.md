---
pr: 918
commit: f1e0008711cf0a5c0f4984df2d7a9bff6ea3da13
merged: 2026-09-22
items: PL-WFFX
subject: PL-WFFX: record the owner's two merge clients and GitHub's public report that the app drops squash bodies (#918)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit f1e00087 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

This records the project owner's answer to PL-WFFX's open question, plus two findings, so that the answer lands on `main` rather than living only on a branch. It changes one file: `docs/items/PL-WFFX-name-the-merge-client-that-sends-an-empty.md`. No code or behaviour changes.

## The answer

Asked which client merges pull requests, the owner answered on 2026-09-22: "Website and app on iPhone." This sits beside their 2026-09-20 answer already in the brief: "I use github app to merge when away and do use auto merge at times."

## Two findings

**The committer timestamp's offset records the merge route, not the client.** I counted the 806 squash commits on `main`, bodies counted net of trailers:

| Committer offset | Body lost | Body kept |
| --- | --- | --- |
| `+00:00` | 9 | 119 |
| `-05:00` | 192 | 486 |

The nine lost at `+00:00` match the nine auto-merges the brief already records as sending an empty message. The 28.3% lost at `-05:00` matches its direct-merge rate. A direct merge carries the owner's local offset whichever client made it, so the git history still cannot name the client.

**GitHub's own forum documents the defect:** [community discussion #51016](https://github.com/orgs/community/discussions/51016), "Squash and Merge on Github Mobile doesn't match the same on web", opened 2023-03-25.
- There is no obvious prompt to confirm the message, and the squash body is omitted.
- GitHub staff said the message editor is behind a cog icon (2023-04-04), cited an internal issue (2024-05-10), and said a fix was in progress (2025-07-09).
- The latest reply (2026-09-16) reports every squash merge that month landing with no body.

That matches this repository's pattern (intermittent, sitting-pure, away from the desk), though it isn't an observation made here.

## The recommendation written into the brief

This is for the owner to decide: adopt the habit now, rather than wait on the one-merge test.
- Squash-merge, and enable auto-merge, from the website rather than the GitHub app.
- On a phone, check the message box holds the pull request's description before confirming.
- If the app is the only option, open the cog and confirm the message first.

The one-merge test becomes optional confirmation, and `tools/pr_body_check.py` stays as the backstop. Once the owner agrees, the habit goes into `docs/maintainer.md` and the item closes. The item stays at `needs-decision` until then.

## Checks

- `make docket` passes.
- `python3 tools/doc_check.py check` passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01W5c544ocp9JdN7HhkyeF6n

---
_Generated by [Claude Code](https://claude.ai/code/session_01W5c544ocp9JdN7HhkyeF6n)_
