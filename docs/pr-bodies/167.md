---
pr: 167
commit: afa81372dbc4fb34566e262a33fa1fd3cceaf69d
merged: 2026-09-01
items: PL-768D, PL-99Y4
subject: PL-768D record pr 166, and PL-99Y4 capture the red-main class behind it (#167)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit afa81372 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Store-only, two commits. **Merging this prevents the next red `main`.**

## `main` was red twice today

Runs on `97573ae` and `3b37a75` both failed on `bin/docket check`:

```
Errors (the store is wrong; fix before committing):
  PL-PF8H-...md: marked done on `origin/main` but records no `pr`; without it
  there is no way back from the closure to the work that made it
```

`PL-PF8H` closed in #162 with no `pr:` — which is the shape the `docket` skill *prescribes*, so the closure ships in the same commit as the work and the merge race that lost `PL-D2GW` cannot reopen.

On its own merge commit `c663c13` the number was recoverable from the merge subject, so the check raised an advisory and CI stayed green. **One merge later it was not.** `.github/workflows/quality.yml` runs `actions/checkout` with no `fetch-depth`, so it defaults to 1, and the commit naming #162 had scrolled out of the clone. Advisory became error, and `main` went red one commit *after* the closure landed rather than at it.

It went green again at `8836845` only because #165 happened to write `pr: 162` and `pr: 164` in as tidy-up of what the local tool calls a grooming advisory. The repair was accidental.

The run history:

| head | outcome | why |
| --- | --- | --- |
| `c663c13` (#162) | success | PF8H's merge is HEAD, number recoverable |
| `97573ae` (#163) | **failure** | it dropped out of the depth-1 clone |
| `3b37a75` (#164) | **failure** | still out |
| `8836845` (#165) | success | that PR wrote `pr: 162` in, by accident |
| `055d1ca` (#166) | — | `PL-768D`'s merge is HEAD, so still only an advisory |

## What this PR does

**`pr: 166` into `PL-768D`**, verified against the merge subject on `origin/main` (`055d1ca`). Its merge is HEAD today, so it is currently an advisory — and would become an error on the very next merge. This is the one-line fix that disarms it.

**`PL-99Y4`** captures the class, since the fix above only defuses this instance. Every item that closes under the prescribed workflow lands with an empty `pr:` and turns `main` red on the next merge unless a follow-up beats it there. 78 items are open.

## The deeper defect, for triage

`docket check` **already** declines to verify *recorded* pull requests on a shallow clone, and says so in its own output: *"the checkout is a shallow clone, so the commits it is missing are the oldest ones and the longest-settled provenance would read as broken."*

The *missing*-`pr` check faces the same limitation and escalates to a hard error instead. Same clone, same unanswerable question, opposite treatment — and that inconsistency is what turned `main` red. `PL-99Y4` records two complementary routes: deepen the CI checkout (one line, and it buys back two checks CI currently skips), and make the missing-`pr` check report "cannot answer" rather than "the store is wrong" when the history is too shallow to hold the merge commit.

## Checks

`make check` green: ruff, mypy, 883 tests, `bin/docket check` 0 errors, `doc_check` 0 errors.

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01JZNs4wWcM7NndyffsN5tgV

---
_Generated by [Claude Code](https://claude.ai/code/session_01JZNs4wWcM7NndyffsN5tgV)_
