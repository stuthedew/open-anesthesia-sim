---
pr: 531
commit: 465733af30120780bba08dcdd37ca3988fc30a1f
merged: 2026-09-13
items: PL-XJ5P
subject: PL-XJ5P: adopt the private reference corpus as the route, and say what a miss means (#531)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 465733af landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-XJ5P`. Unblocks `PL-5NR5`, which is `blocked-by` it.

## The answer is a candidate the item does not list

`PL-XJ5P`'s *Decision needed* offered three routes for a terminal source: record the gap and ask the owner; carry an owner-supplied extract; or re-aim the item at something reachable. `PL-5NR5` found a fourth while this item sat open — a private companion repository, `stuthedew/open-anesthesia-sim-references`, attached per session with `add_repo` and cloned through the GitHub proxy.

It beats all three for a reason none of them could reach: **it is storage.** The middle candidate had been exercised three times by today, and every time the reading survived only as prose in an item. That route scales with *sessions*; this one scales with *sources*.

## Verified before the rule was written, not from the documentation

`add_repo` → `git clone --depth 1` → `rev-parse`, end to end from this environment. The clone authenticates through the proxy with no token in the sandbox and the environment stays Trusted. The corpus holds four sources, including Mapleson 1963 (PMID 13932730) and Smith, Zwart & Beneken 1972 (PMID 5050101) — **two of the seven terminal cases this item measured**.

Writing a rule that points at a repository nobody had opened would have been the same defect the item is about.

## What landed

- **`.claude/rules/citing-sources.md`**, under the fourth limit: what a session does on reaching the terminal state — attach the corpus, with the exact commands and the `poppler-utils` / `pdftotext -layout` note — and, as importantly, **what a miss means**. The corpus holds what the owner has supplied, not the literature, so a source not among its holdings has genuinely run out of route: record the gap and put the reading to the owner. The two exits this item was filed on — narrowing the claim until something reachable supports it, and taking a search summary for a reading — are named and refused there.
- **`docs/references/README.md`**, a section beside Redistribution: what now stands where owner-supplied full texts used to, what may lawfully cross back (facts and citations, per *Feist* at 344–45 and 17 U.S.C. § 107, never a table in its published arrangement), and that reading a source owes an extraction note in that directory.

## Deliberately not decided here

What an extraction note *contains*, and the first worked example, are `PL-Z3V5`'s — live on another branch. This item settled only that the obligation exists and where it lives, both of which sit in its own `touches` and neither of which `PL-Z3V5` could settle without them. That split is stated in both the rule and the item so the two sessions do not collide.

## Checks

`make check` green: 2506 passed, 0 store errors, 0 documentation errors. The close-out `verify:` was checked both ways — both greps fail against `origin/main`'s pre-work copies and pass on this branch.

Riding this commit: `pr: 529` on `PL-WHG6`, which `bin/docket record` was owed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01DojjYosF555mysW7636Xhw

---
_Generated by [Claude Code](https://claude.ai/code/session_01DojjYosF555mysW7636Xhw)_
