---
pr: 359
commit: 260632ea2a95853461d877fc992185fd7879a1ac
merged: 2026-09-05
items: PL-J7Y7
subject: PL-J7Y7: cut v0.4.1, the apparatus patch (#359)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 260632ea landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Eleven items since v0.4.0.

## Safety statement

**No shipped code changed at all.** `src/`, `tests/` and `docs/MODEL.md` are **byte-identical to v0.4.0**, so no equation, parameter, numerical method, unit or displayed value moved, and the v0.0.2 circuit, v0.1.0 sevoflurane and v0.2.0 multi-agent reference cases pass against exactly the code that validated them.

Changed: `.github/workflows/`, `CLAUDE.md`, `ROADMAP.md`, `docs/items/`, `docs/resident-instructions.md`, `docs/consultant-brief.md`, `subprojects/docket/`.

This is the fourth release to change nothing shipped, after v0.2.5, v0.2.9 and v0.3.5.

## Why it exists

It comes straight after the largest capability release the project has had, and that ordering is the point rather than an accident: cutting v0.4.0 walked the plan end to end for the first time in weeks and found it out of agreement with the tooling that reads it. **Eight of the eleven items exist because of that walk.**

**This is not the exact matrix exponential.** That work — planned-milestone item 29, "the code is the model" — is the `v0.4.x` track's content and has not started. This patch is the ground being cleared in front of it.

## What it carries

**The v0.4.x track settled** (`PL-YYL2`, `PL-JJ8P`). `bin/docket wave` was classifying the `v0.4.1` row as a *milestone*, and a milestone freezes a debt gate when scoped — that gate computed to **105 entries against Gate 0's 21**, five times the largest this project has cleared, in front of a patch. The row is now `v0.4.x` and the beat reads *scope v0.5.0*, which is what the timeline always intended. The score architecture (`PL-T691`, `PL-2FM6`, `PL-P1Z3`, `PL-8LXM`, `PL-49R8`) is placed in v0.5.0 — where forking needs it, and where `PL-011`'s dropped retention debt is actually paid. The track also stopped promising a specific patch number, which this release taking `0.4.1` demonstrates working as intended.

**A check that reported clean over a real edge** (`PL-GGCN`). The advisory catching a prose prerequisite never declared in `blocked-by` read only the *first* id after a cue, so "blocked on A and on B" was checked for A and never B. `PL-VZL0` is the live instance and nothing fired.

**Three contradictions in the next release's own briefs** (`PL-BWTB`, `PL-R95V`, `PL-KBJT`) — each would have stopped a worker on day one, including a two-item deadlock where `PL-GS5X` and `PL-X9KD` each waited on the other.

**CI bounded** (`PL-FSH9`, `PL-FFLL`, `PL-QD9K`). Every job now carries `timeout-minutes` against the 360-minute platform default, chosen past any plausible honest run because the failure being bounded is a hang rather than slowness. Superseded pull-request runs are cancelled — for the runner slot, not the billable minute, since the repository is public and standard runners are free.

**The apparatus got a stated bar and an outside seat** (`PL-9J2W`, `PL-1H3H`).

**v0.4.0's own cut** (`PL-647D`), which cannot be inside the release it names.

## Verification

`make check` green: **1815 tests, 100% coverage, 0 errors**, advisories unchanged at 3. `bin/docket record` rode the commit, writing the eight pull-request numbers the base was owed.

## After the merge

```
git tag -a v0.4.1 <merge commit> -m "v0.4.1"
git push origin v0.4.1
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YJuqLCJFfLaXiCDEQPHxxZ

---
_Generated by [Claude Code](https://claude.ai/code/session_01YJuqLCJFfLaXiCDEQPHxxZ)_
