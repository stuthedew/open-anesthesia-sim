---
pr: 1125
recorded: 2026-09-26
---

Before: `ROADMAP.md` and `docs/interface-provenance.md` said, in the present tense, that every `blender.org` host is refused by the session egress proxy, and `docs/worker.md` told every worker the proxy rejects publishers and indexes outright. A session repeated the first to the project owner without probing, though the owner has `blender.org` and `*.blender.org` on the environment's allowed domains. The owner's words: "This is an issue if you assume things are blocked when they aren't."

After: rule 14's re-verification bullet in `.claude/rules/instruction-writing.md` names a host's reachability as external state, probed before a reply repeats that it is blocked, with only a refusal at CONNECT counting as a block. Each stale statement now says when the block was seen and that it no longer holds.

How:

- `.claude/rules/instruction-writing.md`: one sentence at the end of rule 14's re-verification bullet, 326 characters of prose. Nothing resident is cut for it: the bullet's general sentence is the rule this applies, and it did not fire, because a block written into a checked-in file reads as a fact about the tree. `docs/resident-instructions.md` records the carrier test. The owner's request lifts the generator pause for this rule (`PL-6Q9L`).
- `ROADMAP.md`: the `PL-PV5Q` bullet in v0.6.0's out-of-scope list, and planned-milestone item 34's note on the manual pages.
- `docs/interface-provenance.md`: the evidence paragraph on the prose, and the warning that opens § "What the code cannot answer". Both now say the hosts were refused when written (2026-09-15) and answered on 2026-09-26; `web.archive.org` is still refused.
- `docs/worker.md` § "Reaching a source": the proxy allows some publishers and indexes and refuses others, host by host, from a list no session can read, so probe first; a `403` after the tunnel opens is the publisher's own. A dated line records what answered on 2026-09-26.
- Two open briefs, declared in this item's `touches`: `PL-PV5Q` (re-read Blender's stated rationale at the source) records that its pages can be read now. `PL-P5NB` (the doi.org-blocked note in `docs/references/README.md`) records that Crossref indexes the Jugel et al. paper as `10.14778/2732951.2732953`, so its planned wording, that no reachable registry indexes the venue, would now be false.

Probes on 2026-09-26, reading curl's `%{http_connect}`: 200 at CONNECT for every `blender.org` host, `doi.org`, `api.crossref.org`, the NCBI hosts, Wiley, Springer and ScienceDirect (Wiley and ScienceDirect then answering 403 themselves); 403 at CONNECT for `web.archive.org`, `www.w3.org`, `www.vldb.org`, `doc.qt.io`, `www.gstatic.com`, `journals.lww.com`, `pubs.asahq.org` and `www.bjanaesthesia.org`. The present-tense claims elsewhere in the tree about `w3.org`, `gstatic.com` and `vldb.org` are still true and stay as written.

Verification: `bin/docket verify --self PL-M701` ACCEPT, which runs the item's `verify:` command and `make check` (5,068 tests passed); `tools/doc_check.py candidates` finds no documentation naming anything the diff changed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015yyB5vqeNrV2Efg477HcWu

---
_Generated by [Claude Code](https://claude.ai/code/session_015yyB5vqeNrV2Efg477HcWu)_
