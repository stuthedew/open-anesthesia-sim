---
pr: 233
commit: ac8c1ccd87e573b67b30233aa9d3ec6a75d0c6fa
merged: 2026-09-02
items: PL-QTSB
subject: PL-QTSB: capture the advisory path that has no in-flight check (#233)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ac8c1ccd landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

One captured item, opened as a pull request rather than left on the branch because `PL-Q8QX` was permanently lost off a branch and this session is otherwise finished.

`PL-5KR2` routed the in-flight answer from `docket next` into `show`, and later into `triage`. A third surface was missed: a **grooming advisory** names an item and tells a session to edit it, and nothing on that path checks whether another session is already doing so.

Both halves were measured today, hours apart, by this session:

- **Unguarded, it duplicated.** `docket check` said to write `pr: 227` into `PL-2XTF`. This session did, in #230; another session read the same advisory and did the same in #229 — one file, one insertion, byte-identical. Git auto-merged them only because the tool dictated the content.
- **Guarded, it did not.** The same advisory then named `PL-D1ST`. `bin/docket show PL-D1ST` answered `IN FLIGHT on a branch`, and the backfill was left alone.

Nothing about the advisory changed between the two. That is the argument for wiring the answer into `docket check` — which already receives the flight report for `_offered` (`PL-3576`) — rather than adding a rule a session has to remember at the moment it has been handed what looks like a chore rather than an item.

Not covered by `PL-MC8Z` (neither session was starting an item) nor by `PL-D4MZ` (the recommendation came from a tool both sessions ran locally, not from a reply anyone could reserve against).

`make check` green: 1152 tests pass, 0 store errors, 0 documentation errors.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Uk6BxpCwS8QoBwjL7mVocn

---
_Generated by [Claude Code](https://claude.ai/code/session_01Uk6BxpCwS8QoBwjL7mVocn)_
