---
pr: 549
commit: 6cd1a1da85a21526b0129a44c64a7731c12bbe3e
merged: 2026-09-13
items: PL-4KKR
subject: PL-4KKR: cut v0.4.21 - the coefficients checked against the measurement, and a specification that argues backwards (#549)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 6cd1a1da landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts v0.4.21 over the twenty-one items finished since v0.4.20. Two idle sessions closed by asking whether to cut it; this is the answer to them rather than a third independent offer.

## What the release contains

**The stored coefficients now answer to a primary human measurement.** The project owner supplied Yasuda, Targ and Eger's 1989 tissue-solubility full text (Anesth Analg 69(3):370-3, PMID 2774233). `PL-B9K7` checked its Table 1 against the nine stored tissue:gas coefficients: all nine inside 0.71 SD of the measured mean, two of them the measurement itself. `PL-FN5F` split the decision that follows and the owner took both halves - adopt the paper, do not store its figures - so one boolean moves and no value does. `PL-8GJ6` closed by dissolving; `PL-LS3H` quantifies what the ideal inert circuit omits, from the same authors' companion paper.

**The specification learned to argue backwards.** `PL-BLHV` writes the positive intended-use statement `docs/MODEL.md` never had, drawing the exclusion at the data rather than at physical proximity, and records Class C uniformly. `PL-FDBK` adds the first hazard table - six rows, each naming the test that holds its mitigation, with `check_named_tests` keeping that column from decaying into decoration.

**Fourteen apparatus items, nine of them a command asserting something untrue**: a commit count for a branch with none, a diff line that had been removed, a checkbox contradicting the figure above it, a rules block missing a rule it presented as complete, an argument order that swallowed the title, a placeholder a shell read as redirection, a recovery that cleared the wrong ref, a beat calling a finished milestone unfinished, and a feature survey with no plan placement.

## Measured, not quoted

Two claims in the release prose were checked here rather than taken from the items:

- `src/anesthesia_sim/app/` and `tests/reference/` resolve to the same tree objects at v0.4.20 and at this release (`7586231`, `e519afd`), so no interface changed and no pinned reference state was recomputed.
- Every numeric leaf of all five files under `src/anesthesia_sim/data/` is identical across the two trees - zero differences - so "no stored number moved" is a measurement rather than a reading of the items' own summaries. `src/anesthesia_sim/core/` changed by one docstring.

## Mechanics

- `make release VERSION=0.4.21`: `pyproject.toml` bumped, `docs/releases/v0.4.21.md` written, `milestone: v0.4.21` stamped onto all 21 items, `uv.lock` relocked.
- `ROADMAP.md`: version-table row added, the `current baseline` mark moved off v0.4.20, baseline section replaced with this release's long form.
- `bin/docket record` rode the cut commit, writing `pr: 547` to `PL-BLHV`, `PL-FDBK` and `PL-NBCS`, which had closed on `main` without one.
- `make check` green: 2615 tests, 100% coverage, `docket check` and `doc_check` 0 errors.
- Docs swept: `ROADMAP.md` (edited), `docs/releases/v0.4.21.md` (read back), and the eight candidate lines `doc_check candidates` named are each about the release mechanism rather than which version is current. No document outside `ROADMAP.md`, `docs/releases/` and `docs/items/` names 0.4.20.

## Outstanding after the merge

The tag is the project owner's to run - a session cannot push tag refs here (`PL-N936`). The three commands are in the reply that accompanies this pull request.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LxWLAwijmg3muaDLQ1JELy

---
_Generated by [Claude Code](https://claude.ai/code/session_01LxWLAwijmg3muaDLQ1JELy)_
