---
pr: 354
commit: a52eb4fdaa498263874f9c0d8dfb05c3d6697c33
merged: 2026-09-05
items: PL-1H3H, PL-9J2W
subject: PL-9J2W, PL-1H3H: state when apparatus investment is correct, and add a simulator-first consultant brief (#354)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit a52eb4fd landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two changes. The second was the reason for the branch; the first turned out to matter more.

## PL-9J2W — the repository was manufacturing a critique it never answered

`apparatus` appeared in `CLAUDE.md` three times and every one was a warning or a demotion:

- `CLAUDE.md` § "What this project is" — "at **permanent risk of becoming the work instead**"
- `CLAUDE.md` § "Proactive expert review and domain best practices" — "a **lower and different bar**… where the two compete for a session, the simulator wins"
- `.claude/rules/apparatus-standard.md` — polishing it "is **the most common way this project wastes a session**"

And the one sentence arguing for investment is scoped away from it by its own words: *"internal quality **in the simulator** is worth paying for."*

So a session that computed the product/apparatus ratio had **three warnings and no counterweight**. It reliably concluded the project was overinvesting in tooling and said so. The owner then supplied the missing half by hand — that this is a solo hobby project on a multi-year horizon that must not become unmanageable, and that he needs help managing it — the session conceded, and it was never written down. The next session re-derived the same objection.

That is `CLAUDE.md`'s own compounding-friction test met on its second limb: the finding **is being routed around**, and the routing-around is the owner re-explaining.

**What is written is the test, not a defence.** The general objection is foreclosed; the specific one — name the mechanism that should not have been built and what it cost — still lands. The ratio may genuinely be wrong (`dev-tooling` is the largest feature in the queue by a wide margin), and a paragraph that immunised the project against hearing so would be worse than the argument it replaces.

The "permanent risk" sentence is **unweakened**. What the new text rejects is *share of the queue* as the instrument for detecting that risk. Both were re-read together after the edit.

Resident instructions grow **571 characters** (43,974 → 44,545), which `doc_check` reports as an advisory. The argument for resident rather than routed is recorded in `docs/resident-instructions.md`, under the group that already covers this section: the judgment happens while a reply is being composed, with no read before it.

## PL-1H3H — the consultant brief, re-pitched

`docs/consultant-brief.md` is a document pasted into the first message of a fresh session, so big-picture review has a seat outside the queue workflow. Not a skill and not a subagent, for three reasons verified against the tree:

- `.claude/rules/instruction-writing.md` claims precedence over "a project file, **a skill**, a saved preference" for reply shape — a skill-carried review returns in the house format. A pasted prompt arrives as a user message and outranks it.
- `.claude/rules/apparatus-standard.md` fires on `/.claude/**`, `/tools/**`, `/subprojects/docket/**`, so a consultant reviewing the apparatus loads, by reading the thing under review, a rule saying that tree is not worth reviewing. The brief names it and disregards it.
- `tools/doc_check.py`'s `measure_resident` walks only `RESIDENT_ROOTS` and `RULES_DIR`. A skill's description sits in every system prompt where the project's own growth instrument cannot see it.

**Re-pitched before merge on the owner's correction.** The first draft made project direction the subject; what he valued was a deep simulation-architecture review. The spine is now the model, the numerics, the chart and the interaction, with the audience and the plausible-wrong-number asymmetry stated up front. Project-direction questions moved to an optional tail marked deletable. A new § "Objections already answered" carries the too-much-tooling objection and the test it must meet. **The apparatus-ratio question was removed from the question list** — it was the line that manufactured the objection this pass exists to stop re-litigating.

The brief itself adds **zero** resident characters.

## Checks

`make check` green on the merged head: ruff format/check, mypy, `ignore_check`, 1811 tests at 100% branch coverage on `core`, `docket check` 0 errors, `branch_id_check`, `rules_paths_check`, `doc_check` 0 errors, `contrast_check`, `import_boundary_check`.

Both items' `verify` commands were watched failing for the right reason before being recorded, and pass now:

- `PL-1H3H` — `python3 tools/doc_check.py check && grep -q 'apparatus-standard.md' docs/consultant-brief.md`
- `PL-9J2W` — `python3 tools/doc_check.py check && grep -q 'not judged by its share of the queue' CLAUDE.md`

Documentation swept: `doc_check candidates --base origin/main` reports nothing in the documentation mentions anything this diff changed. `docs/ARCHITECTURE.md` enumerates `src/` and `tools/` trees but no `docs/` tree, so no line is owed there. `docs/resident-instructions.md` was updated because it is the ledger for exactly this decision.

An earlier `pr-title` failure on this branch was real and self-inflicted: the second commit added `PL-9J2W`, so the branch closed two items while the title named one. Reproduced locally against `tools/pr_title_check.py`, fixed by renaming the pull request, and green since.

`main` moved twice under this branch — #355 cutting v0.4.0, then #353's queue changes from the simulation-architecture design round. Both merged in rather than rebased, no conflicts, and `make check` re-run green after each.

Also carries `docket record`'s `pr: 351` (PL-LLWN) and `pr: 352` (PL-RCTQ) from the first commit.

## Captured, not fixed here

- `PL-JQVB` — `CLAUDE.md`'s four dispositions for a new rule list no carrier for a paste-able brief or an agent definition, and the skill carrier's cost is invisible to `measure_resident`.
- `PL-WGXJ` — `apparatus-standard.md` structurally exempts the apparatus from the expert-review standard, so the largest part of the tree has no review bar. `docs/WORKING_NOTES.md` already flags this unresolved.
- `PL-4H01` — a block-by-block audit of the resident set against all six tools, all four hooks and the docket skill. Five routing candidates totalling ~1,980 characters; three defensible at **563, or 1.3%** of 44,545, all with carriers that already exist. Records the two I would not move and why, and the four ledger refusals I re-read and did not reopen.
- `PL-NJTZ` — `CLAUDE.md:205` gives checks a retirement test; resident rules have only a protection against removal and a refused ceiling, so the set can only grow (35,721 → 44,545 characters in five days). The ceiling refusal stands; it addressed numeric limits and never considered the qualitative test checks already get.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Y42DM88Gc4BV24NjRboMW8
