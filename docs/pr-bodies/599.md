---
pr: 599
commit: f3c83ebf9c801f13390414aac11b85ed91300809
merged: 2026-09-15
items: PL-B5VM, PL-S5YM, PL-Y1W6
subject: PL-Y1W6: drop as a duplicate of PL-B5VM, and correct PL-S5YM's brief (#599)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit f3c83ebf landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`PL-Y1W6` (main is red on `PL-S5YM`'s stale selector). **Dropped, not done** — `PL-B5VM` landed the same repair in `#598` while this branch was open.

## What happened

This branch was pushed at 22:13 UTC with a rewrite of `PL-S5YM`'s `verify:` command. `#598` merged at 22:59 with a different rewrite of the same line, and `PL-7VSK` is a third independent capture of the same defect sitting on an unlanded branch. **Three sessions reached the same diagnosis inside an hour**, which is what an unowned red `main` produces: the digest names it in every session, no `PL-` id claims it, so every in-flight guard reads it as nobody's work. `PL-66FP` is the same shape one level up — two sessions cutting one release — and its lesson held here too: the duplicate surfaced at the merge, not before it.

Merging this branch as it stood would have **reverted a proven change**, so both conflicts resolve toward `main`:

- `PL-S5YM`'s `verify:` takes `origin/main`'s line verbatim — `PL-B5VM`'s, not this branch's.
- `PL-Y1W6` becomes `status: dropped` with a `reason` naming `PL-B5VM` and `#598`. Never deleted: the reason is what stops the finding being re-raised, and there is already a third copy of it in flight.

## What survives, and why it is worth merging at all

Two findings `PL-B5VM` did not make.

**1. `PL-S5YM`'s brief is factually wrong, and still is on `main`.** It says of the covered-directory branch: *"None is exercised by `tests/unit/test_doc_check.py`"*. That was false when written — `test_childless_directory_covers_its_whole_subtree` has sat at line 208 since `PL-032`, asserting the positive direction against a fixture whose tree draws `harness/` bare with `run.py` under it.

`PL-B5VM`'s own brief repeats a narrower version of the same mistake: *"`covered_dirs` appears in `tools/doc_check.py` and nowhere in `tests/unit/test_doc_check.py`"* — true of the **symbol name**, not of the **behavior**, which that test exercises through `_errors(_repo(...))`. Left uncorrected, both documents tell whoever picks up `PL-S5YM` to write a test that already half exists. Only the negative direction is genuinely missing.

**2. `PL-Z9K5`** — `bin/docket record` re-serializes an item's whole front matter where its stored field order is non-canonical, so the diff is a `pr:` insertion **plus** a reordering, and `PL-ZYQC`'s "pr, not counted" exemption declines. Observed on this branch's own close-out audit: four files from one sanctioned `record` call, two exempted and two reported as outside `touches`. A residual gap inside `PL-ZYQC`'s fix rather than a duplicate of it.

## Also captured

**`PL-X0ND`** — quality run `#2000` on `25abc218` (the `#597` merge) was **cancelled 8 seconds in**, so that commit carries no verdict. `quality.yml` sets `cancel-in-progress` to `false` on `refs/heads/main` precisely to prevent this, citing `PL-QD9K`: *"Every commit on `main` keeps its own run."* The cancellation source was not read and the item says so — the run page names it, and the two readings (concurrency guard broken vs. a manual cancel) want opposite fixes. Filed as an observation to settle in one look, not as a diagnosis.

## Checks

`make check` green on the merge commit. Store clean: `bin/docket check` 0 errors.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01HnwpmKcbhTZjUjD8zVykzV
