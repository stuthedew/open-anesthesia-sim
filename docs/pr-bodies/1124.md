---
pr: 1124
recorded: 2026-09-26
---

A triage pass. It changes item files only.

**Triaged, with each fault reproduced on `origin/main` (`6efd8c41`) before its `verify:` was written:**

- `PL-HK4J` (tag line in a shallow clone): P2, ready. Reproduced in a depth-5 clone: the printed lookup names the clone's boundary commit for v0.5.11 and v0.5.9 instead of the cut. A post-close instance of `PL-QHCW`.
- `PL-M2NV` (`git push --mirror` / `--prune` pass the prune guard): P2, ready.
- `PL-R5N0` (floor guard misses `python3 -m pytest tests`): P2, ready.
- `PL-YFT4` (prune guard refuses harmless reads): P3, ready.
- `PL-Q9LK` (doc_check reads a workflow script one line at a time): P3, ready. A post-close instance of `PL-PVW2`.
- `PL-RX0W` (possessive quotation of a brief is read by no check): P3, ready. One-off.
- `PL-877T` (a new project's threads start outside `Default`): P3, ready, docs. The line goes in `docs/maintainer.md`.
- `PL-DCHW`, `PL-QMN0`, `PL-W9XN` (gate-guard spellings nobody writes): P3, blocked on `PL-61FT`.

**New generator head, `PL-61FT`, recorded live.** The three Bash guard hooks decide what a command does from its spelling. Five of the six guard fixes merged on 2026-09-26 filed the next spelling their sessions probed: nine members from six fixes, and none of the twelve was met in ordinary work. The item holds the decision about what the guards promise, and it recommends bounding them by use.

Left to the branches already carrying them: `PL-7VWK`, `PL-K9XQ` and `PL-X2XP` (claude/pr-body-storage-cnnpme), and `PL-YYV9` (claude/sweet-hypatia-yfwf2l).

Verified: `make docket` passes with 0 errors after the triage and after bringing in `origin/main`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QsGzeLTVKskjpo9v8miz8Y

---
_Generated by [Claude Code](https://claude.ai/code/session_01QsGzeLTVKskjpo9v8miz8Y)_
