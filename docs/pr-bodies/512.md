---
pr: 512
commit: bc21c320876d301a06f2e5c3e8dbdc0bf1a18437
merged: 2026-09-13
items: PL-B9VL, PL-D143, PL-D4GS, PL-KGNF, PL-V13T, PL-VZL0, PL-ZX12
subject: PL-B9VL, PL-D143, PL-D4GS, PL-KGNF, PL-V13T, PL-VZL0, PL-ZX12: the stale-name batch of Gate 1, and the triage behind it (#512)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit bc21c320 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Four Gate 1 entries, one rider they turned out to need, one owner decision applied, and the triage pass behind them. Gate 1 goes from 79 open of 159 to **75**.

The batch is one class of defect: **a name or a pointer in the simulator that no longer describes what it names.** None of it changes an equation, a parameter, a numerical method, a unit or a displayed value. `make check` and `bin/docket check --verify` are both clean; CI is green.

## The four gate entries

**`PL-ZX12` — `RunScore` is `RunDefinition`.** `core/run_score.py` held a run as its *score*, the musical sense: the written instruction set a performance is produced from rather than a recording of one. The metaphor is exact and it is also one a domain reader has to be taught, in the package `.claude/rules/core-domain.md` holds to reading like the domain without a translation step. The project owner decided the rename on 2026-09-08.

`ScoreSegment` is `RunSegment`, `score_segments` is `run_segments`, and the vocabulary moved with the identifiers — ~200 uses across docstrings, local names, twelve `test_a_score_*` names and the specification. Renaming three symbols and leaving the metaphor everywhere else would have answered none of the item's argument.

Deliberately not rewritten: `docs/releases/` is history, and `ROADMAP.md`'s completed rows keep their prose. The exception is two path citations in v0.4.8's row, and the project's own gate is what decides it — `doc_check` errors on a cited path that does not exist, confirmed by reverting them. The module docstring carries the join instead: what the word was, why it was retired, and that the release notes still speak it.

`PL-49R8` was blocked on this solely because its rule file and `verify:` both named `run-is-its-score.md`. Both now name `run-is-its-definition.md`, and it is unblocked to `ready`.

**`PL-B9VL` — the validation module names both directions.** `tests/reference/test_published_wash_in.py` carries $`F_A/F_I`$ at 30 minutes of wash-in *and* $`F_A/F_{A0}`$ at 5 minutes of elimination. Both names said only the first, so the only gate in the elimination direction was the hardest one in the suite to find.

The item counted five citation sites; there are **twenty-one files** — the module path is cited as widely as the section heading, and `doc_check` reads the queue and source docstrings too. Two things the checks caught: one citation wrapped across a line break so no single-line pattern matched it, and the sweep re-pointed the `verify:` of four *closed* items, which `bin/docket check` refused.

**`PL-KGNF` — `docs/MODEL.md` says "the agent" where it means any agent.** Isoflurane and desflurane have been selectable since v0.2.0 using these equations unchanged, which the document states twice in prose while the headings, symbol table and structural definitions around that sentence read sevoflurane-only. A reader running desflurane met a symbol table calling $`\lambda_{b:g}`$ the "Sevoflurane blood:gas partition coefficient".

Twenty-three sites, all structural. 113 mentions stay, each historical, a per-agent measured value, or provenance citing `data/agents/sevoflurane.json`.

**`PL-VZL0` — `core/` cites the section it implements.** Scope re-measured after `PL-GS5X`, as the brief asked: § "Selected method (as implemented)" now names two modules, and **no solved form needed restating** — the matrix assembly *is* the governing equations. The two time constants now say what distinguishes them: $`\lambda_{b:g}`$ cancels in the venous pool because the pool and the blood through it are one phase, so $`\tau_v = V_v/Q`$ is a mixing time where $`\tau_i = V_i\lambda_{i:b}/Q_i`$ is an uptake time. `matrix_exponential.py` is labelled numerics rather than physiology, in as many words.

## The rider, and why it had to come first

**`PL-V13T` — doc_check could not read the citation form this project writes.** `QUOTED_SOURCE_RE` allowed `,` or `:` between a document and its quotation. This repository writes `` `docs/MODEL.md` § "Supported input ranges" ``. That form matched nothing, and matching nothing is silent.

**285 citations in the section-mark form were unchecked, against 39 in the comma form** — 88% of the tree's document-section citations invisible to the check that reported they all resolved.

This is upstream of `PL-VZL0`, whose whole rationale is that its citations be *enforced*. `PL-X2XX` was believed to have delivered that and had not, for the form the project writes. So `PL-VZL0` could not honestly be closed until this was fixed, and it was mutation-checked afterwards.

A second defect was hiding behind the first: a quotation inside a `>` blockquote carries the marker of every line it wraps onto, so eleven citations of a heading that is present were reported stale. Seven more were genuinely stale and had been invisible, each repaired by what actually happened to its target.

The connective set was then **measured rather than guessed a second time**, and each candidate tested in isolation. `under` and `(` are in; `(` found a real stale citation. **The possessive is excluded on the evidence**: admitting it reported 28 quotations of correct prose as stale headings — `PL-KJ63`'s over-reach exactly. `§` is checkable precisely because it has no second use. `PL-316G` is filed for the only honest way to reach the remaining 150.

## `PL-D143` — the five Qt-port entries, applied on the owner's answer

`PL-3355`, `PL-Q4VH`, `PL-THXF`, `PL-TG60` and `PL-W8DQ` each carry the owner's 2026-09-10 note deferring them to the Qt port, and each was still `ready` with no `blocked-by` — so `docket next` offered them, and this session picked all five as its batch before reading the files. They are now `blocked`.

**The blocker is the port item, not the version, and the first attempt had it wrong.** `blocked-by: v0.5.1` raised "v0.5.1 is scoped and every other blocker has closed; it is ready to promote" on all five, every run — a permanently-firing advisory. The store is right: `blocked-by: <version>` means *waiting for a milestone to be scoped*, and `docket check` promotes such an item once that section carries its four subsections, which v0.5.1's does. Each is now blocked on the port item that rewrites the code its defect lives in:

```text
  PL-3355  readouts wrap                 -> PL-25KS  port the dashboard
  PL-TG60  six decimals of the integral  -> PL-25KS  port the dashboard
  PL-Q4VH  percent axis vs gridlines     -> PL-G59B  port the chart
  PL-THXF  legend swatch vs dash pattern -> PL-G59B  port the chart
  PL-W8DQ  slider track contrast         -> PL-L9RD  re-express theme.py for Qt
```

The decision is unchanged; only the mechanism expressing it moved. It narrows the contradiction rather than removing it: `docket gate` counts a blocked item as open debt, so Gate 1 still reads 75 rather than 70. What is fixed is that a session can no longer pick work that cannot be done until the port lands.

## The triage pass

Seven items were untriaged; six are answered and `PL-1MKQ` is left to the session already working it. Two are decisions rather than work, so they carry the question and a recommendation rather than an answer: `PL-316G` (convert the 150 possessive citations — recommendation: the simulator half only) and `PL-B8V1` (date the 2026-09-08 gate arithmetic in place, or restate it in ratios).

`PL-D4GS` is dropped as a duplicate of `PL-YTDN`, filed independently the same day by two sessions; its three unique contributions were folded into `PL-YTDN` first. Classing five of them as debt is what made `ROADMAP.md` owe them a disposition — declined to Gate 2 on the ground the sixty-one before them were.

## What `docket check --verify` caught that `make check` cannot

CI runs that flag and `make check` does not, so three defects passed locally and went red here. All three were this branch's:

- **`PL-L9FC`'s `verify:` started passing because of this branch.** It was `grep -qi 'elimination' README.md`, and `PL-B9VL` renamed the cited module to `test_published_wash_in_and_elimination.py` — so the grep began matching the *filename in the citation* while the README paragraph it is about was untouched. Left standing, `docket verify` would have ACCEPTED a branch doing none of that item's work. It now greps `F_{A0}`, absent from README today.
- **`PL-YTDN`'s `verify:` was unsound and had been "watched failing" anyway.** It piped `docket check` into `grep`; a nested run does not replay open items' commands, so the pattern matches nothing either way and the exit code was right for the wrong reason. It now queries the store through the same `filename_for` comparison the check uses.
- **`PL-D143`'s `blocked-by`**, above.

Both cases are appended to `PL-879R` (verify commands are checked for outcome but not shape) as evidence rather than filed again — the shape check caught one of the two and missed the other, which is the argument for extending it.

## Notes

- Five `verify:` commands in this branch were wrong in the way the `docket` skill warns about — written away from the work and never executed. Corrected where the item was open, restored where it was closed.
- `origin/main` was merged once, when v0.4.15 stamped `milestone:` onto items this branch also edits and `bin/docket check` errored — the genuinely-conflicted case, not habit.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01TbTxB69KPQh3d1PB2FUy7a
