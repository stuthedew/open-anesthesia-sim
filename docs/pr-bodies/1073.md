---
pr: 1073
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_01Jr391t5LXLAeRjbzMQrhwb?thread=cmsg_01Jr391t5LXLAeRjbzMQrhwbQVsPnTyx4oLWdxpChUEUd3)_

**Before:** the grooming-advisory paragraph in `subprojects/docket/README.md` ended on "Closed blockers never fire, since most in-body mentions name work that has since landed." A reader deciding whether a stale wait in a brief will be caught learned that naming a closed item there is never reported, while two other passages of the same README said it is.

**After:** the sentence says what `docket check` does today. A closed item is no edge to declare, so the undeclared-prerequisite advisory still skips it, and `_ended_waits` names the passage instead; rewording it as history or opening it with `[superseded YYYY-MM-DD]` clears it. The paragraph keeps the reason the old rule stood, the prediction `PL-8YXJ` counted and refuted on 2026-09-22 (7 closed-item hits, 5 of them live waits worded as current), and points at `CLOSED_PREREQUISITE_CUES` for the phrases read and why `requires` is not one.

**PL-WXPR** (the README's closed-blocker sentence). One paragraph of `subprojects/docket/README.md`, plus the item's closure. The new text was checked against `_undeclared_prerequisites`, `_ended_waits`, `_prerequisite_matches` and the cue lists in `subprojects/docket/src/docket/checks.py`. Still relevant on `origin/main` 9bd852c0: the sentence stood there unchanged.

**Verification.** `bin/docket verify --self PL-WXPR` returned `ACCEPT` on 5d4fd8b6: the item's `verify:` command (`! grep -qF 'Closed blockers never fire, since' subprojects/docket/README.md`) passed, and `make check` passed. `make doc-check` reported 0 errors and 0 advisories, and `tools/doc_check.py candidates --base origin/main` found no other documentation naming what the diff changed. The README's two other passages on this reading, in "The item format" and under "A write that moves `status` or `blocked-by`", already agreed with the check and are unchanged.

**Filed.** Nothing.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01TeB6wKR6zGHPXcZeszrrCt

---
_Generated by [Claude Code](https://claude.ai/code/session_01TeB6wKR6zGHPXcZeszrrCt)_
