---
pr: 514
commit: a70912ec2a017ac16d9ca73e32e2d7d4c160004c
merged: 2026-09-13
items: PL-9FMV
subject: PL-9FMV: cut v0.4.16 - the release where one quantity stopped being stated two ways (#514)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit a70912ec landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts v0.4.16 over the thirteen items finished since v0.4.15.

## What the range turned out to be

Unusually, all thirteen entries are one shape: two statements of one fact, drifted apart or free to drift. A clock reading `5400.0 s` beside a chart axis reading `1h30m`. `FLOW_FRACTION_TOLERANCE` defined at `1e-12` in two modules. A module docstring promising a `SimulationConfigurationError` boundary that three exception types walked through. A validation module named for wash-in that also covers elimination. `docs/MODEL.md` naming sevoflurane four releases after three agents shipped. `doc_check` reporting that every citation resolves while reading 39 of 324. A debt gate counting five entries the project owner had already deferred to v0.5.1.

The baseline section is written around that shape rather than the usual grouping by class.

## Why a patch and not a minor

`ROADMAP.md` "Versioning decision" chooses the number by the capability boundary a release crosses, and this one crosses none:

- `src/anesthesia_sim/data/` and `.github/` are byte-identical to `v0.4.15`
- `src/anesthesia_sim/core/` changes only in docstrings, one moved constant whose value is unchanged (`1e-12` in both places before and after), and the `RunScore` to `RunDefinition` rename

So no equation, parameter, unit, numerical method or solver step moved. What did move is what the interface displays: the clock, the recorded control stamps and the chart axis now share one compound time form. v0.5.0 is not available in any case, being behind Gate 1.

## Changes

- `pyproject.toml` and `uv.lock` to 0.4.16; `docs/releases/v0.4.16.md` generated by `bin/docket release`
- `ROADMAP.md` gains the v0.4.16 version-table row, the `current baseline` mark moves onto it, and the baseline section is rewritten
- `PL-9FMV` filed and closed for the cut itself
- `bin/docket record` writes the eleven `pr` numbers the base was owed (#510, #512) and repairs one drifted item filename in passing

## Checks

`make check` passes: 2423 tests, 100% coverage, 0 errors, 1 grooming advisory that another item owns (item-file slug drift).

The v0.4.16 tag is the project owner's to push after merge; `PL-N936` records why a session cannot.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01ARGrMfQiQA1Sqy6o5ib6L2


---
_Generated by [Claude Code](https://claude.ai/code/session_01ARGrMfQiQA1Sqy6o5ib6L2)_
