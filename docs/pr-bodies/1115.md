---
pr: 1115
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGCTPyE9wGt6eQgLX26FbR3G)_

Before: 131 items had finished since v0.5.11 without a release, and the session-start digest offered 0.5.12 in every session.

After: v0.5.12 is cut. `docs/releases/v0.5.12.md` lists the 131 items by class, each citing its pull request (79 of them, #1003 to #1112); every one carries `milestone: v0.5.12`; `pyproject.toml` and `uv.lock` read 0.5.12; and `ROADMAP.md` has a v0.5.12 row marked current baseline and a new baseline section saying what the release was for. All nine generator heads the Fix generators project set out to retire close in it, and `bin/docket generators` lists PL-MT3R alone as still generating. Thirteen items put the three Bash guards on one shared splitter that reads a command the way bash runs it.

It is a patch on both halves of ROADMAP.md § "Versioning decision": nothing a learner can reach changed, and every minor number from v0.6.0 up is given to a milestone. `src/`, `tests/reference/` and `README.md` are byte-identical to v0.5.11, and `docs/MODEL.md` moved only in its cross-references: 101 section marks, two references repaired to a heading's current name, and four quoted headings of other works reworded so they cannot read as its own.

How: PL-5ZLQ was filed with `resource: release-train` and claimed, so its claim held the release train. `make release VERSION=0.5.12` stamped the items, wrote the notes, bumped the version and relocked; the ROADMAP.md row and baseline section are written by hand. PL-5ZLQ closes in its own commit after the cut, and PL-84Z4 files the tag, which only the owner can push from this environment. The owner chose to cut now rather than wait for PL-MT3R's build and its two members (2026-09-26, on the thread's decision card), and PL-5ZLQ's brief also records how the next version number is chosen, for the version suggestions the owner asked for the same day. `make check` passes.

Once this has merged, the tag:

```bash
git fetch origin main
git tag -a v0.5.12 "$(git log --first-parent --diff-filter=A --format=%H origin/main -- docs/releases/v0.5.12.md)" -m "v0.5.12"
git push origin v0.5.12
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01FiGaJQBpi9g4oX3VQ4VwWE

---
_Generated by [Claude Code](https://claude.ai/code/session_01FiGaJQBpi9g4oX3VQ4VwWE)_
