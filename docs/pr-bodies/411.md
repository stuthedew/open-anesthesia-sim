---
pr: 411
commit: c0ea26c16fc937d5c19f7013d552e31d90ac71b0
merged: 2026-09-06
items: PL-5VR1
subject: PL-5VR1: cut v0.4.6 — provenance read at the source (#411)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit c0ea26c1 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-5VR1` (cut the v0.4.6 release). **Twelve** items since v0.4.5, stamped `milestone: v0.4.6`.

Version **named** rather than incremented, per `ROADMAP.md`'s "Versioning decision" — 0.4.6 was also the mechanical guess, because this release crosses no capability boundary. It is gate-clearing and provenance work inside the `v0.4.x` track.

## Rebased onto the current main, and one claim corrected

`#408` and `#409` merged while this sat open, so the cut no longer described the tree it would land on. `origin/main` was merged in rather than re-cutting — the in-flight guard correctly refuses a second cut, and it cannot tell this session's own branch from another session's.

- **`PL-GJYL` is now in the release** (twelve items, not eleven).
- **`PL-HB58` is not.** `#408` merged its work but left the item `ready`, so it is correctly excluded — and its `verify:` names `..._matches_published...` while the test that shipped is `..._against_published...`. Captured as `PL-0M32` and left to the session that owns it.
- **The release prose repeated a claim `PL-GJYL` had just removed.** It said the exact propagator "solves the governing equations for any positive volume". Measured: the alveolar compartment raises from 1e-9 L down, rolls the step back at 1e-100 L, and at 1e-300 L advances and returns exactly zero with the accounting passing. Replaced with the measured statement.

## What did not move

`src/anesthesia_sim/core/` is byte-identical to v0.4.5, and every stored value in `src/anesthesia_sim/data/` is unchanged — checked field by field. No equation, parameter, numerical method, solver step or displayed value moved.

## What the release is about

**The reference patient's numbers had never been traced to a document anybody opened.** `PL-6Q8N` found the three Mapleson papers unreachable from a session and was re-aimed at what PubMed delivers: five of eleven parameters gained a measurement cited alongside and explicitly *not* adopted, six record why none exists. The fat flow fraction implies roughly twice the reachable resting adipose perfusion — a factor of two on that compartment's time constant, recorded in "Known limitations" rather than fixed.

**Then the Workbook arrived and the guess collapsed.** `PL-XTMB`: the table is Appendix B p. 168, the citation had named the interface-controls section, the file's claim to be sourced entirely from that table was false (seven of eleven), `venous_blood_volume_l` = 1.0 is absent from it, and the lineage is Lowe and Ernst 1981 — the Workbook attributes nothing to Mapleson.

**Three checks were asserting more than they knew** — `contrast_check`'s eight line citations all wrong and no way to express an either-channel requirement; the mass-balance gate's tolerance tracking whichever dial its test ran at; `workflow_paths` naming three `tools` tests by path.

**Plus** two queue mechanics and the gas-volume boundary correction.

## ROADMAP.md, by hand

Version-table row; `current baseline` moved off v0.4.5; baseline section rewritten; release-narrative entry — *"a citation nobody has opened is a guess wearing a reference's clothes"*, now carrying `PL-GJYL` as its second instance: a citation chosen from a title and a claim generalised from an argument are the same failure, a statement about a region nobody visited in the voice of one about a region somebody did.

`PL-5VR1` deliberately carries **no** `milestone:` field: a cut item ships in the next release, as `PL-VR27` did.

## Verification

`make check` green: 1995 tests, 100% core coverage, `docket check` 0 errors, `doc_check` 0 errors and 0 advisories — including the release-train, current-baseline and release-tag checks.

**Gate 1: 17 of 121 cleared** — checked with `bin/docket wave`, not assumed; `PL-GJYL` was captured after the freeze and is not a gate entry. The tag is outstanding and is yours to run.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CZD5jvkBTTi6T4Md8bVTga
