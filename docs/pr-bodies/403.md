---
pr: 403
commit: c9dd3a138d119f3a36124a26d8012e0420643f55
merged: 2026-09-06
items: PL-YKXQ
subject: PL-YKXQ: capture the stale-clone finding, and record the three pull request numbers the base was owed (#403)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit c9dd3a13 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Bookkeeping plus one capture — no code, no item closed. Two commits, both from the aftermath of merging [#399](https://github.com/stuthedew/open-anesthesia-sim/pull/399).

**`PL-YKXQ`** — this container's initial clone had local `main` at `9a10124` ("Merge pull request #102"), and `git rev-list --count main ^origin/main` returned **407**: the ref had diverged into the history the `filter-repo` pass replaced, not merely lagged behind it. Fixed locally with `git checkout -B main origin/main`; tags `v0.4.0`–`v0.4.5` were checked in the same pass and all six were already reachable from `origin/main`, so the tag half of `PL-YGF3` did not recur.

The sharper consequence isn't the purged content being reachable in an ephemeral container — it's that `bin/docket` at that commit predates `branch` and `stranded`, so a session that runs `git checkout main` silently reads a four-hundred-merge-old item store and an old tool, with nothing in the working tree saying so. That happened here; it was only obvious because the subcommands errored.

**`bin/docket record`** — `pr: 399` onto `PL-6Q8N`, `pr: 400` onto `PL-GJDW` and `PL-N2X4`, riding the capture commit rather than composing one of its own, per the skill. This clears the three "records no `pr`" advisories. The command also normalised `PL-N2X4`'s frontmatter field order; no value changed.

`make check` green — 1948 tests, 100% core coverage, `docket check` 0 errors, `doc_check` 0 errors and 0 advisories.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CZD5jvkBTTi6T4Md8bVTga

---
_Generated by [Claude Code](https://claude.ai/code/session_01CZD5jvkBTTi6T4Md8bVTga)_
