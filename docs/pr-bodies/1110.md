---
pr: 1110
recorded: 2026-09-26
---

The project owner asked a session to clean up unused branches. Under the housekeeping rule, it filed `PL-P99G` and opened #1092 to carry the item. The owner judged that not worth a pull request: deleting remote branches changes nothing in the repository, so the item only ever recorded itself. #1092 was closed unmerged, and the owner agreed that branch cleanup should skip the item and the pull request from now on (2026-09-26, ratified).

**What changes.** Three sentences, one in each place a session meets the rule:

- **`CLAUDE.md`, the housekeeping bullet.** Deleting remote branches, or clearing this clone's tracking refs for them, changes nothing in the repository. So nothing is filed, claimed or committed, no pull request opens, and the reply hands the owner the list and the command. The sentence carries the ratified-decision stamp.
- **Rule 14 of `.claude/rules/instruction-writing.md`**, the bullet that turns a closing-block line into an item when it could outlive the sitting: a list of remote branches to delete stays in the reply.
- **The docket skill's capture mode**, "how small is too small to file": the work takes no commit, so there is nothing for an id to lead, even when it is why the session exists.

A side effect: a session asked only to clean up branches now commits nothing, so its own branch never reaches the remote and adds nothing to the list.

**What stays filed.** Recovering a stranded item changes the tree, so it keeps its item. A release tag keeps its item too, since `PL-H1JD` lost a tag request to an archived session.

**Also here:** `PL-P99G` is recorded as dropped, with its reason. Deleting the file instead left the item in the branch's history and out of its tree, which `docket check` reads as a lost item.

**Resident set:** +503 characters (`CLAUDE.md` +389, rule 14 +114). Nothing is cut: no existing sentence states this behaviour, so there is nothing for it to replace.

**Verification**

- `PL-S8LZ`'s `verify:` failed before the edits and passes after them.
- `bin/docket verify --self PL-S8LZ PL-P99G`: ACCEPT for both, with `make check` passing on the committed tree.
- Docs checked: `CLAUDE.md` (the housekeeping and commit-and-push bullets), rule 14 of `.claude/rules/instruction-writing.md`, `.claude/skills/docket/modes/capture.md` (the stranded-branch recovery, which already hands the deletion to the owner), `docs/worker.md` § "Ref operations a session cannot perform" (which already says a cleanup pass ends by handing over a list), `.claude/skills/docket/modes/release.md`, `docs/maintainer.md`, and the docstring of `tools/branch_id_check.py`. None needed a change.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01EijGPSEYAbG6htqve8ucUu
