---
pr: 1110
recorded: 2026-09-26
---

The project owner asked a session to clean up unused branches. Under the housekeeping rule, it filed `PL-P99G` and opened #1092 to carry the item. The owner judged that not worth a pull request: deleting remote branches changes nothing in the repository, so the item only ever recorded itself. #1092 was closed unmerged. The owner then agreed that branch cleanup should skip the item and the pull request from now on (2026-09-26, ratified).

**In this pull request so far:**

- **`PL-P99G`, recorded as dropped, with its reason.** Deleting the file instead left the item in the branch's history and out of its tree, which `docket check` reads as a lost item.
- **`PL-S8LZ`, the rule change, claimed.** Its brief names the three sentences to add and where each goes:
  - one in `CLAUDE.md`'s housekeeping bullet;
  - one in rule 14 of `.claude/rules/instruction-writing.md`, so the deletion list stays in the reply;
  - one in the docket skill's capture mode.
- **The exemption covers deleting remote branches only.** Recovering a stranded item still changes the tree, and a release tag keeps its item, since `PL-H1JD` lost a tag request to an archived session.

**Still to come:** the three edits, `make check`, and `PL-S8LZ` closed. The session stopped for length before the edits. This stays a draft while the claim holds.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01EijGPSEYAbG6htqve8ucUu

---
_Generated by [Claude Code](https://claude.ai/code/session_01EijGPSEYAbG6htqve8ucUu)_
