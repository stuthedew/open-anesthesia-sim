---
pr: 763
commit: 3cc31cce87088ade7324ccd7f7b015b2c8e483cf
merged: 2026-09-20
items: PL-KRZW
subject: PL-KRZW: record the decision to declare the interface's palette on the application (#763)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 3cc31cce landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Your answer to `PL-KRZW`, written into the item so it outlives the conversation that produced it. **No code changes** — the implementation is a fresh session's, because this one is past the context budget `CLAUDE.md` sets for *starting* an item (427k used against a ~150k budget).

`"Agree with krzw recs"` is recorded as **ratified** rather than specified — it was this session's recommendation, so ordinary evidence reopens it — and names what it was chosen over: leaving the host appearance alone (option 2), and growing a second palette for dark support (option 3).

The item moves `needs-decision` → `ready` and now carries:

- **The mechanism**: `QApplication.setPalette`, not `styleHints().setColorScheme`. The hint was observed being ignored outright in this container, and Qt documents it as unsupported on some platforms, so no legibility guarantee may rest on it.
- **Where the call goes**: a `declare_application_colours` function in `app/qt_widgets.py` sharing `declare_interface_colours`' role set, called from `main()` before any widget exists — rather than a bare `setPalette` in `main.py`, which `check_authored_disabled_colours_are_measured` would refuse, since it admits one only from a function a requirement cites and `main` is not where a colour is drawn.
- **Why the per-widget declarations stay**: `tests/integration/test_dark_appearance.py` asserts that a widget's own declaration survives a *hostile* application palette, which is the property that fails if the only declaration is global — and a view is built by tests, and could be embedded, without `main()` ever running.
- **Done when**, including the vacuity guard the chrome test needs and the three documents whose "this is an open question" wording has to change.

`verify:` was run and fails correctly before the work (exit 1, naming the test the implementation owes). `theme.py` left `touches`: the ten roles map onto constants that already exist, so no new colour or pair is introduced.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01SVwqyje1Epdx7dqputBYok

---
_Generated by [Claude Code](https://claude.ai/code/session_01SVwqyje1Epdx7dqputBYok)_
