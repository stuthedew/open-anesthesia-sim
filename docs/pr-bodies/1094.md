---
pr: 1094
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGVsTHG2ZP4XCcPZTwedUzDw)_

Before: `tools/branch_id_check.py` took an id in a branch name or leading a commit subject on the id grammar alone, and the grammar passes English words, so a branch named `claude/pl-ctrl-hotkeys` or a subject reading `PL-HTML export` passed as attributed work with no item behind it. `tools/fixture_id_check.py` read any word after `PL-` as an attempted id, so a sentence such as "Every PL-prefixed token" under `.claude/` or in a Python message failed `make check`.

After: an id attributes a branch's work only where the base's copy of the store or the branch's own copy holds it, and the refusal names the ids that named no item. A `PL-` token is read as an attempted id only where it is written as one, `PL-` and one whole word of capitals and digits, so prose naming the prefix passes, while `PL-A1B2`, `PL-DEMO` and `PL-K7QX1` are still refused. On this tree the new rule judges the same 4,798 tokens as the old one and finds the same 34 malformed, every one marked.

This is step 4 of PL-GPJ7's split, recorded in the item, and it closes PL-SN2T, PL-VH5V and the head, whose generator is recorded spent. PL-WK57 was filed on the way: `claims.holdings` still holds an item by a branch name whose id no copy of the store holds, so `docket flight` lists such a branch under an item that does not exist and `claims_nothing` exempts its unclaimed work. That changes the one reading every in-flight guard answers from, so it is its own change rather than part of a step confined to the two id gates.

How: `branch_id_check.held` reads each tree's item listing with `docket.vcs._item_paths_on` through `_Silences`, so a listing git did not answer is reported as a partial read and leaves the grammar to decide, rather than reading as an empty store that refuses every id. `attribution` still reads the branch name the way `claims.holdings` does, first match only, so it is never looser than `flight`. `fixture_id_check.CANDIDATE_RE` is now `PL-` followed by `[A-Z0-9]+` bounded on both sides, shared by the Python and `.claude/` halves, and `ID_RE` still judges what it admits. Of the seven new tests, six fail on the old code, and the seventh, which holds that either copy of the store counts, fails when either listing is dropped (checked). `make check` passes locally (4,856 tests), and `bin/docket verify --self PL-GPJ7 PL-SN2T PL-VH5V` accepts all three.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_0197iV7xkQ4ZfkRYft2Pykqm

---
_Generated by [Claude Code](https://claude.ai/code/session_0197iV7xkQ4ZfkRYft2Pykqm)_
