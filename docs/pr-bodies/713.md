---
pr: 713
commit: 54b7a97fd5641cd71a05e41f63b3eb56f128a460
merged: 2026-09-19
items: PL-TFWR, PL-YTDN
subject: PL-YTDN: record the PL-TFWR rename as a ratified decision, not a specified one (#713)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 54b7a97f landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follow-up to #712, which merged before this could ride it. One commit, two item files, no code.

## What this records

You agreed with the recommendation to rename `PL-TFWR` and repair the two `touches:` declarations, rather than hold it back behind `PL-Y5JX`'s recorded skip. `CLAUDE.md` distinguishes a decision you **ratified** from one you **specified**, and the two have different bars to reopen — so the record has to say which, and name in one clause what it was chosen over. The reply that put the case is gone; the item is the only carrier.

Recorded in both places a later session would actually read it:

- **`PL-YTDN`** — where the reasoning for the choice lives: `(project owner, 2026-09-19, ratified, over leaving it drifted behind PL-Y5JX's recorded skip)`.
- **`PL-Y5JX`** — because the session that builds the coupling guard reads *that* brief rather than `PL-YTDN`'s, and it is the brief whose own recorded skip was overridden.

Ratified rather than specified means **ordinary evidence reopens it** — a measurement, a cost the case did not carry, a constraint since appeared — because it was a session's own recommendation wearing your signature. `PL-Y5JX` now says so explicitly, so a later session knows it may revisit the call without needing a compelling argument.

## Why a second pull request rather than a push to #712

The ratification commit was pushed minutes after `#712` squash-merged, so it landed on a branch nothing would merge — the `PL-3D2M` shape. The branch was restarted on the merged `main` and the commit replayed onto it, per the docket skill's recovery procedure, rather than pushed to the merged branch again.

## Checks

`make check` passes; `bin/docket check` reports 0 errors and no filename drift. Documentation edits only — no source, no tests, no behaviour.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_012GprbZsiPaTDdeUzAmdv5c

---
_Generated by [Claude Code](https://claude.ai/code/session_012GprbZsiPaTDdeUzAmdv5c)_
