---
pr: 1066
recorded: 2026-09-26
---

Two item-file changes follow #1060. One records the owner's answer on the item that pass left at `needs-decision`. The other triages the one capture it left because the session ran past its context budget.

## PL-0JDP: `bin/docket claim` refuses an item that is already `blocked`

The owner agreed with the recommendation on 2026-09-26: **A, only a block the branch writes releases its claim.** The answer sits beneath the question, which stays standing, dated and marked as ratified over B. B would have kept the refusal and documented it in `start.md`. The item moves from `needs-decision` to `ready`:

- **Done when** now states A alone:
  - `claim` on an item another branch blocked writes a live claim.
  - A block the claiming branch writes still releases its claim, which `test_the_branch_s_own_copy_reaching_a_releasing_status_releases_the_claim` already tests.
  - The comment on `RELEASING_STATUSES` and the README's "A claim ends in one of five ways" paragraph record the answer.
- **touches** drops `.claude/skills/docket/modes/start.md`, which only B needed. It adds `subprojects/docket/README.md`, whose paragraph states the rule A narrows.
- **verify** greps for the two tests the work adds and for `PL-0JDP` in `claims.py`. It was run on e586612e and exits 1, as it should before the work.

## PL-KS01: the digest, `list`, `status` and `concurrent` read the working tree's copy of each item while `next` and `show` read `origin/main`'s

This capture arrived with #1058, partway through the earlier pass. It is triaged as P3, M, defect, `needs-decision`, in the `one-snapshot` feature, and deferred from v0.6.0's gate like the rest of the post-freeze debt.

- **Premise re-read on e586612e.** `cli._from_base` has two callers, `cmd_show` and `cmd_next`. `cmd_digest`, `cmd_list`, `cmd_status` and `cmd_concurrent` call neither.
- **Decision needed.** This is a session's call rather than the owner's.
  - A: apply the base's newer copy wherever a read command loads the store.
  - B: apply it only to the digest lines that are `next`'s pick.
  - **Recommended: A.** It carries PL-XBV4's one-snapshot fix to the one input that fix left to each command.
- **Generator check.** This is the first post-close instance of PL-XBV4's fact, filed in the commit that closed its member PL-Y48N. It is not a new head.

## Not triaged

PL-0T5X, PL-NK1L, PL-Y2L6 and PL-PQ0R are listed in PL-PVW2's `root-cause-of:`. PL-PVW2 is a head on the Projects trial's Order list (PL-NZC0), so those four are left for Stream A's thread to triage.

## Checks

- `bin/docket check`: exit 0.
- `python3 tools/doc_check.py check`: exit 0.
- `bin/docket arm`: `arm`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01AwyrzasLsMyUTG1THA1dRW

---
_Generated by [Claude Code](https://claude.ai/code/session_01AwyrzasLsMyUTG1THA1dRW)_
