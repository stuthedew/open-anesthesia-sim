---
pr: 1085
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGRDAujq4waEc8xWaaw2sKzs)_

Before: PL-63TT (a glued `)` hides a separator from two Bash guards), PL-R5RF (the gate guard reads a backslash-newline as a separator) and PL-39LD (all three Bash guards drop everything after the first heredoc operator) were untriaged, and PL-PVW2's reopened step 2 named the splitter they need without saying how to build it.

After: the three are `ready` - PL-63TT and PL-39LD at P2, PL-R5RF at P3 - each deferred from the v0.6.0 gate as a member of PL-PVW2, with a `verify:` grep for the test its fix adds and `.claude/hooks/shell_split.py` in its `touches`. PL-PVW2's brief carries the splitter's design for the thread that builds it. The thread that wrote this stopped at the context budget before building, not for the work.

How: item files only. The design is one standard-library lexer beside the hooks, imported by all three, with bash's own rules for operators (longest match over bash's table), quoting, comments, line continuations, newlines and heredoc bodies; `words`, `segments` and `command_text` as its three answers; and the tests each member's `verify:` already names. It also files PL-WGFY: `no-prune-guard.sh` reads a `;` inside a quoted argument as a separator, which refused this session's own Bash call. `make docket` passes with 0 errors.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_0134Eg7ynkjABcL545buwV7P

---
_Generated by [Claude Code](https://claude.ai/code/session_0134Eg7ynkjABcL545buwV7P)_
