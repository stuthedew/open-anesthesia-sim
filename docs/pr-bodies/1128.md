---
pr: 1128
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGQgEaD6X69XtPAkYF27Mfjs)_

Before: `PL-61FT` held the decision about what the three Bash guard hooks promise, with three routes named and route 3, bounded by use, recommended in one paragraph. Nothing said what each guard's bound is, where it is written, how a spelling outside it is recorded, or what happens to the six open members and to the `PL-QMN0` fix parked on `claude/project-thread-qt3onr`.

After: the item carries the design round. It records the count the decision rests on: each of the twelve members was found by probing a guard and none by a session meeting it, while every spelling sessions have met is already pinned. It decides the parts that change nothing about what sessions file: what each guard promises, that the prune guard's promise is wider because a pruned ref cannot be brought back, that a false refusal is worked when met and never probed for, where the promise is written, how a known gap is recorded in the guard's test file, the disposition of each member under the bound, and that the parked `PL-QMN0` fix stays on its branch as the fix ready should its spelling be met. The half that changes what sessions file is put to the owner, marked recommended. Two findings the bound itself owes are filed and join the head's members: `PL-R295`, `git remote remove` deletes every remote-tracking ref of a remote unrefused, and `PL-N6JP`, a tools check run with `--help` piped to `head` is refused, met in ordinary work by this session.

The pull request changes item files only and moves no status. It stays a draft while the claim on `PL-61FT` holds, and `bin/docket arm` answers `hold` until the owner's answer is recorded and the claim yielded.

How: the design round is appended to `PL-61FT`'s brief under `**Design round, 2026-09-26.**`, with the owner's question under `**The owner's half**` and `**Recommended: yes.**` beside it; `root-cause-of:` gains the two new members. Verified: `bin/docket check` passes with 0 errors after the edit.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_0153GoVziuNjVwcbVc6qCfrj

---
_Generated by [Claude Code](https://claude.ai/code/session_0153GoVziuNjVwcbVc6qCfrj)_
