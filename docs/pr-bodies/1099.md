---
pr: 1099
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGMdNDrQC3L5uJbMysNNa35j)_

Before: `tools/doc_check.py` split a CI step's `run:` line and a Makefile recipe line with `COMMAND_SPLIT_RE`, a pattern of its own that cut inside quotes. `python3 "tools/my file.py"` read as `tools/my`, which `check_workflow_paths` reported as a missing script and `check_gate_parity` never saw at all. It was the last spelling of how a shell command splits outside docket's `shell.py` and the hooks' `shell_split.py`, so generator head PL-PVW2 stayed live on it.

After: both readers take `docket.shell.shell_words`, so a quoted path is one word and a path inside a command substitution is still read. A line whose quote, substitution or backslash runs past its end is declined by name instead of being guessed at. PL-CWBJ is triaged as PL-PVW2's member and closed, with the owner's Yes from the coordinator's decision card recorded in it, and PL-PVW2 closes with `generator: spent`.

How: a new `_shell_words` helper reads the line and declines it where docket's reader cannot, `_command_paths` and `_gate_scripts` read its words, and the backquote joins `UNRESOLVABLE`, since a word keeps a substitution as written. On 39a7a31f every line of the 42 `run:` steps and every recipe line reads exactly the paths and gate scripts the pattern read, and none is declined.

Verification: the three new tests in `tests/unit/test_doc_check.py` failed before the change and pass after it. `make check` passed locally with 4,907 tests. `bin/docket verify --self PL-CWBJ PL-PVW2` accepts both.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QykJVbJp9qqkLGMfbcFqSj

---
_Generated by [Claude Code](https://claude.ai/code/session_01QykJVbJp9qqkLGMfbcFqSj)_
