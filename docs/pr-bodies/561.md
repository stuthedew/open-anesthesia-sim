---
pr: 561
commit: d2b4caaeef15df4b46876e86e9ab59929052caef
merged: 2026-09-14
items: PL-6KNM, PL-6TQH, PL-BDNB, PL-BQ46, PL-KL2Q, PL-XP6W
subject: PL-6KNM, PL-BQ46, PL-KL2Q, PL-BDNB, PL-XP6W, PL-6TQH: the core's dimensionless vocabulary, decided in one round — and the closure main is red without (#561)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d2b4caae landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Four `needs-decision` entries and one `ready` docs item, which together close the `core-domain-language` feature at 16 of 16. They were one question rather than five: how many dimensionless kinds `core/` carries, which of them a type may separate, and which names describe a quantity against a range. A sixth item, `PL-6TQH`, was added after `main` went red — see the last section.

**No equation, parameter, numerical method or displayed value changes.** Every executable change is a `NewType` erased at runtime, a rename, or a local moved. `make check` is green — 2710 tests, 0 lint, 0 mypy, 0 docket errors, 0 doc errors.

## PL-6KNM — the guard names a representation; the module and the `NewType`s name the quantity

`require_concentration_fraction` is now `require_fraction`. Three pieces of evidence rather than a preference:

1. All eleven call sites in `core/` pass a `..._partial_pressure_fraction` string — not one passes a concentration, so a reader met two names for one kind on one line.
2. `core/validation.py`'s two other guards (`require_positive_finite`, `require_nonnegative_finite`) already name a property of the number. This was the module's only exception.
3. The same [0, 1] range already guards `perfusion_fraction` and the MAC-awake ratio at the payload layer, neither of which is a concentration.

`core/concentration.py` and the `Fraction` / `Percent` `NewType`s keep their names: the module is the implementation of `docs/MODEL.md` § "Concentrations" and takes its subject from that section, which is the strongest traceability a module name here can carry.

Consequence recorded rather than acted on: `tools/core_vocabulary_check.py` matched whole identifiers because two live names would otherwise have been reported, and this removes one of them. Measured across `core/`, **no live identifier now contains a retired name as a substring**, so a substring rule would pass today and would have caught this guard by itself. Filed as `PL-JW9J`; the tool docstring, its test and `docs/ARCHITECTURE.md` now say so instead of citing a name that is gone.

## PL-BQ46 — a MAC-relative type earns its place

`MacMultiple` joins `Fraction` and `Percent`, because § "Concentrations" is also where the MAC multiple is defined, as a rescaling of the same F by one per-agent constant.

The hazard is reachable, and the new test is the demonstration. Sevoflurane's MAC-awake band is 0.34 ± 0.05 MAC against a MAC of 2.0%, drawn at **0.58–0.78%**. Pass a circuit fraction at the vaporizer's own 8% maximum into the same call and every runtime guard accepts it, because 0.08 − 0.05 is still positive — the band is then drawn at **0.06–0.26%**, below the awakening concentration, where a falling trace crosses it late or never, with nothing on screen saying so.

Annotated at the boundaries only, since the module docstring already measured that arithmetic erases a `NewType`. One type rather than two: a ratio to MAC and a multiple of MAC are one kind differing only in range, and the bound that matters is held by `_MacAwakePayload`'s validator. `tools/ignore_check.py` reports both new directives live, so the refusal is real rather than asserted.

## PL-KL2Q — the rule is the layer, not the quantity

`core/parameters.py` now states at the aliases which vocabulary a new field takes: a payload field takes a `Positive*` alias, a public dataclass field takes the `concentration` `NewType`, and a dimensionless quantity that is neither a concentration nor a ratio to MAC takes `PositiveFraction` and stays a bare float publicly. The merged `Annotated[Fraction, BeforeValidator(...)]` form is recorded as priced and refused — it would delete the payload boundary `_StrictPayload`'s own docstring calls deliberate. `MacAwakeReference`'s two bare floats were the rule's only departure and are gone with `MacMultiple`.

## PL-BDNB — one rename, four deliberate leaves

`integrated_circuit_fraction_s` becomes `integrated_inspired_partial_pressure_fraction_s`: it carried both defects `PL-9SH6` removed, and it is the integral of that accessor exactly. The short locals beside it stay — each is bound one line below the accessor it reads. `governing_equations.py`'s `delivered_fraction` stays too, on its own argument: every matrix row that reads it sits under a comment writing the equation in the specification's own symbols, so the reader has F_D in view at the row. Both reasons are now comments at the bindings.

## PL-XP6W — the missing symbol row

§ "Symbols" gains a row for f_i, the stored per-group perfusion fraction its own § "Tissue groups" already uses in Q_i = f_i·Q. The cell count `tests/unit/test_core_vocabulary_check.py` pins moves 24 → 25.

## PL-6TQH — the closure `main` is red without

Added after this branch was already green, because `main` was not. Quality run #1898 on `a002f919` failed with one store error: `PL-6TQH` (tag v0.4.23 on the merge commit of #559) was still `ready` while its `verify:` command already passed. It passed because the work was done — `git ls-remote --tags origin v0.4.23` resolves `refs/tags/v0.4.23` at `7dd1a03`. The session that cut v0.4.23 ended `failed` with "tag 7dd1a03 verified on main; closing out ticket" as its last action, so the tag went up and the closure did not.

**No pull request could have surfaced this.** `.github/workflows/quality.yml` runs the full `bin/docket check --verify` replay only on a push to `main`; a pull request gets `--verify --verify-base`, scoped to its own diff. Reproduced and fixed here: the same command that reported `1 error` on `a002f919` reports `0 errors, 6 advisories` on this tree, 106 commands in 129.0 s.

`origin/main` is merged in deliberately rather than by habit, against `PL-WC72` — the item file being closed exists only on the base, so the merge is what carries the fix.

## Also in this branch

- `bin/docket record` rides both commits with the ten pull-request numbers (#556, #558, #559, #560) the base was owed.
- Two captures: `PL-JW9J` (substring matching in the vocabulary check) and `PL-W3Q5` (`mac_percent` is a `Percent` in two `app/formatting.py` signatures and a bare float in five others).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01WPbJFsANrhb3WgmxE97adv
