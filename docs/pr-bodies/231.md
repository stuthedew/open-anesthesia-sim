---
pr: 231
commit: 22f1d7d5e447f955dc64ecb81d56696edbbb68ee
merged: 2026-09-02
items: PL-D1ST, PL-WB0X
subject: PL-D1ST: settle v0.4.0's scope - the core-domain step, PL-WB0X's split, and every bullet's id (#231)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 22f1d7d5 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Records four decisions the project owner took on 2026-09-02, each where it fires. No source file changes; no equation, parameter, numerical method or displayed value is touched.

## 1. `core/` reads like the domain moves behind v0.4.0

Planned-milestone item 29 sat between v0.3.0 and v0.4.0 as the `v0.3.x` row, which made an unscoped pass over `core/` the gate on the whole teachable-case milestone. Its stated purpose is fluency for reviewing the coupled-gas equations of items 6 and 7, and v0.4.0 changes no equation, so it bought nothing there. It becomes the `v0.4.x` row, still ahead of the substance generalization — the placement's actual constraint.

`bin/docket wave` now reports `step 3 of 10: v0.4.0 — the teachable case` and the beat as implementing it, where it previously reported the project between numbered steps.

## 2. PL-WB0X splits

PL-WB0X (split `simulation_view.py`) stated three placements that could not all hold — stage 1 before PL-DHV7 which ships in v0.4.0, the whole item at Gate 1 which ships inside v0.5.0, and "a `v0.3.x` patch step". That is the contradiction PL-4C41 was filed against.

Resolved by splitting rather than choosing, since one item cannot sit in two milestones:

- **Stages 1 and 2** — the formatters into a Flet-free module, the chart-series shaping beside `app/chart_downsampling.py` — enter v0.4.0's Required scope. They gate PL-DHV7 (which rewrites every formatter and extends `docs/MODEL.md`'s "Displayed precision" derivation, today terminating in a private static method on a 1265-line Flet class) and the milestone's four chart items.
- **Stage 3**, the `SimulationView` decomposition proper, becomes **PL-B9PY** and stays at Gate 1, where v0.5.0's two-runs-at-once comparison is what genuinely requires it. Blocked on PL-WB0X.

PL-WB0X's `verify:` command, which `docket check` reported as already passing and therefore proving nothing, is replaced with one that was run and exits 1. Its stale line numbers and line count are refreshed against the current file.

## 3. PL-W3DD carries the substance-keyed history record

v0.4.0's Required scope had twelve bullets and eleven queue items. The `SimulationHistorySample` keyed-by-substance bullet had no item behind it, and the milestone's Definition of done did not name it either, so it could have been missed twice. Both are corrected. It matters because planned item 12 (forking) writes its element-wise reproducibility proof against this record's shape.

## 4. MAC-awake sourcing

Desflurane's MAC-awake is the one value PL-F52R still lacks — Katoh 1993 covers sevoflurane and isoflurane only. Recorded in the item: a session proposes it with its primary citation and the owner reviews rather than searches, nothing reaches a data file before that review, and the existing Katoh values are owed the same read since they were verified by an external review rather than by any session here.

## Closed alongside

**PL-D1ST** — v0.4.0's Required scope described items whose ids it never printed. Taken here rather than separately because this change was already rewriting that section and because v0.4.0 has just become the current step: four of its thirteen items reading as unplanned is a cost every session working the milestone would have paid. All thirteen bullets now print their id, checked by parsing the section rather than by eye.

Also backfills the two pull request numbers `docket check` recovered from their merge commits (PL-2XTF → #227, PL-P0QT → #228).

## Checks

`make check` green: ruff format, ruff check, strict mypy (42 files), 1152 tests, `docket check` 0 errors, `doc_check` 0 errors, contrast check 0 errors. `tools/pr_title_check.py` passes on this title.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_019TRvkWuYAx2D9xJVir1adE


---
_Generated by [Claude Code](https://claude.ai/code/session_019TRvkWuYAx2D9xJVir1adE)_
