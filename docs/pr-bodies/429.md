---
pr: 429
commit: 447b202da214e4178f4ccdabacdf6a6013d4226e
merged: 2026-09-07
items: PL-Q2PX
subject: PL-Q2PX: triage the five captures open on 2026-09-07 (#429)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 447b202d landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Folds the 2026-09-07 captures into the queue. Four are triaged here; `PL-HLD5` was yielded to #427, which answered it.

| Item | Band | Status | Classes | Feature |
| --- | --- | --- | --- | --- |
| `PL-THXF` legend swatch is solid for a dashed trace | P2 · S | ready | defect, ux | presentation-safety |
| `PL-H2K2` no signal before the run-length halt | P2 · M | ready | ux | presentation-safety |
| `PL-CZFY` clock reads `86400.0 s` at the 24-hour limit | P2 · M | needs-decision | ux | presentation-safety |
| `PL-WT07` MODEL.md overstates the binding manoeuvre's margin | P1 · S | needs-decision | docs, science | model-spec-accuracy |

Both `ready` items name a `verify:` command that was run and watched fail for the right reason — the suite half passing, the `grep` half failing on the test the work owes. The three `needs-decision` items carry a **Decision needed.** section rather than an answer: triage sets fields and does not resolve the question an item poses.

### PL-WT07 changed on reading, and is why one item is P1

Its title says `docs/MODEL.md` states a 3% margin as "throughout". The same paragraph also quantifies the narrowing gap against the **case opening**, 7× behind at 300×, while the manoeuvre actually 1.03× behind is not named in the comparison at all. The stated margin is off by about a factor of seven in the reassuring direction, in the authoritative model spec, which is why it seats at P1 with `science`.

`PL-SR8F` is that paragraph's predecessor and is still open with a passing `verify:` command. The phrase it names was present through `a6da69e` and gone by `67b279b` — `PL-ZVS7`'s own commit — so its work landed and the item was never closed. `PL-WT07` now says to close it first or work the two together.

### PL-CZFY rests on a stale docstring

`format_elapsed`'s docstring makes one shared format for the clock and the control-change stamps an invariant, and says a compound form "would round the stamp away". Measured today, that is no longer true of the compound form that shipped: `format_chart_time_label` renders `1h23m45.6s` and `24h`, keeping the tenth. The decision is narrower than the docstring implies, and the item records the three options.

### PL-8MJ3, captured and triaged here

`bin/docket triage` marked four of the five items as already edited on a branch, which `SKILL.md` says to skip. All four marks were evidentially wrong: each named branch's copy of the item file is byte-identical to `origin/main`'s, both branches having squash-merged as #421 and #422. The mark is computed from the item files a branch's commits touched, without asking whether those edits are on the base — and a squash merge never puts the branch's own commits there, so the ref reports forever. `vcs.py` already has the right question in `_base_blobs` and `_landing_split`; the per-item mark does not consult it.

`PL-HLD5` then turned out to be genuinely in flight on a branch the mark did **not** name, found by re-fetching before opening this pull request. That is the defect's central example rather than an exception to it.

### Checks

`make check` green: 2096 tests, 100% coverage, 0 store errors, docs clean. CI's PR-scoped verify replay run locally: 3 commands, 0 errors.

Also records `pr: 426` on `PL-L2F2`, which the base was owed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01TZXigxVrz2kWp6sdAkoYeN

---
_Generated by [Claude Code](https://claude.ai/code/session_01TZXigxVrz2kWp6sdAkoYeN)_
