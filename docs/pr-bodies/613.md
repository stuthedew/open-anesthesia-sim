---
pr: 613
commit: 2fed8975f5458160df551695d604a72d3003bad0
merged: 2026-09-15
items: PL-SMN4
subject: PL-SMN4: record the second instance, which took the commit unblocking main (#613)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 2fed8975 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

**Closes nothing.** `PL-SMN4` (a push run on `main` can be cancelled by a later
merge) stays open and `ready`. This is evidence plus the `pr:` backfill — queue
files only, no code.

## Why now rather than filed for later

The evidence is perishable. It was observed live, from the session that caused
it, and Actions history is the only record.

## What happened

| run | commit | pull request | created | outcome |
| --- | --- | --- | --- | --- |
| 2036 | `df3b1de` | #610 — *"This unblocks `main`"* | 01:13:38 | **cancelled** 01:14:14 |
| 2037 | `18bc132` | #611 | 01:14:13 | pending, then ran |

Run 2036 was cancelled **one second after** 2037 was created, on the mechanism
`PL-SMN4` already confirms: 2036 was pending in `quality-refs/heads/main`, and
2037 arriving took the single slot. My own merge of #611 is what evicted it.

## Three things it changes about the item

1. **It is not rare.** Two instances in 2h15m, on an ordinary evening's merges.
   The first brief could be read as describing a freak burst; this one cannot.
2. **It fires hardest exactly where it costs most.** `df3b1de` was the fix for
   a red `main` — `PL-GN8C`'s `verify:` matching a sentence about another book,
   which had `bin/docket check --verify` failing on the default branch. So the
   one commit whose verdict everybody was waiting for is the one that lost it.
   That is not incidental: an outage is precisely when merges cluster, so this
   failure's rate is **correlated** with the moments its verdict matters. Any
   threshold analysis of whether to fix it has to price that correlation rather
   than the average rate.
3. **Two merges is the real threshold, not three.** The item's scope paragraph
   reasons about a commit lost between an in-progress run and a later merge.
   What actually happened is one running, one pending, one arriving — the normal
   shape on a repository worked by merging several approved pull requests in a
   sitting.

The recovery is linear-history luck, and the item now says so: `18bc132`
contains `df3b1de`, so the tip is verified either way. What is permanently
unknown is whether `df3b1de` **alone** was green — the reporting half
`PL-SMN4`'s own **Done when.** names as its second candidate shape.

## Second commit: the `pr:` backfill

`bin/docket record`, bare, riding the commit above rather than composing one of
its own, per the `docket` skill. Sixteen numbers, every one recovered from the
default branch's own merge commits and none typed. It clears 16 of `bin/docket
check`'s 24 advisories, `pr: 611` for `PL-0C6W` among them.

## Checks

`make check` green: 2919 passed, `docket check` 0 errors (advisories 24 → 8),
doc-check 0 errors 0 advisories.

---
_Generated by [Claude Code](https://claude.ai/code/session_01CdhThYVReR1QAYh3HY9aon)_
