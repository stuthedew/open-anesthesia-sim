---
pr: 506
commit: de6baee59fc191b59bbc13a2cc412c1b6016e4e2
merged: 2026-09-12
items: PL-3833, PL-CW14, PL-KJ63, PL-MGF9, PL-WTQ1, PL-Z0G0
subject: PL-3833, PL-CW14, PL-KJ63, PL-MGF9, PL-WTQ1, PL-Z0G0: six gate entries where a check over-reached (#506)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit de6baee5 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Six Gate 1 entries, in two clusters of one mechanism each plus one drop. Five are the same defect wearing different clothes — **a check reaching past what it can actually decide**, and charging a session for the difference. The sixth is an item that specified a check which could never have fired.

**Gate 1 on merge: 74 → 80 cleared, 85 → 79 open** (measured against `origin/main` at `b755281`, with #505 and #507 already in). That is the first time the gate has been more than half closed.

## Cluster one — `subprojects/docket/src/docket/checks.py`

### PL-CW14 — the top-band advisory prescribed an action the checker rejects

It read *"demote what is not genuinely next"*, while the same checker refuses to seat a `safety`- or `science`-classed item below the top band. Measured on this store: of the **12 startable P1 items, 12 are class-pinned and 0 are demotable** — the prescribed action was unavailable on every one of them.

The count is unchanged. `docket.toml`'s `top_band_limit` comment considered exempting pinned items and rejected it — *"a band that grows to twenty safety items is a genuine problem a session should be told about"* — and that decision stands. Only the remedy narrows to what is available:

- something can move: `… 5 are pinned there by a safety or science class, 1 is demotable; demote what is not genuinely next`
- nothing can: `… all 6 are pinned there by a safety or science class, so the band is large because that much safety-critical work is open rather than because anything is over-prioritized`

The second is today's store, and it is the debt gate's own signal rather than advice nobody can take.

### PL-3833 — an item file's name could drift from its title, unchecked

`_check_filenames` derives the slug with the same `filename_for` the store writes with, and names every drifted file on one line. **9 of 810 today.**

**An advisory rather than the error the brief first asked for**, on the brief's own later evidence. Comparing the slug is exactly decidable; renaming is not, because whoever does it has to know which open branch holds the file. An error would fail `make check` until all nine were renamed in one pass — the rename-against-edit conflict `PL-36R4` already cost once. `PL-YTDN` carries the rename pass; promoting the check to an error is safe once that count is zero.

## Cluster two — `tools/doc_check.py`

### PL-WTQ1 — the math check read a shell regex as LaTeX

`check_math_delimiters` scanned YAML frontmatter, so a `verify:` command escaping a parenthesis failed the run as unrenderable math. GitHub hides frontmatter or shows it as a table; there is no math there to render wrongly. The skip is the frontmatter's line span and nothing else — a test pins that an escaped parenthesis in the *body* still fails.

### PL-KJ63 — a quoted measurement was read as a section citation

`"~88-256 B each" above` hard-failed as a citation of a heading that does not exist, and the only repair available was to reword prose that was not wrong. The `directed` branch now has to open on a letter or a code span, which is what a heading or a `**Bold.**` marker does and what a measurement does not. **Measured before changing it: 40 directed citations across the documents this reads, none of which opens on anything else — zero dropped.** The `see`/`under` branch is untouched, because those say outright that a citation is being made.

### PL-Z0G0 — a candidate line was labelled by sort order

`hits.setdefault` kept whichever term arrived first, and terms are walked sorted, so a line plainly about `render.py` printed as `(render)`. Now distinctive first, then longest — the distinctive half carries more, because since `PL-B2NS` the label also tells the reader whether the term had to be narrowed to code context.

## The drop — PL-MGF9

**Dropped on the project owner's decision, 2026-09-13**, as superseded by `bin/docket trend` (`PL-QPJZ`, #424).

`trend` landed 2026-09-07 — **five days after `PL-MGF9` was captured** — and already reports the process-to-product balance over the whole open queue, which is this item's own **Done when**. It places an item by its `touches` rather than its `classes`, the reading `docket.toml` records as better (the `classes` reading put 33 of 145 open items on the wrong side), and it carries a seven-day history the item never asked for.

The advisory it specified also **could not fire**: measured 2026-09-13, 74 of 244 open items are process-classed and a majority needs 123. Building it as written would have added a permanently silent advisory — the shape `CLAUDE.md` calls a defect in the check. This is the `PL-LKGL` pattern: an item whose problem was solved another way stays red and reads as outstanding work.

The one residual — `trend` answers only on demand, and the session-start digest does not carry its balance line — is recorded in the item's brief and **deliberately not refiled**, since re-scoping to it was the alternative on the table and was not the option chosen. The file is never deleted, which is what the drop semantics are for.

The drop takes no position on the apparatus-to-product balance itself. It says only that this mechanism was the wrong instrument for measuring it.

## Also in this branch

- `bin/docket record` rides the first work commit with the ten `pr: 504` numbers the base was owed.
- **Captured:** `PL-YTDN` (rename the nine drifted files), `PL-LBR6` (`docket record` renames a drifted file as a side effect of writing a number), `PL-MSFB` (two workarounds these fixes make unnecessary).

## Verification

All five implemented items' `verify:` commands were run and watched to **fail before the work and pass after** — for the `doc_check` three, by reverting `tools/doc_check.py` alone and confirming exactly those three tests fail.

```
make check
  2387 passed, 100% coverage
  docket: 0 errors, 1 advisory (the new filename check, doing its job)
  documentation: 0 errors, 0 advisories
```

Docs swept. `doc_check.py candidates` returned 39 hits for `doc_check.py` and 7 for `checks.py`; the `checks.py` seven are all the CI `checks` *job*, a false positive on the word. The only two documentation lines that state the behaviour these items change are `ROADMAP.md:1610` and `:1620` — Gate 1's **frozen** list, which quotes the item titles and is a snapshot rather than a live claim, so it is deliberately not edited. `subprojects/docket/README.md` § "What is checked, and what is left alone" gained the new advisory and the reason it is not an error; `PL-3833`'s `touches` was amended to match.

`PL-JRPP` (closed, #346) carries a `verify:` command that greps `PL-MGF9`'s file; the drop leaves that file in place and does not touch the quoted sentence, so the command still resolves.

The branch merges cleanly against `main` at `b755281` and the base is deliberately not merged in — a merge commit here would re-run the whole of `quality.yml` and discard a green result, on content `main`'s own run already proved (`PL-WC72`).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CBJzQiBVA7p5WLSADYhvrR
