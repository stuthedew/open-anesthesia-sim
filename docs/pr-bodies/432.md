---
pr: 432
commit: b03a7d03f80d31ea73b1354543ec1a8557a2bbb8
merged: 2026-09-07
items: PL-0GTC, PL-CL8J, PL-SR8F
subject: PL-0GTC, PL-CL8J, PL-SR8F: close two items whose work already landed, turning main's verify replay green (#432)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit b03a7d03 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-CL8J` (filed in this session). Turns `main` green. Frontmatter and closing notes only — no source or documentation changes, because the work these items describe already shipped.

## What is wrong with `main`

Its `quality` run has concluded `failure` on its last four merges — #424, #425, #426 and #431 — on one error, with every other step green:

```
PL-0GTC, PL-SR8F are open but their `verify:` command already passes (2 of 116 checked)
```

`PL-0ZGK` diagnosed why nobody sees this: the whole-store verify replay runs only on push to `main`, so a green pull request turns `main` red *after* it lands. Its own closing note says the repair is **not** that item and should be done first. Since `PL-0ZGK` is `needs-decision` on which notification mechanism to build, the repair itself belonged to nobody while `main` stayed red.

## Which horn of the error this is

That message covers two opposite conditions, and it is worth being explicit about which, because the remedies are opposite:

- **benign** — the work landed under another id and the item was never closed. Close it.
- **dangerous** — the `verify:` command does not discriminate and proves nothing, so `docket verify` would *accept* a branch that did none of the work. Rewrite it.

Both are the benign horn:

```
PL-0GTC   'RespiratorySystem' removed by d8deea5 (#376)
PL-SR8F   'two orders milder'  removed by 67b279b (#420, PL-ZVS7)
```

## How that was established, and why the obvious method fails

By replaying all 80 commits that touch `docs/MODEL.md` and testing each revision under **the same whitespace normalisation the items' own `verify:` commands use**.

That is load-bearing rather than pedantry. `docs/MODEL.md` is hard-wrapped prose, so a phrase can span a newline, and both `grep` and `git log -S` are line-based. My first pass with `grep` reported "two orders milder" absent from *every revision in history* — which is false, and would have left the removal commit unrecoverable. `PL-SR8F`'s own verify command joins on whitespace before testing, which is the clue that the phrase is wrapped in the source.

`PL-SR8F` is `classes: safety`, so its substance was verified rather than inferred from the phrase being gone. `docs/MODEL.md` § "Supported simulation step" now carries the per-rate relation the item asked for — "the case opening is about 21× milder at 1×, and **the gap narrows as the grid coarsens** — to 19× at 20×, 16× at 60× and about 7× at 300×" — in place of a "two orders milder at every rate" that held at no rate. `PL-0ZGK` independently named `67b279b`; the replay agrees.

## `docket record` rides this commit

As the store's own advisory asks, rather than composing a commit for it. Seven items on `main` were owed their PR numbers: `PL-B1WW` and `PL-D3XX` (#431), `PL-D188` and `PL-JL2M` (#428), `PL-HLD5` (#427), `PL-Q2PX` (#429), `PL-T691` (#430).

This is not tidiness. Unrecorded numbers are what forced the v0.4.7 cut to be reverted and re-cut, and nine items are releasable now.

## Verification

`bin/docket check --verify` — the whole-store replay `main` fails on — reports **0 errors** here against the **1** it reports on `main`. `make check` green: 2139 tests, 100% core coverage, ruff/mypy/doc-check clean.

`PL-0ZGK` stays open deliberately: **this is the repair, not the guard.** The next item finished without being closed turns `main` red the same way and just as silently.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01SBwGDgp8sR2MtWzAuFezJm

---
_Generated by [Claude Code](https://claude.ai/code/session_01SBwGDgp8sR2MtWzAuFezJm)_
