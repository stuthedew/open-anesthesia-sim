---
pr: 1079
recorded: 2026-09-26
---

Queue-only change: three item files under `docs/items/`, no code.

## What was checked

`PL-QKXZ` (Projects threads ran in Anthropic's default cloud environment, whose uv 0.8.17 and missing `libegl1` stop `make check` until repaired) recommended pointing both projects at the account's `Default` environment. `PL-7TH9` records that the owner switched one of them on 2026-09-26 at 02:43 UTC. This reads that switch back.

- **"Clean up PL" is in `Default`.** `get_session` reads `env_01JWikdPctorEBXoFK7bJnCq` for "Slam-dunk batch 16", a thread created 03:31:44 UTC.
- **"Fix generators" is not.** "Stream A: PL-PVW2, continued", a thread created 04:34:03 UTC, still reads `env_011111111111111111111119`. Both projects' coordinators read the old environment too, which the Projects docs predict for any session already running: "Changes to instructions, repositories, plugins, and environment in **Project settings** reach new threads, not threads already running."
- **`Default` needs no repair now.** Its setup snapshot was rebuilt at 03:48 UTC. This session, started from it at 05:07, found `/root/.local/bin/uv` a regular file reading uv 0.12.19, no `/usr/local/bin/uv`, `libegl1 1.7.0-1build1` installed, and `make check` exiting 0 with 4,745 tests passed and no repair.

## What changed

- `PL-QKXZ` stays at `needs-decision`, with the question narrowed to "Fix generators". Step 2 ("Clean up PL") is marked done. Step 4's fallback setup-script line now links uv only where pip put it under `/usr/local/bin`. It was replayed against scratch paths in its three cases: pip landing there, pip landing in the user site, and pip failing, which still exits 0. One stale sentence about `PL-H2V3` is corrected in place: it was dropped as a duplicate of this item.
- `PL-7TH9` (the setup script's unconditional `ln -sf` left uv a dangling link) is dropped as not reproducing. It is not closed as fixed, because no session can read the setup script to confirm the edit that the rebuild implies.
- `PL-877T` is captured: nothing the owner reads when creating a project says to set its cloud environment, so the next project will pay the same repair.

## Verification

- `make check` exit 0 in this container before any edit, with no environment repair.
- `make docket`: 0 errors after the edits.

## Left for the owner

In the "Fix generators" project, open **Project settings > Environment** and set **Cloud environment** to **Default**. `PL-QKXZ` step 1 has the exact path and step 3 has the read-back.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Dz21t2boBcppikKLySLrPb

---
_Generated by [Claude Code](https://claude.ai/code/session_01Dz21t2boBcppikKLySLrPb)_
