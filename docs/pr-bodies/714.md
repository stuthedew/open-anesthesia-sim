---
pr: 714
commit: c979d94f301a3da43747753e15e8e0599427012d
merged: 2026-09-19
items: PL-Y5JX
subject: PL-Y5JX: hold a touches entry naming an item file to naming one that exists (#714)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit c979d94f landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

An item's `touches` may name another item's file by full path, which is legitimate — some work is editing briefs. The store names a file from its title, so any command that re-renders a retitled brief moves it, and the declaration then points at nothing with nothing saying so.

`PL-Y5JX` anticipated this as a hazard of acting on the stale-slug rename advisory. It was already live, and by a route the item did not anticipate: both dangling declarations on `origin/main` were left by a command re-rendering a file, not by a session acting on the advisory.

- `PL-3V6C` named `PL-XQRK`'s old path, moved by the v0.4.28 cut (`5901ba4`). `PL-YTDN` later edited that very `touches` line to repair its sibling entry for `PL-TFWR` and did not see this one.
- `PL-GNXG` named `PL-J3ZK`'s old path, moved by `2a90152`.

## What landed

- **`_check_touched_items`** — an error on a `touches` entry naming an item file no item lives in. It names the file that item is really in, so the repair is the error message. Judged by the id the filename leads with, which leaves the store directory itself alone: 48 of this store's 95 entries under `docs/items/` are the bare directory, written by release cuts.
- **`_check_filenames`** — the stale-slug advisory now names the items whose `touches` declare each drifted file, so it stops being an instruction to break a declaration issued to a reader with no reason to look.

Both read `_declared_item_file`, so the error and the advisory cannot come to different views of what a declaration is. An item built in memory carries no filename, so a declaration naming it is declined rather than failed.

The filename pattern is `DECLARED_ITEM_RE` rather than `ITEM_FILE_RE`, which `vcs.py` already uses for the same id capture without the suffix anchor: that one matches a path prefix, this one judges a whole string a person typed.

## Repairs and riders

The two live declarations are repaired here — an error-level check that leaves `make check` red is not landed.

Riding the work commit, per the `docket` skill's close-out: the `pr:` numbers the base was owed for twelve landed closures. Eleven came from bare `bin/docket record`. `PL-YTDN` needed the explicit `record 712 --merge ceb9385`, because its closure commit changed only item files and `_carried_work` reads that as a closure that landed without its work. That is what left `main` red on quality run #2304 with an error no documented command could clear; it is filed as `PL-YFXG` and not fixed here.

## Verification

`bin/docket verify --self PL-Y5JX` reports `ACCEPT`. `make check` passes: ruff, mypy, 3216 tests at 100% core-branch coverage, `bin/docket check` at 0 errors. Docs swept — `subprojects/docket/README.md`'s "What is checked, and what is left alone" gained the new error and the advisory's coupling sentence; the five `candidates` hits in `ROADMAP.md` and `docs/WORKING_NOTES.md` concern unrelated parts of `checks.py` and remain true.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01ARi5jg6uiSRdiZre3D2rVe

---
_Generated by [Claude Code](https://claude.ai/code/session_01ARi5jg6uiSRdiZre3D2rVe)_
