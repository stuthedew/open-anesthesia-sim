---
pr: 130
commit: f1a6fa66ee7972e18e952ed45a378fa1cf43d863
merged: 2026-08-31
items: PL-HDY6
subject: PL-HDY6: admit the scope reader's mention-as-membership defect to the v0.2.8 gate (#130)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit f1a6fa66 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Found while verifying the gate state on `main` after #129 merged, and admitted at the project owner's agreement. The frozen list goes to **thirty-seven entries with eleven open**.

## The defect

`Scope.placement` calls an item in scope when the current milestone's section names its id *anywhere* between that heading and the next. Naming is the whole test, so a section that names an id in order to say it is **not** an entry marks it as one.

`bin/docket next` on `origin/main` at `7ee92ea`:

```
2. P2 PL-68XK Check that a recorded commit hash resolves, now that it is
   the optional half of provenance (S, ready)
  In scope for v0.2.8 — the workflow works, the step the project is on.
```

read out of the paragraph in that same section which says:

> `PL-68XK` … is a different case and is *not* admitted by this rule: it predates the freeze and was excluded from the approved list deliberately.

So the ranking places it second, above three genuine entries, with a reason stating the opposite of its source. The section names 39 ids and records 37 entries; the extras are the ones it names to exclude.

## Why it is a gate entry

It is the queue's ranking — one of the six pieces of machinery this release's goal enumerates — and unlike the four admitted earlier today it is misdirecting `docket next` on `main` right now rather than costing a correction later. A session asking what to work on is told this item clears the current gate; it does not.

It is also the mirror of two closed entries: `PL-1TPM` and `PL-0RS6` fixed out-of-scope work leading the list *unmarked*; this is in-scope marking applied to work the section excludes.

## Not fixed here, and why

Preferring the gate subsection over the section's prose is the obvious rule and is wrong on its own: v0.4.0 names nine open ids as its **own Required scope** rather than as gate entries, and those are genuinely in that milestone's scope. The item carries both candidate rules — read only the section's list structures, or let a section mark an exclusion explicitly — with what each costs, and a `verify:` command that was run first and selects nothing today.

## Also in here

`ROADMAP.md`'s nine counts corrected again, an hour after the last correction — recorded on `PL-H8MQ` (the entry count stated in six places and held to none), which now reads that the tax was paid twice in one day. `docs/WORKING_NOTES.md`'s gate thread records the part worth remembering: the pass that wrote the exclusions down is what made the reader mis-place them.

`make check` green: ruff format, ruff check, mypy, 797 tests, `docket check` (0 errors, 0 untriaged), `doc_check`.

---
_Generated by [Claude Code](https://claude.ai/code/session_012fWUfUETciARrvvabtid2w)_
