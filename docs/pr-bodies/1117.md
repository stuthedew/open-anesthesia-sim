---
pr: 1117
recorded: 2026-09-26
---

Before: `ROADMAP.md` planned in horizons without naming the model. Each scoped milestone had a detailed section, later releases had one-line timeline rows, and a catalogue was ordered by the Development pathway. Nothing said which part holds the order of a release, and nothing forbade a second plan beside the file. The two orders the file did hold had already come apart. The pathway says Phase 1 "follows it unchanged" after v0.4.0, while the timeline places Phase 1's substance generalization at v0.9.0.

After: a new section at the head of § "The plan", "The planning model: a rolling wave", does five things:

- It names the model and cites the source: the PMI *Lexicon of Project Management Terms* entries for rolling wave planning and progressive elaboration.
- It tabulates what each horizon is detailed to and where it is written.
- It says the timeline holds the order of every release it names, and the pathway only the order of what lies past it.
- It says when detail moves inward.
- It states the prohibition: no second plan of releases is kept beside this one.

Generated views (`bin/docket wave`, the digest's plan line) and dated records of decisions fall outside the rule by construction. The worked instance is the external proposal in #59 and #60. Its `docs/PLANNING.md` stated the same prohibition and restated the order in the same file. That copy has since fallen out of date: v0.2.8 was inserted ahead of it, and the schematic and multi-substance releases moved from v0.6.0 and v0.7.0 to v0.8.0 and v0.9.0.

The opening sentences of the pathway and of the catalogue are narrowed to match. The pathway's now also says that its phases are the stale side wherever they disagree with the timeline.

The brief was re-checked first. It was filed 2026-08-26, 283 `ROADMAP.md` commits ago. The problem still held, but its row numbers and near-horizon versions had gone stale, so it now names each horizon by section and has a `verify:` command and a `payoff:` line.

Captured, not fixed:

- `PL-5XG1`: rewrite the pathway to cover only what the timeline has not placed, and put the known-risk question to the owner.
- `PL-RH88`: `docs/ARCHITECTURE.md` still says "Editor contract" where `PL-GPYV` renamed it View.

Checks, run locally:

- The item's `verify:` command exited 1 before the edit and exits 0 after.
- `make check` passes.
- `bin/docket verify --self PL-LJVD` answers ACCEPT.

This session's container could not reach pmi.org (the host is blocked), so the two PMI definitions were checked against search-engine extracts of the Lexicon. The new section therefore paraphrases them rather than quoting them.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01RydrAMhkHMypLkb9tRzSwn
