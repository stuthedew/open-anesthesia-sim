---
pr: 266
commit: b4d0e7c8e1e8ae6817b6a70e8287f8e6803c30ef
merged: 2026-09-03
items: (none)
subject: Release v0.3.3 — the closure records its own number, and the formatter leaves the view (#266)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit b4d0e7c8 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts **v0.3.3**, carrying five items closed since v0.3.2 and completing the `delegation` feature at 12 of 12. Closes no item itself, so no id is owed in the title.

## What did not change, verified rather than asserted

- **`src/anesthesia_sim/data/` is byte-identical to v0.3.2** — `git diff v0.3.2..HEAD -- src/anesthesia_sim/data/` is empty.
- **The only `src/` change is `PL-WB0X`'s extraction**: `app/formatting.py` and `app/chart_series.py` out of `app/simulation_view.py`, which falls 1265 → 1072 lines. Behavior-preserving, held to the view tests that already existed.

No capability, equation, parameter, numerical method, unit or displayed value changed.

## What the release is for

`PL-WTQR` moved the `pr:` write into a job fired by the merge, and its first run proved that job both correct and unlandable in the same minute: it computed `pr: 261`, committed it, and `main` refused the push — `GH006`, "3 of 3 required status checks are expected". A push made with `GITHUB_TOKEN` starts no workflow, so the required checks can never report on the commit such a job pushes; the ruleset was already disabled at the time and the push failed anyway; and every configuration that would accept it weakens the gate that makes a red `main` mean something.

`PL-N5WZ` changed the trigger rather than the mechanism. `bin/docket record`, bare, writes every number the base is owed — the same reading `docket check` prints its advisory from, so the two cannot disagree — and `make fix` runs it, so the field rides whatever commit the session was making anyway. The workflow is deleted rather than left parked, since a parked workflow reads to a later session as a mechanism that works.

Alongside it, `docs/MODEL.md` § "Displayed precision" now terminates in `app/formatting.py` instead of in a private static method on a Flet view class, `core/` gained a 100% statement-and-branch gate over the saturation it already had (`PL-22Z3`), and `tools/pr_title_check.py` runs at the declared Python floor (`PL-79N5`).

## Also in this diff — the queue housekeeping this branch started as

- **`PL-W8XP` triaged to `ready`** (`P2 / S / infra / dev-tooling`): `blocked-by:` gains a milestone entry rather than a second field beside it. Classed `infra` rather than `defect` — the `needs-decision` workaround on `PL-B9PY` is honest as far as it goes, so it is not gate debt. Its `verify:` was run and watched fail for the right reason before being written down.
- **`pr: 263` and `pr: 264`** written onto `PL-WB0X` and `PL-22Z3` by `bin/docket record`, never by hand.
- **`PL-9K7K` captured**: `bin/docket next` ranks `PL-ZRSP` first among the v0.4.0 items, while `PL-ZRSP`'s own **Safety notes.** say to land it after `PL-DR1Z`, because F_A/F_I is the textbook wash-in curve only while inspired concentration is held constant. Captured rather than fixed — #264 was editing that file at the time.

## Verification

`make check` green locally and in CI on `ce0b2c9`: ruff format, ruff check, strict mypy, the `type: ignore` checker, pytest under the new 100% `core/` gate, `bin/docket check` (0 errors), `tools/doc_check.py` (0 errors), `tools/contrast_check.py` (0 errors). The one documentation advisory is the expected untagged-release window.

Docs swept: `python3 tools/doc_check.py candidates --base origin/main` lists only mentions of `pyproject.toml` and `uv.lock` as files — `README.md:71`, `README.md:111`, `docs/ARCHITECTURE.md:301`, `:311`, `docs/worker.md:100`, `ROADMAP.md:46`, `:1263`, `docs/WORKING_NOTES.md:643`, `subprojects/docket/README.md:1031` — and none of them states a version, so nothing became stale.

**After merge**, per `ROADMAP.md`'s tag rule — run it before anything else lands on `main`, so `origin/main` is still the squash commit for this pull request:

```bash
git fetch origin main
git tag -a v0.3.3 $(git rev-parse origin/main) -m "v0.3.3"
git push origin v0.3.3
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01MshJxxtvhYZhuPSN3SA2DT
