---
pr: 1044
recovered: 2026-09-26
commit: 6d2f05311924be037e9b46b293df313363d481e7
merged: 2026-09-25
items: PL-1WV7, PL-PRSF, PL-YS9F
subject: PL-1WV7, PL-YS9F, PL-PRSF: release reads the train from the shared holdings read, the hand-off reads the pre-bump plan, and notes keep angle-bracket placeholders (#1044)
squash: empty
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_01Jr391t5LXLAeRjbzMQrhwb?thread=cmsg_01Jr391t5LXLAeRjbzMQrhwbVcbUwSiGfPjhew1LvcJquK)_

Slam-dunk batch 7: three `bin/docket release` fixes that share `cmd_release` in `subprojects/docket/src/docket/cli.py` and `subprojects/docket/src/docket/release.py`. One commit per item, each item closed in its own commit, plus one formatter commit for PL-PRSF's test. Each item was re-checked against `origin/main` before it was started, and each gap was still there.

## PL-1WV7: a digest offering a release walked the branch refs twice

**Before:** `_cuts` built its own `Holdings` for the release train beside the one `_holdings` caches for the in-flight read, so a digest printing `Releasable:` walked every unlanded ref twice. `cmd_release` also built its read directly.

**After:** both read `_holdings(args)`, the invocation's one cached read. Each keeps its own decline for a store git addresses as the empty prefix, because `_holdings` passes that prefix straight through. In `cmd_release` the shared read is first asked after the command's own fetch, so it is no older than the refs the guards read.

**Verified:** `test_a_digest_offering_a_release_reads_who_holds_what_once` counts two `holdings` calls before the change and one after. `test_a_cut_from_a_store_at_the_repository_root_refuses_on_who_holds_the_train` pins the release decline. The item's `verify:` passes.

## PL-YS9F: the hand-off named both edits for a milestone number the cut reached

**Before:** at the hand-off the version table lacks the cut's own row, so a milestone number the cut had exactly reached was stated both ways: its table row is owed if this release is that milestone, and a new number if it is not.

**After:** `cmd_release` reads the plan just before the bump and hands it to `_hand_off`. A `release` beat asking for the cut's own version says this release is that milestone and its table row is owed. Any other beat says a patch has taken its number and the row owes a new one. Without a plan the statement is unchanged, and so is a resumed cut whose interrupted run already bumped, since the only plan left to read is the post-bump one. `outstanding_roadmap_edits` takes the plan as an optional argument, and `roadmap.stale_milestones` takes the reading as an optional `releasing` argument; its other caller is unchanged.

**Verified:** `test_the_reached_number_statement_reads_the_plan` covers the three readings on a fully scoped port fixture. A second test shows a `release` beat for another number is the second reading. An end-to-end cut on the release-train fixture prints the first reading, and fails if the plan is read after the bump (checked on a scratch edit). The item's `verify:` passes. PL-YS9F is on v0.6.0's frozen gate list, which stands at 56 of 187 entries cleared with this branch.

## PL-PRSF: an angle-bracket placeholder in a title vanished from the rendered notes

**Before:** a notes bullet and a new capture's Problem line copied the title into markdown verbatim. GitHub read a placeholder such as the n in refs/pull/n/head, written in angle brackets, as an HTML tag and dropped it, so v0.5.9's notes print `refs/pull//head`.

**After:** a new `release.markdown_title` puts a backslash before each opening angle bracket outside a code span, which CommonMark renders as the bracket itself. A code span and an already-escaped bracket are left as they stand, and the front matter keeps the title as typed. The six notes files already shipped are left alone, as the brief says. Fifteen item titles on `main` carry such a placeholder today.

**Verified:** `test_an_angle_bracket_placeholder_in_a_title_survives_the_rendered_notes` and `test_a_captures_problem_line_keeps_an_angle_bracket_placeholder` both fail with the two call sites reverted. A third test covers a hand escape, an arrow, an unclosed backtick and a double-backtick span. The item's `verify:` passes.

## The whole branch

`origin/main` was first merged in after `#1038`, `#1039` and `#1040` landed. `bin/docket verify --self PL-1WV7 PL-YS9F PL-PRSF` prints ACCEPT for all three on that merged tree, which includes each `verify:` command and a green `make check`. Main was merged in again, never rebased, each time it moved while this pull request waited. Each of those merges was checked before its push: a full `make check` passed after every merge that brought in code, and `tools/doc_check.py` and `bin/docket check` after one that brought in only instructions, docs and items. Docs checked for statements the change made false: `subprojects/docket/README.md` (the release hand-off section and root resolution), `ROADMAP.md`, `docs/resident-instructions.md` and `docs/ARCHITECTURE.md`. None needed an edit.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01E1QhkRULBLwPiBEmnRfco2

---
_Generated by [Claude Code](https://claude.ai/code/session_01E1QhkRULBLwPiBEmnRfco2)_
