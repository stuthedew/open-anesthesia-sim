---
pr: 597
commit: 25abc21836157d41c6d188fbb3c901540a7ce480
merged: 2026-09-15
items: PL-16ZC, PL-4D1M, PL-7G5M, PL-JFXG
subject: PL-16ZC, PL-7G5M, PL-4D1M, PL-JFXG: close the show/hide question, and record the window-management decision on item 34 (#597)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 25abc218 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Four items, in two halves.

## `PL-16ZC`, `PL-7G5M` — the show/hide question, closed on the spec that already answered it

`PL-16ZC` (a show/hide control for the two clinical references and the control marks) asked whether those elements should be hideable at all. `docs/MODEL.md` § "Minimum displayed outputs" already answered it:

> The chart's compartment traces are the reader's to show and hide … and a required value must not leave the display with the curve that draws it. The chart entries in this list — the MAC-awake band, the 1 MAC line, the control marks, and $`F_A/F_I`$ — are requirements on the chart itself and are not selectable.

`git blame` puts that on `PL-CG7J` (#315), the same change that gave the six compartment traces their checkboxes, completed by `PL-RCTQ` (#352) the next day. `PL-16ZC` was filed that same day and cites the section for the reference-labelling obligation but never for this sentence.

Hiding a trace is safe because that section separately requires all six compartment values in the **numeric readouts**, which never leave the display. The two references have no readout equivalent, so the chart is the only place they exist. Closed on the project owner's delegated call and on their steer that no part of the interface is near its final form. **Nothing is built and the specification is unchanged.**

`PL-7G5M` closes with it: `PL-16ZC`'s brief still read "sequenced past v0.5.0, not clearable before it begins" after `PL-G59B` (the pyqtgraph chart port) closed and promoted it to `ready` — prose saying stop beside fields saying go. The block now records the deferral as discharged rather than deleted; Gate 1 § "Sequenced past v0.5.0" is left untouched as the frozen snapshot it declares itself to be.

Both items' `verify:` commands were replaced. The old one grepped for a test asserting the references *can* be hidden, which a "not hideable" closure can never satisfy.

## `PL-4D1M`, `PL-JFXG` — the window-management decision, on planned-milestone item 34

Item 34 gains the project owner's 2026-09-15 decision and the additions their description carried that it did not:

- **Tiling by default, break-out rather than floating panels.** An area may be broken out into its own top-level window, itself tiled — the Blender Manual's "Areas" section documents this as *View > Duplicate Area into New Window*. That is the reading of "floating" recorded, and it preserves the no-overlap invariant the section's safety argument rests on. Floating panels overlapping the tiled areas **within** a window are recorded as refused, with the covered-value reason, so the word alone cannot re-open it.
- **The widget catalogue is separate work and the layout comes first**, in the owner's own wording, with one refinement: a container abstraction exercised by a single view is not exercised, so item 34 validates against two views that already exist. Planned-milestone **item 36** is the catalogue.
- **What a preset differs in, by example** — induction zoomed in, a big-picture workspace for maintenance, learner-created ones beside the shipped defaults, and a central-graph default shape.
- **Item 37** is the opioid/hypnotic interaction display, after items 13 and 15, recording that a response-surface model is its own model class whose measured quantity is the synergy — so it owes its own parameter file and provenance rather than riding item 15 — and what the display owes under the clinical-output standard. No parameter set is named: none was verified against its source here.

## Left open deliberately

`PL-WLWY` is triaged to `ready`, not closed. Break-out sharpens its question rather than answering it: a broken-out window can be dragged over the one carrying the minimum display, so which windows owe `docs/MODEL.md` its minimum is now a live question, and the three candidate readings are written into the item.

Its brief also **corrects the case first made for the persistent strip**. That argument rested on Blender's Topbar and Status Bar being always present; the Manual's "Status Bar" section documents *Show Status Bar* as a way to hide it. So this project follows Blender on the structure — a region outside the area system — and departs from it on hideability, because Blender has no class of value whose absence is a safety failure and this one does.

## Checks

`make check` green in full locally on both pushes: ruff format/check, mypy, pytest at 100% branch coverage on `anesthesia_sim.core`, `docket check` at 0 errors, doc-check, contrast, agent-identity, import-boundary, workflow-paths, core-vocabulary and glyph.

**Note on `main`:** it was red independently of this branch, on `docket check --verify` — `PL-S5YM`'s bare `-k covered` selector started matching a test #593 added. Another session is on it; nothing here touches it.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013h3Jan3WHon75CwFVbAUfj
