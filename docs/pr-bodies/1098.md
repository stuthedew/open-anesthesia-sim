---
pr: 1098
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGQVv5Nj81CscwCPYmHEu5Dc)_

Before: docket read a `verify:` command two ways. `checks.py` used its quote-aware `_shell_words`, while `verify.py`'s five readers used regexes and `shlex` of their own. So `grep -qF "it's" a.md && bin/docket verify PL-K7QX && grep -qF "it's" b.md` was not refused as a recursion into `docket verify`, because the single-quote regex ran from one apostrophe to the next across the command between them. `grep -qF 'x' a.md|curl -s https://example.com` was not seen to ask the remote, because `shlex.split` kept `a.md|curl` one word. And a quoted `&&` cut a clause, so the word after it read as a program rather than as a path the command reads.

After: both files read a command through one lexer, `subprojects/docket/src/docket/shell.py`, and neither keeps a quote regex or `shlex`. The lexer also reads the body of a `$( )` or a backquote as the command bash runs, so a `docket verify` or `docket check` inside one is found wherever it stands. Over the store's 1,353 `verify:` commands (1,340 distinct), the admitted-shape rule, both pytest rules, `never_fails`, `reenters_verify` and `reads_check_output` answer exactly as before. `reaches_outside_tree` changes one closed item's answer: PL-SRBR's `test "$(git ls-remote ...)"` now reads as asking the remote. `command_paths` drops a program name from 16 commands, 15 of them run by a substitution's body and one behind an apostrophe inside double quotes. Only one of those 16 items is open, PL-2C53, and what it loses is the bare word `grep`, which names no file in the tree. Every refusal message is unchanged. PL-P7J7 is closed. PL-PVW2 stays open with `generator: live`, on PL-CWBJ: `tools/doc_check.py` still splits a CI step's `run:` with a pattern of its own, and whether that is PL-PVW2's member is a decision put to the project owner in the project thread. PL-PVW2's `verify:` passed once this landed, so it gains that step's predicted test, `test_workflow_paths_reads_a_quoted_path_as_one_word` in `tests/unit/test_doc_check.py`.

How: `_shell_words` and its types move from `checks.py` to `docket/shell.py` as `shell_words`, `Word`, `Clause` and `Reading`, reading a command with no substitution exactly as before. A `$(` or backquote, quoted or not, now opens a body read by the same rules over the same string: a `$(` ends at the `)` that closes it, and a backquote at the first backquote no backslash escapes (Bash Reference Manual §3.5.4). The word keeps the substitution's raw text, `Reading.substitutions` holds every body in order, and `Clause` carries each token's offsets. `verify.py`'s readers then read words. `never_fails` joins the one clause's words; `reenters_verify` and `reads_check_output` look for the word `docket`, or a path ending in it, followed by `verify` or `check`; `command_paths` cuts commands at `||`, `;`, `&` and the `case` terminators and scans each clause as written; and `reaches_outside_tree` reads every word. `subprojects/docket/tests/test_verify.py` adds `test_an_apostrophe_in_double_quotes_hides_no_command`, the name PL-PVW2's `verify:` predicted, and a test for the answers bash's reading changes, a quoted `'true'` among them. Both fail against `main`'s readers and pass here. `bin/docket verify --self PL-P7J7` says ACCEPT, with `make check` passing (4,904 tests).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01WPXU7KgKFri8veu8Eggasb
