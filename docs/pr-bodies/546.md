---
pr: 546
commit: 2fe09de828ed5a91b5ac2c11eb88545aa6d688ce
merged: 2026-09-13
items: PL-0R8J, PL-YPWT
subject: PL-YPWT, PL-0R8J: triage the twenty-five untriaged captures (#546)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 2fe09de8 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Folds twenty-five of the twenty-seven untriaged items into the queue. Two are left alone: `PL-B9K7` (the Yasuda 1989 tissue-solubility full text) is in flight on `origin/claude/determined-hamilton-atx5bi`, and `PL-N701`'s file is edited and retitled on that same branch, so a second answer here would be a second resolution of one file.

## Two items close

**`PL-YPWT`** (PL-4QCJ names PL-Z3V5 as a prerequisite in prose without declaring the edge) — done. It asked for one of two remedies and the first was taken: `PL-4QCJ` now carries `blocked-by: PL-Z3V5` with `status: blocked`, which is the half `bin/docket next` reads. `docs/references/README.md` forbids inventing the extraction-note format elsewhere, so the dependency is a blocker rather than an overstatement, and `bin/docket check`'s own advisory recommended the same disposition. The same edit corrects `PL-4QCJ`'s now-stale claim that `PL-Z3V5` exists only on an abandoned branch — `PL-ZGK2` recovered it.

**`PL-0R8J`** (the YAML-list `touches:` parse defect) — dropped as a duplicate of `PL-FX0K`, captured in the same commit on the same day. `PL-FX0K` is the one kept, because its title carries the word that matters: the parse is *silent*.

## One item enters Gate 1

**`PL-DJYF`** is seated at `P1`, effort `S`, `defect, science, docs` — the same reading its twin `PL-4YY1` (record provenance for the circuit volume and default fresh gas flow) closed under in v0.4.20. `AlveolarCompartment` carries `gas_volume_l` and `alveolar_ventilation_l_min` as dataclass field defaults while `data/patients/reference_adult.json` stores both with their provenance, and unlike the circuit nothing pins the two copies equal. It is admitted to the gate under the unconditional science exception; the v0.5.0 timeline row's entry count moves 160 to 161.

## Thirteen more are declined to Gate 2

Recorded in `ROADMAP.md`'s existing `### Declined to Gate 2` subsection rather than a new one, since `_declined_ids` reads only the first (`PL-82B0`). Nine on the refilling-queue ground, four on grounds of their own, with the reason for each stated rather than stretched.

## Every `verify:` command here was run first

Each was executed against this tree before being written down, and each failed for the right reason — the file's suite green, the `grep` for the work absent, exit 1. `PL-YPWT`'s command passed after the edit and failed before it.

## Checks

`make check` passes: 2571 tests, 0 `docket` errors, 0 documentation errors. The `docket record` advisories for `PL-D89D` and `PL-ZGK2` are cleared by `bin/docket record` riding this commit.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01VRJN1jqMpoAEneRAqxnz5V

---
_Generated by [Claude Code](https://claude.ai/code/session_01VRJN1jqMpoAEneRAqxnz5V)_
