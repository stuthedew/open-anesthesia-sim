---
pr: 499
commit: 355b604c77a9044bc007df1bc4408bc7734d44bc
merged: 2026-09-12
items: PL-01CK, PL-21GS, PL-27S8, PL-CPLD, PL-K2YF, PL-M58K, PL-V4LS, PL-VV4D
subject: PL-01CK, PL-21GS, PL-27S8, PL-CPLD, PL-K2YF, PL-M58K, PL-V4LS, PL-VV4D: threshold analysis before a tightening proposal, the left-behind check decided, and six verified consolidations (#499)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 355b604c landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-27S8` (require a threshold analysis before any tightening proposal) and `PL-VV4D` (whether the left-behind check should compare against `refs/pull/N/head`). Carries the in-progress work of `PL-6ZQY` (the workflow-lane consolidation pass), which is still verifying and will push more onto this branch.

## Why this branch exists

The project owner asked how many outstanding workflow items there were, then for the lane to be consolidated, then for it to stop accumulating. The count is 105 workflow-lane items of 244 open. The accumulation question turned out to be the interesting one.

## What the measurement found

Tracing the commit that created each item file back to the id its subject leads with: the workflow lane **generates 0.69 new workflow items per workflow item worked**, and 52 of the 105 open ones were captured by a session working another workflow item. Draining the stock therefore costs about 337 closures rather than 105, and leaves the rate untouched at the end.

A 17-agent fan-out then read all 134 open workflow-lane and crossing items in full and checked each against the tree. Six of fifteen clusters — **63 items, 47% of the queue** — share one mistake: the apparatus infers a fact it could have recorded. That is the generator behind the 0.69.

## PL-VV4D — the left-behind check

Decided: build the exact `refs/pull/N/head` comparison, put it in `tools/`, and keep it **beside** `vcs.orphaned` rather than replacing it.

- **Build it** because the content comparison is a heuristic that has already produced two recorded false positives in a report whose other half prevents lost work.
- **`tools/`** because `PL-SK88` decided `docket` must not learn about the harness, and `refs/pull/*` is a GitHub ref namespace.
- **Beside, not instead** because `bin/docket` runs from a bare checkout that has not fetched those refs, and retiring the portable half would leave no detection at all in exactly the case the queue is built to survive.

The reasoning and the costs accepted are recorded on the item so a later reader does not re-litigate them. `PL-R808` is filed for the build, carrying the invariant and the two known test vectors from `#312` and `#284`.

## PL-27S8 — the rule

A session proposing to tighten anything must now name what the suppressed side would have to be worth for the proposal to be wrong, and count it.

This exists because a proposal in this same session failed that test. Having measured the self-generation rate, I proposed raising the apparatus capture bar on the strength of it — a number for the cost side and none for the benefit side. The count never run was that **67% of the queue is still-real findings**, which killed the proposal. The owner objected on instinct first; the number only confirmed what the objection already said.

The evidence was verified rather than recalled. The quantification-fixation abstract was read via PubMed (Chang et al., PNAS 2024;121(46):e2400215121, 21 preregistered experiments); Circular A-4's threshold-analysis paragraph was read from the PDF the owner supplied, not from a search summary — and the primary source turned out to state the failure as well as the remedy: "the most efficient alternative will not necessarily be the one with the largest quantified and monetized net-benefit estimate."

Resident instructions grow 2255 characters. Kept rather than routed, because the moment the rule governs is a design round in a reply, which no read precedes — the same argument that already made `expert-review.md` resident under `PL-WWDT`. The estimate given to the owner beforehand was ~1400, wrong by 60%, and that miss is recorded on the item because the rule is about not trusting the comfortable number.

## Also here

- `PL-LKGL` records why no automatic check can spot a stale item: a `verify:` command tests for the presence of the fix, not the fault, so all six dead items carrying one still fail. A churn-based advisory was tested and rejected — `partly` churns less than `yes`, so the signal is not monotone — and is recorded so it is not rebuilt.
- `bin/docket record` wrote `pr: 498` onto `PL-3Z9X`, riding a commit rather than composing one.

## Checks

`make check` green: 2361 tests, 100% coverage, 0 errors across docket, doc-check, branch-id, rules-paths, contrast, agent-identity, import boundaries and workflow paths. The one advisory is the resident-growth line, answered above.

---
_Generated by [Claude Code](https://claude.ai/code/session_01PSFju1Ymvn4Un2kDicWht4)_
