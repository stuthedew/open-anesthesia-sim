---
pr: 1116
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGGH2gkkwZT4WeRwigREWcSs)_

Before: docket kept no record of what the remote holds, so each reader that needed it took one of the clone's own copies instead, and four did so wrongly after PL-4Q9B closed. `claim` asked `git ls-remote` about its own branch alone, while `show`, `flight`, `next`, `new` and the release-cut read took every tracking ref as a live holder, including one for a branch the remote had deleted. A claim no fresh clone can see still held its item.

After: a command that reads the remote takes one `git ls-remote --heads origin` listing and hands it to every reader beneath it. A tracking ref whose branch the listing lacks holds nothing and names no cut in flight, while `stranded` and `orphaned` still read it as the surviving copy of that branch's work. Where the listing fails, readers fall back to the tracking refs and the refs line says so.

The owner chose this record on the thread's decision card, over `git fetch --prune` (which deletes those surviving copies) and further per-reader fixes.

How: `vcs.RemoteHeads` and `vcs.remote_heads` sit beside `Snapshot`, which now carries the listing a read command takes once its own fetch has answered. `_unlanded_refs` drops an `origin` tracking ref only when handed a listing that answered, so every caller that passes none reads exactly as before. `claim` and `yield` take one listing each and `_on_remote` reads it; `_publish` takes a fresh one after its push, because the earlier one lacks a rival branch pushed in the same minute. One visible change: where the listing fails, `claim`'s message names the failed command but no longer quotes git's own error text, since the module's runner does not carry it.

Not in this pull request: `claiming._on_copy` (PL-C3MN, a rival's stale tracking ref) and `arming.arm` (PL-21KN, the checkout's HEAD read as the pull request's branch) move onto the same listing in their own pull requests, each with the real-git test its brief names.

Verification: `make check` locally, 5,048 tests passed, ruff and mypy clean. New tests cover the listing itself, a deleted branch's hold and cut, `flight` and `show` with and without `--no-fetch`, and the failed-listing refs line.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01JhPGcM9LtJx9AVx7HdiXCy

---
_Generated by [Claude Code](https://claude.ai/code/session_01JhPGcM9LtJx9AVx7HdiXCy)_
