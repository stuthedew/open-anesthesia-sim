---
pr: 509
commit: bc4c258588e3c5d1beb149fa911492f56f7bfd3a
merged: 2026-09-12
items: PL-LBR6, PL-T6F9, PL-YTDN
subject: PL-YTDN, PL-LBR6: file PL-T6F9 for the v0.4.15 cut, to be run in a fresh session (#509)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit bc4c2585 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

One new item file. No code, no closures.

The project owner asked for v0.4.15 to be cut in a **new session**, so `PL-T6F9` is filed here rather than there. That way the paste line carries an id, the new session's name and branch derive from it, and every in-flight guard can see the work from the moment it starts — `CLAUDE.md`'s "file the housekeeping before you do it", applied to housekeeping somebody else will do.

**This needs to land before that session starts**, since `bin/docket show PL-T6F9` has to find the item on `main`.

## What the brief carries

Everything that session would otherwise reconstruct from scratch:

- **The hard prerequisite** — #508 had to merge first, because this release stamps `milestone:` onto the same fifteen items whose `pr` numbers #508 wrote, and the notes cite those numbers. *(Satisfied: #508 merged at `ce020b2` while I was writing the item.)*
- **The three threads** worth leading the baseline prose with — the checks that stopped over-reaching, Gate 1 passing half at 80/159, and `PL-MGF9` dropped for being solved another way. Nothing generates that prose, and it is the only record of what a range of work was *for*.
- **The procedure in short form**, with the reasons the two easy mistakes are mistakes: `bin/docket release` alone bumps `pyproject.toml` and leaves `uv.lock` stale, failing the next `make check` on `uv sync --locked` for an unrelated-looking reason; and `make check` run before the ROADMAP edits fails on edits nobody has been asked for yet.
- **The tag commands, filled in**, with `PL-N936`'s warning that a session *cannot* push a tag — `git push --dry-run` reports `[new tag]` and the real push dies on `send-pack: unexpected disconnect` while `git ls-remote --tags` shows nothing. Plus: read `git ls-remote --tags` **sorted**, because the raw output is lexicographic and `v0.4.9` sorts after `v0.4.14`. I got that wrong earlier today and briefly thought five tags were missing; they were all present.

## Two deliberate choices

**The commit subject leads with `PL-YTDN, PL-LBR6`, not `PL-T6F9`.** Leading with `PL-T6F9` would make `bin/docket show PL-T6F9` report it in flight on this branch and `bin/docket next` exclude it — blocking the session this exists to unblock. The subject names the items this session was actually working.

**This branch was restarted rather than pushed to.** #508 merged (squashed) mid-write and its branch was deleted, leaving my local branch carrying already-merged history plus one unmerged commit — the `PL-3D2M` shape. The merge was confirmed (`merged: true`, `01:13:55`, `ce020b2`) before the single unmerged commit was rebased onto the new base, per the skill's stranded-branch recovery.

## Verification

`verify:` was run before being written into the item and fails for the right reason — the `doc_check` half passes on the current tree, the version grep does not.

```
make check
  2388 passed, 100% coverage
  docket: 0 errors, 1 advisory (filename drift, PL-YTDN's to clear)
  documentation: 0 errors, 0 advisories
```

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CBJzQiBVA7p5WLSADYhvrR

---
_Generated by [Claude Code](https://claude.ai/code/session_01CBJzQiBVA7p5WLSADYhvrR)_
