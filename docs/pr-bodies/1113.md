---
pr: 1113
recorded: 2026-09-26
---

Two queue items, one of them the live remainder of the other.

**PL-GVC0 (make the `PL-` id prefix configurable) is dropped**, on the project owner's word and on its own brief's condition: it was to be built "at the moment a second consumer appears, and not before", and no second project uses docket or is planned. The drop records what a reopening session needs. The prefix lives in `store.ID_PREFIX`, and about twenty regexes compile from `store.ID_PATTERN` at import across `checks`, `claiming`, `claims`, `notes`, `release`, `roadmap`, `vcs` and three tools. `config.load` needs a root those patterns are compiled before, so the seam means threading `Config` to each or a global mode. The trigger is recorded: reopen when a second consumer appears.

**PL-PB8V (BRANCH_ID_RE's unguarded second spelling of the id grammar) is fixed.** `vcs.BRANCH_ID_RE` wrote the id alphabet out a second time in lower case. `tools/fixture_id_check.py`'s `GRAMMAR_RE`, which exists to refuse exactly that, matched the prefix case-sensitively and so could not see it. Had `store.ID_ALPHABET` ever changed, the store and the check would have moved together and branch-name attribution (`claims.py`, `tools/branch_id_check.py`) would have silently kept the old alphabet.

- `BRANCH_ID_RE` now embeds `store.ID_PATTERN` under `re.I`, as `ANY_ID_RE` beside it already did. The separator groups either side are stricter than the pattern's own lookahead, so the set of matching branch names is unchanged.
- `GRAMMAR_RE` matches the prefix in either case. Over the repository it now flags the old `vcs.py` line and nothing else, and is clean with the fix.
- Recorded as a member of PL-ZJ6X (the spent "which strings are valid item ids" generator head): the one site its verdict missed, and the channel it slipped through is closed here.

**Verification.** `test_the_branch_id_pattern_is_the_stores_grammar` (docket `test_vcs.py`) and `test_a_lower_cased_restatement_is_refused` (`tests/unit/test_fixture_id_check.py`) each fail with their fix reverted and pass with it. `ruff format`, `ruff check` clean on the changed files; `make docket` 0 errors. `docs/ARCHITECTURE.md`'s map line for `fixture_id_check.py` now says the prefix is matched in either case.

`bin/docket arm` answers `hold` (the diff reaches `tools/` and `docs/ARCHITECTURE.md`), so this waits on a read and auto-merge stays off.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UU7gmYsczUYQ1EpS924TSd
