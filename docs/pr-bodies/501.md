---
pr: 501
commit: 00c8919dee5bf6bb35b77d956795f41849bed241
merged: 2026-09-12
items: PL-8M8H, PL-JTXX
subject: PL-JTXX: record the _landing_split qualifier decision on PL-8M8H (#501)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 00c8919d landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-JTXX`. Records the project owner's answer to `PL-8M8H`'s part two, which `PL-3ZZT` opened in #500.

## The decision

`PL-8M8H` may **not** adopt the unqualified `_landing_split` reading. Any restart verdict it produces must carry `_commits_by_landing`'s `took_one_whole` narrowing at `vcs.py:2801`, or an equivalent excluding the same shapes.

The unqualified reading — "a branch whose landed side is non-empty has had work taken by a pull request" — admits two documented false-positive shapes:

- `PL-Y31G`: a branch forked before a history rewrite, its pre-rewrite blobs duplicated on the base, with nothing merged.
- `PL-XLQ5`: the base holds a superset of the branch's blobs.

Today those cost a spurious line in `bin/docket stranded`. On `PL-8M8H`'s path they would drive the **restart** arm, telling a session whose pull request is still open to run the three commands `.claude/skills/docket/SKILL.md:216` describes as destroying a branch. So the qualifier is not a refinement of the answer — it is what keeps a wrong answer non-destructive. `PL-JHJ3` and `PL-5TRV` added that same guard to `orphaned` after it fired falsely on its first live run.

The reasoning is written onto the item rather than left in a commit message, so a later session cannot reopen it as a simplicity or performance question without meeting the argument.

**What it does not settle:** part one — whether `disposition` gains a fifth state or `format_branch_state` keeps four and adds a line beside them — is untouched, so `PL-8M8H` stays `needs-decision`. The qualifier constrains whichever shape part one picks.

**Carried into the build:** a qualified reading is strictly more conservative and will decline to call some genuinely-merged branches merged. For a verdict whose wrong answer destroys work, declining is the right failure direction.

## PL-JTXX dropped

Dropped by the session that filed it, with the reason on the item. Its argument was correct about the state it was written in: while support ticket 4733783 was an open-ended wait, `PL-0SCG` was unstartable and `docket next` could offer a legal-exposure item to a session with no move available.

GitHub Support's reply ended that state. `PL-0SCG`'s remaining work is now a bounded check any session runs in one command against a named range, so `status: ready` became correct and there is nothing left here to work. The file is kept, not deleted, so the reasoning is findable if the status is questioned again — the answer being that it depended on the ticket's state, which changed.

## Also here

`bin/docket record` rode the commit, writing `pr: 500` onto `PL-3ZZT` and `PL-NGLV` rather than being given a commit of its own.

## Checks

`make check` green: 2361 passed, 0 errors across docket, doc-check, branch-id, rules-paths, contrast, agent-identity, import boundaries and workflow paths.

---
_Generated by [Claude Code](https://claude.ai/code/session_01PSFju1Ymvn4Un2kDicWht4)_
