---
pr: 270
commit: 82c5c3d29b27fd5139c39392b7f8c13a5850362a
merged: 2026-09-03
items: PL-PGZK
subject: PL-PGZK: recover the stranded capture, and strip its duplicated template stub (#270)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 82c5c3d2 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`bin/docket stranded` reported `PL-PGZK` as the only item existing on a branch
and nowhere the default branch can see — `origin/claude/what-next-rsmqeu`,
whose newest commit is seven hours old.

That branch does carry its own recovery, so this may arrive twice. The content
is byte-identical, because it was checked out from that branch, so git merges
it. **Duplicating a recovery costs nothing; losing the only copy is the failure
mode**, and `PL-HKF4` came within one `git fetch --prune` of exactly that
earlier today.

## The template stub

The file carried `docket new`'s scaffolding above the real brief — a
`**Problem.** <title>` line and three empty headings — so it read as an empty
brief followed by a full one. That also defeats the check that those headings
exist: they were present and blank. Removed; the brief underneath supplies all
four.

## Riding along

`pr: 269` on `PL-9K7K` and `PL-P23D`, written by `make fix`. Third run of the
arrangement `PL-N5WZ` shipped, and still no commit or pull request of its own.

`make check` green, coverage gate included.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_012babDGoxjnCEvrDoQEFzCw

---
_Generated by [Claude Code](https://claude.ai/code/session_012babDGoxjnCEvrDoQEFzCw)_
