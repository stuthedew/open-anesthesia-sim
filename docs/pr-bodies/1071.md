---
pr: 1071
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_01Jr391t5LXLAeRjbzMQrhwb?thread=cmsg_01Jr391t5LXLAeRjbzMQrhwbQFahs2ZWug4G14Qq5KHmC7)_

Before: `bin/docket set --verify` wrote a command that already passed, and the next `make check` refused it. `bin/docket trend --no-git` handed the report an empty churn reading and let the report guess the cause from its shape. `BranchState` and `StrandedReport` each carried a `fetched` field that repeated `Snapshot.fresh` and that nothing read.

After: `set` runs the command it is writing and refuses one that already passes, in the replay's own words. `trend` is told when git was not asked. Freshness is carried by `Snapshot.fresh` alone.

Three slam-dunk items from the 2026-09-26 run, batched because all three edit `subprojects/docket/src/docket/cli.py`. One commit per item, each closing its item.

### PL-FTDB: `set` refuses a `verify:` command that already passes

`cmd_set` now writes the line, then runs the replay's own `already_passing` scoped to that one item through a new `_replayed` helper, and keeps only the errors that run adds to `analyze`. The command has to run with the line written, because a `grep` for a string the command itself spells passes on no other tree (`PL-CBDX`'s case). So a refusal writes the file's original bytes back, and so does an interruption during the run. Whatever the replay reads as no finding refuses nothing here either: a command not found, one killed at the limit, one that reads past the tree, or a status the replay does not ask about.

Verified: the new `test_set_refuses_a_verify_that_already_passes` failed on `e991944d` before the fix (the self-matching `grep` was written and `set` exited 0) and passes after. The item's `verify:` passes.

### PL-PWH6: `trend` takes the command's word for whether it asked git

`cmd_trend` passes `None` under `--no-git` instead of a bare `Churn()`. `trend._churn_reading` reads `None` as not asked, the same `None`-means-a-caller-that-did-not-ask idiom `checks.py` uses for the replay. A history git answered with no line counts now reads as read and draws its columns at zero, where it used to read as not asked. A partial reading with days in it stays read. The `test_cli.py` test that runs `--no-git` is renamed `test_trend_omits_the_churn_columns_under_no_git`. A new `test_trend.py` test holds both readings.

Verified: on `e991944d` the old `_churn_reading` reads an empty undeclined `Churn()` as not asked and `analyze(None)` raises, so the new test fails there. It passes now, and the item's `verify:` passes.

### PL-Z909: retire `BranchState.fetched` and `StrandedReport.fetched`

Both fields and the `fetched` arguments to `branch_state()` and `stranded()` are gone, so no caller can state freshness for itself again. `cli._stranded` keeps its `_snapshot(args)` call for the fetch it makes: dropping the call along with the argument would have read the stranded report from unrefreshed refs, under a refs line naming the fresh moment. The `test_vcs.py` assertions on either field now assert `Snapshot.fresh`.

Verified: the item's `verify:` passes, and nothing under `subprojects/docket/src/` read either field (`grep`).

### Checks

`set -o pipefail; make check 2>&1 | tail -40` exited 0 after each item's commit, and again on the tree with `origin/main` (`9bd852c0`) merged in, inside the audit below.

`bin/docket verify --self PL-Z909 PL-PWH6 PL-FTDB` accepts PL-PWH6 and PL-FTDB. It rejects PL-Z909 on "no existing assertion removed" alone, the case the close-out mode expects for a commissioned rewrite, since PL-Z909's Done when asks for exactly these changes. Its five lines are one `stranded()` call losing a `fetched=True` argument the function no longer takes, the two `.fetched` assertions now asserted on `snapshot(...).fresh`, and the two `snapshot()` tuple assertions losing their `branch.fetched` member. PL-Z909's `verify:` and `make check` pass.

Docs checked: `subprojects/docket/README.md` (the `set` refusal passages near "`docket set` refuses any write `docket check` would then fail", and the `docket trend` section), `.claude/skills/docket/modes/triage.md` and `docs/worker.md` for what they say about running a `verify:` command, and every `.md` file for `BranchState.fetched`, `StrandedReport.fetched`, `fetched=` and `Churn()`.

Filed: `PL-YYV9`. The triage mode's paragraph on when a passing `verify:` command gets caught predates `PL-71P4` and now `PL-FTDB`, and it is outside all three items' `touches`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01HzN3puXbabad3QTmvZPYvp

---
_Generated by [Claude Code](https://claude.ai/code/session_01HzN3puXbabad3QTmvZPYvp)_
