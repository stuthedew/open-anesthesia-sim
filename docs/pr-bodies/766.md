---
pr: 766
commit: bb5c3899973d61d92223c5f9f79b30cc242634d0
merged: 2026-09-20
items: PL-CTD7
subject: PL-CTD7: a MAC target halts on every crossing, and the two detector properties that follow (#766)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit bb5c3899 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The project owner decided `PL-CTD7`'s open question on 2026-09-20: **a MAC target halts the run on every crossing**, chosen over "once until re-armed", which was this session's recommendation. `PL-CTD7` moves from `needs-decision` to `ready`.

## Why the recommendation was wrong

A re-arm flag is a **hidden mode** — two targets identical in the list, behaving differently because one has fired and one has not, with nothing on screen distinguishing them. `.claude/rules/expert-review.md` asks that hidden modes and context-dependent behavior be minimized, and the proposal added one to save an interruption the learner could simply resume through. The cost side was also overstated: resuming a paused run is one control, and the learner is already looking at it.

It would also have contradicted what the store already said. `MacTarget`'s docstring records the same day's decision to carry no crossing direction, in these words: *"a height is a height, and a run that passes through it on the way up and again on the way down has reached what the learner marked both times."* Halting only once would have made that sentence false for the second crossing.

## Two properties now follow, and neither is the rejected mode in disguise

Both are properties of the detector rather than states a target carries or a learner sets.

1. **A crossing is a transition between steps, not a comparison at one step.** Tested as "the value is at or above the height", a run that settles exactly at `0.8 × MAC` satisfies it on every subsequent step and can never advance. The test is that the value was on one side at the previous step and is on the other at this one — which is also the only form that can report *which* step crossed, the thing this item exists to get right.

2. **The crossing just halted on is not a new crossing when the run resumes.** The halt leaves the run paused *on* the crossing step, so a detector re-evaluating from nothing would halt again immediately and the run could never pass a target. Carrying the side the value was on at the previous step prevents that, and it is the same state clause 1 already requires — not a per-target armed flag, and nothing a learner can see or set.

`Done when.` is extended by two tests the decision adds: a run taken up through a target and back down halts twice; a run that settles exactly on a target's height halts once and then advances. `docs/MODEL.md` states that a target halts on every crossing in either direction.

## Also in this commit

- `verify:` written and **run and seen to fail** (exit 1): `grep -q 'def test_a_target_halts_on_every_crossing_in_either_direction' tests/unit/test_bookmarks.py`. `touches` widened to name `app/bookmarks.py`, which the command reads.
- Two headings inside `PL-CTD7` had gone stale within the sitting — "Unblocked but not startable" and the open re-arming question — repaired in place per `.claude/rules/citation-drift.md`, which keeps the record of why it waited without leaving it readable as a live state.
- `bin/docket record` rode this commit and wrote the five `pr` numbers the base was owed: `#765` for `PL-8G48`, `PL-439V`, `PL-CHQY` and `PL-V3QB`, and `#764` for `PL-K4R5`.

## Checks

`make check` green — 3405 tests, 100% coverage, `docket check` 0 errors, `doc_check.py` 0 errors and 0 advisories, resident instructions unchanged.

The branch was restarted from `main` after its previous pull request squash-merged as `#765`; GitHub had auto-deleted the remote branch, so this is a fresh branch of the same name carrying only the commit above.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01RX3eYWJto4jrWdyXmPrE8d

---
_Generated by [Claude Code](https://claude.ai/code/session_01RX3eYWJto4jrWdyXmPrE8d)_
