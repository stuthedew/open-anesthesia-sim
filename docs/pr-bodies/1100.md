---
pr: 1100
recorded: 2026-09-26
---

Before: `claims.holdings` read any token in a branch name that fits the id grammar as an item the branch holds. `HTML`, like `CTRL`, is in the alphabet, so a branch named `claude/fix-pl-html-export-abc123` held `PL-HTML`, an item no copy of the store has. `bin/docket flight` listed it as filed there, `claims_nothing` exempted the branch's unclaimed work from the branch-id refusal, and the first-edit hint stayed silent for it. `flight`'s unattributed row also took a subject reading `PL-HTML export` as attribution.

After: a branch name holds an item only where the base's copy of the store or the branch's own holds its id, the rule `PL-SN2T` gave `tools/branch_id_check.py`'s attribution. That branch now holds nothing, its unclaimed work is refused like any other branch's, the hint asks it for its claim, and a branch whose subjects lead only with such an id is reported as unattributed.

How: the rule is one function, `claims.named_id`: a name's first id, read as every reader reads it, where a copy of the store holds it. `holdings` asks it of each name against the base's listing and the branch tip's, which the loop already lists to read the item's status, and asks the same store about each subject's leading ids for the unattributed row. `claims_nothing` exempts a branch only where `holdings` recorded a name hold for it, so the refusal and the `flight` row are one answer. The hint cannot take that answer, because `holdings` does not read a branch with no commits yet, so it asks `named_id` against the ids `branch_id_check.held` lists from the base and `HEAD`. Four tests hold the reproduction, written ahead of the fix and failing on `db20183a`; two older fixtures that named an item no copy held now file it.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01EMETr16b2DaTWU9QdA6pnc

---
_Generated by [Claude Code](https://claude.ai/code/session_01EMETr16b2DaTWU9QdA6pnc)_
