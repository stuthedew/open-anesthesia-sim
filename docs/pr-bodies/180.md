---
pr: 180
commit: 32fdf625b6393244929f9514557cbd76c552e4ba
merged: 2026-09-01
items: PL-0NQ1, PL-9T8T, PL-K2YF, PL-NX4H
subject: PL-9T8T, PL-0NQ1, PL-NX4H, PL-K2YF: triage the captures, none into P1 (#180)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 32fdf625 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Clears the untriaged captures from 2026-09-01/02, captures two docket defects the pass surfaced, and records two recoverable pull request numbers. No source file changes; the store only.

**`PL-1J0P` (`docket next` naming the wrong release) landed on `main` in #179 while this pass was triaging it.** Its triage here is superseded — `main`'s version is taken whole, and its brief now records that the captured diagnosis ("the ranking is right and only the label is wrong") was itself wrong: measuring it found a second, worse half in the placement. That leaves **seven** items triaged by this branch rather than eight.

## The line applied

Every one is a *record* rather than a modelled value, so none takes a `science` class and P1 stays at ten of the twelve `docket.toml` allows. `science` is for the content of the model; `defect`/`docs` for the record of it — which matches how the six existing `model-spec-accuracy` items are already classed: none carries `science`, three are `defect, docs` at P2 (`PL-2HTF`, `PL-4YY1`, `PL-C1KK`), two are `docs` at P3 (`PL-KQKM`, `PL-MS54`), one is `defect, test` at P2 (`PL-Y5BV`). Each of the three provenance items says in its own **Done when** that no modelled number changes, which is what puts them on the record side of that line.

## What each got

| Item | Band | Classes | Feature |
| --- | --- | --- | --- |
| `PL-9T8T` — the sole partition-coefficient citation names a non-existent author | P2/S | defect, docs | model-spec-accuracy |
| `PL-0NQ1` — `reference_adult.json`'s perfusion note is unreconciled with De Wolf Table 1 | P2/S | defect, docs | model-spec-accuracy |
| `PL-NX4H` — no vessel-poor group in **Known limitations** | P2/S | docs | model-spec-accuracy |
| `PL-K2YF` — the release narrative skips v0.2.6 | P2/S | defect, docs, infra | planning-cadence |
| `PL-N092` — rewrite README as an introduction | P2/M | docs, ux | project-introduction |
| `PL-G1MF` — `Finishes <feature>` with the count still open | P3/S | defect, infra | dev-tooling |
| `PL-4MHK` — `pyproject.toml` metadata | P3/S, blocked | defect, docs | project-introduction |

`PL-9T8T` is the arguable call: it breaks traceability for all twelve partition coefficients, which `CLAUDE.md` lists among the safety-critical requirements, but no displayed value is wrong and none becomes wrong. Left at P2 with the project owner's agreement.

`PL-G1MF` was re-checked against the merged tree — `plan.py:252` still prints the `Finishes` wording and its `verify:` still exits 1 with the test file green, so #179 did not overtake it.

New `project-introduction` feature groups `PL-N092` and `PL-4MHK`, and `PL-4MHK` is now `blocked-by: PL-N092` — the sequencing its own brief asked for in prose, in a form the store can act on.

## Verify commands

Every `verify:` command was run against this tree first and watched exit 1, with its `pytest` or `doc_check` half green, per the skill's rule. `bin/docket check` reports none of them among the commands that already pass, and none among the ones that select nothing.

## Repairs, captures, backfill

`PL-G1MF` and `PL-K2YF` each carried an empty `**Problem.**`/`**Why it matters.**`/`**Where.**`/`**Done when.**` template stub above their real briefs — which is what `brief_gaps` was reading, and why the checker reported empty headings on items whose briefs plainly fill them. Removed here; the cause is captured as `PL-JL2M`.

`PL-4WQS` captures a second find: `checks.py:119` defines `open_items` as open-and-not-untriaged, and `render.py:600` labels that count "open" beside a separate untriaged count. Measured in this checkout — 88 items were neither done nor dropped while the summary said `87 open, 2 untriaged`. The session-start digest reads from the same property.

`PL-1J0P` and `PL-JT6L` gained the `pr:` numbers `docket check` recovered from their merge commits (#179, #173) — the one-line follow-up the skill routes to the next branch. Advisories drop from four to two.

## Checks

`make check` green on the merged tree — ruff format, ruff check, mypy, 914 tests, `bin/docket check` (0 errors, 2 advisories), `tools/doc_check.py` (0 errors). Both remaining advisories are pre-existing grooming lines naming none of these items.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Cz27tPaqjNvUFfkNHX1JPw
