---
pr: 1015
recovered: 2026-09-26
commit: fdc76b160c1b4897220d5b99efca6f714f110987
merged: 2026-09-25
items: PL-KR69
subject: PL-KR69: every read of which paths a change touched names both sides of a rename, so a core file moved out fails verify's protected-path audit (#1015)
squash: empty
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGSrkCKjzezrdy4YpwyAJiNY)_

Before: `git mv src/anesthesia_sim/core/alveolar.py tools/moved_core.py`, committed, and `bin/docket verify --self` printed `PASS  no protected path modified - none touched`. git pairs a deleted path with a similar added one and prints the pair as a rename, which under `--name-only` is the new name alone, so the audit never saw the core file leave. Reproduced at triage against `origin/main` on git 2.43.0. Four reads answered "which paths did this branch change" and disagreed about renames: `verify.changed_paths` and `vcs.files_in_flight` kept git's rename detection, while `arm` and `claims.work_under_record` passed `--no-renames`.

After: the same branch fails the audit and names `src/anesthesia_sim/core/alveolar.py`, whether or not the item's id leads the commit. `concurrent`'s observed half lists both names of a moved file, so a session working in the old path is told a branch has touched it.

How: `vcs.changed_path_args(subcommand, *args)` returns the argv with `--no-renames` after the subcommand, and every read in the apparatus that lists the paths a change touched builds its argv there: 19 call sites across `vcs.py`, `verify.py`, `arming.py`, `claims.py` and `tools/doc_check.py`, seven of which already passed the flag inline. Reads that want git's pairing keep it, because there the pairing is the answer: `--diff-filter=A` reads asking which file is new (a retitled item is not a new filing), `-M`/`--follow` reads asking where a file came from, the orphaned walk (`_commits_by_landing`), whose paths are matched against `_landing_split`'s content sets that a deletion never enters, and `_unmerged_commits`' walk, which PL-J16N holds. The item's brief lists every read on both sides.

Behaviour that changes beyond the two named reads: `tools/doc_check.py candidates` now searches the docs for a renamed file's old name, the one stale prose still cites; `verify`'s touches check counts a renamed file's old path as changed, so an item renaming a file declares both names; and `ref_walk`'s item-edit count counts a retitle as two paths.

Tests, both against real git and both failing with the old argv: `test_moving_a_protected_file_out_is_rejected` in `subprojects/docket/tests/test_verify.py`, over the item's own commits and the branch diff, and `test_files_in_flight_lists_both_names_of_a_rename` in `subprojects/docket/tests/test_cli.py`. Three fakes in `test_vcs.py` read the argv by position and now read it by content.

Triage: PL-KR69 was untriaged. This branch triaged it `P1` `defect`, effort `M`, deferred from the v0.6.0 gate as captured after the freeze, and wrote into its brief which reads the helper takes and why.

Captured on the way:
- PL-8HSX: the same audit's other blind spot. Every changed-path read but `claims.work_under_record` leaves `core.quotePath` on, so a new `src/anesthesia_sim/core/café.py` reaches `_within` as `"src/anesthesia_sim/core/caf\303\251.py"`, quotes included, and passes as "none touched". Reproduced.
- PL-TFF9: the orphaned walk leaves a commit that deletes a file unclassified, so a left-behind commit that deletes one file and edits another is never reported. Read from the code, not reproduced. `docket new` matched it to PL-WNQT on title words; the match is withdrawn, with the reason in PL-TFF9's brief.

Docs swept: `tools/doc_check.py candidates --base origin/main` listed filename mentions only. `subprojects/docket/README.md`'s `since_filed` paragraph ("Rename detection is off, so a renamed path reads as gone under the name the brief uses") and its `verify` section are still true; nothing else states how these reads treat a rename.

Checks run locally before opening: `bin/docket verify --self PL-KR69` says ACCEPT, which ran the item's `verify:` command and `make check` (format, lint, mypy, the full suite with core coverage at 100%, `make docket` with 0 errors). This container lacked `libEGL.so.1`, which the Qt tests load, so it was installed for the run; CI brings its own.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01KDc7rtfxMex8RnuaLtvvJx


---
_Generated by [Claude Code](https://claude.ai/code/session_01KDc7rtfxMex8RnuaLtvvJx)_
