---
pr: 911
commit: ffea4063901ca4626e33682d9d643e9582422b5e
merged: 2026-09-22
items: PL-CZTR
subject: PL-CZTR: delete ResumePoint.elapsed_s, so a branch's fork instant is read only as fork.instant_s (#911)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ffea4063 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

**What.** `ResumePoint.elapsed_s` was a property returning `self.fork.instant_s` (since PL-B8MK): a second name for one value, and a name that says "elapsed duration" on what is an instant on the case's axis. The fork path read it both ways: `_branch_from` as `resume_point.elapsed_s`, `_open_at` as `resume_point.fork.instant_s`. This deletes the alias, and every reader now writes `fork.instant_s`.

**Approach.** Deleted rather than renamed to `fork_instant_s`, as the item's brief decided on 2026-09-22: a second spelling beside `fork.instant_s` would keep two names for one value, which is the state being removed. What the property's docstring said (the fork instant is what a reset returns to and where `drawn_window` stops drawing to the left) now sits on the `fork` attribute.

**Readers changed**
- `app/controller.py`: `began_at_s`; `_branch_from`'s settings replay and its refusal (the local `elapsed_s` is now `fork_at_s`, matching the halt fork's); the refusals in `set_agent`, `_require_forkable` and `BranchedCase`.
- `app/chart_frame.py`: `_run_frame`'s `branch_point_s`.
- `app/simulation_view.py`: `add_run`'s "belongs to another case" refusal. It was missing from the brief's list of readers; mypy found it once the property was gone. Its local holding the `ResumePoint` is renamed from `opened_at` to `opened_from`, as in `chart_frame.py`.
- Docstrings naming the alias: three in `app/bookmarks.py`, one each in `tests/unit/test_chart_frame.py` and `tests/integration/test_simulation_view.py`.
- Two assertions in `tests/integration/test_controller.py` now read `branch.opened_from.fork.instant_s == fork_s`.

No displayed text changes. The three reflowed refusal literals concatenate to the strings they built before (checked by parsing both versions), and the value interpolated is the same float.

**Verification**
- `make check` is green: ruff format, ruff check, mypy, 4,135 tests with the core coverage gate at 100%, `docket check` and `doc_check` at 0 errors.
- The item's `verify:` command, widened on this branch to also catch `ResumePoint.elapsed_s` in prose, fails on the base (15 matches in 6 files, plus the `def`) and passes here.
- `bin/docket verify --self PL-CZTR` returns **REJECT** on one guard, which is the expected result for this kind of change: `FAIL no existing assertion removed - 2 line(s)`, naming `assert branch.opened_from.elapsed_s == fork_s` twice. Neither assertion was removed. Each is rewritten in place to `assert branch.opened_from.fork.instant_s == fork_s`, with the same subject and the same expected value, because the attribute it read no longer exists. Every other guard passes, including the item's own command and `make check`. PL-TKFD holds the open question of how `verify --self` should treat an assertion that an item's own work has to change; no `falsifies:` was added here to clear it.

**Docs checked.** Grepping every Markdown file for the alias's spellings found only `ROADMAP.md`'s gate entry, which is the item's title and stays. `tools/doc_check.py candidates` listed lines in `docs/MODEL.md`, `docs/ARCHITECTURE.md`, `ROADMAP.md`, `.claude/rules/citation-drift.md` and `.claude/rules/run-is-its-definition.md`. They either cite the touched files by path or mean `SimulationState.elapsed_s`, `snapshot().elapsed_s` or `resumed_at`'s parameter, and all of them still hold.

**Riding along (queue only)**
- Records `pr:` for six items already merged on `main` (PL-14QR #904, PL-4KSZ #903, PL-59WB #908, PL-FPY2 #905, PL-Q8RQ #907, PL-SZJ2 #906), written by `bin/docket record` as `docket check` advised.
- Captures PL-BT2J: `resumed_at`, `BranchedCase.fork_at`, `_resume_point_at` and `_setting_at` still take the case instant under the name `elapsed_s`. Renaming public parameters is a decision this item did not take.

Gate v0.6.0 ("the layout is the reader's"): 140 of 185 entries left for this gate to clear, down from 141.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XMESfWhaSAmzY8NdLLkaKX

---
_Generated by [Claude Code](https://claude.ai/code/session_01XMESfWhaSAmzY8NdLLkaKX)_
