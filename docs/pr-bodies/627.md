---
pr: 627
commit: e04ec6dc952fce7909b81657b73df09d6e9397cf
merged: 2026-09-16
items: PL-NLP4
subject: PL-NLP4: record the seven owed pull request numbers, and capture a stale count in Declined to Gate 2 (#627)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit e04ec6dc landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two things, one commit — the capture is what the commit is filed under, and `bin/docket record` rides it rather than a commit composed for it, per the close-out rule.

## The record sweep

Seven closures standing on `main` were owed a pull request number and none could name it before its own merge. `bin/docket record` wrote all seven in one pass: `PL-3J2P`, `PL-C842`, `PL-FTP5`, `PL-P5QX` (#623), `PL-NW76` (#624), `PL-BNYF` (#625), `PL-LPHT` (#626). That clears six advisories from `bin/docket check`, which drops from 17 to 11.

## The capture

`PL-NLP4` — `ROADMAP.md` § "Declined to Gate 2" closes with a paragraph saying the subsection holds **66** dispositions, written to make the cost of a second `### Declined to Gate` heading legible. `doc_check`'s `_declined_ids` intersected with the open debt set returns **132**, and it returned 132 before #626 as well as after, so this is not new drift from that pass. Nothing checks the figure: `check_gate_counts` holds the count-carrying group headings and the `not-delegable` claims to the list, and this number sits in a body paragraph.

`PL-B8V1` is the same defect with the same recommended fix — date the claim in place rather than recompute it — but it is scoped to the "The arithmetic is the argument" paragraph and says so, having been found sweeping `PL-WSDY`'s done-when over release narratives. So it does not reach this paragraph, and the two are cheapest answered together.

It matters because the sentence exists to size `PL-82B0`'s defect, which is the workflow lane's current top pick: a reader taking 66 from it is out by a factor of two, in the one paragraph written to make that cost readable.

**Left untriaged deliberately.** Classing it `defect` makes it gate debt, which would owe a disposition in the very subsection it is about — and triaging one's own capture is the pass becoming the fix.

`make check` is green: 0 errors, 2941 tests.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YQPu4LFgyes1frb7XDq7Qf

---
_Generated by [Claude Code](https://claude.ai/code/session_01YQPu4LFgyes1frb7XDq7Qf)_
