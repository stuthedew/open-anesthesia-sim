---
pr: 1102
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGLovYWFm2rwZ9dvHK8W1EXv)_

Before: `.claude/hooks/no-prune-guard.sh` refused `git fetch --prune` but let through other spellings that delete remote-tracking refs just as surely: `git pull --prune` and `-p`, `git remote update -p`, a bundle such as `git fetch -tp`, and a prune setting passed ahead of the command name, as in `git -c fetch.prune=true fetch`.

After: each of those is refused with the same message and recipe, and so are `git fetch -P` and the `pruneTags` settings, as `--prune-tags` already was. Spellings that prune nothing still pass: a setting whose value reads as false, `git fetch -j4p` (jobs `4p`, an error), `git -p fetch` (the pager), and `git grep -c fetch.prune`, whose `-c` is grep's own.

How: the hook now reads the options git takes as its own ahead of the command name, stepping over those whose value is a separate word (`-C`, `-c`, `--config-env`, `--git-dir`, `--work-tree`, `--namespace`, `--attr-source`), and refuses a prune setting there unless its value reads as false. `fetch`, `pull` and `remote update` read a bundle of short flags up to the first letter that takes a value, as git's option parser does. The option tables come from git 2.43's own `-h` output, and every spelling in the tests was first run against a scratch remote to see whether it really deleted the ref; PL-R17X's brief records the results, including that `git fetch -P` on its own deletes nothing.

Verified: `make check` green locally (4,937 tests), and `bin/docket verify --self PL-R17X` accepts. The guard's tests go from 38 cases to 67.

Filed while fixing it, not worked here:
- PL-TRMN: all three Bash guards pass a guarded command run through a wrapper, `timeout 600 make check | tail -5` among them.
- PL-M2NV: `git push --prune` with a wildcard refspec, and `git push --mirror`, deleted another branch on a scratch remote and pass the guard.
- PL-YFT4: the guard refuses reads that prune nothing, such as `git config --get fetch.prune` and `git remote prune -n`.

This branch continues the claim #1101 held on `claude/filed-findings-g90x9s`, whose session stopped for length, so #1101 closes unmerged and its body record goes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01BtHmWyPTrtKZpcPKjEJQnf

---
_Generated by [Claude Code](https://claude.ai/code/session_01BtHmWyPTrtKZpcPKjEJQnf)_
