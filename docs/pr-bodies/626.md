---
pr: 626
commit: b4d19fe9b5382ee1173a271910b49c16fe21f23b
merged: 2026-09-16
items: PL-LPHT
subject: PL-LPHT: triage the 29 untriaged items from the 2026-09-16 sweeps (#626)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit b4d19fe9 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Twenty-eight triaged. `PL-PX7V` (nothing runs /doctor's CLAUDE.md trim check) was skipped — `bin/docket show` reports its file already edited on `origin/claude/wizardly-maxwell-dyzpjt`, and a second answer here is a second resolution of the same file.

## How the twenty-eight landed

**Twelve `ready`**, each with a `verify:` command that was run and watched fail for the right reason before it was written down — the paired shape the skill prescribes, exiting 1 rather than selecting nothing. Three touch `subprojects/docket/src` and name a target under `subprojects/docket/tests`, which is the rule `PL-6YL1` (a check for the verify-target class) proposes, applied to itself.

**Five `needs-decision`**, each with a `**Decision needed.**` section saying what has to be settled and on whose side it falls: `PL-NMTF` and `PL-KQHN` are the project owner's, `PL-1T6T`, `PL-2XM2` and `PL-B11M` are a session's.

**Eleven `blocked-by: PL-NMTF`** — the head of the area-model cluster. No open item builds `ROADMAP.md` item 34's area system, and none of its design prerequisites can be answered against an unscoped milestone. `blocked-by` names an item rather than a version because item 34 is a planned-milestone entry that § "The timeline" gives no row, so `checks.py` `_check_references` has no milestone to hold a version form to — which is `PL-D584`'s finding, applied.

Four of the eleven are `safety`-classed and seated `P2` under `anticipated`, the exemption `checks.py` grants a `blocked` item whose concern does not exist until a later feature is built. The splitter handles are inert at `src/anesthesia_sim/app/qt_widgets.py:784` until item 34 makes them live, so no reader can close, replace or cover a required value before then.

## The ROADMAP.md change, and why it is here

The pass creates eighteen open debt items, and `tests/unit/test_doc_check.py::test_this_repository_records_a_disposition_for_every_open_debt_item` is a hard gate: `make check` went red on eighteen undisposed items. Each is now dispositioned in v0.5.0's existing `### Declined to Gate` subsection, in three groups — eight apparatus on the refilling-queue ground already stated there, nine area-model on the "the gate cannot precede the thing it is about" ground `PL-TH35` and `PL-VN6M` already use, and `PL-KQHN` on the owner's-decision ground `PL-MN4J` uses. Written as prose with reasons rather than as list entries, following `PL-MN4J`'s precedent, so the group heading's entry count is unchanged.

## What it left behind

`make check` is green: 0 errors, 2941 tests, one advisory. That advisory is `PL-R0Q0`'s defect — `doc_check`'s safety-class gate advisory reads only the frozen list and not the declined subsection, so the four newly deferred safety items fire alongside `PL-MN4J`. One named item became five. `PL-R0Q0` is `P2`, `S`, and now `ready`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YQPu4LFgyes1frb7XDq7Qf

---
_Generated by [Claude Code](https://claude.ai/code/session_01YQPu4LFgyes1frb7XDq7Qf)_
