---
pr: 916
commit: e7120e232968f9af94e9b0662251cb0ad78b4631
merged: 2026-09-22
items: PL-6Q9L, PL-G40Z
subject: PL-G40Z, PL-6Q9L: pause new workflow mechanisms while generators are live, and let repairs ride (#916)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit e7120e23 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two decisions the project owner ratified on 2026-09-22, both from PL-KVDK's first pass over the 161 non-product items filed 09-20 to 09-22 (#915).

**PL-6Q9L: the pause.** `CLAUDE.md` § "What this project is" now says that while any open item carries `generator: live`, no new workflow mechanism is built. A new command, check, field or rule, or a re-specification of the generator rule, is captured and not built, and it is not offered as the answer to a finding. Fixing a live generator, or a defect in what exists, is not new. The pause ends when `bin/docket next` shows the generator tier empty. A request from the owner lifts it for that request. The standing-approval sentence for deterministic tooling now names the suspension. Why: at least nine mechanisms, each with its own tail of items, arrived on 09-20 and 09-21 while six closed generator heads were still producing.

**PL-G40Z: repairs ride.** Fix-now test 2 now says that a no-behaviour repair to another item's file or to `docs/WORKING_NOTES.md` is not a fix-now use. The session declares the file in the current item's `touches`, and the repair rides the commit already being made. Why: eleven such repairs became items in three days, because the licence lived only in the path-scoped citation-drift rule, which loads after the decision to file. `citation-drift.md`'s claim that `verify --self` already expects such edits is corrected: `verify.sanctioned_queue_edit` sanctions only capture, `pr:` and recurrence edits.

**Resident growth.** +1,361 characters in `CLAUDE.md`, and nothing cut. Both rules have to fire before any read: one when a reply recommends a mechanism, the other when a session decides whether to file. The path-scoped copy of the second is what failed.

**Rides along.** `docket record` backfills the `pr:` numbers `origin/main` was owed.

**Verification.**
- `make check` exits 0.
- `bin/docket check --verify --verify-base origin/main` reports 0 errors. This is the replay CI runs and `make check` skips (PL-0HPV).
- Both items' `verify:` greps exit 1 against `origin/main`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LEsrpKXp4SU9p12NaZBhHX

---
_Generated by [Claude Code](https://claude.ai/code/session_01LEsrpKXp4SU9p12NaZBhHX)_
