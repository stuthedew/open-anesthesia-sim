---
pr: 168
commit: be403fc7e563ba313ad3ce94cfc875e16b4f31ad
merged: 2026-09-01
items: PL-CMCB
subject: PL-CMCB audit the ten type: ignore directives; all live, two removed at the cause (#168)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit be403fc7 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`PL-CMCB` (the ten `type: ignore` directives sitting outside the mypy gate) asked for a measurement rather than a change: run mypy over both test trees, out of the gate, with `warn_unused_ignores` on, and see which of the ten are inert.

## The verdict: none are inert

```
MYPYPATH="subprojects/docket/src:tools" uv run mypy tests subprojects/docket/tests
Found 54 errors in 11 files (checked 38 source files)   # 0 of them [unused-ignore]
```

`warn_unused_ignores` was confirmed live under this config against a scratch file first, so the zero is a negative result rather than a flag that was never on. Each of the ten was then confirmed *positively* by stripping its directive and re-running: every one produced an error at that line carrying exactly the code the directive names.

The ten are not quite the ten the item enumerated. It listed nine at 14:26 on 2026-08-31 while its title said ten; `PL-3CBS` (#128) added `subprojects/docket/tests/test_checks.py:507` five hours later, which is the tenth as audited.

## Two were live but should not have existed

`test_cli.py`'s two `delegable` tests annotate `capsys` as `object` and then reach through it with `[attr-defined]`. That is the exact anti-pattern `PL-WFJ9` removed from `render.py` — a parameter typed `object` read through eleven such ignores — and they are the outliers in their own file: 18 of the 20 `capsys` parameters in `test_cli.py` already say `pytest.CaptureFixture[str]`. Annotating them the way their siblings are annotated makes both directives unnecessary rather than merely explained, so both are gone.

## Six kept, with the reason at each site

Each splats a `dict[str, object]` of defaults-plus-overrides into a frozen dataclass, so mypy matches `object` against every field's type in turn. The only narrower alternatives are `dict[str, Any]`, which drops checking everywhere the dict is used, and an `Unpack[TypedDict]` restating all sixteen of `Item`'s fields in a test helper. A two-line note now records that at each site.

The two `[method-assign]` directives are left without prose deliberately — the line assigns a function to a bound method and mypy says `Cannot assign to a method`; a comment restating that is the "prose restating what a command already prints" `CLAUDE.md` calls bloat, and the Done-when asks for prose only where the reason is not obvious.

## The `[tool.mypy] files` decision is written down, not taken

`PL-J5NN` (nothing evaluates `warn_unused_ignores` on the test trees) carries it, per the Done-when. It records three routes with the numbers measured, recommends a scoped run filtered to `[unused-ignore]` over widening `files`, and records the trap that makes the naive version unsound: run without `MYPYPATH`, `mypy subprojects/docket/tests` reports **all six live splat directives as unused**, because `docket` fails to resolve and the models degrade to `Any`. A runner built without that guard would have produced six false inert verdicts and someone would have deleted six live suppressions.

## Also in this branch

`PL-768D`'s recovered pull request number (#166) is written into its item — the one-line follow-up `docket check` asked for on the next branch.

## Checks

`make check` green: ruff format, ruff check, `mypy` (38 source files, no issues), 883 tests passed, `docket check` 0 errors, `doc_check` 0 errors. The one documentation candidate (`docs/WORKING_NOTES.md:153`) refers to `_shallow_pair` in `test_cli.py`, which this diff does not touch; the statement still holds.

Closing `PL-CMCB` leaves v0.2.8's frozen gate at 36 of 38 cleared.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018Zg7wRso5cPQcJjAN3EFii

---
_Generated by [Claude Code](https://claude.ai/code/session_018Zg7wRso5cPQcJjAN3EFii)_
