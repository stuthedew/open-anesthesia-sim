---
pr: 548
commit: 0295ec86f368f2224ab8ce7be8efcc7a39a92351
merged: 2026-09-13
items: PL-JYR4
subject: PL-JYR4: triage the remaining eight captures, and close the orphaned-branch finding (#548)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 0295ec86 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The second pass of the 2026-09-13 triage, following #546. It folds in the two that pass had to defer plus six that merged in from other sessions while it ran, leaving `docs/items/` with **nothing untriaged**.

## `PL-JYR4` closes

Its condition was met without work here: `origin/claude/determined-hamilton-atx5bi` merged, so `PL-B9K7`, `PL-8GJ6`, `PL-FN5F` and `PL-LS3H` are `done` on `main` with the three agent files' Yasuda adoption beside them, and `PL-N701` became reachable.

Kept rather than deleted, because the mechanism did not improve. Nothing detected that branch and nothing would detect the next one: `bin/docket stranded` cannot see it (its items are closed, not stranded work) and `flight` reported it as in flight, which is the opposite of the truth. `PL-Q664` is the item that fixes that, and this is its worked instance with a cost attached.

## Two enter Gate 1 under the unconditional science exception

Both from Targ, Yasuda and Eger 1989 (Anesth Analg 1989;69(2):218-25, PMID 2764290), read at full text from the private corpus that evening. Neither changes a displayed value; both are the `PL-Q5NS` shape — the specification understating what is known.

- **`PL-QBKQ`** — `reference_circle_system.json`'s `provenance_gap` says a measured circle-system volume "has not been sought yet". One has now been read: 9.86 L by water filling, which does *not* close the gap because it includes a reservoir bag simulating the lungs, and saying so is the work. Three figures in one order and only one measured — this project's 6.0 L, Gas Man's published 8.0 L, that 9.86 L upper bound.
- **`PL-XWCY`** — the same paper strikes circuit-wall absorption as a candidate for desflurane's residual, and in the same measurement says a real circle system measurably retards sevoflurane and isoflurane relative to ideal. This model has no term for that, and the two cohorts in `tests/reference/test_published_wash_in_and_elimination.py` already sit at +3.79 SD and +4.15 SD in the direction it would explain.

The v0.5.0 timeline row's entry count moves 161 to 163.

## Two are declined to Gate 2

In the existing subsection, not a new one. `PL-SCB4` on the refilling-queue ground — with one extra sentence, because it is *about* this gate: `PL-Z34C` is its live case, an entry whose question is answered and which holds Gate 1 open because no status says so. `PL-DHJ7` on its own ground, since it reaches `ROADMAP.md` and so is not wholly apparatus.

## The rest

`PL-N701` takes `blocked-by: PL-Z3V5`, the same format decision `PL-4QCJ` waits on — three papers across two items, cheapest taken together. `PL-21RC`, `PL-C25K` at `ready`.

Every `verify:` command here was run against this tree first and watched fail for the right reason. `bin/docket record` wrote the five `pr` numbers the base was owed (#544, #545, #546).

## Checks

`make check` passes: 2607 tests, 0 `docket` errors, 0 documentation errors, 0 untriaged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01VRJN1jqMpoAEneRAqxnz5V

---
_Generated by [Claude Code](https://claude.ai/code/session_01VRJN1jqMpoAEneRAqxnz5V)_
