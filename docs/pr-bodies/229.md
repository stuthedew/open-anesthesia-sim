---
pr: 229
commit: b378cbd55b361efb870abcfee9dc9b930b462dcc
merged: 2026-09-02
items: PL-2XTF
subject: PL-2XTF: record the pull request that carried it, as docket check asked (#229)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit b378cbd5 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

## Summary

One frontmatter line: `pr: 227` on `PL-2XTF`. What makes it worth a note is the advisory it clears, which is the item's own subject seen from the other side.

## Before and after, same checker, same situation

**#220** closed three items whose squash subject named none of them:

```
PL-P909…: marked done on `origin/main` but records no `pr`; without it
there is no way back from the closure to the work that made it
```

Three **errors**. `bin/docket check` exit 1. `make check` failing on a clean checkout of `main`. The v0.3.0 release blocked until the numbers were read off GitHub by hand.

**#227** closed `PL-2XTF` with no `pr` field either:

```
PL-2XTF: marked done on `origin/main` and records no `pr`, but #227 is
recoverable from its merge commit; write `pr: 227` into the item so the
file carries it too
```

An **advisory**, and `check` exits 0. This PR does what it asks.

## Which half actually did it

Being precise rather than flattering the change: #227's own subject leads with its ids, so the *subject scan* answered and the new fallback was not exercised here. The fallback's proof is the #220 case, where the subject names no id and `PL-YHF1`, `PL-P909` and `PL-SS9Q` each resolve to `#220` from their own file histories alone.

What this PR does demonstrate is the prevention half working in production: #227's title led with its ids because the check now refuses one that does not, and its log shows the step firing on the right set — `pr-title: leads with PL-2XTF`, correctly not demanding `PL-SVRW`, which that branch captured rather than closed.

## Checks

`make check` green: 1138 tests, ruff, mypy, `bin/docket check` 0 errors, `doc_check` 0 errors, contrast 0 errors. One item file, frontmatter only.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01JyWvU5emy3yPSyj4sen6je

---
_Generated by [Claude Code](https://claude.ai/code/session_01JyWvU5emy3yPSyj4sen6je)_
