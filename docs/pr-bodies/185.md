---
pr: 185
commit: 844e2c24f15528dcdf6ee35e9cab9a2f9222e431
merged: 2026-09-01
items: (none)
subject: Release v0.2.9 - validation, accessibility and the queue's own ranking (#185)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 844e2c24 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts v0.2.9 for the seven items finished since v0.2.8, and clears the four untriaged captures ahead of it.

## Why cut it now rather than let the items ride into v0.3.0

`ROADMAP.md`'s v0.3.0 section states that its contents are *exactly* Gate 0's frozen list and that nothing may be added to it. Four of the seven finished items are not on that list, and `bin/docket release` generates notes from everything finished since the previous version — so leaving them in place would write queue-tooling and accessibility work into the one release that explicitly disclaims having any. Cutting now keeps v0.3.0's notes equal to the gate.

It does not perturb `PL-SZ56`'s pre-registered loop trial: every one of its readouts is defined over the window `v0.2.8..v0.3.0`, which spans this tag either way.

## What is in the release

`src/` is **identical** to v0.2.8 — `git diff --quiet v0.2.8..HEAD -- src/` is clean — so no equation, parameter, numerical method, unit or displayed value moved.

- **PL-9Y42** turns verification into validation. Modelled 30-minute `F_A/F_I` is pinned against Yasuda et al. 1991 for all three agents: sevoflurane 0.853 vs 0.850 ± 0.018, isoflurane 0.741 vs 0.733 ± 0.027, desflurane 0.908 vs 0.900 ± 0.010 (+0.17, +0.30, +0.80 SD), at the data file's own cited alveolar ventilation of 4.0 L/min rather than a fitted one. The release prose carries the two caveats that bound the claim — the published subjects breathed 65–70% nitrous oxide, which this fixed-volume single-gas alveolus cannot reproduce, and the partition coefficients share a lineage with the data being tested against — and does not claim more than "the implementation reproduces its parameter set's intent".
- **PL-MMYM, PL-1MK1, PL-HC9P** bind the interface to a recorded WCAG 2.2 AA target, compute its contrast ratios in `make check` via `tools/contrast_check.py` rather than asserting them in comments, and make the target fire when a session picks a colour.
- **PL-1J0P, PL-Y4Q4, PL-D1ZY** close three live defects in the queue's own ranking and in the resident instructions.

## Also in this branch

- **Triage of the four captures** (`PL-4WQS`, `PL-8PZ1`, `PL-JL2M`, `PL-KGNF`), none into P1 — all are process or documentation work. `PL-8PZ1` and `PL-KGNF` had empty stub briefs and now have written ones. Every `verify:` command was run before being recorded and exits 1 on this tree.
- **`pr: 182` backfilled into PL-9Y42**, which `docket check` recovered from its merge commit. #184 made the identical edit in parallel; the two are byte-identical, which is why this merges clean.
- **Two `verify:` commands narrowed** from the whole `docket` suite to one test file each (19.2 s → 0.7 s and 0.6 s), taking ~37 s off every `make check`. Both still exit 1 on this tree.
- **Two findings captured**: `PL-LXR3` (`bin/docket check` spends 49.6 s re-running every open item's `verify:` command on every `make check`, growing with the queue) and `PL-B0YN` (`docket next` ranks by smallest effort among items in underway features, not by which feature is nearest completion, so its rationale line, `CLAUDE.md` and `PL-G1MF`'s brief all describe a ranking `plan.py` does not implement).

## Documentation sweep

Checked `ROADMAP.md` (edited: version-table row, baseline mark moved, release prose written, v0.2.8's baseline section condensed into the release narrative), `README.md`, `docs/ARCHITECTURE.md`, `docs/worker.md` and `docs/WORKING_NOTES.md` for statements this release makes stale — the only `v0.2.8` mentions outside the release notes are historical ones in `docs/WORKING_NOTES.md`, which are correct as history. `docs/MODEL.md` still names v0.2.3 as the current baseline; that is `PL-C1KK`, already open, and untouched here.

## Checks

`make check` green locally: 975 tests, 0 store errors, 0 documentation errors, contrast at 12 of 15 declared pairs with three tracked shortfalls. The one documentation advisory is the expected untagged-release window. CI green on `ef9fdc4`: `checks` and `floor` both pass.

## After merge — tag it

Run these immediately after merging, before anything else lands on `main`, so `origin/main` is still the merge commit:

```bash
git fetch origin main
git tag -a v0.2.9 origin/main -m "v0.2.9"
git push origin v0.2.9
```

If something else has merged since, name the merge commit explicitly instead: `git log origin/main --oneline | grep 185` gives its sha, and it goes where `origin/main` is above.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01S6kgtYNvVAGXdqXgby1BDg
