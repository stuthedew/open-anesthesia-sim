---
pr: 117
commit: 6f54bafc3e16f98b6796ae2393df8116b87b799c
merged: 2026-08-31
items: PL-8MZN, PL-V5XM
subject: PL-8MZN, PL-V5XM: drop two duplicate captures (#117)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 6f54bafc landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two of the eleven untriaged items duplicate items that already exist. Both were captured within minutes of the item they duplicate, by concurrent sessions that could not see each other's work. Dropping them ahead of the triage pass so it runs over nine real items rather than eleven.

Nothing is deleted — `status: dropped` with a `reason` is what stops a finding being raised a third time.

## `PL-8MZN` → duplicate of `PL-J295`, already fixed

`PL-8MZN` (`make check` fails on a fresh clone because `doc_check.py` reads absent tags as missing releases, not as tags never fetched) is the same defect as `PL-J295` (the release-train check reads a tag missing from a shallow clone as a release that was never tagged) — which is `done` and shipped inside this same v0.2.8 gate.

Verified rather than assumed. `python3 tools/doc_check.py check` in this container now reports the shallow-clone case as *not checked* rather than as an error:

```
Not checked (this checkout cannot answer; nothing is claimed):
  release tags: the checkout is a shallow clone, so the 8 releases with no tag
  here cannot be told from a release whose tag was never fetched;
  `git fetch --tags` makes the question answerable
```

That is exactly what the item asked for, and `make check` no longer fails on a fresh clone for this reason. Left open, someone would eventually pick it up and re-investigate a solved problem.

## `PL-V5XM` → duplicate of `PL-D2GW`, the weaker of the two briefs

`PL-V5XM` (the session-start digest offers v0.2.8 as a release while the line below it says v0.2.8's gate has 8 entries open) is the same defect as `PL-D2GW` (the session digest offers a release whose version names a milestone whose gate is still open).

`PL-D2GW` carries the transcript of both contradicting lines, names where the digest reads them, and already works the design question — suppress the release line while the suggested version's gate is open, or keep it and carry the answer — with a recommendation for the second. `PL-V5XM` carries the capture stub: `**Why it matters.**`, `**Where.**` and `**Done when.**` are all empty. Merging them would mean rewriting this one into what `PL-D2GW` already says.

## Effect

Untriaged count 11 → 9. Open count is unchanged at 60, because that figure is the banded total and untriaged items carry no priority.

## Verification

`make check` green: ruff format, ruff check, mypy, 740 tests, `docket check` (0 errors), `doc_check check` (0 errors, 0 advisories).

No documentation sweep needed — the diff touches two item files and no statement anywhere cites either id.

---
_Generated by [Claude Code](https://claude.ai/code/session_018JtCiBsX21xzGigqwdRY9U)_
