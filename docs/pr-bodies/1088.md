---
pr: 1088
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGGpM5rLsR9wBJtcyx42CEcw)_

Before: `no-prune-guard.sh` found where a command starts with a regex of its own, run over text that kept quoted content as written. A `;` inside quotes read as a separator and a quoted flag read as a flag, so commands that prune nothing were refused: the loop over command strings that filed PL-WGFY, `echo 'a; git fetch --prune'`, and `git commit -m 'never fetch with --prune'`. It was the last of the three Bash guards not reading commands through `.claude/hooks/shell_split.py`.

After: the guard reads the commands the shared splitter finds and matches its four pruning shapes on their words, so quoted text starts nothing. Every prune it refused before is still refused, including one inside a `$( )`, quoted or not, and one on a line before a syntax error, which bash runs before it stops. A prune after `{` or `!` is now refused too, as the other two guards already read those openers. PL-WGFY is closed. PL-PVW2 stays open, `generator: live`: PL-B5VZ, docket's own three-way split of a `verify:` command, is the same question inside docket, and its file is held by the thread working PL-GPJ7's step 3.

How: `shell_split.py` gains `commands`, which cuts each segment again at a `(` or `)` operator, adds the commands of every `$( )` read inside double quotes, and, where bash would refuse the command, returns what it read before that point rather than None. It also gains `command_words`, `GROUPING` and `ASSIGNMENT`, which replace the copies the gate and floor guards held, so all three agree on where a command's words start; their behaviour is unchanged and their tests pass as they were. `command_text`, the prune guard's old input, had no other caller and is removed, with the error position only it read. `tests/unit/test_no_prune_guard.py` pins `test_a_quoted_separator_starts_no_command` (the name PL-PVW2's `verify:` predicted), the subshell, substitution and opener spellings, and `test_a_prune_before_a_line_bash_cannot_read_is_refused`. Run against the hook on `main`, the quoted test and the two opener cases fail and the rest pass, which pins that the move lost nothing. Filed from the probing: PL-R17X, pruning spellings git documents that the four shapes never covered, such as `git pull --prune` and `git remote update -p`; and PL-0X0G, a reserved word such as `do`, `then` or `time` in front of a command, which all three guards read as the command's name, so `for f in a; do make check | tail; done` passes the gate guard. `bin/docket verify --self PL-WGFY` says ACCEPT, with `make check` passing (4,837 tests).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_016NHrAFSVyNqg8a7anyVjS8
