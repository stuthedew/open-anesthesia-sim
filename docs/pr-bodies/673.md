---
pr: 673
commit: 17aea0e33001ae843947677b45bf52e45f465fe1
merged: 2026-09-17
items: PL-M2SD
subject: PL-M2SD: close out, and record the pull request numbers the base is owed (#673)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 17aea0e3 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follow-up to #670, which merged as a squash — `main` carries the content but none of that branch's commits, so `PL-M2SD` stayed `untriaged` with its work already shipped. This is a new pull request on a branch restarted from the merged base, not a reuse of the merged one.

## `PL-M2SD` closed out

Marked `done` with `pr: 670`, and given a `verify:` that asserts all four halves of what landed rather than just the obvious one:

    uv run pytest tests/unit/test_generator_check.py
      && grep -q 'python3 tools/generator_check.py' Makefile
      && grep -q 'generator_check.py' docs/ARCHITECTURE.md
      && grep -q 'tests/unit/test_generator_check.py' docket.toml

The last two clauses are deliberate. Those are the registrations a new file in this tree owes — a line in the package map, an entry in `workflow_paths` — and forgetting them is what turned #670 red twice. The verify now fails if either is dropped again, so the same mistake cannot pass silently.

## Four pull request numbers the base was owed

`docket check` reported four items marked `done` on `origin/main` with no `pr`, each recoverable from its merge commit, and its advisory asks for them to ride a commit already being made rather than a commit of their own. `bin/docket record` wrote them in one pass:

| item | pull request |
| --- | --- |
| `PL-DHBX` | #671 |
| `PL-S6WW` | #669 |
| `PL-SWP3` | #667 |
| `PL-XD3C` | #668 |

## Checks

`doc_check.py` 0 errors, `bin/docket check` 0 errors, `branch_id_check.py`, `workflow_paths_check.py` and `rules_paths_check.py` all clean; the new `verify:` command passes (6 tests). Resident instruction size unchanged against `origin/main`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Ae7roBqijcPMQFkf5En9xT

---
_Generated by [Claude Code](https://claude.ai/code/session_01Ae7roBqijcPMQFkf5En9xT)_
