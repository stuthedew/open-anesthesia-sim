---
pr: 1086
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGQPUTMovMYK3H64EWvuaDxV)_

Before: four of `tools/doc_check.py`'s hard gates recognised what they check by wording, so correct prose failed `make check`. A quotation set beside a document name failed hard after any connective, so wording proposed for CLAUDE.md, or since deleted from it, failed when a colon or comma introduced it. A fenced shell comment saying "make sure the virtualenv", or make's own "No rule to make target" output, was read as naming a make target. A regex inside an indented code block was read as prose by the math-delimiter check. And a sentence naming a file that deliberately does not exist (this project has no `setup.py`, a planned tool, a deleted file) failed the path-citation check with no way to say so.

After: each fails hard only on explicit syntax. A quotation fails hard only after `§` or the possessive, the two connectives that claim the file holds the words; after a colon, comma, parenthesis, `under` or nothing, a miss is an advisory, still read for drift. A fenced line names a make target only where `make` is its first word. CommonMark indented code blocks are blanked before the math check, with list items tracked so an item's own prose indented four is still read; the escaped-bracket half stays a hard error, as PL-GPJ7 decided. A path a sentence names as absent is declared by an `absent:` HTML comment under its paragraph, written like the `provenance:` markers; the marker is itself checked, failing when the path exists or its paragraph does not cite it, and the unresolved-path error names it. The real tree still reads 0 errors and 0 advisories.

This is step 2 of PL-GPJ7's split, recorded in the item. It closes PL-HVST, PL-L8VP, PL-XGYH and PL-HJ8G; the head stays open for steps 3 and 4.

How: `QUOTED_SOURCE_RE` captures its connective and `CLAIMING_CONNECTIVE_RE` decides the severity. The count behind keeping the possessive hard is in PL-HVST: 423 `§` and 15 possessive quotations stay hard, and 28 move, all contained today. `_make_mentions` reads a fenced line through `FENCED_MAKE_RE`, which keeps 2 of the 5 fenced mentions; the 109 code-span mentions are untouched. `_without_code` tracks each open list item's content column and blanks a line four columns past it that does not continue a paragraph, which newly blanks 206 lines in 62 files, none holding a delimiter. `_absent_paths` reads the markers, and `_marked_span` gives each the paragraph above it, shared with the provenance reader. Of 24 new test cases, 20 fail on the old code and 4 pin that a list item's own prose indented four is still read. The `under` and parenthesis quoted-source tests now assert the advisory rather than the error, which `bin/docket verify --self` reports as a removed assertion: that is the commissioned change to what they pinned.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XGVbZPjTfU3StQbi3kP8WT

---
_Generated by [Claude Code](https://claude.ai/code/session_01XGVbZPjTfU3StQbi3kP8WT)_
