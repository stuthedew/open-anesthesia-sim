---
pr: 662
commit: 484d1e11db5290f465749f80a40c62b9d0bee325
merged: 2026-09-17
items: PL-7TYC, PL-L40Z, PL-TKFD
subject: PL-L40Z: drop as superseded by PL-7TYC, and carry its structural half to PL-TKFD (#662)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 484d1e11 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`PL-7TYC` merged as #660 at `c5e9776` minutes before `PL-L40Z` was picked up.
Its `is_assertion_line` asks the same file question `PL-L40Z`'s **Approach**
section recommended — only a file Python executes can hold an assertion — so
both halves of that item's "Done when" were already satisfied on arrival.

## Measured rather than read

Replaying each release cut's own diff through the current predicate:

| cut | flagged by the substring grep | flagged now |
| --- | --- | --- |
| v0.4.20 | 0 | 0 |
| v0.4.21 | 2 | 0 |
| v0.4.22 | 6 | 0 |
| v0.4.23 | 4 | 0 |
| v0.4.24 | 4 | 0 |
| v0.4.25 | 2 | 0 |
| v0.4.26 | 0 | 0 |
| v0.4.27 | 5 | 0 |

All 23 are `ROADMAP.md`, and the five on v0.4.27 are the ones the item was found
on. `test_prose_removed_from_a_document_is_never_an_assertion` and
`test_a_genuinely_removed_assertion_is_still_reported` pin both directions and
pass. So the item is `dropped`, not `done`: nothing here built anything.

## The finding that would have been lost

One paragraph of the brief was not `PL-7TYC`'s. `falsifies:` is **structurally**
unreachable for an item filed and closed on one branch, rather than merely
awkward when the need is discovered mid-work: the base holds no copy of the
item, so there is no moment at which any declaration could have been read.
`commissioned_falsification` collapses that into "declares nothing" silently,
because it is also what every capture looks like.

452 of 842 closed items — 54% — share an `added` and `closed` date, so the class
is most of the store's closures. Read as a proxy: it over-reports where a
capture merged before the session that closed it, and under-reports a branch
spanning midnight.

That is appended to `PL-TKFD`, which poses the reachability decision and asked
for instances to count. It also separates that item's three candidates, which
its own framing did not: only the branch-ordered declaration reaches this class,
because an item filed and closed on one branch is never triaged before it is
worked.

## Also in this commit

`bin/docket record` wrote the four `pr` numbers the base was owed — `PL-7TYC`
#660, `PL-H8YD` #657, `PL-LKGL` #658, `PL-TM9J` #659 — riding this commit rather
than one composed for it.

## Checks

`bin/docket verify --self PL-L40Z` → `ACCEPT`, including `make check`.
`bin/docket check` → 0 errors. `tools/doc_check.py candidates --base origin/main`
→ nothing in the documentation mentions anything this diff changed; the diff is
confined to `docs/items/`, and nothing outside it names `PL-L40Z`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CpvZprMCP8eCnopB85uupx

---
_Generated by [Claude Code](https://claude.ai/code/session_01CpvZprMCP8eCnopB85uupx)_
