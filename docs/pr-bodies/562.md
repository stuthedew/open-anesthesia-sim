---
pr: 562
commit: 88c4c0ddb1437a7cfbb33318d0c9461c1dbaaf58
merged: 2026-09-14
items: PL-5328, PL-J2TD
subject: PL-J2TD: answer the driver question against the source, and capture PL-5328 (#562)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 88c4c0dd landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Queue-only. Gate 1 cleared, so this maps v0.5.0's eleven open `Required scope` entries and works the one at the head of the critical path. No source change; the decision it records is the project owner's to take.

## `PL-J2TD` — answered, and left `needs-decision` deliberately

The item asks whether it still delivers a resimulation driver sharing the live advance path, now that `PL-T691` answers any instant from the same timeline in closed form. Its own brief names the deciding evidence — `ROADMAP.md` item 12 — so the answer is the repository's rather than a preference. Three findings, each checked in the source rather than in the prose describing it:

1. **The first clause of Done when is already delivered.** `core/run_definition.py` *is* an applied control timeline: `record_change` keys each stretch at the run's own reach with a canonical opening keyframe, and `state_at` answers any instant as one propagation from it. `tests/integration/test_controller.py` already asserts the property to 6.7e-16 as a fraction of one atmosphere.
2. **The second clause is refused by the rule this milestone already landed.** `docs/MODEL.md` § "The canonical evaluation rule" takes every replayed, compared or branch-opening value canonically. A driver re-driving `SimulationState.advance` composes a third order of floating-point operations — measured in that section at 1.8e-14 from the canonical answer — and `PL-Z3W6` requires a branch to reproduce its parent **element-wise rather than within a tolerance**. So the driver could not be used for the one thing it was wanted for.
3. **What a fork needs beyond a curve is named already and is not a driver.** § "What this requires of a branch" gives two conditions — open at a keyframe, re-base the child's clock by subtraction — and both mechanisms exist: `RunDefinition.__init__` takes an opening state and refuses a `DisplayState` by name, and `AgentUptakeSystem.restore_state` seeds a live system.

The live advance path **does** still exist, which is the half a quick reading gets wrong: `SimulationController.advance` steps `SimulationState` and then moves the definition's reach, and `docs/MODEL.md` says both records are live this release. A fork must still produce something advanceable; what changed is that *reaching* the fork point no longer needs stepping, and stepping to it would forfeit the exactness `PL-Z3W6` requires.

Recommended narrowing: drop the driver, keep the resumption. Left open because `ROADMAP.md`'s Required scope bullet reads "the mechanism by which a point between recorded samples is reached at all", and that sentence becomes wrong under the narrowing — editing it is a scope edit.

## `PL-5328` — a second finding from the same pass

`PL-RD3B` is `ready`, sits in v0.5.0's `Required scope`, and ranks third in `bin/docket next`. Its **Where** specifies moving `RunHistory`, `HistoryWindow` and `SimulationHistorySample` out of `app/controller.py`; `PL-2FM6` deleted all three, and `tests/unit/test_run_history.py` — which its **Done when** rests on — does not exist. Only `RecordedQuantity` survives. A session taking it today would open on a brief describing code that is not there.

## Also in here

`bin/docket record` rides the commit, writing the three `pr` numbers the base was owed (#559, #560).

## Checks

`docket check` 0 errors, `doc_check` 0 errors and 0 advisories. No source or test file changed, so the suite is untouched by this branch.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XjkEmcq1d7BCeUXAfwpUcp

---
_Generated by [Claude Code](https://claude.ai/code/session_01XjkEmcq1d7BCeUXAfwpUcp)_
