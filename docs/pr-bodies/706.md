---
pr: 706
commit: dd745468e13b03018188d54e2662d1ab062b831b
merged: 2026-09-19
items: PL-ZMGR
subject: PL-ZMGR: record the ratified decision and triage it to ready (#706)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit dd745468 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`PL-ZMGR` (a `needs-decision` item whose answer is "retire this" can never carry
`falsifies:`, so every session-decided retirement rejects its own close-out) was
captured during `PL-G6J5` (#705) with three costed repairs and none chosen. The
project owner has now agreed with the recommendation, so this records the
decision and triages the item so `bin/docket next` can offer it.

This is a queue-and-roadmap commit: **the decision record is the deliverable**,
and it has to reach `main` or the fresh session that implements it reads an
untriaged item and re-derives the whole argument.

## The decision

**A `needs-decision` item's closure may declare `falsifies:` in the same commit
that records the decision** (project owner, 2026-09-19, ratified) — chosen over
adding the assertion check to the four guards `--self` already relaxes, and over
leaving every such close-out to report `REJECT` and be explained in prose.

Recorded as **ratified rather than specified**, per `CLAUDE.md`: it was this
session's own recommendation wearing the owner's signature, so ordinary evidence
reopens it.

**Why the field can be trusted here when the general rule is that it cannot.**
`commissioned_falsification` reads `falsifies:` from the *base's* copy precisely
so that a reviewer writes it before the work — "one added beside the deletion it
excuses is the worker's own word for it". That holds for a `ready` item, whose
commission is settled before the branch opens. It does not hold for a
`needs-decision` item: the base's copy saying `status: needs-decision` *is* the
standing statement that the question is open and the answer is the session's to
make, and `.claude/skills/docket/SKILL.md` already treats such an item's
deliverable as the item file itself.

**The gate is the base's status, never the branch's**, which is what stops the
exemption being self-granted — a session cannot set its own item to
`needs-decision` on its branch and walk through, because the status is read from
the same base copy the field is. A `ready` item is untouched, so a delegated
worker still cannot add `falsifies:` to excuse deleting tests. That is the
property the rejected second option would have given away on every self-audited
branch.

## What the implementing session needs

`commissioned_falsification` (`subprojects/docket/src/docket/verify.py:883`)
returns the base's declaration; the refusal it feeds is the `item.falsifies and
not declared` branch at `verify.py:1320`. The change is to let that branch
honour the working tree's declaration when the base's copy reads
`status: needs-decision` *and* this branch's diff closes the item — and to say
in the check's own words that it did, rather than folding silently.

The worked instance is in the brief: `PL-G6J5`'s own close-out printed
`FAIL no existing assertion removed - 29 line(s)`, every line an assertion about
the advisory the item had sanctioned retiring, with `make check` green and every
other guard passing.

## Triage

| field | value |
| --- | --- |
| priority | `P2` |
| effort | `S` |
| classes | `defect, infra` |
| status | `ready` |
| verify | `grep -q 'def test_a_needs_decision_closure_may_declare_what_it_falsifies' subprojects/docket/tests/test_verify.py` |

The `verify:` command was run against this tree and exits 1, so it fails for the
right reason and names the exact test the work owes.

**One correction included.** The item was filed under `feature: verify-replay-cost`
because it surfaced during that work, but it is about the close-out audit rather
than replay cost — left there it would have stopped that feature closing when
`PL-FZ58`, its actual remaining item, lands. The feature is removed.

## The gate disposition the triage made due (`bf2cf84`)

Classing it `defect` makes it debt, so `make doc-check` immediately reported
that v0.5.0's gate recorded no disposition for it — "the presence rule allows
either answer and forbids neither being written down".

**Declined to this gate**, not admitted. The gate froze 2026-09-06 and this was
captured 2026-09-19; it is `P2` and neither `safety` nor `science`; and the
mechanism it describes is itself post-freeze — `PL-7TYC` was still repairing the
assertion check's matcher on 2026-09-17 — so the snapshot rule places it in the
next gate on its own terms rather than by exception.

Written into the **existing** `### Declined to Gate 2` subsection rather than a
new one, because `_declined_ids` reads only the first such subsection after the
gate heading and a second silently orphans the 66 dispositions already there
(`PL-82B0`).

## Checks

`make doc-check` reports **0 errors, 0 advisories** (it reported 1 advisory
before `bf2cf84`, which is what prompted that commit). `make docket` passes with
0 errors, and `bin/docket wave` still reads 175 entries against 175 ids, so no
stated count was broken. No Python file is touched, so lint, typecheck and the
test suite are unaffected.

## Note on the branch

`claude/confident-cori-xczqas` was deleted when #705 merged, so this is a fresh
branch of the same name off `main` — a new pull request, not a reopening of
#705. Before clearing the stale tracking ref I confirmed all six files #705
touched are byte-identical between the old head and `main`, so the squash took
the work whole and nothing was stranded.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015L3HFMrB2NCyW6VeqZSneR
