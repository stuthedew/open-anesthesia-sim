---
pr: 566
commit: e17658a6362242da06fac54640362683efc75e87
merged: 2026-09-14
items: PL-27H0, PL-2FM6, PL-5328, PL-RD3B
subject: PL-5328: re-brief PL-RD3B against the tree PL-2FM6 left, and capture PL-27H0 (#566)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit e17658a6 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The project owner's call was **re-brief or drop**. The measurement says the item is real, so the brief is rewritten rather than the item retired.

## Why re-brief rather than drop

`PL-RD3B`'s *premise* survived `PL-2FM6` intact; only its symbol list died. The premise is that the seam is a fact about the imports rather than one the item invents, and that is still measurably true. Of the four modules importing `app/controller.py`:

| Importer | What it takes from `app/controller.py` |
| --- | --- |
| `app/chart_series.py` | `DrawnWindow`, `RecordedSeries` — and nothing else |
| `app/control_timeline.py` | `ControlChange`, `ControlInput` — and nothing else |
| `app/main.py` | `SimulationController` — and nothing else |
| `app/simulation_view.py` | all of it |

Three of the four already use one half or the other. The file is also *larger* than when the item was filed: 1 064 lines against the 930 its original complained about, of which the controller proper is the last 564.

## What the re-brief corrected, beyond what PL-5328 reported

`PL-5328` reported the dead symbols. Three further staleness defects were in the same brief:

- the three queue items cited as its reasons — `PL-011`, `PL-WRKL`, `PL-RRWV` — are all `dropped`. Replaced by the live instances: v0.5.0's `PL-J2TD`, `PL-TFX5`, `PL-Z3W6` and `PL-8PSW`, all of which operate on the extracted half, plus `PL-CNCF`, a `perf` item on `drawn_window` itself;
- it placed the package map in `docs/MODEL.md`. The map is the tree in `docs/ARCHITECTURE.md`, which `check_package_maps` holds to the files on disk in both directions;
- its `verify:` command ran `tests/unit/test_run_history.py`, a file that does not exist, so it could never have passed. The replacement pairs the two consumer test files with a grep asserting `app/chart_series.py` no longer imports from `app/controller.py`. Run before the work it exits 1, for the grep rather than for a selector that matched nothing.

The brief now names the five symbols that move with their line numbers on `9744d39`, proposes `app/run_series.py` in place of the deleted store's name, and names the control-input half as optional scope.

## The title, and two roadmap references

The title said the controller "now holds the run's storage". There is no storage, and the title is what a session reads first in `bin/docket next`. Retitled, with the file renamed by `git mv` to the slug `docket` generates.

`ROADMAP.md` carried the same stale noun twice — the Gate 1 entry under "Cleared by v0.5.0 itself" quotes the title, and v0.5.0's Required-scope bullet read "without a second copy of the storage". Both corrected, **with the scope itself unchanged**; only the noun was stale. Neither is held to the store by a check: `check_gate_counts` checks counts and `check_gate_reentries` reads `classes`.

## Left ready rather than blocked on PL-B9PY

`PL-B9PY` is in flight in the one importer that takes the whole module, so it lands first and `PL-RD3B` resolves against it. A shared file is a sequencing note rather than a refusal — `PL-VRMK`'s refuted approach — so the item stays `ready` and the note lives in its brief.

## Captured, not fixed

- `PL-27H0` — `docs/MODEL.md` still specifies the per-step recorded sample store and a display operation selecting among recorded samples, both deleted by `PL-2FM6`. Outside this item's `touches`, and an edit to the authoritative model specification wants its own doc sweep.
- `PL-4PC5` (earlier commit on this branch) — `bin/docket wave` counts ids cited in a Required-scope section's prose as scope entries, so v0.5.0 reads 21 ids with 11 closed against the section's own stated eighteen with 8 closed. That commit also carries seven `pr` numbers written by `bin/docket record`.

## Checks

`make check` passes: 2716 tests, mypy clean on 66 source files, `doc-check` 0 errors, `docket` 0 errors. `doc_check.py candidates --base origin/main` reports nothing in the documentation mentioning anything this diff changed.

Closes `PL-5328`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013YSWo5TRZn8zkfn2i2fyhh

---
_Generated by [Claude Code](https://claude.ai/code/session_013YSWo5TRZn8zkfn2i2fyhh)_
