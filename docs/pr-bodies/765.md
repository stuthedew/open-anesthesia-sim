---
pr: 765
commit: 10f52485746267d4337e43a2e9e2299e8d227338
merged: 2026-09-20
items: PL-439V, PL-8G48, PL-CHQY, PL-V3QB
subject: PL-8G48, PL-439V, PL-CHQY, PL-V3QB: groom the four items whose blockers closed, of which one was promotable (#765)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 10f52485 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`bin/docket check` named four items as "every blocker has closed; it is ready to promote". Read against the tree, **one of the four was**.

## The dispositions

| item | disposition | why |
| --- | --- | --- |
| `PL-8PS6` fresh gas flow range is a machine property | `ready`, `P1` | the carrier now exists and the abstraction slots the field; the split is buildable over one profile |
| `PL-439V` make the Tec 6 the concrete second device class | `done` | `PL-FG9D`'s specification already answers all three clauses of its `Done when.` |
| `PL-WZVZ` make an inter-machine difference attributable | stays `blocked`, re-pointed | one machine profile, no selection surface; and its table is a new display surface |
| `PL-CTD7` detect a bookmark crossing inside the advance loop | `needs-decision`, `P1` | two user-facing questions were written into it after its last triage |

**`PL-8PS6`** — `src/anesthesia_sim/data/machines/reference_circle_system.json` is a versioned profile already, and `docs/machine-abstraction.md` slots `deliverable_flow_range_l_min` against survey section (b8) while keeping `core/supported_ranges.py`'s bounds as the model's own envelope. Separating the two claims does not need a second machine. Survey section (b8) found the *value* unknown for every machine and records that ISO 80601-2-13:2022 was unreachable, so the brief's demand for a primary source is corrected in-item to shipping the structure with the value recorded as unknown — which is what the abstraction already provides for.

**`PL-439V`** — `docs/machine-abstraction.md` § "Dial mapping" carries `variable_bypass` and `fixed_volume_percent` as two device classes, names the Tec 6 as the second, sources it to Meyer et al., and honestly records that the two mappings agree everywhere in the model's present domain because there is no ambient pressure, pointing at `PL-5K5C`.

**`PL-WZVZ`** — re-pointed from the closed `PL-FG9D`/`PL-4DCG` to `PL-TH35` (the View contract) and `PL-R1WQ` (the view registry). Planned-milestone item 34's standing rule is that no new display surface is built before that item, and the rule names those two as its own condition. Its second half is a requirement on planned-milestone items 11 and 12 rather than a surface, and is held only by there being one machine.

**`PL-CTD7`** — the re-arming question ("does a MAC target halt on every crossing, or once until re-armed?") goes to the project owner with a recommendation, since it is behavior a learner would see. The inherited-bookmark outcome is settled in-session: a bookmark before a branch's fork instant is unreachable by construction, so reporting it as "not reached within the cap" would imply running longer might reach it.

## Both band moves are the exemption lapsing, not a re-rating

`check_gate_reentries` exempts a `safety` item from the `P1` floor only while `anticipated` **and** `status: blocked` hold together, so leaving `blocked` is what returns the finding to the debt gate. `docket set` refused both moves at `P2` in those words. Neither hazard is live; `anticipated` stays on both.

## `ROADMAP.md`

Re-pointing `PL-WZVZ` at two v0.6.0-placed items is a gate deferral, and § "The gate is a snapshot, not a moving target" permits one only where the deferral section says so and says why — a `blocked-by:` field alone is the `PL-9S30` defect. Two paragraphs said the two entries both wait on `PL-FG9D`; both now say what is true, and record that the entries have separated.

## Riders

- **`PL-V3QB`** closed: `PL-7TVT` recovered from `origin/claude/wizardly-goodall-p5ltid`, an archived session's branch with no pull request. It arrives still `untriaged`.
- **`PL-CHQY`** closed with this — filed the same day from the same advisory over the same three items, and satisfied in full by this pass.
- Two unmerged queue commits were merged from `origin/claude/adoring-einstein-44v9pt` so they land here rather than stranding.
- `bin/docket record` backfilled six `pr` numbers the base was owed.
- `PL-8PS6`'s "Where" block had six drifted line citations, repaired by symbol per `.claude/rules/citation-drift.md`.

## The measurement

It went to `PL-6T44`, the recorded generator at `needs-decision` weighing exactly this. Across this pass and `PL-JFQ3`'s: **13 items the advisory called promotable, 6 genuinely startable.** `PL-WZVZ` is the expensive case — option 1 would have ranked it startable *and* pulled it to `P1`, putting an unbuildable item into the top band and the debt gate with nobody deciding to. `PL-PQC7` captures the fourth route the closed `PL-CHQY` had recommended: ask "what does this unblock?" at close-out, where the session holds the context.

## Checks

`make check` green — 3405 tests, 100% coverage, `docket check` 0 errors, `doc_check.py` 0 errors/0 advisories, resident instructions unchanged. `bin/docket verify --self` ACCEPTs all four closed items. Docs swept: `ROADMAP.md`, `docs/machine-abstraction.md`, `docs/machine-survey.md`, `docs/MODEL.md` and `src/anesthesia_sim/data/machines/reference_circle_system.json` read; `doc_check.py candidates` reports nothing in the documentation mentions anything this diff changed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01RX3eYWJto4jrWdyXmPrE8d

---
_Generated by [Claude Code](https://claude.ai/code/session_01RX3eYWJto4jrWdyXmPrE8d)_
