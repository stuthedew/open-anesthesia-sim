---
pr: 410
commit: 6d659903b861d3ecca1e6b4c55533a5ef9584d81
merged: 2026-09-06
items: PL-XTMB
subject: PL-XTMB: cite the Workbook at Appendix B page 168, and correct what it actually supplies (#410)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 6d659903 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-XTMB` (the Gas Man Workbook citation still rests on a bare vendor URL). The project owner supplied the Workbook's front matter and its appendices — the route the item said it would need. Read at the source; the repository does not hold them, as publisher-copyright material in a public repository. **No stored value changed.**

Three things came out of the reading, and only the first was expected.

## 1. The citation now names the right place

Philip JH, *Gas Man® Workbook and Laboratory Manual*, Med Man Simulations, Inc., Chestnut Hill MA — document dated 2012-05-16, preface October 2010 — **Appendix B, "The Gas Man Approach", Model Parameters table, page 168**.

The old citation named "Default Options: Patient Defaults", which is page 183 in Appendix E: a description of the interface controls that carries none of the values.

## 2. The file was wrong about its own contents

It asserted the Workbook was "the source of every stored value in this file". The table supplies **seven of the eleven**:

| | In Table A-1 (p. 168) |
| --- | --- |
| `alveolar_gas_volume_l` 2.5, the three tissue volumes 6.0/33.0/14.5, the three flow fractions 0.76/0.18/0.06 | present, exactly equal |
| `default_cardiac_output_l_min` 5.0 | only as the sum of the flow column (3.80 + 0.90 + 0.30) |
| `weight_kg` 70, `default_alveolar_ventilation_l_min` 4.0 | interface defaults; the chapters supplied state no number |
| `venous_blood_volume_l` 1.0 | **absent** — the `Blood` row reads 5.00 L → `PL-3YZW` |

## 3. The lineage is Lowe and Ernst, not Mapleson

Printed beneath the same table: *"Values for volume, flow and relative flow are taken from Lowe and Ernst, 1981"* — reference 23, *The Quantitative Practice of Anesthesia: Use of Closed Circuit*, Williams & Wilkins. The Workbook attributes nothing to Mapleson, whom this file named as its primary lineage until [#399](https://github.com/stuthedew/open-anesthesia-sim/pull/399) removed the framing earlier today on the grounds that it rested on titles nobody had read.

Recorded as a **quotation inside the Workbook's own note, not as a `sources` entry of its own**. An unread document with its own citation line is precisely the Mapleson failure again — and the schema made the point before I did, refusing the empty `url` a book with no DOI would need. `PL-7HDS` carries reading it; that is the link deciding whether this chain ends in a measurement or in another compilation.

## Also captured, not worked here

`PL-ZP7Z` — the same table's notes attribute the volatile partition coefficients to Yasuda, Targ and Eger (as the `Anesthesiology 69:A615` abstract, not the 1989 *Anesthesia & Analgesia* paper) and sevoflurane's, one sentence later, to "the package insert and Abbott data". Neither is what the agent files say. Bears on `PL-D6LX`.

The route used here — a document supplied by the project owner, in neither PubMed nor `docs/references/` — is a third route `.claude/rules/citing-sources.md` does not describe. That is `PL-XJ5P`'s gap, now with a worked instance.

## Verification

`verify:` run both ways — exit 1 on the pre-work tree, exit 0 on this one. `make check` green: 1977 tests, 100% core coverage, `docket check` 0 errors, `doc_check` 0 errors and 0 advisories.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CZD5jvkBTTi6T4Md8bVTga

---
_Generated by [Claude Code](https://claude.ai/code/session_01CZD5jvkBTTi6T4Md8bVTga)_
