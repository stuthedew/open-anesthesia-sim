---
pr: 165
commit: 45ec70df3373a163889c713b7cec16367132a583
merged: 2026-09-01
items: PL-768D
subject: PL-768D triage: P2/S, needs-decision on which mechanism closes the floor gap (#165)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 45ec70df landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Store-only. Triages `PL-768D` (the `tools/` import guard reads the running interpreter's stdlib, not the floor's), the last untriaged item, and clears two `pr`-recovery advisories on `PL-NSN9` and `PL-PF8H`.

## Fields set

`P2`, `S`, `defect`/`infra`, `dev-tooling` — the same banding as `PL-QDH7`, which created the guard. `touches` names both candidate files, which is the conservative answer for concurrency while the mechanism is open.

`status: needs-decision` rather than `ready`: the route is genuinely undecided, and a `verify:` command differs per route. Writing one now would mean writing it away from its work, which is how every wrong command in this store came to exist.

## What triage found that the capture had not

**Nothing in CI runs either tree at the floor at all.** `.github/workflows/quality.yml` runs `uv run python tools/doc_check.py check` under the virtualenv, and `bin/docket check` under whatever system `python3` the runner image ships — neither pinned to the 3.11 floor `subprojects/docket/pyproject.toml` declares.

So *both* portability suites are approximations of a run nobody performs in CI: the parse test's `feature_version=floor` gates the syntax CPython version-checks rather than parsing as 3.11 would, and the import test reads the wrong interpreter's stdlib set. Locally the real thing does happen — `make check`'s last line is a genuine 3.11 invocation, and per `tools/ruff.toml`'s own account that is what caught the original `SyntaxError` on 2026-08-31. CI is the half with no backstop.

That reframes the item. The captured question was "how do we approximate the floor's module set more accurately?"; the better question is "why are we approximating it?"

## The recommendation, and why it is a decision rather than a fix

A fourth route, recommended over the three captured: a second job in `quality.yml` — `actions/checkout`, `actions/setup-python` at 3.11, then `python3 tools/doc_check.py check` and `bin/docket check`. It tests the promise instead of approximating it: imports, syntax and runtime behavior at once, with no allowlist, no staleness, and nothing to maintain as the standard library moves. Both commands were run at 3.11.15 during triage and exit 0, so it would land as a guard rather than as a fix.

It does not replace the portability suites — those run in `make check` before a push, name the offending file and import, and catch a lazily-imported module inside a branch a CI run never takes. It makes their approximations no longer load-bearing, which is what lets the docstring paragraph go.

Route 1 (subtract a hand-written set of post-floor names) is argued down in the item rather than left as an equal option: the set must cover every module added between the floor and whatever interpreter runs the suite, forever, so each Python upgrade silently reopens the same fail-open hole. A guard that re-creates its own bug on every upgrade is the wrong shape.

The choice changes which files are touched and rewrites **Done when**, so nothing is built pending it. `needs-decision` keeps the item out of `bin/docket next` until it is answered.

## Also here

`PL-NSN9` and `PL-PF8H` are marked done on `main` with no `pr`. Numbers verified against the merge subjects on `origin/main` (`3b37a75` → #164, `c663c13` → #162) rather than taken from the advisory text. Docket advisories drop from four to two.

## Checks

`make check` green: `ruff format --check`, `ruff check`, `mypy`, 882 tests, `bin/docket check` 0 errors / 0 untriaged, `doc_check` 0 errors.

---
🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01JZNs4wWcM7NndyffsN5tgV

---
_Generated by [Claude Code](https://claude.ai/code/session_01JZNs4wWcM7NndyffsN5tgV)_
