---
pr: 230
commit: 043a8b39dffc6fc5f2a462acbb2ac78a27803dda
merged: 2026-09-02
items: PL-2XTF, PL-P0QT
subject: PL-2XTF, PL-P0QT: record the pull requests that closed them, and capture two findings (#230)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 043a8b39 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Clears the two grooming advisories standing on `main` after #227 and #228 merged, and carries two captures made while doing it.

## The backfill

Both items landed with an empty `pr:`, which is the shape the docket skill asks for: the number cannot be known before the pull request is open, and splitting the closure out to learn it reopens the window a merge can land in. `docket check` recovered the numbers from the merge commits and raised the advisory naming them — this is the one-line follow-up it asks for.

- `PL-2XTF` → `pr: 227`
- `PL-P0QT` → `pr: 228`

`docket check` goes from 4 advisories to 2 on this branch; the two that remain predate it.

## Captured, not fixed here

- **`PL-MC8Z`** — the start-an-item guard asks three ways whether another session is on the *item* (`git fetch`, `docket show`, the `list_sessions` scan) and never whether one is in the same *files*. Measured: `bin/docket concurrent PL-P0QT` listed `PL-2XTF` under "Cannot run alongside" for the whole time both branches were being written, and neither session ran it, because nothing in the guard says to. The command already exists and is already documented — it is simply never reached from the mode that would use it. Explicitly not `PL-YHD3`: nothing here should yield.
- **`PL-YNCW`** — `docket new --touches path "Title"` consumes the title into `--touches` (`nargs="*"` is greedy) and then reports the title as missing. Small, but it is friction on the one path `CLAUDE.md` requires to work when usage is nearly spent.

## Verification

`make check` green: 1152 tests pass, 0 store errors, 0 documentation errors, contrast unchanged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Uk6BxpCwS8QoBwjL7mVocn

---
_Generated by [Claude Code](https://claude.ai/code/session_01Uk6BxpCwS8QoBwjL7mVocn)_
