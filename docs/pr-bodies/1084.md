---
pr: 1084
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeG44Nnur9Z4o6bwXy2FHYTJr)_

Before: whether a branch changes only the queue had two answers, and only one had a name. `claims.in_queue` counts the items, `ROADMAP.md`, `docs/WORKING_NOTES.md` and the body records, and decides whether a change owes a claim. `arm` spelt its own test inline, as a prefix match over the store, `subprojects/docket/` and the body records with `arming.py` held apart, so nothing could import it and neither definition named the other. A `ROADMAP.md`-only triage pass passed `branch_id_check`, showed no `unclaimed:` row, and was held by `arm`, with nothing saying why the two disagreed.

After: arming's answer is `arming.arms_on_green(path, items_dir)`, which `arm` asks path by path. Each docstring names the other and the question it answers: `in_queue` whether a change owes a claim, `arms_on_green` whether it may merge on green CI without the owner's read. The disagreement on the roadmap and the notes stays, as the owner decided: they owe no claim and still wait on a read.

The predicate lives in `arming.py`, not beside `in_queue` in `claims.py` as PL-0JGZ's done-when had it. The owner chose that on a decision card today (ratified, over two predicates in `claims`): the gate holds `arming.py` for a read so that it cannot loosen itself, and `claims.py` arms on green, so a rule for what arms kept there could be widened by an edit that itself merges unread. PL-F6MM keeps `arming.RECORDS` in `arming.py` for the same reason.

How: `arms_on_green` holds the prefix test `arm` spelt inline and answers no for `arming.py` itself. `arm` still asks `GATE in paths` directly, so the gate's hold does not depend on the predicate, and reports every other held path as before. `vcs._annotates_only`, a third spelling that counts the items alone, is left to PL-GJPD under PL-HMZZ, as the item recorded.

Tests: `test_a_roadmap_only_branch_is_queue_work_but_not_armable`, the test PL-PVW2's `verify:` names, asks both predicates about `ROADMAP.md` and `docs/WORKING_NOTES.md`, then builds a real `ROADMAP.md`-only branch: `work_outside_queue` is false, `unclaimed` names nothing, and `arm` holds it with `ROADMAP.md` as its one outside path. `test_what_arms_on_green_is_the_store_the_tooling_and_the_records_but_the_gate` pins the predicate's other answers, `arming.py` included.

PL-0JGZ was the last open member of step 4 of PL-PVW2 (one answer per repeated predicate). PL-PVW2 stays open, because #1082 reopened its step 2 for the one shell splitter the Bash guard hooks import; its item now says so. With this branch's test in place its `verify:` passed while the head stood reopened, which `docket check` refuses, so it gains one predicted test per new member in `tests/unit/test_gate_status_guard.py` (PL-63TT, PL-R5RF, PL-39LD). The thread that builds the splitter updates the line where it names them otherwise.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013NShBnE9iSxr3jkxhWFcMD
