---
pr: 184
commit: 7ea9aa11af9a5a49dd77d8f1053123a0f1578848
merged: 2026-09-01
items: PL-71P4, PL-L9JS
subject: PL-71P4, PL-L9JS: capture and triage the prevention half of the always-passing verify: problem (#184)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 7ea9aa11 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

## What this is

Queue grooming only — no code, no simulator change. Three item files.

Came out of a question about the three grooming advisories `bin/docket check` prints. Two of them (`PL-026`, no `verify:` command; `PL-0MLQ`/`PL-NV9W`, selector matches no test) are deliberately deferred by `docket.toml`'s `verify_required_from` policy and self-clear as their items are started — nothing needed. The third, nine open items whose `verify:` command already passes, is captured as `PL-L9JS` but scoped to repairing the nine, and this change records why that does not converge.

## The finding

`PL-6P9Y` joined the already-passes set between 2026-09-01 and 2026-09-02 with nobody touching it:

- On 2026-09-01 it was in `PL-5QKT`'s selects-nothing list — `-k out_of_scope` matched nothing, exit 5.
- `PL-Q2BJ` (#144) then added `test_the_digest_names_the_milestone_when_only_out_of_scope_work_is_ready` to the same file. Unrelated work.
- The selector now matches, the command exits 0, and its own work has not been started. Confirmed with `git log -S`.

The flip runs the wrong way: exit 5 makes `docket verify` reject a branch (a round trip), exit 0 makes it `ACCEPT` a branch that did none of the work. Fourteen open items still carry `-k` selectors, so the nine can be repaired to zero and be non-zero again from work that did nothing wrong.

`PL-5QKT` left the selects-nothing case an advisory rather than an error deliberately, because exit 5 has a benign reading only the item's author can distinguish. That reasoning is specific to `-k` and does not transfer: exit 0 at `status: ready` has no benign reading, since the skill's own definition is that the command fails without the work.

## Changes

- **`PL-71P4`** (new) — the preventive half. Triaged P2, `defect`/`infra`, `dev-tooling`, `S`, `ready`. Carries the worked case, the argument distinguishing it from `PL-5QKT`, a candidate mechanism (a dated cutoff mirroring `verify_required_from`), and the open design question about `in-progress` items. Also records a sequencing note: doing `PL-L9JS` first may remove the need for the cutoff altogether, left as the implementing session's call.
- **`PL-L9JS`** — cross-reference to `PL-71P4`, plus the check that all nine are non-discriminating commands rather than landed work (the table said eight; `PL-6P9Y` is the ninth).
- **`PL-9Y42`** — `pr: 182`, the number `docket check` recovered from its merge commit.

## Verification

`PL-71P4`'s `verify:` command was run before being written down, per the `docket` skill: the pytest half exits 0 (85 tests, 0.19s) and the `grep` half exits 1 for the test the work owes, so the pair exits 1 — an ordinary failure, not the 5 an empty `-k` selection returns. It uses the paired shape rather than the natural proof because a command invoking `docket check` would be run *by* `docket check` and recurse (`PL-20CQ`).

`make check`: 975 passed, 0 errors across docket, doc_check and contrast_check. The `PL-9Y42` advisory is cleared; the three discussed above remain, as expected.

## Scheduling

Neither `PL-71P4` nor `PL-L9JS` is in the frozen v0.4.0 gate. They are worked together as one `dev-tooling` session after the gate clears, rather than interrupting the current beat (9 of 21 entries open).

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01VgngnVZ2qKHpvzvNKFP3XA

---
_Generated by [Claude Code](https://claude.ai/code/session_01VgngnVZ2qKHpvzvNKFP3XA)_
