---
pr: 508
commit: ce020b2aa75ac6380ca3bcfbe1a601d5fedeed49
merged: 2026-09-13
items: PL-LBR6, PL-YTDN
subject: PL-YTDN, PL-LBR6: write the fifteen owed pr numbers, and record what running it proved (#508)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ce020b2a landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Store maintenance plus two findings the maintenance itself produced. No code changes.

## The `pr` write

`bin/docket record` on `origin/main` at `de6baee`, writing the numbers #505, #506 and #507 were owed to **fifteen** closed items. Grooming advisories drop **16 → 1**; the one left is `PL-3833`'s new filename-drift check, which is `PL-YTDN`'s to discharge rather than this branch's.

**Deliberately before any v0.4.15 cut.** The release stamps `milestone:` onto these same items and its notes cite the pull request that closed each one, so the `pr` write wants to be in place first.

## PL-LBR6 reproduced while doing it

I filed `PL-LBR6` earlier today from `PL-3833`'s brief, as a hazard observed once on `PL-36R4`. Running `record` here **reproduced it live**: the run wrote all fifteen numbers correctly *and* renamed `PL-DZFJ`'s file —

```
D  docs/items/PL-DZFJ-roadmap-md-1816-pl-ylkr-and-pl-7h0x-say-every.md
?? docs/items/PL-DZFJ-roadmap-md-s-gate-text-and-pl-ylkr-s-brief-say.md
```

— its title having been changed on `main` while the filename was not. So this is not a one-off that `PL-36R4` happened to hit; it fires on **any** `record` run that touches a drifted file, which is now a named class since `PL-3833` reports them.

**The rename is backed out here and the `pr: 507` write kept** — the same resolution `PL-36R4` got. That keeps the drift count at 9, matching `PL-YTDN`'s brief, and leaves the rename to the pass that has checked who holds the file. The item now carries this as the argument for fixing the writer rather than expecting every session to know to take a manual step it was never told about.

## What PL-YTDN gained

The rename pass needed one question answered before it starts: **can renaming an item file break something?** Yes — an item's `verify:` command can hardcode another item's full path, and `PL-JRPP` is exactly that shape (it greps `docs/items/PL-MGF9-…md`).

Measured on this tree:

- **0 of the 9 drifted files** are referenced by path in any other item's `verify:` or brief → renaming all nine is safe **today**.
- **34 items do hardcode some item file path** in a `verify:` command. None of those targets has drifted yet, so the coupling is latent rather than absent — the next title edited in place on one of them creates the breakage.

The brief now carries that, the runnable one-liner to re-check it at the time (the store moves, so the note is not to be trusted on its own), and the split: **6 open** (`PL-0VFF`, `PL-5N7T`, `PL-6194`, `PL-TTMF`, `PL-XQRK`, `PL-ZX12`) and **3 closed** (`PL-68XK`, `PL-DZFJ`, `PL-K9HV`), with the reasoning for renaming all nine — findability by `ls | grep` is the point of the slug, and a closed item is precisely what a later session looks up by name when tracing why something was done.

## Verification

```
make check
  2388 passed, 100% coverage
  docket: 0 errors, 1 advisory (filename drift, PL-YTDN's to clear)
  documentation: 0 errors, 0 advisories
```

Gate 1 is unchanged by this branch and stands at **80 cleared, 79 open** after #506.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01CBJzQiBVA7p5WLSADYhvrR

---
_Generated by [Claude Code](https://claude.ai/code/session_01CBJzQiBVA7p5WLSADYhvrR)_
