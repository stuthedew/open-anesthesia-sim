---
pr: 242
commit: 97ec254e986e4b611e6e1a1af2b6f4da48bb201e
merged: 2026-09-02
items: PL-5WFS
subject: PL-5WFS: encode v0.4.0's stated prerequisites so the ranking matches the briefs (#242)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 97ec254e landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Applies the option the project owner approved for `PL-5WFS`: use the existing `blocked-by` field rather than adding an `after:` field to the schema.

`docket next` ranks by band and by which feature is nearest finishing, and does not read a brief's body — so it was offering v0.4.0's items in close to the reverse of the order the briefs state.

## What changed

| Item | Change | The brief's own words |
| --- | --- | --- |
| `PL-F52R` (MAC-awake reference band) | `blocked-by: PL-DHV7` | "Depends on PL-DHV7 for the unit." |
| `PL-DHV7` (concentrations in MAC multiples) | `blocked-by: PL-WB0X` | "Do this after PL-WB0X stage 1 (split `simulation_view.py`; extract `app/formatting.py`)." |
| `PL-WB0X` (split `simulation_view.py`) | P2 → **P1** | — |

`PL-WB0X`'s stage 1 is now the whole item: the `SimulationView` decomposition proper moved to `PL-B9PY` at Gate 1, which that brief records.

## The P1 promotion is the checker's, not a judgment call

`_outranks_its_blocker` refuses a P1 waiting on a P2 — *"raise `PL-WB0X` to P1 or above, because nothing here can start before it does"* — and its rationale is exactly this case: a P1 safety item parked behind a lower-banded blocker is how a live safety defect goes quiet.

So the band here does **not** mean this refactor could mislead a clinician. It means two items that could are waiting on it, which is the one case `docket.toml`'s note about every P1 carrying a safety class does not cover. A note in the item records that, and that dropping it back to P2 requires unblocking `PL-DHV7` first or the checker fails.

## Result

`bin/docket next` now offers `PL-WB0X` and no longer offers the two items whose briefs say they come after it.

**Only the two prerequisites the briefs actually state were encoded.** `PL-ZRSP` (F_A/F_I ratio) and `PL-VM40` (derive simulated time from a step count) now rank above `PL-WB0X`, and `bin/docket concurrent PL-WB0X` lists both as touching `src/anesthesia_sim/app/simulation_view.py` — but neither brief states a prerequisite, and file overlap is not a dependency. That is `PL-MC8Z`'s case (the start-an-item guard never asks the file-overlap question), left for the owner rather than encoded here.

`PL-5WFS` itself stays open at `needs-decision`: the instance is fixed, and whether one instance earns a schema field is still the question its brief poses.

## Checks

`make check` green: 1165 tests pass, ruff and mypy clean, `bin/docket check` 0 errors and **0 untriaged**, `doc_check` 0 errors, contrast unchanged. Two advisories stand, neither this branch's: `pr: 239` on `PL-QSWS` (already handled by open #241) and two `ready` items predating the `verify:` requirement.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UFA8XWoeoqWYZvZ6gG4YU6

---
_Generated by [Claude Code](https://claude.ai/code/session_01UFA8XWoeoqWYZvZ6gG4YU6)_
