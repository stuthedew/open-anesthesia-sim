---
pr: 202
commit: 9d68c67ea5bc63949ac093af52a19942a5151c3f
merged: 2026-09-02
items: PL-GYH2
subject: PL-GYH2 record the decision: the two gas volumes are data-file parameters, not controls (#202)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 9d68c67e landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Records the project owner's answer to `PL-GYH2`'s open question (2026-09-02). `needs-decision` → `ready`, `M` → `S`. No code change — the item file only.

## The decision

Neither gas volume becomes a control. The question was whether to give them a measured range like the flows — which grows the envelope sweep an axis and forces the displayed resolution and supported step to be re-derived over the new domain — or to establish that the data file is the only way to set them. The second, for a knob no teaching case has asked for.

## The half the brief left implicit

Getting this wrong would break the model rather than tidy it, so the item now names it exactly:

- **Goes:** `SimulationController.set_circuit_volume` (`app/controller.py:280`) — the app-layer passthrough no interface control reaches — with its two callers in `tests/integration/test_controller.py` (lines 264, 291).
- **Stays:** `BreathingCircuit.set_circuit_volume` (`core/circuit.py:104`). The controller's own constructor calls it at `app/controller.py:125`, as does `tests/reference/test_coupled_dynamics.py:632`. Deleting it would sever the route by which the data file's value reaches the circuit at all.

The finding is that a *public, unbounded, interface-level* setter exists — not that the core method does.

## `verify:`

```
uv run pytest tests/integration/test_controller.py && python3 tools/doc_check.py check && ! grep -q 'def set_circuit_volume' src/anesthesia_sim/app/controller.py && ! grep -q 'PL-GYH2' docs/MODEL.md
```

Run before being written down: fails today with **exit 1** on the setter still being defined, with the pytest and `doc_check` halves passing alone. About 1 s. Both negated greps match a `def`, not a call, so the constructor's use of the core method does not hold the command red forever. The last clause is the documentation half — `docs/MODEL.md:1317` currently names this item as an open gap, and the work is what removes that pointer.

## Not done here

The Gate 0 admission discussed alongside this. Re-reading ROADMAP.md's own Gate 0 section showed the recommendation behind it was wrong — `bin/docket triage` did not exist at the 2026-08-25 freeze, so two of the three items proposed for admission are post-freeze in substance, and the third is `defect`-classed where the gate's closing line admits only `P0` and `safety`/`science`. `PL-GYH2` itself is the one that does qualify. That case is with the project owner rather than being taken here.

## Checks

`make check` green: 1065 tests, `docket check` 0 errors, `contrast_check` 0 errors. `doc_check` has one advisory unrelated to this change — v0.2.10 is untagged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XcrhhYv1WsKWgM91DSgJDY

---
_Generated by [Claude Code](https://claude.ai/code/session_01XcrhhYv1WsKWgM91DSgJDY)_
