---
pr: 1092
recorded: 2026-09-26
---

The owner asked for unused branches to be cleaned up. This pull request carries the record of that pass. The deletion happens on GitHub, not in this diff.

**What was checked.** `git ls-remote --heads origin` listed 15 heads against `main` at `3eb95a61`. For each head, the pass read its pull requests on GitHub. It also checked each commit the head carries ahead of `main`: are the lines it adds on `main`? Thirteen heads hold nothing `main` lacks. Every item they carry is closed on `main` with its pull request recorded. The only lines missing from `main` fall into two kinds. Some are item fields that `main`'s later copy has moved past, such as `status: untriaged`. The rest are checkpoint drafts of code that a closed item's pull request superseded. `bin/docket stranded` agreed on all 13. `PL-P99G`'s table names each head, its tip, its pull request and where its items stand.

**Left alone.** `claude/pl-0x0g-8kokh9` holds a live claim on `PL-0X0G`. This branch is `PL-P99G`'s own.

**Why the deletion is not in this pull request.** Auto mode refused `git push origin --delete` as a destructive git operation. `docs/worker.md` also records the environment's GitHub proxy rejecting branch deletes with HTTP 403 (`PL-ZM48`). So the owner runs it. `PL-P99G` closes once `git ls-remote --heads origin` prints none of the 13 names.

**Also recorded.** `PL-X3NY` would classify branches by pull-request state, and it now carries this measurement: by that state alone, 10 of the 13 would have been misread. They never had a pull request, yet their work had landed through other pull requests.

Changes: `docs/items/PL-P99G-*.md` (new) and one appended paragraph in `docs/items/PL-X3NY-*.md`. `make check` passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01EijGPSEYAbG6htqve8ucUu

---
_Generated by [Claude Code](https://claude.ai/code/session_01EijGPSEYAbG6htqve8ucUu)_
