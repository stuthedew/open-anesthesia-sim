---
pr: 552
commit: cbd762980ac79ce09187e67ce98a10e111e360c7
merged: 2026-09-13
items: PL-028F, PL-8M8H, PL-GLBF, PL-J7C5, PL-PGZK
subject: PL-GLBF, PL-J7C5, PL-PGZK, PL-8M8H, PL-028F: gate batch — the apparatus's unchecked numbers and undecided verdicts (#552)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit cbd76298 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Five Gate 1 entries, all in `tools/` and `subprojects/docket/`, chosen so that nothing here touches what `origin/claude/gate-items-batch-4fxcog` was changing (`core/alveolar.py`, `docs/MODEL.md`, `reference_circle_system.json`). Three were decided-but-unbuilt, one turned out already built, and one was an open decision.

## PL-GLBF — the one subset count a field can decide

`check_gate_counts` held a frozen list to its own arithmetic and never touched the counts stating a property of its *entries*. One of the three is decidable without judgment — `not-delegable:` is a field on each item — and is now checked. The other two are a near miss rather than a gap, and `ROADMAP.md` records why so it is not re-derived a third time: `touches:` records the files an item is expected to change, where those sentences record the scope it is permitted to reach, and on their own ids a checker over `touches:` computes two against a correct three.

Two failure directions surfaced by writing the tests: `read_items` answers `[]` for an absent store, which would have failed every sentence by its own size on a truncated checkout; and an entry naming an id the store does not hold would have read as "not withheld", failing correct prose and blaming it. Both decline instead.

## PL-J7C5 — done, and its own `verify:` could never have passed

The requirement table already cites by symbol and `check_citations` already refuses a line number — `PL-GJDW`. The item's command, `! grep -qE '[.]py:[0-9]' tools/contrast_check.py`, matches one line: the comment documenting the pattern by showing the form it refuses. It failed on the presence of the guard.

The open question was whether the same mechanism should cover item files. Counted before deciding: **268** line citations across 103 item files, **zero** pointing past end of file, **4** naming a path that no longer exists — and one of those four is a correct sentence whose subject is the file's retirement. One false positive against three finds, on a hard error, is what this project refuses. `PL-VP7N`'s two citations of a file `PL-006` renamed are repaired rather than checked.

## PL-PGZK — the shared-file tier groups by path

38 lines to 3 on the brief's own probe, with the two that carry evidence first. 237 open items declare 136 paths, the largest at 32, and 72 of the 136 are declared by exactly one open item — a short head and a long tail reported one item per line. Ordering groups by size puts the collisions most likely to be real at the top. Nothing is dropped.

## PL-8M8H — a sixth branch state

A squash merge leaves the branch containing none of the commits that landed its content, so a merged branch that gains one commit reads `ahead=1, behind=N`, which is `MERGE`. Nothing merges a merged pull request again, so that push lands nowhere — it is what the session holding `#284`'s follow-up was told.

`LANDED` is read from the content by `vcs.landed_whole`, which reuses `orphaned`'s pipeline whole rather than re-deriving it, and sits after `REWRITTEN` and before `MERGE`. Both placements are the recorded decisions, not fresh design. `branch_state` asks only on the arm that would otherwise say `MERGE` — 87 ms of its 107 ms there, and nothing on the other four.

The render arm is the half that would have failed silently: `MERGE` is `format_branch_state`'s `else`, so a new state with no arm renders as the exact wrong advice with no error anywhere. The test asserts the negative.

## PL-028F — report, do not act

Counted before deciding, because "structural rather than a one-off" was an assertion: across **47** tagged spans, **12** closing pull requests merged inside a tag's span its own notes never named, in **11** of them — close to one release in four. `PL-D9WD` reproduces exactly as recorded.

A second measurement decided where the check goes: a squash gives the release commit one date on all 42 releases, so `main`'s history cannot say how long a branch was open, and anything reconciling afterwards would work from a record that cannot describe what it is reconciling.

Re-stamping at merge has no trigger here — `PL-N5WZ` established that a `GITHUB_TOKEN` push starts no workflow, so required status checks can never report on a commit a merge-fired job pushes. `vcs.cut_window` reads `HEAD` alone; `checks._check_cut_window` advises with both dispositions and the remedy that already exists. `ROADMAP.md` § "Tags" records what the span includes, which the item's Done-when requires either way.

## Also

- Docs swept: `subprojects/docket/README.md`'s "two commands and no third" was made false by the new state; the concurrent section gained the regrouped tier; `.claude/skills/docket/SKILL.md` says `docket branch` now names the merged-branch case while the commit is still recoverable. Checked `ROADMAP.md`, `docs/ARCHITECTURE.md`, `docs/worker.md`, `docs/maintainer.md`, `CLAUDE.md`.
- `bin/docket record` rode the first commit with the two numbers the base was owed (`PL-4KKR` #549, `PL-JYR4` #548).
- Captured: `PL-R0P3` (the new `_read_store` duplicates a read two checks spell inline) and `PL-P669` (the eleven historical spans are not repaired here — a scoping call, deliberately not folded into an M item).

`make check` green: 2638 tests, coverage gate at 100%, every tool check clean.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LVX9BVkhKGUjMgTe2bkwwL

---
_Generated by [Claude Code](https://claude.ai/code/session_01LVX9BVkhKGUjMgTe2bkwwL)_
