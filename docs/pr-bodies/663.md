---
pr: 663
commit: 075f054fd704b2fb1c4903655833a5973949fbbb
merged: 2026-09-17
items: PL-0J9K, PL-3DC7, PL-JH3T, PL-MMVF, PL-RC86, PL-RCQM, PL-Y4D6
subject: PL-Y4D6, PL-0J9K, PL-MMVF, PL-JH3T, PL-RC86, PL-3DC7, PL-RCQM: triage the 26 untriaged items (#663)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 075f054f landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Fields, the missing brief sections, and a `verify:` command run and seen to fail
for every item set to `ready`. Then the gate dispositions triaging debt items
makes `ROADMAP.md` owe.

## Six were not work at all

The queue was overstating what is left.

| Item | Now | Why it closed |
| --- | --- | --- |
| `PL-0J9K` (batch the blob reads into one `cat-file`) | `done` | The code is on `main`, landed by `#635`, pinned by `test_git_runner.py`. The item was never taken out of `untriaged`. |
| `PL-MMVF` (memoize the runner) | `done` | Same commit, same test file. |
| `PL-JH3T` (attribute the session-start hook's 20.7 s) | `done` | Its own brief says "answered and it should close at triage". |
| `PL-RC86` (overlap the hook's network stages) | `dropped` | Refuted before any work started: both network stages are 2.28 s of the owner's 20.01 s. |
| `PL-3DC7` (`verify --self` returns before its integrity checks) | `dropped` | Fixed on `main` by `#655` (`PL-L4KX`); `verify.py:746-758` is the exemption branch it specified. |
| `PL-RCQM` (`PL-4L6Z`'s command killed at the 120 s limit) | `dropped` | Duplicates `PL-6YWK`, which carries the `LANDED_TIMEOUT` reasoning this one lacked. |

`PL-0J9K` and `PL-MMVF` had their work merged and their items left `untriaged`,
so `bin/docket next` has been offering landed work. The one obligation they
recorded and did not meet — the re-measured `show` time on the project owner's
clone — is carried by `PL-XD3C` alone rather than by three, since it is the same
single command.

Two measurements `PL-RCQM` held and `PL-6YWK` did not are cross-referenced into
`PL-6YWK` rather than left to be re-taken: the sweep's 233.8 s for 164 commands,
and `PL-P1P6` at 97.2 s against the 120 s limit — the evidence that the budget
has no headroom, and so that narrowing `PL-4L6Z`'s command is the only remedy.

## One reversal the pass found while writing the gate dispositions

`PL-B396` (agent amounts in litres of vapour) was triaged as a drop: its unit
question is answered and building the readout is planned-milestone item 28's.
Then `PL-0S0V` and `PL-VJZK` were read — both `safety, anticipated`, both
`blocked-by: PL-B396`. Closing it would have reported them ready to promote, and
promoting an anticipated safety item is the event that returns it to the debt
gate (`PL-ZF2G`), while the hazard they describe still does not exist.

It is `needs-decision` instead, on whether item 28 is scoped. Five items now
depend on that milestone and none can say so in `blocked-by`, because `docket
check` accepts only an item id or a version the roadmap names.

## The gate

Triaging a debt item is what makes the gate owe it a disposition, so
`tools/doc_check.py` went red the moment the fields landed.

- **Three placed** on v0.5.0's frozen list under the unconditional
  safety/science exception — `PL-S6WW` (the model's undocumented reference
  temperature), `PL-H4N8` (item 28 bills the exhausted amount where cost is the
  delivered one) and `PL-QBX0` (item 24's panel gate misses the ISO 5360
  colours). Deferring is not available for those two classes and the checker
  says so in terms. All three are `S`; two are one sentence in a
  planned-milestone entry, which is the cheapest moment such a correction will
  ever have.
- **Ten declined to Gate 2** — nine on the refilling-queue ground, and `PL-KZ99`
  on the anticipated one, which it takes because it is `blocked` as well as
  `anticipated`. The two dropped above left this list, since a closed item is
  not open debt: 163 entries.

The frozen list's own subsection ends at a `###` heading partway through it, so
a block written after that heading is silently outside the gate — which is
`PL-82B0`'s defect, met from the other side.

## One finding the pass made about itself

`PL-3DC7` was triaged `ready` with a `verify:` command that was run and seen to
exit 1, exactly as the rule asks — and it failed for the wrong reason. The
behaviour was already on `main` under three differently-named tests, so the
`grep` half was pinning a name nobody had agreed to rather than a behaviour. A
negative `grep` for an invented test name cannot distinguish *the behaviour is
absent* from *the behaviour is present under another name*, and the two failures
look identical.

Captured as `PL-6TN8` under `feature: verify-command-health` with `PL-LBW5`, and
left untriaged for the next pass. Its brief asks for the count before any rule
is written, on `PL-879R`'s precedent — one instance is not a population.

## Two edits yielded to sessions that landed first

`PL-XF2Y` and `PL-XMNC` were both triaged here and both landed on `main` during
the pass, under `#658` and `#654`. The merge takes theirs wholesale — theirs
reached the same disposition on `PL-XF2Y` independently. `PL-MQHN`'s `pr: 654`
was likewise already recorded by `#656`.

## Checks

`make check` passes: 2992 tests, 100% `core` branch coverage, `docket check` 0
errors, `doc_check` 0 errors and 0 advisories. Every `ready` item's `verify:`
command was executed: all 16 exit 1 — an ordinary failure, not a selector that
matched nothing — with the health half (`doc_check check`, `docket check`, the
named pytest file) passing on its own. The two `done` items' commands pass,
which is correct for work already on `main`.

`bin/docket verify --self` returns ACCEPT for all seven closed ids.

Three captures that arrived while the pass ran are deliberately not in it:
`PL-7TYC` and `PL-TKFD` from `#655`, `PL-XZD0` from `#657`. `PL-7TYC` is being
worked by another session right now, so triaging it here would be a second
resolution of a file somebody holds.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_014sxowbt852po4f8GRPWTxt
