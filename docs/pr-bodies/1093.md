---
pr: 1093
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGJ8KSKPV7aACkTYaUbrpm53)_

Before: all three Bash guards read a shell reserved word in front of a command as that command's name. `for f in a; do make check | tail; done`, `if true; then make check | tail; fi` and `time make check | tail` each read as a command named `do`, `then` or `time`, so the gate guard let a red `make check` reach a session as exit 0 there, which is PL-2JRC's false green walking around the guard built to refuse it. The floor guard missed a bare `python3` parsing `src/` in the same positions, and the prune guard missed `git fetch --prune`.

After: each of the item's seven reproductions is refused by the guard it belongs to, and so is the same command after `elif`, `else`, `while`, `until` and `time -p --`. The gate guard also reads the `if` or loop the gate sits in, the way bash runs it. A gate ending an `if` branch leaves with the `if`, so `if true; then make check; fi; echo done` is refused, and so is `...; fi | tail` unless pipefail holds. A gate that is the test of an `if`, `while` or `until` is read by it, as a `$?` reader is. A gate ending a loop body is refused, because the next pass replaces its status and a red pass before a green one exits 0; that refusal says how to keep every pass. A `set -o pipefail` inside a piped `if` or loop ends with it, as one inside a piped `{ }` group already did. `docs/worker.md` names the loop case beside the other four losses. Two false refusals went with it: a multi-line `if` with `make check` alone on a line, and `make check && { echo a; echo b; }`.

How: `shell_split.command_words` drops `if`, `then`, `elif`, `else`, `while`, `until`, `do`, and `time` with its `-p` and `--`, at the head of a segment along with the openers it already dropped, so all three guards see the command after them; the prune and floor guards needed no change of their own. In the gate guard, `pipefail_by_separator` treats `if`, `while`, `until`, `for` and `select` as groups closed by `fi` and `done`, and returns how many groups are open at each separator. The walk uses that depth to step over a whole compound command after `&&` or a pipefail pipe, so in `make check && if true; then echo; fi; git status` the `;` after `true` belongs to the `if`, and the `;` after `fi` is the one that loses the status. What the splitter still does not read is stated in its docstring: the `)` ending a `case` pattern, `coproc`, a reserved word that is not the first word of its segment, as in `if (true) then` or `for f do`, a quoted `if`, and `time` after `|`. Each of the three test files pins `test_a_reserved_word_opens_the_command_after_it`, the name the item's `verify:` checks. The gate guard's also pins a table of `if` and loop spellings, each checked against what bash 5.2.21 exits with when the gate fails once; where the two differ, the construct is reading the status by design, as the `if` test and `until` do. Filed from the probing: PL-KQ4Q, the gate guard refusing `make check || exit 1` and other fallbacks that keep a failing gate red. `bin/docket verify --self PL-0X0G` answers ACCEPT on `ba4f1327`, `make check` included; the one commit after it records this pull request's number and body.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01FxWJkVC1EmG7CHmuwJpE4v

---
_Generated by [Claude Code](https://claude.ai/code/session_01FxWJkVC1EmG7CHmuwJpE4v)_
