---
pr: 1120
recorded: 2026-09-26
---

Captures three items the project owner raised in one session. Item files only; nothing else in the tree changes.

- `PL-77DZ` (stale `inert_splitter` line citations). Four `ROADMAP.md` citations and the open briefs of `PL-W9P6` and `PL-9LNF` anchor the inert splitter at `src/anesthesia_sim/app/qt_widgets.py:784`. The function is at line 1651 now, and line 784 sits inside `set_readouts`. `.claude/rules/citation-drift.md` refuses re-pointing at a new number, so the brief replaces each anchor with the symbol. The closed briefs that cite it are records and stay as written. Filed at the owner's request; a fresh session fixes it.
- `PL-V1Y7` (break-out as an eventual to-do). The owner does not want an Area moving into its own window ruled out, and does not want it scheduled either. The brief records their words (specified, 2026-09-26) and a plan that needs nothing further from them: remove timeline row 9 so break-out stays as item 34's unscheduled second half, renumber the releases after it, keep v0.6.0's window-set and per-window-region reservations, and re-point the live v0.7.0 references. It is due before v0.6.0 ships, because Gate 3 freezes then. Left untriaged.
- `PL-M701` (stale "blender.org is blocked" notes). `ROADMAP.md` and `docs/interface-provenance.md` say blender.org is refused by the session egress proxy. A probe on 2026-09-26 found every blender.org host answering. The brief carries the owner's allowed-domain list as a dated snapshot, the probe results, and the fix: re-date each stale statement, and add to rule 14 of `.claude/rules/instruction-writing.md` that a host's reachability is external state, probed before a session repeats a claim that it is blocked. This session works it next, in a pull request of its own.

Verification: `make docket` exit 0 on the head, after `origin/main` was merged in.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_015yyB5vqeNrV2Efg477HcWu

---
_Generated by [Claude Code](https://claude.ai/code/session_015yyB5vqeNrV2Efg477HcWu)_
