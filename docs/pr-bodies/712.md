---
pr: 712
commit: ceb93850cdb4be1e9f8cd30bcf30fbb59d509721
merged: 2026-09-19
items: PL-TFWR, PL-YTDN
subject: PL-YTDN: rename the seven drifted item files and repair the two touches declarations naming PL-TFWR (#712)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ceb93850 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Discharges the stale-slug filename advisory, which had fired in every session since `PL-3833` added it with nobody whose job it was to act on it. `bin/docket check` no longer reports filename drift, and `PL-YTDN`'s own `verify:` — a `filename_for` comparison over the whole store — exits 0.

## The set had moved, so it was re-measured rather than trusted

Nine at the 2026-09-13 measurement, eight in `PL-D4GS`'s recount, **seven** here: `PL-0VFF`, `PL-5N7T`, `PL-68XK`, `PL-H4N8`, `PL-K9HV`, `PL-TFWR`, `PL-TTMF`. Four of the original nine had been renamed under other work in the meantime and two had drifted in since. `PL-5QLP` (`record` no longer renaming a drifted file while writing `pr:`) had landed, so the set no longer moves underneath a pass — which is what unblocked this one.

All seven are pure renames (`R100`); no file content changed in the rename.

## No branch held any of them

The one candidate was `origin/claude/optimistic-brahmagupta-63pa72` against `PL-H4N8`. Comparing the branch against `origin/main` on that path reports its merge commit, which reads as an edit — but the test that actually decides it is a diff from the merge-base to the branch tip, and that was empty. The branch is behind `main` on that file, not carrying work.

## PL-TFWR was renamed rather than held back

`PL-Y5JX` deliberately skipped it on 2026-09-19 because two items name its file by full path in `touches:`, and a rename leaves those declarations pointing at nothing — silently, since a `touches` entry naming a missing path is not yet an error.

The hazard there is *a declaration broken without anybody noticing*, not the rename itself, so it is removed by repairing the declarations rather than by deferring the rename indefinitely. Both are repaired in the same commit:

- `PL-3V6C` — closed, so its `touches` is provenance.
- `PL-77SV` — **open**, and the one that matters: a live item's `touches` is read by `bin/docket concurrent`, by the lane split, and by `docket verify`'s inside-`touches` audit.

Holding `PL-TFWR` back would also have failed this item's own `verify:`, which demands zero drift, and left the advisory firing on one file in every session indefinitely.

## The re-check one-liner under-reports, and that is now recorded

`PL-YTDN`'s brief carried a one-liner for re-measuring the couplings before renaming. It scans `verify` and the body and **not** `touches` — which is where both real couplings lived — so it returned "0 of the 9" here too, while the true answer was two live declarations. Found by hand instead.

That is exactly the failure `PL-Y5JX` names, reproduced by the pass that repaired it, so `PL-Y5JX`'s brief now says so: its guard is still owed, and its test fixture must be synthetic now that the live instance is gone. The fenced quote in that brief is left at the old path deliberately — it records what was found on the day.

## Not promoted to an error

The brief asks whether the check should become a hard failure now the count is zero. Not yet: nothing warns a session that the file it is about to rename is named by another item's `touches`, so a hard error would compel exactly the rename that breaks a declaration. Captured as `PL-3GKR` and grouped under `PL-Y5JX`'s `slug-rename-on-write` feature.

## Checks

`bin/docket verify --self PL-YTDN` returns `ACCEPT`; `make check` passes. Docs swept: `docs/WORKING_NOTES.md`, `ROADMAP.md`, `README.md`, a repo-wide grep for every old basename, and `tools/doc_check.py` in both `check` and `candidates` modes — nothing outside `docs/items/` referenced any renamed file.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_012GprbZsiPaTDdeUzAmdv5c

---
_Generated by [Claude Code](https://claude.ai/code/session_012GprbZsiPaTDdeUzAmdv5c)_
