---
pr: 643
commit: 262b38bfe8333eda5837ace064a569c41a646c6f
merged: 2026-09-16
items: PL-C7XV, PL-D1RT, PL-KF0T, PL-SY1J, PL-XF2Y
subject: PL-C7XV, PL-KF0T, PL-XF2Y, PL-SY1J: capture four findings from the main-is-red thread; the PL-D1RT close stands down to #645 (#643)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 262b38bf landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

**This pull request no longer closes `PL-D1RT`.** It was rebuilt on `4167e2a` at 21:25Z and now carries four captured items and nothing else. The history before that is on `backup-643-full` locally; the close commit it dropped was `0d5fac0`.

## Why it stood down

#645 (`claude/awesome-heisenberg-gp7ev6`) closes `PL-D1RT` on the same clause-by-clause reading this branch used — independently, and reaching the same verdict on the same superseded "Done when" clause — and does two things this branch did not:

- it **re-points `PL-C4RS`'s sentinel** to pin a sentence the correction must add, leaving that item open because its work is genuinely outstanding. That is the error's other disposition, and the right one for it;
- it reports `bin/docket check --verify` at **0 errors** on its tree merged with `4167e2a`.

Measured here on this branch's old head merged with `4167e2a`, in a scratch worktree: **1 error, `PL-C4RS`**. So this branch took `main` from two errors to one and #645 takes it to none. Two branches closing one item also conflict at the merge, so there was nothing to gain by keeping both.

By the `docket` skill's first-commit rule this branch held the item — `0d5fac0` at 19:52:55Z against `0db05f1` at 20:01:37Z — so this is a yield on the merits rather than on the ordering, and the work is handed over rather than discarded.

## What is left here, and why none of it is duplicated by #645

| Item | Feature | What it records |
| --- | --- | --- |
| `PL-C7XV` | `interface-pass-narrative` | The notes thread's "What was decided that day: the placement above" now points at a sentence carrying two placements, so `PL-PHKP`'s 2026-09-16 move reads as a 2026-09-08 decision by the project owner — the ratified-vs-specified distinction `CLAUDE.md` reads a reopen bar off |
| `PL-KF0T` | `interface-pass-narrative` | Planned-milestone item 33 still calls `PL-2CS8`, `PL-NGF7` and `PL-B9PY` debt that will "reach the gate on their own class"; all three are `done`, and `docs/WORKING_NOTES.md` records their dispositions correctly, so the two documents disagree |
| `PL-XF2Y` | `sentinel-verify-commands` | The three instances of the class — `PL-Y1W6` (dropped), `PL-D1RT`, `PL-C4RS` — with the merged-tree measurement. Marked where #645 supersedes it |
| `PL-SY1J` | `inflight-verdict` | `bin/docket show PL-D1RT`, run from this branch after a fetch, named only the **later** of the two carriers and said "do not start PL-D1RT again". Mechanism not determined, and the item says so |

`PL-XF2Y` keeps the part #645 does not settle: one defect produced two **opposite** remedies — a close and a re-point — which is the evidence that the fault is the sentinel's *shape* rather than any item's state. That is `PL-879R`, still `needs-decision`.

## Checks

`make check` exits 0 on `9849dc5`. `bin/docket check`: 0 errors. The diff is four new files under `docs/items/` and nothing else, so it conflicts with no open branch.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01B9edWfUjPS8jEowbCFcJoC
