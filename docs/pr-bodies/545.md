---
pr: 545
commit: f645d98dabe8a3c3d1ee68dca547d44d630f4008
merged: 2026-09-13
items: PL-F0L8, PL-GLBF, PL-H1JD, PL-LL9Y, PL-PGZK, PL-Z34C
subject: PL-PGZK, PL-Z34C, PL-GLBF, PL-H1JD, PL-F0L8, PL-LL9Y: six Gate 1 decisions, four of which the project had already answered somewhere else (#545)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit f645d98d landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Item briefs and one rule bullet. No simulator code changes.

Gate 1: **31 open → 29**. Two entries close, four are narrowed to a single question each.

## Closed

**`PL-H1JD`** (an outstanding `needs_action` is lost when its session is archived) → `done`. `CLAUDE.md`'s capture rule already covered it; what was missing is that nobody had pointed it at the closing block. One bullet added to rule 14 of `instruction-writing.md`: the block is a handover, not a store, so a line that could outlive the sitting is `bin/docket new` as well. The three mechanism candidates were each refused on a rule this project already holds — reading archived rows contradicts `PL-SK88` and reintroduces `PL-D4MZ`'s false collision; blocking archive is a harness action `docket` cannot reach and must not; mirroring the field into the store *is* `docket new`, reached by machinery instead of by the rule that already asks for it.

**`PL-F0L8`** (what accessibility Flet's backend can deliver) → `dropped`. Not a new decision: `ROADMAP.md` § "Items this port moots or transforms" already records that "after the port the question is `QAccessible`'s, which is a different investigation against a different backend." It cannot simply wait, because Gate 1 clears before v0.5.0 and the port ships after it. The investigation dies; the roadmap commitment it carried survives as **`PL-QR6Q`**, `blocked-by: v0.5.1, PL-25KS`.

## Scope changed by measurement

**`PL-PGZK`** (`concurrent` dominated by `docs/MODEL.md`) → `ready`, effort M → S. Both probes the brief names now return **`Cannot run alongside: nothing`**, against 22 on 2026-09-03 — `PL-VRMK`'s tiering closed the filed defect, and a hub path can no longer produce a false refusal at all. The recommended ranking option existed to stop false refusals; that count is now zero, so it is not built. What remains is list length, not a wrong verdict: 54 shared-file entries for `PL-ZRSP`, 25 of them `docs/MODEL.md`-only, across 136 declared paths of which **72 are declared by exactly one open item**. Decided: group the tier by path. `docs/MODEL.md` is 13.5% of open items, not "a large fraction" — the brief overstated it roughly fourfold.

**`PL-GLBF`** (`ROADMAP.md`'s hand-maintained subset counts) → `ready`, scoped to the one decidable count. Two findings changed the answer:

- The brief says no field records what an entry's work touches. `touches:` does, on 88% of open items — and it still cannot decide these. On this item's own example it computes **2** where the prose says **3**: `PL-ZN0N` declares only `pyproject.toml`, because `touches:` records expected file changes and the definition-of-done sentence records permitted scope. A check on the near-miss field would fail a correct sentence.
- All ten ids the three counts name are closed (9 `done`, 1 `dropped`) on **v0.2.8**'s frozen list, in a shipped release. They cannot go stale, so `PL-H8MQ`'s rule — which targets counts that *drift* — does not reach them.

Build the `not-delegable` check; record why the other two are neither checked nor removed.

## Answered, and narrowed to one question each

**`PL-Z34C`** — retire, on `CLAUDE.md`'s own retirement test. The grandfathered set is **15** (39 on 2026-08-31, 22 on 2026-09-03), so the item is decided and unworkable, and the store has no status for that. Left `needs-decision` deliberately rather than forced to `ready`, which would put unworkable work in front of `bin/docket next`. Filed as `PL-SCB4`.

**`PL-LL9Y`** — answered on the code for the surface that exists. All five `WARNING` uses are bold `ft.Text`, one of them the **permanent disclaimer**; there is no indicator, banner or flash, so IEC 60601-1-8's visual alarm signal has nothing to bind. Painting an always-present element in an alarm colour would teach against the convention rather than with it, which is the argument the brief could not have without looking at where the colour is used. Red/high and yellow/medium were sourced; the flash rates and the low-priority colour were **not reachable from this container** — the standard is paywalled and every candidate secondary source is blocked by the egress proxy — and are recorded as unsourced rather than written from memory, on `PL-BLHV`'s precedent. The open half is what happens when the Qt port supplies a real indicator surface, and it rests on what the simulator is for.

## Also in this branch

Captured: `PL-SCB4`, `PL-C25K` (v0.2.8's definition of done asserts `PL-J786` was confirmed in effect; it is `dropped`), `PL-DHJ7`. `bin/docket record` wrote 13 owed `pr` numbers, riding this commit as the skill directs.

## Checks

`make check` green: 2586 tests, 100% coverage, `docket check` 0 errors, `doc-check` 0 errors, every other gate clean.

Resident instructions **+768 characters**, all of it rule 14's new bullet. Kept rather than routed, on `PL-H7XN`'s test: the rule fires when a closing block is written, which no read precedes, so none of the other three dispositions reaches it. The reasoning is recorded in `PL-H1JD` rather than only here.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01DChZeVWCohKwvKibmWgvmQ

---
_Generated by [Claude Code](https://claude.ai/code/session_01DChZeVWCohKwvKibmWgvmQ)_
