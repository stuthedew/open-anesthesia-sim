---
pr: 1111
recorded: 2026-09-26
---

Closes PL-QKXZ (Projects threads' cloud environment shipped uv 0.8.17 and no libegl1). The owner's remaining step - point the "Fix generators" project at the account's `Default` environment - is in effect, read back from the setting's effect rather than stated:

- `get_session` reads `env_01JWikdPctorEBXoFK7bJnCq` (`Default`) for both "Fix generators" coordinators started since the last reading (05:57 and 15:03 UTC) and for all four threads they started that claimed an item (08:04 to 15:49 UTC). The switch fell between 04:34 UTC, when a thread still started in `env_011111111111111111111119`, and 05:57 UTC. "Clean up PL" was moved at 02:43 UTC, as the item already recorded.
- This session, a web session in `Default`, found `uv 0.12.19` and `libegl1 1.7.0-1build1` from a snapshot rebuilt at about 08:46 UTC, and `make check` ran `uv sync --locked`, ruff, mypy and 5,041 tests with no repair step.
- None of the recorded bodies of those threads' pull requests (#1090 to #1109) mentions a `libegl1` install or a `uv` upgrade.

Item-file change only: a closing note, the answered question and step 1 marked done, `status: done`, and a `not-delegable` reason in place of a `verify:` command, since the fix is a Projects setting outside the repository and CI runs in GitHub Actions, in neither cloud environment. Recorded as ratified: the owner took the session's recommendation, over leaving the repair each thread's under PL-SPZT's `docs/worker.md` route.

Left open, unchanged: PL-877T (a new project starts in Anthropic's default environment until pointed at `Default`; untriaged) and PL-SPZT (a stale `uv` in a container outside `Default`).

Verification: `bin/docket verify --self PL-QKXZ` ACCEPT, `make check` passing inside it; `make doc-check` exit 0, and `tools/doc_check.py candidates` finds no documentation naming anything the diff changed. Docs checked by hand for a statement of which environment Projects threads use: `docs/maintainer.md`, `docs/worker.md`, `docs/WORKING_NOTES.md`, `docs/ARCHITECTURE.md`, `CLAUDE.md` and `.claude/rules/` state none; PL-NZC0's setup line ("Environment: `Default` ... chosen explicitly") is now true rather than stale.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_012TKiRDc26Dc3q51fVbimyQ
