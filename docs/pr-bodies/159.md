---
pr: 159
commit: d2be9ed1d24ee4811331c37f099613ebe9822b6f
merged: 2026-09-01
items: PL-7QKY, PL-8B1K, PL-X2XX, PL-YLZQ
subject: PL-X2XX, PL-8B1K, PL-7QKY, PL-YLZQ: triage the four working-notes captures (#159)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d2be9ed1 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Triage pass on the four items captured in #157. No code changes; queue store only.

| Item | Band | Effort | Status | Classes |
|---|---|---|---|---|
| `PL-X2XX` — `doc_check` reads neither the queue nor source docstrings | P2 | M | ready | defect, infra |
| `PL-7QKY` — the working-notes discovery instruction is circular | P2 | M | needs-decision | defect, session-cost |
| `PL-YLZQ` — `PL-026`'s brief sends a reader out for its own reasoning | P2 | S | ready | docs |
| `PL-8B1K` — two citations name sections that no longer exist | P3 | S | ready | docs |

All four are process work, so none enters the top band. `docket.toml` seats P1 for "a clinician could be misled", and every P1 this project holds carries `safety` or `science`; promoting workflow items into it would stop that band meaning anything.

**Order.** `PL-X2XX` is the root cause and the cheapest first move: widening `doc_check` past `docs/*.md` to the queue and to source docstrings turns `PL-8B1K`'s two stale citations into `make check` errors rather than leaving them to be found by eye. That is why `PL-8B1K` sits at P3 — landing `PL-X2XX` makes it mandatory to keep the gate green, so it gets fixed as a consequence rather than as scheduled work. It touches `src/anesthesia_sim/core/respiratory_system.py`, so `protected_paths` makes it non-delegable without anything having to say so.

**`PL-7QKY` is `needs-decision`, not `ready`.** Reading the `##` headings is the same work whichever way it goes, but where the pointer is delivered is not settled. `docket check` refused the status until the brief said what the decision was, which was correct — it now states three candidates (`bin/docket show ID`, the session-start digest, `bin/docket next`), what each costs, and recommends `show` on the grounds the skill already gives for that guard: naming an item is the path with no other check on it.

**Every `verify:` command was run before being written down**, per the skill's rule, and each exits 1 today for the reason its work will fix — the `pytest` or `doc_check` half passes, and the paired `grep` is the half that fails:

```
PL-X2XX  exit=1   pytest tests/unit/test_doc_check.py 117 passed; grep for the new test fails
PL-8B1K  exit=1   doc_check exit 0; both stale citations still present
PL-YLZQ  exit=1   doc_check exit 0; "Decided, not yet implemented" still in working notes
```

`bin/docket check` confirms none of the four appears in the advisory for commands that already pass.

Also records `pr: 156` on `PL-DL1X`, which `docket check` recovered from its merge commit and asked for on the next branch.

## Verification

`make check` — 875 tests passed, Ruff and mypy clean, `bin/docket check` 0 errors and 0 untriaged, `doc_check` 0 errors and 0 advisories, resident instruction count unchanged against `main`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QXFS7aWpgSJxBEMLgCbd6D
