---
pr: 252
commit: 84fca3909eedbd590820ab72657ba3850b147730
merged: 2026-09-02
items: PL-9T8T
subject: PL-9T8T: attribute the partition-coefficient table to the authors who wrote it (#252)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 84fca390 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-9T8T` (the partition-coefficient citation names an author who is not on the paper), and captures seven findings from a read-only audit of the testing suite.

## The fix

Every partition coefficient in this project — all twelve, across three agents — is sourced to Table 1 of one paper, cited in ten places as "Stadler M, et al." There is no Stadler among that paper's authors. It is:

> De Wolf AM, Van Zundert TC, De Cooman S, Hendrickx JF. *Theoretical effect of hyperventilation on speed of recovery and risk of rehypnotization following recovery - a Gas Man simulation.* BMC Anesthesiol 2012;12:22. PMID 22989260. PMC3502091. doi:10.1186/1471-2253-12-22

Journal, year, volume, article number and the PMC URL already recorded were correct; only the attribution was wrong. Fixed in the three agent data files, `docs/MODEL.md`'s v0.2.0 provenance subsection, and the two test docstrings that cite the table. `PL-4YY1` is corrected in the same pass — it had the authors right but mistitled the paper "risk of rehyperventilation", which inverts its subject, and used a third form of the initials.

**Attribution only. No modelled number changes**, verified by parsing each data file before and after and comparing every non-`sources` field. The values were also re-confirmed against the PubMed Central full text: Table 1 gives blood 0.42 / 0.65 / 1.3, VRG 0.54 / 1.1 / 2.1, muscle 0.97 / 2.4 / 4.5 and fat 13 / 34 / 70 for desflurane / sevoflurane / isoflurane, matching the data files exactly, and the MAC sentence quoted in all three `mac_percent` notes appears verbatim in Methods.

Initials follow the PubMed-indexed form because the paper's own text is not self-consistent — Authors' contributions writes "TCVZ" and "JHX" while Competing interests writes "JFH".

The item's open question, whether this class of defect can be checked deterministically, is answered in it: no. Nothing in the tree knows the paper's author list, so a checker could only pattern-match the one wrong name or fetch a record over a network `tools/` deliberately cannot reach.

## The captures

A read-only audit of the testing suite, measured rather than reviewed: the full suite at 1169 passing with 100% statement and branch coverage of every `core/` module, a 24-mutation probe over `core/` and `app/`, 200 randomised 20-minute runs, an 8 h run at the envelope corner, and an independent reproduction of the perturbation table in `test_published_wash_in.py`.

| Item | |
| --- | --- |
| `PL-B7ZV` | Nothing pins the three constants the conservation guard's sensitivity depends on — the only three survivors of the 24 mutations |
| `PL-HB58` | Validate washout against the same published cohorts the wash-in gate already uses |
| `PL-GZP6` | Two of `docs/MODEL.md`'s required tests are not implemented |
| `PL-2M9N` | Nothing binds `docs/MODEL.md`'s "Required tests" headings to the tests that satisfy them |
| `PL-22Z3` | Gate coverage at 100% on `core/`, so today's saturation cannot silently regress |
| `PL-ZLNN` | No test drives a trajectory where several controls change at moments nobody scripted |
| `PL-F5GN` | The oracle's independence check walks only `ImportFrom`, so a plain import bypasses it |

`PL-4GN8` is extended rather than duplicated: its finding was that the reference tests' `absolute_error_l <= 1e-12` assertion tracks whichever dial the test runs at; measured here, it also has a horizon — the residual crosses 1e-12 L at 6212 s at those tests' own settings and at 876 s at the envelope corner.

## Checks

`make check` green: ruff format, ruff check, mypy, `ignore_check`, 1174 tests, `docket check` (0 errors), `doc_check` (0 errors, 0 advisories), `contrast_check` (only its three pre-existing tracked shortfalls). Documentation swept with `doc_check candidates --base origin/main`; `README.md`, `ROADMAP.md` and the rest name no author for this paper, and `docs/MODEL.md:78` is a different BMC Anesthesiology paper, correctly untouched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01NVhuN4rACT9jbGptwt7w9j

---
_Generated by [Claude Code](https://claude.ai/code/session_01NVhuN4rACT9jbGptwt7w9j)_
