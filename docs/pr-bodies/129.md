---
pr: 129
commit: d2ac0c5cc9bd840de3b0788d32243dd0bc83a3f2
merged: 2026-08-31
items: PL-1Q3S, PL-3576, PL-H8MQ, PL-RWZV
subject: PL-3576, PL-1Q3S, PL-RWZV, PL-H8MQ: triage the eight captures and admit four to the v0.2.8 gate (#129)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d2ac0c5c landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two passes over the queue: triage every untriaged capture, then read **every** open item against `ROADMAP.md`'s scope test rather than only the new ones.

## Triage — eight captures

| Item | Outcome |
| --- | --- |
| `PL-3576` (docket check's advisory reads a partial flight answer) | P3, `ready` — **admitted to the gate** |
| `PL-MZH2` (`PL-D9H6`'s verify: is quoted and exits 127) | P3, `ready` |
| `PL-W1LN` (the walk guard's uncaught shape) | P3, `ready` |
| `PL-L9JS` (eight verify: commands pass without their work) | P2, `ready`, `not-delegable` |
| `PL-01CK` (the content test's per-blob walk) | P3, `needs-decision` |
| `PL-K2ZK` (deepen the clone at session start) | P3, `needs-decision` |
| `PL-T63T` (half the recorded `commit:` hashes resolve nowhere) | P2, `needs-decision` |
| `PL-Y08X` (the digest offers a release the plan says is not ready) | `dropped` — duplicate of `PL-D2GW` |

Every `verify:` command recorded here was run first and seen to **fail** on this tree. `PL-Y08X`'s measurement is folded into `PL-D2GW` so the duplicate loses nothing. The four-heading empty stubs on `PL-K2ZK` and `PL-W1LN` are removed — they are exactly the shape `PL-RWZV` says the brief check accepts.

## Gate review — four admitted, list now thirty-six entries with ten open

Admitted under the scope test (a **defect** in machinery the release's goal names, **workable here**):

- **`PL-3576`** — `docket check`'s offered-item advisory is computed from an in-flight answer that may be partial and says nothing about it. `PL-S1P1` carried that answer to six readers as a frozen entry; this is the seventh, left out as a placement question rather than as out of scope.
- **`PL-1Q3S`** — a merged pull request's stale tracking ref makes the stop hook demand a push that would recreate a dead branch, after every merge. The hook is upstream; the deliverable is a line in the instructions a session reads, which is in the tree.
- **`PL-RWZV`** — the brief check passes an empty section and fails an elaborated heading. It is the gate deciding whether an item may reach `ready`, and two captures triaged today carried the stub it accepts.
- **`PL-H8MQ`** — the entry count is stated in six places and held to none; three different totals (eighteen, thirty-one, thirty-two) were live in `ROADMAP.md` at once.

**`PL-3CBS` is closed** (pull request 128). Its work merged and the status was never set, so the gate had been reporting seven entries open when it was six — found by the check `PL-3CBS` itself added, on its first run against the store.

Excluded, each naming a boundary the test draws:

- the **delegation gate** is not machinery the goal names — `PL-L9JS`, `PL-MZH2`;
- an **improvement to correct behaviour** is not a defect in it — `PL-K2ZK`, `PL-01CK`;
- a **recorded limit** is not an observed misfire — `PL-W1LN`;
- `PL-T63T` follows the precedent `ROADMAP.md` already records for `PL-68XK`.

## Also in here

`ROADMAP.md`'s nine stale counts corrected by hand — the tax `PL-H8MQ` describes, paid again. `docs/WORKING_NOTES.md`'s gate-membership thread carries what the pass decided and why each exclusion was drawn where it was.

Captured: **`PL-20CQ`** — `docket check` runs every open item's `verify:` command, so a `verify:` that invokes `docket check` recurses without bound. It was the natural command for `PL-L9JS`, which is why that item records `not-delegable:` instead.

`make check` green: ruff format, ruff check, mypy, 797 tests, `docket check` (0 errors, 0 untriaged), `doc_check`.

---
_Generated by [Claude Code](https://claude.ai/code/session_012fWUfUETciARrvvabtid2w)_
