---
pr: 1072
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_01Jr391t5LXLAeRjbzMQrhwb?thread=cmsg_01Jr391t5LXLAeRjbzMQrhwb33KMdCDXELyL2zApLurBax)_

**This pull request adds a check while the generator pause holds.** PL-5DPF adds one grooming advisory to `bin/docket check`. It is part of the machinery that finds and ranks generators, which `CLAUDE.md` says the pause does not hold, and the request "use fable to do non-deligible ones" named this item; the pull request is left unarmed so that Stuart's read decides whether the pause lifts for it. The batch was the three ready items in the passed-over nine that all touch `checks.py`: PL-5DPF, PL-3GKR and PL-8JY7.

Before: a generator head marked `spent` that `bin/docket new` keeps matching new captures onto sits demoted on its band, and nothing in the project says the verdict has been overtaken. Two closed item files on `main` carry a slug their title no longer generates, and every `make check` prints the advisory about them.

After: `bin/docket check` prints a grooming advisory for an open spent head whose `recurrences:` records a filing its `root-cause-of:` never weighed, naming the verdict, the entries and the three repairs. The two drifted files are renamed to the names docket writes, so the advisory about them is gone.

**PL-5DPF** (a spent generator head that accrues a new recurrence has had its verdict falsified, and nothing says so): closed. "After" is read from the cluster rather than from a date, because `generator:` carries none: a live recurrence naming an item outside `root-cause-of:` is a member the verdict never weighed. The brief's preferred rule, firing on any spent head carrying recurrences at all, would have fired every run on the two spent heads this store holds (`PL-8FJK`, `PL-YRYR`), both closed and each naming its one recurrence inside its cluster, so it shipped as a check crying wolf. Open heads only, as the verdict check beside it, since `docket new` records a recurrence onto the open item it matched and a closed head can be re-ranked by nothing. An advisory rather than an error because the repair is judgment. Three tests: the report, silence for a recurrence the cluster carries or a withdrawn match, and silence for a live or closed head. Verified by the item's own `verify:` command, `make check` (4651 passed, 100% coverage, 0 errors) and `bin/docket verify --self`.

**PL-3GKR** (promote the stale-slug filename advisory to an error): blocked and yielded, with the two renames kept. The count was not zero: `PL-843V` and `PL-8543` carried drift on `main`, both closed and edited on no remote branch, so both are renamed here with `git mv`. The promotion itself was written and reverted on this branch (commit `b5665a6d` holds a copy): its own tests pass, and `make check` then fails 17 tests in `subprojects/docket/tests/test_cli.py`, whose `_store` fixture writes every document as `item-N.md`, so every store it builds fails the new error. Naming the files as docket would clears those and fails 20 that read the positional name. The item records the observation and a recommendation: migrate that fixture first, as its own commit, then land the promotion as written.

**PL-8JY7** (a declared `touches` path that history held and the tree does not): claimed and yielded with nothing landed. The clock reached the 04:00 UTC cutoff before it could be started.

Nothing was filed with `bin/docket new`. Docs swept for the advisory wording: `subprojects/docket/src/docket/store.py` and `checks.py` still describe the slug check as an advisory, which is true again after the revert; item files and pull-request bodies that mention it are records.

Left unarmed for Stuart's read.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01GhyX5DAeDCWdAZidJ65XBt

---
_Generated by [Claude Code](https://claude.ai/code/session_01GhyX5DAeDCWdAZidJ65XBt)_
