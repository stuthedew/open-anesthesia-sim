---
pr: 400
commit: 6d02ea5ce605d87662cef553211e63866d3e70a9
merged: 2026-09-06
items: PL-GJDW, PL-N2X4
subject: PL-GJDW, PL-N2X4: cite the code a colour pair is drawn in by symbol, and resolve every citation (#400)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 6d02ea5c landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`REQUIREMENTS` in `tools/contrast_check.py` carried line numbers into `app/simulation_view.py`, and all fourteen had rotted into unrelated code: `:682`, cited as the run-status word, was a list of chart control marks; `:1127` named a line past the end of the module it was written against. The citation is the only thing telling a reviewer *where* a declared pair is actually drawn — the judgment the tool deliberately does not make — so a wrong one made the check's own coverage unauditable while looking audited.

Two halves, and the second is what makes the first hold.

**Cite by symbol.** Each of the nine affected descriptions now names the method or attribute the colour is set on, in backticks, as three entries already did. A symbol survives every edit that does not rename it. Four sites the line numbers had obscured are named for the first time — all of them already-declared pairs, so no ratio moves: the time-base dropdown's INK text and MUTED border, the wash-in axis caption, and the three legend-item builders.

**Then make it decidable.** Citing by name is a convention, and a convention is exactly what failed here silently. `check_citations` now refuses any `*.py:<line>` in a description outright and resolves every backticked symbol against `app/theme.py` and `app/simulation_view.py`, read with `ast` alongside the palette. Both are `make check` errors, so a rename reddens the gate instead of rotting. Whether the named symbol is really where the colour matters stays a reader's judgment — the same line `tools/doc_check.py` draws between resolving a path and judging the sentence around it.

`PL-N2X4` asked which way to go: hold the line numbers to the file, or drop them for control names. Answered against the first, for the reason that item gave — a line-checking tool would fire on ordinary reordering of `app/simulation_view.py` without changing a decision, which `CLAUDE.md` treats as a defect in a check.

## Docs

- `.claude/rules/ui-color.md` — the citation form, under judgment 1, which is the moment a session writes a new entry.
- `docs/ARCHITECTURE.md` — what the tool now decides about the prose, and what it still leaves to a reader.
- Checked and unchanged, each still true as written: `docs/MODEL.md` § "The ratios are computed, not asserted", `docs/ARCHITECTURE.md`'s tree comment and both portability paragraphs, `.claude/rules/ui-color.md`'s opening statement.

## Tests

Six new tests in `tests/unit/test_contrast_check.py`, following the file's existing pattern of a break-it test beside a stays-quiet test: the symbol reader finds constants, classes, sync and async methods and `self.X` attributes; a resolving citation is quiet; a missing symbol is an error; both line-number forms are refused; an unresolved citation fails the run; and a backticked path is not read as a symbol. Plus `test_every_requirement_names_a_symbol_that_exists` as the regression guard on the shipped table.

`make check` green: 1948 passed, 100% coverage, 0 errors from every gate.

## Gate

Both entries are in Gate 1, recorded under "v0.5.0 — the case you can branch": 121 entries, now 11 cleared and 110 open.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01WqWk8onJWPHN9i2mUHZZBF

---
_Generated by [Claude Code](https://claude.ai/code/session_01WqWk8onJWPHN9i2mUHZZBF)_
