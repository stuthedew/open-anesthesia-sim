---
pr: 450
commit: 86185f230ad9ec1131d0609d9aa9e3a0704b9fdc
merged: 2026-09-07
items: PL-20CQ
subject: PL-20CQ: warn on a verify: that reads docket check's output, and close the item (#450)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 86185f23 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follow-on to #448, which merged as `1b47e47`. That pull request landed the refusal for `docket verify` re-entry and left one question recorded on the item; this closes it.

## The decision

The project owner took **option 2** of the three recorded on `PL-20CQ`: an advisory rather than an error, scoped to the shape that can actually pass vacuously rather than to `docket check` re-entry at large. The options were (1) do nothing, since the vacuous shape has never been written; (2) an advisory naming a `verify:` that pipes or captures `docket check`'s output; (3) an error on that same shape. Two was taken because the discriminator - whether a shell line reads a command's *output* or its *exit status* - is a judgment rather than an exact rule, and `CLAUDE.md` reserves hard failure for the exact ones.

## What it does

`reads_check_output` reports a `docket check` whose output is piped before the pipeline ends, or captured in a command substitution. A nested run is told not to replay the open items' commands, so it never prints the landed advisory - a `grep` for that answer matches nothing whether the work is done or not, and an inverted one passes on the strength of it.

The exit-status form stays silent, which is what keeps the ten open items recording `bin/docket check && grep -q ...` correct. The pipeline-end test is what keeps `PL-39B7`'s recorded command correct too: `bin/docket check && ! bin/docket stranded | grep -qE ...` pipes `stranded`, not `check`, and the segment stops at the `&&`. That case has a test of its own, because firing there would have put an advisory on a correct item.

## A correction it forced

Writing the test for command substitution exposed a bug in the quoting model both predicates share. Blanking every quoted span hid `test -z "$(bin/docket check)"`, because `$(...)` still runs inside double quotes - so the detector was blind to a real invocation. `_outside_quotes` now blanks single-quoted spans always, and double-quoted ones only where they carry neither `$(` nor a backtick. Reading the store stays allowed; a substitution stays visible.

## Item closed

`PL-20CQ` goes to `status: done` with the decision and its reasoning recorded on it, and the advisory documented in `subprojects/docket/README.md`. `pr` is left empty for `bin/docket record` to write after this merges. `PL-20CQ` is not a debt-gate entry, so there is no gate movement to report.

## Checks

`make check` passes on the rebased base: 2215 tests, 0 docket errors, 0 documentation errors. Docs swept - `subprojects/docket/README.md` updated; the `doc_check candidates` lines for `checks.py` are all references to CI's `checks` job and unaffected.

The branch was restarted on merged `main` and this commit carried forward, rather than pushed onto the branch #448 had already taken.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013ETYt46BRHu7M9xu1xpTzu

---
_Generated by [Claude Code](https://claude.ai/code/session_013ETYt46BRHu7M9xu1xpTzu)_
