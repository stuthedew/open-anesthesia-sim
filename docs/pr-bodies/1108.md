---
pr: 1108
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGHSUsWQ989zgGyvfTtNaFyd)_

Before: a pull request opened as a draft at its claim push, with its description, always failed `pr-title` on that first head. `docs/pr-bodies/N.md` cannot be in the tree before the pull request has a number, since `python3 tools/pr_body_check.py --record` fetches the body from GitHub. The failure held nothing, because a draft cannot merge, but it stayed in the check history and woke the session to a CI failure it had to investigate. `#1106` (`PL-V2X5`'s close-out) did exactly that, and nothing in the queue recorded it.

After: `PL-K9XQ` records it, untriaged, under `pr-body-integrity` beside `PL-979D`, with the evidence from `#1106` and two routes left for triage to weigh: have `--check` pass a draft and add `ready_for_review` to `pr-title.yml`'s triggers, or open the draft with an empty description and write it at the closure push.

How: one item file, captured with `bin/docket new` after `#1106` merged, on the same branch restarted from `main`. `make docket` and `doc_check` pass on it.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018ikECf3sgfz6iMm8EmSTrE

---
_Generated by [Claude Code](https://claude.ai/code/session_018ikECf3sgfz6iMm8EmSTrE)_
