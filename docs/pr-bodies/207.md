---
pr: 207
commit: 73f7f7a6436e6ea6dfed021d3cca65169e42ca9a
merged: 2026-09-02
items: PL-GW37
subject: PL-GW37 Lead a closing commit subject with every id it closes (#207)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 73f7f7a6 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

PL-GW37's convention half, which you approved. The guard half stays in the item.

## The change

`docket check` recovers a closed item's pull request from the newest commit subject naming its id, and `_leading_ids` already reads a *comma-separated run* — so a subject leading with every id it closes works against the code exactly as it stands, and fixes every future rider closure at no code cost.

`CLAUDE.md`'s "Name the work after the item" bullet and the `docket` skill's close-out step both say so now.

## Why resident, not skill-only

The routing question `CLAUDE.md` requires, answered against evidence from this session rather than in the abstract:

- **A check?** No. A check can only notice after the commit is written — that is precisely PL-GW37's remaining guard half, and it cannot prevent the mistake.
- **The `docket` skill alone?** No, and this is the decisive one. **This session closed `PL-026`, `PL-KQKM` and `PL-YLZQ` in one commit having never loaded the `docket` skill** — it invoked it only later, for the triage and the release. A rule that fires when a commit subject is written cannot live behind a skill the closing session may not have read.
- **A path-scoped rule?** No. A commit subject is not a file read, so nothing triggers it.

So: resident, per the fourth disposition — something a session could violate before it would think to look anything up.

`doc_check` reports resident instructions 4 lines up on `origin/main`. That is the advisory working, not a problem: it names "text the project owner asked for" as one of its two acceptable answers, and the routing above is the other. No other resident text was trimmed to offset it, which that advisory explicitly forbids.

## PL-GW37 after this

Still `ready`, still P2/S, `verify:` unchanged and still failing. What remains is the guard: reject a recovered commit older than the item's own `closed:` date. `CLAUDE.md` is dropped from its `touches`, since the remaining work no longer reaches it.

## Checks

`make check` green: ruff, mypy, 1096 tests, `docket check` (0 errors, 0 untriaged), `doc_check` (0 errors, 1 advisory — the resident-line one above), contrast unchanged.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01B3YC5LpCyFmcySxHqKAkK4

---
_Generated by [Claude Code](https://claude.ai/code/session_01B3YC5LpCyFmcySxHqKAkK4)_
