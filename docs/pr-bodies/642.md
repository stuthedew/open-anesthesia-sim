---
pr: 642
commit: 7caf9857f87868e21f115ecdaed820fcd0c6f4fd
merged: 2026-09-16
items: PL-KQHN
subject: PL-KQHN: record the ratified decision that the guard withholds the Qt port's number, and correct the four roadmap sites it settles (#642)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 7caf9857 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The project owner agreed on 2026-09-16 with the `doc_check` safety-class gate advisory session's recommendation on `PL-KQHN` (the roadmap paragraph saying both that a patch cut takes the Qt port's section number and that the guard withholds it). Two commits: the decision, then the prose it settles. `PL-KQHN` closes.

## The decision

A patch cut on the `v0.4.x` track **no longer takes `v0.4.26` by default**. The reserved-version guard withholds the number and names it, so a cut at it is a deliberate act rather than the release path's default.

Marked **ratified** rather than specified, per the marker `CLAUDE.md` gained the same day, naming what it was chosen over: keeping the documented mechanic — a patch takes the port's number and the section is renamed each time — and narrowing `PL-VFD8`'s reservation to match. A later session therefore knows the bar to reopen it is ordinary evidence rather than a compelling argument.

**What it does not change**, stated in the roadmap as its own paragraph because the opposite reading is the one this correction could otherwise invite: ordinary patch numbering on the `v0.4.x` track. The track still promises no particular number, and a cut still takes the next genuinely free number. What changed is what *free* means — a number the roadmap has given to a milestone section ahead of the current one is not free, which is exactly what `#606` taught the guard to see.

## Two measurements carried it, neither available when the mechanic was written down

1. **The rename is no longer cheap.** It was recorded when the `v0.4.25` → `v0.4.26` move carried nothing with it. Eight sites in five files outside `ROADMAP.md` now name `v0.4.26`, four of them bare, where no check reads them — counted by `#639` and re-counted there after `PL-7SVX` closed.
2. **A cut at the port's number silently reverses a decision of record.** `PL-Y1L0` measured it: `wave` builds its unreleased set as `section.version > current`, so a cut at `0.4.26` *or* `0.4.27` drops the port's section out of the plan and moves the beat to v0.5.0 — reversing `PL-RKWB` — while `outstanding_roadmap_edits` returns a byte-identical list at all three numbers, so nothing reports it.

`PL-G7RD`'s 2026-09-14 renumber stands as what happened and is recorded as superseded as practice.

## The four sites

| Site | What it said | What it says now |
| --- | --- | --- |
| The `v0.4.x` timeline row | "the next free number … any patch cut before the exact step lands would take it" | narrowed to a number that is *genuinely* free, which since 2026-09-16 excludes one given to a milestone section ahead of the current one |
| The port's risk paragraph | opened with the mechanic as the default, then marked the question open | opening sentence moves to the past tense — the mechanic is the precedent, not the default — and the open-question paragraph becomes the decision, with what it was chosen over, the two measurements, and `PL-G7RD`'s standing |
| The `### Declined to Gate 2 …` entry | "it waits on an answer rather than on attention" | the answer, kept in place as the record of why it sat there |
| Planned-milestone item 29's version note | "a patch cut before this work lands takes it" | the same narrowing as the timeline row |

The declined subsection's heading count is untouched: `PL-4RHP` holds that it is unchecked and wrong by other means, and correcting it here would pick a form that item is open to decide.

## Provenance of the recommendation, recorded because it was reconstructed

The recommending session's branch `claude/doc-check-safety-gate-advisory-gmspbu` was deleted when `#636` merged, and no cross-session channel reached that session, so its reply text is gone. The recommendation was read from its own closing-block line — *"decide `PL-KQHN`: withhold number + fix prose or keep mechanism"* — where the recommended option carries its consequence and the alternative is named in shorthand. It agrees with both measurements above, with the direction `#606` had already moved the apparatus, and with the brief's own `Done when.`, which states the withhold branch first and in more detail. The item records this reading so it can be corrected rather than inherited silently.

## Why two commits

`#639` was open and rewriting the port's risk paragraph from `main`'s older text when the decision arrived, so the decision was committed on its own first — it survives whichever session lands the edit. `#639` merged as `4690412` minutes later, and the four sites were then corrected on top of it in this same branch. No conflict, and no green run discarded.

## `PL-SYG4` takes the same answer to its question 1

Its question 2 — what the digest's `RESERVED` sentence should say — narrows to the third of its five candidates (keep the refusal, add that a patch-track cut is available by naming a version), which is what its own recommendation selected under this answer. It stays `needs-decision`: the owner ratified the `PL-KQHN` recommendation, not that one, and the two were put separately. Its brief also records that the last clause of its own `Done when.` — the answer written into `ROADMAP.md` — is satisfied by this PR, leaving it `render.py`'s sentence, `format_status`'s second copy of it, and the test that pins them.

## Checks

`bin/docket verify --self PL-KQHN`: **ACCEPT**, with the item's own `verify:` command passing and `make check` passing inside it. `bin/docket check`: **0 errors**. The one commission NOTE is `PL-SYG4`'s brief, outside `touches` and declared above.

The `verify:` command was run before it was written down and fails on a tree without the work: `python3 tools/doc_check.py check` exits 0 and the paired `grep` exits 1, so the pair exits 1 before the edit and 0 after.

Docs swept: `ROADMAP.md` (the four sites, plus a grep for any residual "takes this section's number" — the one hit left is the past-tense description inside the rewritten declined entry), `docs/WORKING_NOTES.md` (names no part of this thread), `CLAUDE.md`, `docs/maintainer.md`, `docs/worker.md`, `.claude/skills/docket/SKILL.md` (its release mode describes the refusal, not the roadmap's mechanic, and is unaffected).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_014iRFhQVMbY2bPyEP2L5jA3
