---
pr: 1076
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_01Jr391t5LXLAeRjbzMQrhwb?thread=cmsg_01Jr391t5LXLAeRjbzMQrhwb7QSz3jZXffGuV9dRtq1Mpr)_

**Fable batch 2, for Stuart's read.** One item, `PL-CH3Z`, which deletes code and adds no command, flag, check, field or rule. It was `blocked` on `PL-N162` and `PL-J9S0`, both done, and its start condition, `bin/docket flight` printing `legacy refs: 0`, held on `origin/main` at `35532902`; this branch readied it before claiming it.

Before: `claims.holdings` read a commit whose tree lacked `claiming.py` (`CUTOVER_MARKER`) by the old rule, under which a subject leading with ids claimed them where the diff reached outside the queue, and every reader carried a `legacy` flag for such a hold. `flight` printed a `legacy claim` row and a `legacy refs: N` line, `claim` refused to write on a tree without the marker, `claiming` re-read the log to renew a legacy hold and tested for a tie on one shared commit, `show` grouped the claim order by commit, and `branch_id_check` skipped legacy holds in its fence and skipped commits made before the record in its unclaimed-work refusal.

After: every commit is read by its trailers alone. A commit made before the record carries no `Claim:` trailer, so it claims nothing and its leading ids are attribution; a work branch that never claimed is an `unclaimed:` row and CI refuses it, whatever its tree held. The marker, the flag, the `legacy refs:` line, the writer's refusal, `claiming._recorded`, the tie test and `show`'s grouping are gone, each dead once no hold could read as legacy. `claims.work_under_record` is now `work_outside_queue`, since the record no longer divides commits.

How: `claims._events` drops the marker lookup and the old-rule branch; `work_outside_queue` returns on the first non-merge commit with a path outside the queue; `branch_id_check.ahead_of` reads the order whole. The tests that modelled a claim by an id-leading subject on a marker-less tree (`_flight_repo`, `_unevenly_truncated_pair`, and three `show` and `flight` tests) now record a `Claim:` trailer as a session would have, and the fixtures that planted the marker stop. Two regression tests pin the deletion: `test_a_commit_made_before_the_record_claims_nothing_whatever_its_subject` in `test_claims.py`, and `test_a_commit_made_before_the_record_owes_a_claim_too` in `test_branch_id_check.py`. The README's claims section, `cli.py`'s `show` and `flight` docstrings and `arming.py`'s comment no longer describe the old rule. `ROADMAP.md`'s v0.5.10 notes still call the reading pending; they record that cut and were left.

Verified: `bin/docket verify --self PL-CH3Z` ran the item's `verify:` command (`! grep -q -i legacy` over `claims.py` and `tools/branch_id_check.py`, PASS) and `make check` (PASS). Its one FAIL, `no existing assertion removed - 34 assertion(s)`, is the settled deletion case (`PL-K4R5`): the removed assertions belong to the ten tests that asserted the old rule and were deleted with it, the `unmarked` case of one parametrized test, and the `legacy claim` and `legacy refs: 0` strings in four tests that now assert the plain form; the two regression tests above are the rewrite. `touches` widened from three files to the fifteen the deletion reached, which the audit reports as this branch's own widening. 3,976 tests pass across `subprojects/docket/tests` and `tests/unit`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01BP85pfngwJMFT4GWtc8AXX

---
_Generated by [Claude Code](https://claude.ai/code/session_01BP85pfngwJMFT4GWtc8AXX)_
