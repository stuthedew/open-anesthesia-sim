---
pr: 672
commit: 22c1f292a77349b7d9076c6c38a1a68c60a43707
merged: 2026-09-17
items: PL-VX5H
subject: PL-VX5H: rank a recorded generator above every band but P0 (#672)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 22c1f292 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follows #670, which is now merged. This branch was stacked on it while it was open; `main` squash-merged it, so a base merge was needed to clear the resulting conflicts — see **The merge** below. The diff is this branch's own work only.

## The four parts

1. **`root-cause-of:` on the `Item` model**, parsed and rendered like any other list field.

2. **`docket check` validates it.** Every id names a real item, the list carries three *distinct* ids, and an item may not list itself. `model.root_cause_faults` is one rule read by both the checker and the ranking, so they cannot disagree about what a claim is. A typo and a short list stay two findings, because they are two repairs.

3. **`docket next` ranks a sound claim directly below `P0`** — above `PLACEMENT_ORDER` as well as above `band`, which is what "above everything but `P0`" has to mean, or an in-scope `P3` would still win. The reason opens `Ranked as a generator:` and names the ids. An unsound claim ranks on its band: `docket check` runs separately, so a store is routinely ranked before it is validated and a typo must not buy a promotion.

4. **`generator_check.py` moves from verdict to candidate surfacing.** `MIN_CLOSED = 8` is gone — eight closures is a state a cluster reaches only after long work, so a test that fires only then reports the weed once it has seeded. The floor is now the definition's own, three open items, and it selects what to *show* rather than what is true.

## Two things the build turned up

**The digest needed the mark too.** `format_digest`'s `Top:` line prints marks and not the reason, so every session would have opened on a `P2` leading a queue with `P1`s in it and nothing saying the ranking meant it. `Recommendation` now carries the count, the way it already carries `scoped_to`.

**The reason line contradicted itself.** The unplaced-scope clause closes with "so it is neither preferred nor excluded and ranks on its band alone" — true of everything except the one kind of item that does not rank on its band. `PL-BHVM` came back asserting both. The placement fact stays; the ranking conclusion comes off for a generator.

## Marking: two of six clusters have a causing item, not six

`PL-BHVM` (nineteen items re-decide what evidence proves a ref is done) carries `root-cause-of:` naming its **ten** still-open instances — ten rather than nineteen because nine closed as one round on 2026-09-13. Its 2026-09-17 retraction said it "should not be ranked as one"; that sentence is about the *ratio*, and a new paragraph records that both readings stand.

`PL-L4YG` (triage has no write command) carries five: the same finding re-filed, nine item files whose slug drifted from a title edited in place, and three ways `bin/docket record` — the only other writer — renames a file or reorders a hand-typed front-matter block while inserting `pr:`.

**The other four clusters have no item that could carry the field**, and each obvious candidate fails the same way: correctly filed, scoped deliberately narrower than the mechanism above it.

| cluster | nearest candidate | why it cannot head it |
| --- | --- | --- |
| What a `verify:` command proves | none | `PL-LKGL` is the measurement, is `done`, and explicitly refused the general contract |
| The roadmap is prose the tooling parses | `PL-6P9Y` | its own triage note: `P3` "because it moves exactly one id today" |
| Prose that asserts facts about the tree | `PL-036` | its `Decided` section: *"whether a documented statement is still true stays human"* |
| The checkout is a cache nobody validates | `PL-F48B` | scoped to tags after a rewrite; the permitted-ref-operations half has no item at all |

Writing the field onto one of those would record a claim its own brief contradicts **and** promote a narrow item above every band but `P0`, where nobody could work the cluster from it. So the four are filed instead — `PL-6TP8`, `PL-HWW1`, `PL-4FBP`, `PL-4Q9B`, under `feature: generator-heads` — each with its mechanism, its candidate members, and what the head would have to decide. Membership is recorded as unverified: the field's worth is that it is a recorded fact rather than an inference, which is the whole of why it outranks a `safety`-classed `P1`.

**Deliberately not marked, and that is the one open decision here.** Writing four heads would mean marking four items this session composed. `docs/WORKING_NOTES.md` carries why, and the general finding under all four: `PL-6ZQY`'s map is a list of *mechanisms*, the queue holds *items*, and nothing connects them — so a cluster can be correctly diagnosed and still have nowhere for the diagnosis to live.

## The merge

#670 squash-merged, so `main` carries its five commits as one while this branch still carried them individually. The merge base predates all of it, so every file #670 created and this branch then modified came back as a conflict — an artifact of the squash rather than a disagreement. Five, all resolved to this branch's side, each checked rather than assumed:

- `tools/generator_check.py`, `tests/unit/test_generator_check.py` and this item's own file are **byte-identical** on `main` to what this branch inherited at `3bc3920`, so this branch's copies are that same content plus the part-4 rewrite and the close-out.
- `docs/ARCHITECTURE.md` — `main`'s side is verbatim #670's package-map line, describing the verdict behaviour the rewrite removed.
- `PL-BHVM` — `main`'s side of the hunk is empty; the generator paragraphs are purely additive after #670's own correction.

The two files that auto-merged were read rather than trusted: `render.py` kept both #670's changes and the digest's generator mark, and the `docket` skill's ranking paragraph survived. Nothing of `main`'s was reverted — `PL-DHBX` arrives `done` from #671, and `PL-M2SD` reads `untriaged` here because it reads that on `main` too, byte-identical. The diff against `main` is the same 20 files at 1297/160 this branch had before the merge.

## One REJECT, reported rather than cleared

`bin/docket verify PL-VX5H --self` fails `no existing assertion removed`, naming four lines from `tests/unit/test_generator_check.py`:

```
assert [row[1] for row in reported] == ["src/thing.py"]
assert ratio >= 1.0
assert produced == closed
assert [c[1] for c in carriers] == sorted(c[1] for c in carriers)
```

Three are the verdict part 4 was commissioned to delete, and their replacements are tighter — `found.ratio == 1.0` plus the signal firing. The fourth had genuinely lost coverage and is restored as `found.open_ids == sorted(found.open_ids)`.

No `falsifies:` was added. The declaration only counts as the reviewer's text written before the work, and adding it beside the removal it would excuse folds nothing — so this is reported here instead, as the `docket` skill requires.

## Checks

On the merged tree: `ruff format`, `ruff check`, `mypy` (73 files), `pytest` (**3068 passed**), `python3 tools/doc_check.py check` (0 errors, and 0 advisories — the resident-instruction growth advisory retired itself once the `CLAUDE.md` rule landed on `main`), `python3 tools/workflow_paths_check.py`, `bin/docket check` (0 errors) and `make check` all clean locally. No new tool or test file, so no `docs/ARCHITECTURE.md` or `docket.toml` registration is owed — the map entry for `generator_check.py` is rewritten in place for its new behavior.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01DbXhdZWbFML13QYpQCGq3k
