---
pr: 625
commit: d4f7f187a89a18182b6456fb4ea58071c1c0c557
merged: 2026-09-16
items: PL-BNYF
subject: PL-BNYF: audit the queue and ROADMAP against the area/workspace layout model (#625)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d4f7f187 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Swept 49 items against the area/workspace model — the 36 open items declaring `app/` in `touches`, plus 13 of the 14 untriaged captures. The untriaged half was added on the project owner's instruction mid-session, and it closed a real gap: `bin/docket new` writes no `touches`, so no path filter can reach those items at all.

## No item landed in disposition 1, 2, 3 or 5, and that is the finding

Fourteen candidate findings were raised and thirteen were refuted by two independent skeptics each. They died on one shared ground: every one rested on a step like *"item 34 makes this surface a separate Editor in an Area the reader can close"*, and no such step is decided. `ROADMAP.md:4632` defers the roster — "Item 36 is the catalogue of them" — with item 36 sequenced after item 34 and unwritten. `.claude/rules/ui-areas.md` says the same: "That contract is not designed yet and is not yours to invent mid-change."

So the exposure this item was filed against is real but **deferred**. It fires when item 36 is scoped, not now. `PL-L6QR` carries the re-run with that single trigger.

The second refutation ground caught the rest: the "finding" was the item's own stated premise read back. `PL-RTG9` is the clean example — called a sequencing win because the README attribution is owed once the area system ships, which is the item's own title.

## `ROADMAP.md` contradicted itself about who turns the port's handles live

All three ordering lenses found it independently. Item 33 (the visual pass) was named at `:1540`, `:4545` and `:1633`; item 34 (the area system) at `:1615` and `:4770`. The 2026-09-16 un-absorption of item 33 introduced it — that note pulled the visual pass back out of the port and attached the reservation's next step to it. A session scoping the interface pass off `:4545` would have believed it owned building split and join.

Corrected at all four sites. The fourth (`:1633`, "reserving for item 33") was missed on the first pass and found by this audit's own completeness critic — a grep for "turns them live" does not return it. Item 33's lever list no longer claims `layout`, because under item 34 the arrangement is the reader's and so is not a thing a design pass decides once.

## Fourteen prerequisites filed, four contracts routed into `PL-TH35`

**Read `PL-NMTF` first: no open item builds item 34's area system at all.** The decisions are closed (`PL-3J2P`, `PL-C842`), the provenance is written (`PL-FTP5`, `PL-P5QX`), the Qt port has already paid for the reservation — and nothing builds it. That is why two other problems here have no clean fix: `PL-TH35` has nothing real to point `blocked-by` at, and the timeline cannot place item 34 as a release without a scope to place.

From the main sweep: `PL-W54S` (what a broken-out window owes the unconditional display set — `PL-WLWY` named choosing between its three readings as "this item's real work" and closed `done` without choosing), `PL-7Z84` (a run has no identity a workspace can pin to), `PL-L8RN` (nothing enforces the one-adapter `QSplitter` confinement), `PL-SSQW` (the saved-workspace file), `PL-WV9K` (Workspace as an object above the layout tree), `PL-HJPY` (no multi-window representation), `PL-NWTM` (the unconditional region has no structural home), `PL-9LNF` (three more state-ownership sites beside `PL-VN6M`'s), `PL-L6QR` (the re-run).

From the completeness critic: `PL-NMTF` above, `PL-J4NW` (`docs/ARCHITECTURE.md` routes every new panel by which container holds it, loaded on every `src/` read alongside `ui-areas.md`, which asks the opposite), `PL-R1WQ` (the view registry and kind-tag stability), `PL-9PD6` (`docs/interface-provenance.md` contradicts itself about the README attribution), `PL-D584` (`PL-TH35`'s `blocked-by` names an item that closed 2026-09-12, so the store advises promoting the Editor contract on every run).

Four further contracts became clauses of `PL-TH35`'s Editor contract rather than items of their own, which is where the store check put them.

## Checked at the source and reported clear

The port's reservation **was** built as described: `inert_splitter()` at `src/anesthesia_sim/app/qt_widgets.py:784` disables every handle and refuses collapse, and `QSplitter` is imported by that module and no other under `src/`. The largest available false finding — that the port built the area system and it will be undone — does not hold.

## Left for the project owner

Item 34 has no row on "The timeline". That is a defect of record, but which row it takes is a scheduling call rather than a session's, so it is raised here rather than written in — and `PL-NMTF` is why it cannot simply be written in anyway.

## Checks

`make check` green: 2941 passed. `bin/docket verify --self PL-BNYF` → ACCEPT on both commits.
