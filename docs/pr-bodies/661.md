---
pr: 661
commit: 0a370c0695c193e3cd41c942676701dd994febeb
merged: 2026-09-17
items: PL-FXBS, PL-GPYV
subject: PL-GPYV, PL-FXBS: View replaces Editor across the queue, and two verify: commands that can pass (#661)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 0a370c06 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The 2026-09-17 decision (project owner, ratified) named `View` as the word for
what occupies an Area. `ROADMAP.md`, `.claude/rules/ui-areas.md` and
`docs/interface-provenance.md` were swept when it was taken; this is the queue
and the two files that sweep had missed.

## The judgment, recorded as a file list

Five item files legitimately keep Blender's word and are excluded from
`PL-GPYV`'s `verify:` (project owner, 2026-09-17 — that list is the sweep's one
judgment): `PL-FTP5` (the Blender study record), `PL-C842` (the Blender read
behind the container decision), `PL-H620` (the item that adopted Blender's
vocabulary, quoting the manual's definitions table), `PL-GPYV` (whose brief must
name `set_editor` to record the rename) and `PL-FXBS` (the verify-command
finding, whose title must name it too). Everything else is swept — 17 files.

Kept everywhere else, because the word is right there: citations of a
`docs/interface-provenance.md` section title, Blender source and doc paths,
Blender's own findings in `PL-TH35`, "editorial", and VS Code's
`editor.formatOnSave`.

## Two verify: commands that could never pass

`! grep -rl` over a literal in `docs/items/` asks the item store to prove a
string absent from itself, while the `verify:` line stating it lives in
`docs/items/`. `PL-FXBS` has the measurement: n=2 of 170 open items.

- **`PL-GPYV`** was falsified by five lines of its own file, four of them the
  record of the rename the item is required to keep. Now matches `set_editor`
  and the capitalized noun `Editor`, excluding the five files above. Checked
  against `origin/main`: fails there, naming the 17 files this sweep changes;
  passes here.
- **`PL-0BSC`** was falsified by three files. `! grep -rq 'wave counts the four
  entries' docs/items/` became `! grep -q '…' docs/items/PL-WZBX-*.md`, leaving
  exactly the file whose title is the work. Exit 1 before the correction, 0
  after. `PL-0BSC` itself stays open — only its command changed.

## Three corrections, all from the earlier rename having been a blind sed

- `ROADMAP.md` carried six occurrences of "an **View**" — the article left
  behind when `Editor` became `View`.
- `ROADMAP.md` still carried `set_editor` twice, in Required scope entry 1 and
  in the deferred-docking list, though `PL-GPYV`'s brief recorded the roadmap as
  swept.
- `docs/WORKING_NOTES.md` and a comment in `src/anesthesia_sim/app/run_view.py`
  still named this project's object `editor`. The comment cites
  `.claude/rules/ui-areas.md`, already renamed, and used *view* twice and
  *editor* once for the same thing.

Four quotations of project text that had itself been renamed now match their
sources again: `PL-L6QR` and `PL-BNYF` quote `ROADMAP.md` item 34, `PL-9LNF`
quotes `.claude/rules/ui-areas.md` test 2, `PL-YHWG` quotes item 34's scope line.

`PL-TH35`, `PL-K285` and `PL-50PZ` changed title, so their files are renamed to
the slug the title now generates. Shipped release notes are **not** swept: they
record the vocabulary of their own day, and `_check_release_notes` matches ids
rather than titles, so nothing depends on rewriting them.

## PL-FXBS was recovered from a stranded branch

It was captured on `origin/claude/awesome-brown-x29b1e`, whose session stood
down on `PL-GPYV` (held here) and was then archived with the item never merged —
so its own handoff, "merge the branch or hand `PL-FXBS` to the `PL-GPYV`
session", was deleted with it. `bin/docket stranded` still named it.

**A second branch also recovered it.** `origin/claude/optimistic-brahmagupta-63pa72`
carries a copy at `status: untriaged`, committed 04:13 against this branch's
04:22. Neither is on `main` yet. That copy is the capture alone; this branch
carries the same file plus the fix and the close-out, so it is a superset and
the resolution is to take this one whichever lands second.

## Checks

`make check` green: ruff format, ruff check, mypy (72 files), 2992 tests, 0
docket errors, 0 documentation errors.

`bin/docket verify --self PL-GPYV PL-FXBS` → **ACCEPT** on both, every check
passing, including both items' own `verify:` commands and the `touches` audit.

An earlier run of that audit returned REJECT on `no existing assertion removed -
2 line(s)`, and both were false positives: Markdown prose in item briefs
containing the word "asserts", with both lines still present and only
`Editor`→`View` and `editor chooser`→`view chooser` changed. No `falsifies:`
declaration was added to clear it, per the rule that a session may not weaken
what measures it. #660 (`PL-7TYC`, decide what a removed assertion is by shape
and by file) fixed the check on `main`; bringing that base in cleared the REJECT
on its own, which is what the two `PASS  no existing assertion removed` lines
now show.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01PPGPHs6EK389n3Cq1X3hye
