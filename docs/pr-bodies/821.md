---
pr: 821
commit: 165ec1727d35f862e4c332448b47952971549d17
merged: 2026-09-20
items: PL-PT7M, PL-XF5V
subject: PL-PT7M: record the ratified citation-drift drain decision, triage it, and note PL-XF5V's approval (#821)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 165ec172 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

## Why this is a pull request and not just a branch

It carries item files only, which `CLAUDE.md` normally keeps out of a pull request. The exception here is that what it carries is a **ratified decision**, not a capture: `PL-PT7M` does not exist on `main` at all, so the decision the project owner took on 2026-09-21 would live only on a branch, and the next session would meet it as stranded work to recover rather than as a queue item to pick.

## What the owner asked and what was decided

The question was whether the generators and their clusters are dealt with. Answering it took a script over the whole store, and the answer is: all 12 recorded heads (11 `root-cause-of:`, 1 `impairs-generators:`) are closed, and 60 of the 99 distinct members they explain are not - the same 60 `PL-XF5V` measured on 2026-09-20. `PL-4FBP` (21 open of 24) and `PL-G424` (19 of 21) carry 40 of the 60.

Read individually rather than counted, those 40 are three kinds of work: 9 check-building items in `tools/doc_check.py`, 27 single live-document repairs across four files, and 4 that are not startable. 37 of the 40 were filed before `.claude/rules/citation-drift.md` existed, and that rule refuses some of them outright - its closed-brief clause, and its ban on re-pointing line numbers.

**Decided (project owner, 2026-09-21, ratified)**, over working the whole three-step recommendation now and over deferring all of it:

- **Step 1 only.** Re-judge all 40 against `.claude/rules/citation-drift.md` and drop what it refuses, each drop citing the clause.
- The 27-repair editing sweep is **deferred** behind v0.5.0's product beat.
- The 9 check-building members **leave this item** and rank on their own merits.
- `PL-XF5V` (a command reporting each generator cluster's drain) is the **approved next workflow-lane item**.

## What changed

- `PL-PT7M` filed, narrowed to step 1, and triaged `P2 / M / ready` with `classes: planning, docs, housekeeping`. Its brief carries the measurement, the three-way split, the ratified decision and a `**Done when.**` requiring one disposition row per member.
- Its `verify:` counts those 40 rows, so it proves the pass ran without prejudging what any row says. It exits 1 today, by design, and exits 0 when the pass is complete.
- `PL-XF5V` records the approval, the second derivation of its own numbers as the evidence for it, and one addition that second derivation argues for: report each cluster's drain against the count at its head's close date, not only the present split.

No item is closed here, and `tools/pr_title_check.py` agrees no id is owed in the title beyond the leading one.

## Verification

`make check` passes on this branch, exit code 0 - `ruff format`, `ruff check`, `mypy`, `pytest`, and all nine repository checks including `doc_check.py check` (0 errors, 0 advisories) and `bin/docket check` (0 errors).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LsQ8a8GfB4gEWeLtxEoUD4

---
_Generated by [Claude Code](https://claude.ai/code/session_01LsQ8a8GfB4gEWeLtxEoUD4)_
