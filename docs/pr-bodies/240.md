---
pr: 240
commit: ab02b9bb2ac5f4e9b47e046032ec6d6d915f8dac
merged: 2026-09-02
items: PL-ZBJ0
subject: PL-ZBJ0: triage the 14 untriaged captures (#240)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ab02b9bb landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Triages the 14 items untriaged at session start. `bin/docket check`: 0 errors, and **0 untriaged once #239 merges** (see the note at the end).

## Dispositions

**Two go to `needs-decision`, each with the decision stated and a recommendation:**

- `PL-5WFS` — `docket next` cannot read a brief's stated prerequisite, so it offers `PL-F52R` (MAC-awake reference band) ahead of the `PL-WB0X` extraction that gates it. Recommends the no-code fix: `blocked-by` on `PL-DHV7` and `PL-F52R`.
- `PL-D4MZ` — nothing reserves work at the moment it is recommended. Recommends deciding it *after* `PL-CP74`, `PL-MC8Z` and `PL-QTSB` land, since each removes a slice of the collisions a reservation mechanism would catch.

**One goes to `P1`:** `PL-4GN8` — the reference tests assert an absolute mass-balance tolerance (`absolute_error_l <= 1e-12`) on a quantity that scales with the delivered concentration, so the tolerance `docs/MODEL.md` documents as a release gate certifies a different thing at each dial. Checked while triaging: `core/agent_simulation_validation.py` already computes and exports the dial-independent `relative_error`, and its own halt threshold already accepts either; only the tests ignore it. Classed `science` because what is wrong is the meaning of a documented scientific guarantee.

**One is dropped:** `PL-ZBJ0` — both halves of its *Done when* are met by other work. `CLAUDE.md:183` now states the retirement test beside the build test, and `PL-V87X` was fixed outright (#236) rather than re-banded. Its own brief argues against the remaining decidable half.

**One open question settled at triage:** `PL-LKCN` asked whether to assert a frame-cost ceiling, only report, or assert something machine-independent. Took the third — a test that a refresh allocates no chart points when the drawn count is unchanged. Deterministic, so it cannot flake on shared CI; it tests what actually regressed. Title updated to match.

**Four briefs completed** that capture had left as templates: `PL-40PL`, `PL-4GN8`, `PL-NB4D`, `PL-YNCW`. **One item captured:** `PL-L4YG` — `bin/docket` has one write command, `new`, so every triage answer is hand-edited front matter.

## `PL-40PL` was written from measurement

`PL-SZ56`'s readout 2 (net queue change) names no command. Its baseline and its score were produced by different rules:

| Method | v0.2.7..v0.2.8 (t=0) | v0.2.8..v0.3.0 (scored) |
| --- | --- | --- |
| Front-matter dates, whole days | 140 in / 102 closed (+38) | 117 in / 84 closed (+33) |
| `git log --diff-filter=A` over `docs/items/` | 95 in / 83 closed | **57 in** / 38 closed |
| Recorded | **141 / 104 (+37)** | **57 / 37 (+20)** |

The score matches the git method; the baseline matches the date method. The windows are tag-to-tag (`v0.2.8` at 2026-09-01 18:57, `v0.3.0` at 2026-09-02 13:01) while the date fields are day-granular, so no field-reading method can resolve them. Readout 2 passed at exactly its threshold, and the alternative rule scores +33, which fails.

## Verify commands

All eleven were run before being written down, and all eleven exit 1 today with their `pytest` / `doc_check` half passing. Two were then narrowed after `bin/docket check` reported them setting the floor for the whole run: `PL-4GN8` ran the full `tests/reference` suite (39s) and `PL-NB4D` the full `test_verify.py` (21s). The verify pool goes from 40s to under 1s.

## `PL-QSWS` was backed out of this branch

This branch originally triaged `PL-QSWS` (toolchain drift) to `needs-decision`. Open pull request #239, from another session, closes it as `done` and records the owner's decision — the same option this branch recommended, already answered. Merging both would either conflict or silently revert a closure, so the file was restored to its `origin/main` state and this branch no longer touches it. #239 lands cleanly whichever order the two merge in.

The id is deliberately absent from that commit's subject: `docket check` recovers a closed item's pull request from the newest subject naming it, so leaving it would attribute the closure to this pull request instead of to #239 (`PL-GW37`). The store therefore reads 1 untriaged until #239 merges.

This is the second collision between the two sessions inside an hour, and evidence for both `PL-D4MZ` and `PL-QTSB`: the item was captured and started inside the other session's own pull request, so no ref-based guard could see it.

## Checks

`make check` green: 1165 tests pass, ruff/mypy clean, `docket check` 0 errors, `doc_check` 0 errors, resident instructions unchanged against `origin/main`. `doc_check candidates --base origin/main` reports nothing in the documentation mentioning anything this diff changes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01UFA8XWoeoqWYZvZ6gG4YU6
