---
pr: 241
commit: 7a3cdb2b21af86adbfa9c8cee844c2368a380d20
merged: 2026-09-02
items: PL-8JY7, PL-QSWS
subject: PL-QSWS, PL-8JY7: record #239's number, and correct PL-8JY7's design before it ships a check that cries wolf (#241)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 7a3cdb2b landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two pieces of queue bookkeeping from auditing whether the rules added earlier in
this session leave any historical work to do. **No behaviour change.**

## `PL-QSWS` — record `pr: 239`

Answers the post-merge advisory `bin/docket check` raised after #239 merged. The
item closed on the branch that carried its work, so the number could not be
known at the time — the same sequence #237 and #238 answered for their own
merges.

## `PL-8JY7` — every dangling `touches` path is forward-looking, not stale

This is the substantive half. `PL-8JY7` briefs a check that a declared `touches`
path resolving nowhere is stale. Audited against the whole store today, every
such path is a file **its own item is going to create**:

| Item | Path | Created by |
| --- | --- | --- |
| `PL-LWMS` | `.mailmap` | its own "Done when" — collapse the owner to one identity |
| `PL-XH1D` | `CONTRIBUTING.md` | its own "Done when" — document the attribution rule |
| `PL-Y0RZ` | `tools/import_boundary_check.py` | the checker it exists to write |

So the check as briefed would fire three times on the current store and be
**wrong all three times**. It would ship already crying wolf — the failure
`CLAUDE.md`'s retirement test now names, arriving before the check is even
built.

`Path.exists()` cannot separate "was real" from "not yet real". The brief now
says so and names two routes that can:

- **Ask git.** A path no commit reachable from the default branch has ever held
  is forward-looking; one the history holds and the tree does not is stale —
  exactly the `item.py` → `model.py` rename this item was filed for. Belongs
  beside the other git-derived reads that decline in a shallow checkout rather
  than answering wrongly.
- **Scope it to `done` items**, whose `touches` should all resolve. Cheaper, no
  git, catches nothing until an item closes — late, but never wrong.

Prefer the first. The item stays open with a design it can now be built from.

## What the audit found overall

Nothing else needs fixing. The rules added in #236 are **errors with no cutover
date**, which was only affordable because `PL-L9JS` repaired the store first —
so `bin/docket check` reporting 0 errors across all 106 open items is itself the
proof that no historical violation survives, re-established on every run.

The 16 open items still using a `-k` selector are deliberately left alone: that
is the *recommended* shape for an item whose work has yet to write its test, and
the flip from "correctly failing" to "falsely passing" is now caught loudly by
the new error at the moment it happens, which is what `PL-71P4` asked for.

## Checks

`make check` green. `bin/docket check` at 0 errors; the `pr: 239` advisory is
cleared. One advisory remains and is unrelated: three `ready` items predating
the `verify:` requirement.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018rVJTxSXWM8jdcndjU2p6s
