---
pr: 600
commit: c8f52f6d132b25b7f2c0060475db46659578237f
merged: 2026-09-15
items: PL-0HPV, PL-7VSK, PL-B5VM, PL-MR6S
subject: PL-7VSK, PL-MR6S: resolve against main, and drop both as superseded by PL-B5VM and PL-0HPV (#600)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit c8f52f6d landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

**This PR no longer fixes anything, and that is the outcome.** It was opened to un-red `main` by repairing `PL-S5YM`'s non-discriminating `verify:`. `PL-B5VM` merged as #598 while it was open with the same diagnosis and the same repair, so what is left here is the honest record of a race this branch lost.

Three sessions reached the same red `main` independently — `PL-99YZ`, already on `main`, is the item recording that.

## The conflict, and why it resolved to main

`git merge origin/main` conflicted on exactly one file: `docs/items/PL-S5YM-doc-check-s-covered-directory-branch-now-has-no.md`, which both branches had rewritten. Taken **wholesale from `origin/main`** — nothing of this branch's rewrite survives, deliberately, because theirs is the better command:

```
grep -q 'covered_dirs' tests/unit/test_doc_check.py
  && ! grep -q 'no tree draws one today' docs/ARCHITECTURE.md
  && uv run pytest tests/unit/test_doc_check.py
```

This branch's replacement named one test the work must create. `PL-B5VM`'s holds **both** halves of `PL-S5YM`'s own "Done when." without dictating any name — and the second clause is what this branch missed: `docs/ARCHITECTURE.md` still claims no tree draws a bare directory, and that claim has to go with the work.

## Both items dropped rather than deleted

| Item | Superseded by | Why theirs |
| --- | --- | --- |
| `PL-7VSK` | `PL-B5VM` (#598) | Same diagnosis, same repair, merged first. It also captured the general case as `PL-Q8RQ` — four open items rest on a bare `-k`, and whether a command can discriminate is decidable enough for `docket check` to refuse it. That is the durable fix, and `PL-7VSK` never reached it. |
| `PL-MR6S` | `PL-0HPV` | Filed a day earlier, already `needs-decision`. The difference is substantive, not wording: `PL-MR6S` argued from "147 commands in 150 s", and `PL-0HPV` records that that measurement predates `--verify-base` narrowing the replay — so the figure, and the recommendation built on it, were already superseded. |

Dropped rather than deleted per the `docket` skill: the reason is what stops a finding being re-raised, and an item left only on a closed branch is what `bin/docket stranded` would later resurrect as a duplicate.

## What merging this is worth

Only the record. `main` is already un-redded by #598. Merging lands two `dropped` items explaining why these findings were raised and why they are closed, so the next session that hits a red `main` finds the history rather than re-deriving it. Closing unmerged is a reasonable alternative — it just leaves both items on an abandoned ref.

`make check` green: 2 915 passed, 100% core coverage, `docket check` 0 errors, `doc_check` 0 errors 0 advisories.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01X5DaXrhiqwwsedcXzAtqLE
