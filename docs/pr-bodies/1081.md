---
pr: 1081
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeG2KqtBDvLpGNL25Gb53fLJe)_

Before: a session whose branch holds its own claimed capture opened on a digest line calling it `Filed on a branch, not yet on origin/main` with `no copy here to start from`, while `bin/docket show` said the item was in flight on this branch as the session's own work and `bin/docket stranded` listed nothing. `bin/docket flight` marked the same row `filed there`, "not in this checkout's queue at all". An item another branch alone held was named twice, on the digest's filed line and again on its stranded line.

After: the digest names the session's own claim in its in-flight line as `(live claim, this branch)`, `flight` marks `filed there` only what neither the default branch nor this checkout holds, and an id the filed line names is not repeated on the stranded line.

This is the first half of step 4 of PL-PVW2's split (one answer per repeated predicate); PL-0JGZ follows.

How: `vcs.only_on_a_branch(identifier, on_base=..., here=...)` is the one spelling of "is this item only on a branch": the default branch lacks it and so does this checkout's store. `stranded` already asked both halves, through `known_ids`, and now asks through the function; the digest passes its report's store ids, and `flight` the store `_load` reads. The in-flight line's `this branch` mark reads `_on_head`, the test `show` already uses.

Tests: `test_digest_counts_this_branch_as_here` builds a real repository whose checked-out branch holds its own claimed capture beside another branch's, and checks that the digest, `show` and `stranded` agree about both; `test_flight_marks_filed_there_only_what_this_checkout_lacks` does the same for `flight`. `test_next_and_the_digest_say_what_holds_each_item_they_leave_out` now expects the `this branch` mark, since its claim is on the checked-out branch.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01JB5k2ZRb4zvLYtGtSCBT6Z

---
_Generated by [Claude Code](https://claude.ai/code/session_01JB5k2ZRb4zvLYtGtSCBT6Z)_
