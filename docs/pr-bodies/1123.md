---
pr: 1123
recorded: 2026-09-26
---

`PL-FTDB`'s docs sweep found it, and filed it as `PL-YYV9` because the file was outside that item's `touches`. The paragraph "Run it and watch it fail, not merely run it" in `.claude/skills/docket/modes/triage.md` still told a session writing a `verify:` command two things. One: `docket check --verify` "raises an advisory for the ones that pass". Two: "CI is what passes that flag, so it is caught after the item is written *and* after it is pushed". Three changes have made both untrue:

- The replay reports an open item whose command already passes as an error (`PL-71P4`, #236).
- `make check` runs the replay on the items the branch has edited, committed or not (`PL-0HPV`, #920).
- `bin/docket set --verify` runs the command it is writing, and refuses one the replay would report as already passing (`PL-FTDB`, #1071).

The same section of the file already said the replay reports it as an error, so the paragraph contradicted its own file.

**What changes.** The paragraph now says a passing command is refused before the push: by `set --verify` as the line is written, and by the next `make check` for a line written by hand. It keeps the advice to see the command fail first, and gives the reason that advice still holds. Neither tool reads why an ordinary failure happened: a pattern misspelt so that it never matches exits 1, exactly as an unstarted item does, and keeps exiting 1 after the work.

**Also here:** `PL-YYV9` is triaged (P3, S, `docs`), its brief completed and its generator check recorded (a one-off, the same shape as `PL-397Q`), and it is closed.

**Verification**

- `PL-YYV9`'s `verify:` command failed before the edit (exit 1, both clauses) and passes after it.
- `bin/docket verify --self PL-YYV9`: ACCEPT, with `make check` passing. The item was triaged and worked on this branch, so the audit runs this branch's `verify:`. `origin/main` holds the item untriaged, with no command.
- `make doc-check` is green, and `doc_check.py candidates --base origin/main` names nothing.
- Docs checked: all of `.claude/skills/docket/modes/triage.md`, including the paragraph "What the exit status is read to mean once the command is recorded", which the rewrite now agrees with. Also checked for the same claim, and none restates it: the rest of the docket skill (`SKILL.md` and `modes/*.md`), `docs/worker.md`, `docs/maintainer.md`, `CLAUDE.md` and `subprojects/docket/README.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01BwRtP2yEbAAq5gSi9tNAmY
