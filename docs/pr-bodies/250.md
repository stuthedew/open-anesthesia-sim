---
pr: 250
commit: d01bdbde0c2ec16c5ca2c44950ea86ad292a94a4
merged: 2026-09-02
items: PL-1TF4, PL-J49T
subject: PL-J49T, PL-1TF4: record #249, and capture the transcription it needed (#250)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d01bdbde landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two frontmatter lines and one capture. No source change. Closes nothing, deliberately — so it generates no further provenance chore of its own.

## Recording #249

`docket check` recovered #249 from the merge commit and raised the expected advisory against both items it closed. This writes `pr: 249` into each, so the files carry the provenance rather than only the git history.

## `PL-V4LS` — why that advisory exists at all

The number **cannot** be known when the closure is written. `.claude/skills/docket/SKILL.md` requires the closure to be committed *with the work*, in one commit, precisely because splitting it out to learn the number reopens the window where a merge takes the work and leaves the queue still calling the item open (`PL-D2GW`, then `PL-P5S0`). So the transcription is structural: **every item that ever closes will need it afterwards**, and the tool has already found the number before it asks a person to copy it.

This session paid it twice — after #246 and after #249 — each time as a commit and a pull request whose entire content was two frontmatter lines.

It is `PL-J49T`'s shape one layer along: a decidable fact delivered as prose that works only if somebody reads and acts on it. The difference is that here the tool knows not merely *whether* the field is missing but *what it should say*, which removes even the judgment half.

Recorded as `needs-decision` because the real question is whether a checker may write — `check` runs inside `make check`, and its read-only property is worth something. `docket record-pr` is recommended over a `--fix` flag, with the counter-argument stated rather than buried: the transcription is genuinely one line, and this item is process work about process work, which `CLAUDE.md` warns is the most common way this project wastes a session. If the answer is "leave it", recording that closes the question instead of leaving it to be re-opened by the next session that pays the toll twice.

## Checks

`make check` passes: 1174 tests, 0 docket errors, 0 untriaged, 0 documentation errors, contrast unchanged at 3 known tracked shortfalls.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01MA7d2hcMtJpqVMHcZHHNyP

---
_Generated by [Claude Code](https://claude.ai/code/session_01MA7d2hcMtJpqVMHcZHHNyP)_
