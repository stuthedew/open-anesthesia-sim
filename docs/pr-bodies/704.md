---
pr: 704
commit: 5901ba42742ca90fddba2848270a336387217b97
merged: 2026-09-19
items: PL-T2LH
subject: PL-T2LH: cut v0.4.28 from the 58 items finished since v0.4.27 (#704)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 5901ba42 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Cuts `v0.4.28`: the version bump, `uv.lock`, `docs/releases/v0.4.28.md`, the
`milestone:` stamp on 58 items, and the `ROADMAP.md` edits `bin/docket release`
leaves by hand.

## Why a patch

Nothing crosses a capability boundary, which is § "Versioning decision"'s test,
and `v0.5.0` through `v0.9.0` are in any case spent by milestone sections.
Measured by tree object rather than read off the diff:

- `src/anesthesia_sim/data/` resolves to `d9f9c5b` at both `v0.4.27` and here,
  so no parameter moved.
- `tests/reference/` resolves to `fcb3eca` at both, so every
  published-reference expected value is byte-identical and still met.
- The three files that change under `src/` are all under `app/`, so no
  equation, numerical method or unit moved either.

Two of the 58 are reachable by a learner and both are presentation fixes:
`PL-DHBX` (the Start, Pause and Reset labels, illegible under the macOS Dark
appearance) and `PL-MN4J` (the chart hover naming which of two drawn runs it
reads). `docs/MODEL.md` gains 314 lines for `PL-S6WW`, which states the
reference temperature the specification had asserted and never named.

## The two checks `PL-TM9J`'s cut also ran, answered differently

`PL-TM9J` (the v0.4.27 cut) cleared the cadence's no-interim-release sentence
by carrying no gate entry and no Required-scope item at all. This one carries
one of each, so both are argued in the item's brief rather than assumed:

- **`PL-S6WW` is a Gate 1 frozen entry** (added 2026-09-17 under the
  unconditional safety/science exception). Gate 1 stands at 168 of 175 cleared,
  so this cut *is* partway through the clearing. Checked rather than assumed:
  every one of the 175 frozen entries was read for the `milestone:` it closed
  under, and cleared entries have shipped in **twenty-one separate `v0.4.x`
  patches**, `v0.4.5` through `v0.4.26`. The sentence bars cutting a release
  *for* the gate; it has never barred a patch cut for its own content from
  carrying an entry that already merged.
- **`PL-MN4J` is v0.5.0's own `Required scope` entry**, shipped early in the
  `v0.4.x` track on the disposition § "The timeline" row 5 records — a seventh,
  after v0.4.25's five and v0.4.26's `PL-8PSW`. Row 5's running tally gains it.

## ROADMAP edits

Three that `bin/docket release` names — the version-table row, the
`current baseline` mark moved onto it, the baseline section rewritten — plus a
fourth it does not: row 5's early-shipped tally, which is `PL-Y1L0`'s finding
that `outstanding_roadmap_edits` returns the same three statements whatever the
version.

## Verification

- `bin/docket verify --self PL-T2LH` → **ACCEPT**. All four integrity checks
  pass, including `make check` and the item's own `verify:` command; the four
  commission checks report rather than refuse, as `--self` prescribes.
- Docs swept: `make doc-check` at 0 errors and 0 advisories (package map,
  provenance table, citations, release train, current baseline, release tags),
  and `doc_check.py candidates --base origin/main` read against
  `docs/ARCHITECTURE.md`, `docs/WORKING_NOTES.md`,
  `docs/interface-provenance.md`, `docs/resident-instructions.md`,
  `subprojects/docket/README.md` and `.claude/skills/docket/SKILL.md` — every
  candidate line is about the files existing, none states a version this cut
  moves. No occurrence of `0.4.27` survives outside `ROADMAP.md`,
  `docs/releases/` and `docs/items/`.

## One incidental rename

`bin/docket release` re-derived `PL-XQRK`'s filename slug from a title that had
been edited on `main` without the rename — the `PL-5QLP` behaviour. The file is
byte-identical apart from its `milestone:` stamp.

## Outstanding after the merge

The tag is the project owner's to run; a session cannot push a tag ref here
(`PL-N936`), so `PL-T2LH` is not finished on the merge alone.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01VartoWGZ8YwbAJ4KJF4XpG

---
_Generated by [Claude Code](https://claude.ai/code/session_01VartoWGZ8YwbAJ4KJF4XpG)_
