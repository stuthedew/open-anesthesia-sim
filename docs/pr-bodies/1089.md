---
pr: 1089
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGNa38Qdja1Ekmy5FzwGrd6R)_

Before: `docket check` read a brief's required headings wherever a line opened with them, fences included, so a brief quoting the capture template in a fenced example failed with "brief has nothing under **Why it matters.**" while every section below it was written. A `needs-decision` brief saying it "lacks a recommendation:" counted as marking one, because the label was matched in any case. And `tools/possessive_section_check.py` told a possessive quotation of a bold lead sentence, such as `CLAUDE.md`'s "Capture, always, and capture cheaply", to use the `§` mark: a hard gate refusing a quotation that was right.

After: a fenced block is a literal to the brief-section reader, by CommonMark's rules. A backtick opener's info string holds no backtick, so a triple-backtick code span wrapped to a line's start opens nothing, and a fence left unclosed is read as written rather than hiding every section below it. The recommendation label is `Recommendation:`, capitalised, outside code spans. The possessive check reports only a quotation that matches a `#` heading. On the store, no open item's reading changes.

This is step 3 of PL-GPJ7's split, recorded in the item. It closes PL-NQ3X and PL-FKH6. PL-6G8T, the same function's false pass, waits on Stuart's choice of heading rule, recorded in that item, and this pull request stays a draft until it lands here too.

How: `_without_fences` blanks each closed fenced block to spaces with every offset kept, and `_section_text` and `_stub_above_brief` find headings in that copy through one `_heading` helper while still slicing the text from the brief, so a fence under a heading is text under it. `_marks_recommendation` drops code spans with `release.CODE_SPAN_RE`, the package's existing CommonMark code-span reading, before it matches. `doc_check._hash_headings` is the `#`-heading half of `_headings`, and the possessive check now reads that half alone. PL-FKH6's other two parts close as the rule working: `dead_ends.check` refuses any line under an entry that opens none, since the file is emitted one line per entry, and an item describing a broken line citation fences it, as `.claude/rules/citation-drift.md` says. Of the six new tests, three fail on the old code; of the other three, two fail under a naive toggle fence reader (checked) and one pins that the text is sliced from the brief. PL-92MY captures the three readings of a fence that now disagree across `docket` and `doc_check`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_019q9v997mDKQkjVjo1pPy4S

---
_Generated by [Claude Code](https://claude.ai/code/session_019q9v997mDKQkjVjo1pPy4S)_
