---
pr: 1089
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGNa38Qdja1Ekmy5FzwGrd6R)_

Before: `docket check` read a brief's required headings wherever a line opened with them, fences included, and judged only the first. A brief quoting the capture template in a fenced example failed with "brief has nothing under **Why it matters.**" while every section below it was written, and a quotation of a heading that a wrapped sentence carried to a line's start stood in for an empty real heading below it, so an item with no closing condition passed. A `needs-decision` brief saying it "lacks a recommendation:" counted as marking one, because the label was matched in any case. And `tools/possessive_section_check.py` told a possessive quotation of a bold lead sentence, such as `CLAUDE.md`'s "Capture, always, and capture cheaply", to use the `§` mark: a hard gate refusing a quotation that was right.

After: a fenced block is a literal to the brief-section reader, by CommonMark's rules. A backtick opener's info string holds no backtick, so a triple-backtick code span wrapped to a line's start opens nothing, and a fence left unclosed is read as written rather than hiding every section below it. Every line opening with a required heading's words, outside a fence, is that heading and needs text under it. The recommendation label is `Recommendation:`, capitalised, outside code spans. The possessive check reports only a quotation that matches a `#` heading. On the store, no open item's reading changes.

This is step 3 of PL-GPJ7's split, recorded in the item, and it closes PL-NQ3X, PL-6G8T and PL-FKH6. The split named a different heading rule for PL-6G8T, a heading recognised only where a paragraph opens. Measured first, as the split asked, that rule would have refused 63 headings in the store, 18 of them in 15 open items, so it was put to Stuart, who chose the every-heading rule instead; PL-6G8T records the decision as ratified.

How: `_without_fences` blanks each closed fenced block to spaces with every offset kept. `_sections` finds each heading in that copy and slices the text under it from the brief itself, so a fence under a heading is text under it; `_section_text` is empty when any heading's text is, and `_stub_above_brief` asks of each empty heading whether a `**Problem.**` follows it. `_marks_recommendation` drops code spans with `release.CODE_SPAN_RE`, the package's existing CommonMark code-span reading, before it matches. `doc_check._hash_headings` is the `#`-heading half of `_headings`, and the possessive check now reads that half alone. PL-FKH6's other two parts close as the rule working: `dead_ends.check` refuses any line under an entry that opens none, since the file is emitted one line per entry, and an item describing a broken line citation fences it, as `.claude/rules/citation-drift.md` says. Measured on the built code over all 1,756 items, two readings change, both in closed items (PL-Y2GG, PL-Z4GF) whose old capture template's empty `**Done when.**` sits below the real brief, which the brief checks do not ask of a closed item. Of the seven new tests, four fail on the old code; of the other three, two fail under a naive toggle fence reader (checked) and one pins that the text is sliced from the brief. PL-92MY captures the three readings of a fence that now disagree across `docket` and `doc_check`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_019q9v997mDKQkjVjo1pPy4S
