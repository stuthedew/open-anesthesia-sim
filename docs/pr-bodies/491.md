---
pr: 491
commit: 7e0ff5bb9f3adc06f9757f5e9bb83df587c7a587
merged: 2026-09-08
items: PL-X9T3
subject: PL-X9T3 Record the owner's run of the Qt spike, and repair the verify command promoting it exposed (#491)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 7e0ff5bb landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Queue-only. No source change.

## What the project owner reported

Two observations, and the pair is worth more than either.

Before running the spike, about the **Flet** build after #488 removed the
sample store: *"now that we dropped the M4 stuff, I don't really notice a
difference in performance. Maybe we don't need pyside."* Then, after running
it: *"I like it better."*

**So the premise `PL-QXSB` was opened on has expired.** That item was raised on
"It shouldn't be this laggy", and the lag is what the seven items before it were
spent on. The owner now reports the Flet build as acceptable. A migration
argued from lag would be arguing from a symptom that is gone — which is worth
saying plainly, because it makes the remaining case *narrower and better*: the
arguments still standing are headroom, the fidelity affordability `PL-GS3R`
turns on, and headless verification (`PL-2QMK`). None of them is speed.

**"I like it better" is the third of the three things `PL-X9T3` asks for** —
whether six traces are distinguishable, whether the type is legible, whether a
drag feels immediate. It is not the first two. Frame cost and input latency are
on the spike's own instrument panel and remain unrecorded, so the item is
promoted to `ready` rather than closed. Its blocker `PL-55DH` merged as #490.

## The verify repair, which promoting it is what would have exposed

`PL-X9T3`'s command was `grep -q 'PL-X9T3' docs/WORKING_NOTES.md`. #490 wrote a
section whose *heading* cites that id — correctly, since that section is where
the spike's own measurements live and where this item's will sit beside them.
That satisfied the grep with none of the work done.

It did not fire on main's run #1697 only because the item was still `blocked`
there; the sweep checked 110 commands, the same count as before. Promoting it
is what exposes it, so the repair rides the promotion.

The command now greps for a phrase the recorded measurement must carry, and the
brief states that phrase, so it is a specification rather than a bet on a name.
Run against this tree: exits 1, as it should with the work undone.

## Same finding as PL-X7VY, by a different route

`PL-X7VY` gains this as a second instance. In both cases *another item's work*
satisfied an open item's `verify:`, and in both cases nothing local reports it —
`PL-XH1D`'s because #487 created the `CONTRIBUTING.md` it tests for, this one
because a note cited an id. The shared rule is the `docket` skill's own: a
`verify:` must name something **only this item's work** creates.

Main's run #1697 on `f393e16` is red, and it is `PL-XH1D` alone — confirmed
from the job log, one error, nothing the spike brought with it. `PL-XH1D` is
the one still outstanding, and it stays the project owner's: the item is
`not-delegable` because its wording states publicly what he did and did not
write.

## Also here

`bin/docket record` wrote the `pr` number the base was owed for `PL-55DH`
(#490), riding this commit rather than one composed for it.

## Checks

`make check` green: 2 328 tests, 100% branch coverage on `core/`, mypy clean,
`bin/docket check` 0 errors, `doc_check` 0 errors 0 advisories.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01KJs4hU4PaybvQUdh4ySY6u

---
_Generated by [Claude Code](https://claude.ai/code/session_01KJs4hU4PaybvQUdh4ySY6u)_
