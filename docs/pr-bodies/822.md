---
pr: 822
commit: d1249edabefb8e06a1ddf6895f137448ccd323ea
merged: 2026-09-20
items: PL-34BG
subject: PL-34BG: record the annotate-over-delete decision as the project owner's, ratified (#822)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d1249eda landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Follow-up to #820, which merged while this was being written. The branch was restarted from `main` rather than stacked on the merged history.

`CLAUDE.md` distinguishes a decision the owner *ratified* from one they *specified*, and gives them different bars to reopen: a ratified one goes back on ordinary evidence, because it was a session's own recommendation wearing their signature. Without the word recorded, a later session cannot tell the two apart, and the reply that put the case is gone.

The case was whether `docket withdraw` should annotate the entry or delete it. `PL-34BG`'s brief sketched the deletion — "it costs one line to delete" — and the recommendation was to keep the entry and mark it, on the grounds that a deleted entry makes the withdrawal the one event in this mechanism's life with no record. Agreed on that recommendation, so `subprojects/docket/README.md` now carries it as ratified with what it was chosen over.

**No behaviour changes**: an attribution clause and the reflow of the paragraph it lands in.

## Riding this commit

`bin/docket record` wrote the six `pr:` numbers the base was owed — `PL-34BG` (#820), `PL-CJ5R` (#818), `PL-N3N5` (#819), `PL-S8JT` (#815), `PL-W7WL` and `PL-WXX8` (both #817).

## Verification

- `ruff check`, `make docket` (0 errors) and `tools/doc_check.py check` (0 errors, 0 advisories) all green on the rebased branch.
- Before the restart, the merged state of `#820` plus the new `main` was tested in a scratch worktree, because `#818` had changed the same three recurrence-signal functions: 1,389 docket tests pass, `ruff check` clean, `mypy` clean over 77 files, `bin/docket check` 0 errors. The two changes are orthogonal — `#818` filters which items get named, this one filters which entries count.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Fxn32bJTvx1XY4HojVyyHS

---
_Generated by [Claude Code](https://claude.ai/code/session_01Fxn32bJTvx1XY4HojVyyHS)_
