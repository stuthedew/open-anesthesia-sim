---
pr: 547
commit: a0c34f0b6a5df0a2e99f60060b112be973635422
merged: 2026-09-13
items: PL-BLHV, PL-FDBK, PL-NBCS
subject: PL-NBCS, PL-BLHV, PL-FDBK: the three approved Gate 1 decisions, built (#547)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit a0c34f0b landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

The three decisions from #541 that you approved, implemented. No behaviour change anywhere: `docs/MODEL.md` gains two sections, `ROADMAP.md` one moved bullet and one convention, and `tools/doc_check.py` three new checks.

Gate 1 goes from 47 open to 37 (28 clearable here) — three of those are this branch.

## PL-NBCS — the exclusion moves, and checks hold it there

The live fix is one `ROADMAP.md` edit. `PL-B9PY` was named inside a `Required scope` bullet *in order to exclude it*, and that subsection is read in full, so the parser placed it as v0.4.0 scope. The `PL-WB0X` bullet's head already said "stages 1 and 2 only", so the excluding sentence is **removed** rather than reworded, and stage 3 is now a bullet under "Explicitly out of scope for v0.4.0" with the same reasoning and attribution.

- **Convention**, in `ROADMAP.md` § "Development rules for scientific milestones", beside the frozen-Goal rule it resembles.
- **Parser**: `MilestoneSection` gains `excluded_ids`. `scope_ids` is unchanged — placement does not care which structure placed an id, and only the scope subsection can contradict the exclusion heading, so the check reads `own_scope_ids`.
- **Checks** in `doc_check.py`: a hard error for an id under both of one milestone's scope headings (exact, no judgment), and an advisory for exclusion language in a scope bullet.

**Two corrections to the recorded decision, both from building it.** The advisory is *not* the marginal half — the error half cannot catch the original shape, since an exclusion written only inside a scope bullet leaves no contradiction to find, so the advisory is the sole guard against recurrence. And "excluded by v0.4.0" was wrong: `milestone_scope` reads no section at or below the anchor, and `PL-B9PY` is placed by Gate 1 regardless. Only `PL-6P9Y`'s *parsing* half was needed, which this does; `PL-6P9Y` keeps the placement half and is smaller for it.

**One defect found because the check was watched failing.** `_subsection_line` returned a 0-based index where `roadmap.py`'s convention is 1-based, so the bullet walk started on the heading and stopped on it — zero bullets, no advisory, a green run on a tree that had one. Caught only because the first rewording deliberately contained "out of scope".

## PL-BLHV — intended use, and Class C

The exclusion is drawn at the **patient's data, not their location**, per your answer. The section keeps the reasoning, because that is what stops a later reader simplifying it back: proximity forbids a resident running the simulator on a workstation during a case, which carries no hazard, and permits a run built from a real patient's weight, age and cardiac output at a desk, which is a prediction about that patient whatever the window is labelled. FDA's carve-out turns on not facilitating assessment of a *specific patient*, which picks the same boundary independently.

Class C uniformly, on the three arguments the withdrawn segmentation left behind.

**The brief's "IEC 62304 defaults an undocumented classification to Class C" claim is not written down** — it is not in the reachable sources. The section cites § 4.3's class definitions and Annex B.4.3's probability-set-to-1 rule instead, which is stronger anyway because it makes the class turn on severity alone. It also records that the standards are paywalled and were read from secondary summaries, rather than implying a reading of their text.

## PL-FDBK — the hazard table, and the check that keeps it honest

Six rows. The first five cite ten tests, every name verified to resolve before it was written.

The sixth says "partially mitigated" and says why: `README.md` has the global claim and a learner never opens it; the timeline has "Settings only — not a measurement." covering marks only; the standing notice is a *use* disclaimer. Nothing tells a reader the compartment readouts and chart traces are modelled. That is filed as `PL-2K1R`, with the timeline's phrasing named as the pattern.

`check_named_tests` is what lets the table carry test names at all — an unchecked right-hand column is decoration, and worse than none for looking authoritative. **Scoped to `docs/MODEL.md` on a count**: 161 test names are cited across the tree, 21 resolve to nothing, and all 21 are in `docs/items/`, where naming a test the work will add is correct. A tree-wide check would fail the queue for doing its job.

It is the cheap half of `PL-8LDF`, which keeps the eighteen-invariant annotation pass. `PL-8LDF` stays separate rather than merging: exactly one of these rows maps to an existing Required invariant, so a merged table's "test" column would mean implementation verification in some rows and presentation validation in others.

One defect fixed before landing: the check first declined on any repository whose `MODEL.md` names no test, which broke three existing tests and was a real defect — a decline claims a gap, and there is no gap until a name is cited.

`docs/ARCHITECTURE.md`'s enumeration of what `doc_check` validates was stale for both new checks; nothing catches that, since the map check only validates that the file exists.

## The merge (third commit)

`origin/main` was brought in after #544, #545 and #546 landed and left this branch `dirty`. One conflicted file, and it was a real duplication rather than a textual clash: this branch had added `MilestoneSection.required_scope_ids` for the contradiction check while `PL-KD98` added `own_scope_ids` on #544 to ask whether a milestone's own content is finished — the same tuple under two names, computed twice.

Resolved to **`own_scope_ids` alone**, since it was already on the base with its own consumers and renaming a landed field to suit a later branch is the more expensive half of the trade. Its docstring now records both readers so the next session does not re-split it; `excluded_ids` now uses main's `_deduped` helper rather than its own. Verified the parse is identical to pre-merge: v0.4.0 has 16 own-scope ids with `PL-B9PY` absent, and `PL-B9PY` in its `excluded_ids`.

## Checks

`make check` green on the merge: 2615 tests, mypy and ruff clean, docket 0 errors, doc-check 0 errors. Both new `verify:` commands were run and fail against the pre-change copy of `docs/MODEL.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XW1ii46qb6L7JEzrBwHTMo
