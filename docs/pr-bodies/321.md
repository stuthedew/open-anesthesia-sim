---
pr: 321
commit: 88f010838be07fc7c4dbbccf562256fbec6c57e3
merged: 2026-09-04
items: PL-YBWT
subject: PL-YBWT: call 8% sevoflurane common rather than standard (#321)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 88f01083 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-YBWT`. Follow-up to #319 (`PL-CC23`, the MAC-denominated chart axis).

## What changed

`PL-CC23` justified the off-scale notice — the line naming a compartment trace drawn above the top of the chart — by observing that sevoflurane's 8% dial is 4.00 ×MAC and so exceeds the fixed 3 ×MAC ceiling. It described 8% as **"the standard inhalational-induction setting"**, in five places: `docs/MODEL.md`, two source comments, a docstring, and a test docstring.

"Standard" asserts a practice norm. 8% is *common* — it is what makes the off-scale case real rather than contrived — but calling it the standard claims more than this repository's evidence supports, and none of the five statements carried a citation. Every other clinical number in that change traces to an agent file's own cited provenance, which is the bar `docs/MODEL.md` holds itself to.

Reworded to "a common inhalational-induction setting", and the surrounding framing from "a technique rather than an edge case" to "in common use rather than an edge case".

> Project owner, 2026-09-04: "I would describe 8% as common. Not necessarily standard."

## Why nothing else moved

The argument the sentence exists to make is unaffected — a setting in common use is already enough to make a clipped trace worth naming — so no assertion, threshold, or displayed value changed. This is a claim-strength correction in prose and comments only.

## Riding this commit

`bin/docket record` wrote `PL-CC23`'s `pr: 319`, which could not be known until #319 merged. The store's advisory asks for exactly this — that the write ride a commit a session is already making, rather than one composed for it.

The branch was restarted from the merged `main` (`b9c1179`) rather than pushed to again, per `CLAUDE.md`'s merged-branch rule.

## Checks

`make check` green: 1532 tests at 100% coverage, ruff, mypy, docket, doc check, contrast, import boundaries.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YBV6fEqTN4kP5xRKz7snRF

---
_Generated by [Claude Code](https://claude.ai/code/session_01YBV6fEqTN4kP5xRKz7snRF)_
