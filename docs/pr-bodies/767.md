---
pr: 767
commit: d218dfcbb7b15b01108bd574f75c41afbdf597a4
merged: 2026-09-20
items: PL-0RZ0
subject: PL-0RZ0: every compartment inside the hover radius answers, not the nearest one (#767)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit d218dfcb landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-0RZ0`. The rule `PL-JVHL` settled for the run, one axis over.

## What was measured first

`PL-0RZ0`'s own `Done when` asked for the contention measured before any rule was proposed. `PL-JVHL`'s geometry reproduced exactly — 60-minute axis at 900 x 360 px, so 4.00 s/px and 0.0167 %/px against a 6.00% axis top — every pixel of the plot scanned, and the scan's selection held against `nearest_trace_point` itself at 400 random pointer positions per case before any number was kept.

| Chart | Compartments | Two or more in reach | 2 px flips the compartment |
| --- | --- | ---: | ---: |
| one run | all six | 37.0% | 12.7% |
| two runs, vaporizer off | muscle + fat | 45.7% | 7.5% |
| two runs, 1.25 MAC | muscle + fat | 19.1% | 9.2% |
| two runs, 1.25 MAC | mixed venous + vessel rich | 29.4% | 6.6% |
| two runs, 2 MAC | alveolar + fat | 0.5% | 13.6% |

The two compartments print different numbers on 99.9% of flips, by a median 17.1-17.9x for muscle against fat. The box does not move when it happens: the two winning points are a median 0.9 px apart and 100% within 2 px, so only the words and the numbers change.

Three findings the item did not have. No compartment becomes unreachable — every one is answered for at 98%+ of the axis columns it is drawn at — so the sharper failure `docs/MODEL.md` already refuses does not apply. There is no aim to respect at 0.9 px, which is `PL-QYBW`'s own second consequence, so a rule that picked would be picking arbitrarily. And the answer-for-both rule the item ruled out is affordable: one value line on 21-93% of hovers, never more than four.

## The change

`nearest_trace_point` stops choosing: every `(run, compartment)` whose drawn point is inside the radius answers, at that trace's own nearest such point, in the frame's drawing order — compartment by `ChartFrame.visible`, run by `ChartFrame.runs`. `HoverTarget.quantity` became `quantities` and `HoverReading` gained one, so a reading carries the trace it belongs to rather than the box carrying one for all of them.

`format_compared_trace_hover` keeps two forms. Where the readings share a compartment the output is byte-identical to what it was — 54.3-99.5% of hovers. Where they span compartments each value line opens with its own compartment, gloss included, then its run while more than one is drawn:

```
Modelled sevoflurane
Muscle · Run 1 · 20m   0.19%   0.10 ×MAC
Muscle · Run 2 · 20m   0.09%   0.04 ×MAC
Fat · Run 1 · 20m   0.01%   0.01 ×MAC
Fat · Run 2 · 20m   0.01%  <0.01 ×MAC
```

## Two things the work turned up that the measurement had not

1. **Two compartments of one run do not share an instant.** A run draws all six traces at one set of times, but each keeps its own nearest point and nearest is measured in two dimensions, so the pointer's height decides which column a trace answers at where two are near-equidistant in time — muscle at 19m56s and fat at 20m for one run under one pointer. The per-line instant is therefore required rather than merely consistent. `PL-1K9G` carries whether the column should be chosen by time alone.
2. **The branch's alveolar trace contends with fat on the real case.** `tests/integration/test_qt_chart.py`'s branched case moved a pointer 2 px and the box gained an alveolar line: by 20 minutes the branch's vaporizer has been off for ten and its alveolar has washed down to within the radius of fat.

## Documentation

`docs/MODEL.md`: the rule and its measurements, the section heading — which named only runs — and a new hazard-table row for reading one compartment's concentration as another's, a hazard the run's row did not cover. `doc_check` caught the rename leaving a dangling citation in the item. Docstrings in `app/chart_frame.py` and `app/qt_chart.py`.

## What the audit still reports, and why two items came out of it

`bin/docket verify --self PL-0RZ0` ends `REJECT` on `no existing assertion removed - 6 line(s)`. The branch does not declare `falsifies:` to clear it, because that field is only worth anything when a reviewer writes it first. The net diff removes five assertion occurrences across four distinct lines:

- Four occurrences, three distinct lines, are the `HoverTarget.quantity` -> `quantities` rename this item's work required, each preserved as the equivalent assertion on the new field.
- One, `assert chart.readout_at(time_s, on_trunk + 2.0 * percent_per_pixel) == readout`, this item's work makes false — the box legitimately gains an alveolar line there. It is replaced by a full equality against `format_compared_trace_hover` over the three readings actually in reach, which is stronger than what it replaces because it states which traces answer rather than only that fat's lines did not change.

The sixth the check counts is not in the net diff at all, and chasing it produced two findings, filed together under `verify-assertion-evidence`:

- `PL-087W` — `verify_item` unions removed assertions across the commits naming an item and never nets them against a later restore. The sixth is `assert chart.readout_at(time_s, on_trunk - 2.0 * percent_per_pixel) == readout`, removed in f7d9afa and restored **verbatim** in ed360cd *because the first run of the check flagged it*. Doing what the check asks cannot clear the check.
- `PL-7NKD` — the evidence is `dropped[:5]` while the detail is `len(dropped)`, so that block prints `6 line(s)` above five of them and names neither the cap nor the line it omitted.

`make check` is green: 3,407 tests, 100% core coverage, `doc_check` 0 errors, `docket check` 0 errors.

## Also on this branch

- `PL-HS1V` — captures the contention scan itself, which `PL-MN4J`, `PL-JVHL` and this item have each rebuilt from scratch and discarded, and which `PL-QYBW`'s axis decision wants a fourth time.
- `PL-1K9G` — the two-dimensional column choice above.
- `bin/docket record` wrote six `pr` numbers the base was owed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01SsQZDA7cjruveSpQBJhUeL
