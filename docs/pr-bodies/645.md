---
pr: 645
commit: 16a0a4ad23ba3bf152d73ab76b3b2b08c178466c
merged: 2026-09-16
items: PL-C4RS, PL-D1RT, PL-KND7
subject: PL-D1RT, PL-KND7: take main green — close PL-D1RT on landed work, rewrite PL-C4RS's sentinel (#645)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 16a0a4ad landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

`main` has been red since `52205f6` (#634). `bin/docket check --verify` over the whole store — the exact command the push-to-`main` job runs — now reports **0 errors** on this branch.

## `PL-D1RT` — closed, because the work landed

Each clause of its `Done when.`, measured rather than argued:

| Clause | State |
| --- | --- |
| Neither `ROADMAP.md` nor `docs/WORKING_NOTES.md` cites a `v0.5.x — the interface pass` timeline row | **Satisfied** — 0 matches in both, since #634 |
| Item 33's placement note says it is **absorbed by the port** | **Superseded** — that absorption was reversed (project owner, on `PL-L9RD`), and `ROADMAP.md:308` records it |
| The notes thread says where the interface pass now runs | **Satisfied** — `docs/WORKING_NOTES.md:668` carries both orderings, dated |

`PL-PHKP` and `PL-L9RD` did the work. The second clause is why this looked like an open question: it asks the roadmap to say something a decision of record has since made false, so meeting it would mean undoing `PL-L9RD`. Applying a decision already on the record is ordinary work — nothing here rests on what the project is *for*. The `PL-037Y` half stays that item's, as `PL-D1RT`'s brief already said.

## #641 reached the same conclusion but did not act on it, so `main` is still red

Worth stating plainly, because #641's title — "`PL-D1RT`'s work landed, so closing it is the remedy" — reads as though this is already fixed. It settled `PL-32Z9`'s open half and wrote the conclusion into that brief. It changed no other file. On `main` today:

- `PL-D1RT` is still `status: ready`, with its absence sentinel intact.
- `PL-32Z9` is still `untriaged`.

This branch performs the close. #641 is merged in here with no conflict — it touched only `PL-32Z9`'s brief — and the whole-store check below was re-run **against the merged tree**, not the pre-merge one, since that is what `main`'s push job will actually run.

## `PL-C4RS` — command rewritten, item left open

`bin/docket check --verify` reports one error and stops, so closing `PL-D1RT` uncovered a second item behind it. Same shape, broken the same way: `! grep -q 'Nineteen items, in the order the dependencies allow'`, whose phrase was present at `d7a3b05` and gone by `4690412`.

But this one takes the error's **other** disposition. Its work is genuinely outstanding — three ids still need reconciling against `Required scope` — and its own brief records that *which side is wrong is a judgment on the prose*. Closing it would have decided that by accident.

## Both replacements pin presence, not absence, and both were run first

The defect in both is a sentinel asserting an **absence**: satisfiable by anyone who deletes the string, which is the inverse of the presence-pinning shape `.claude/skills/docket/SKILL.md` prescribes.

- `PL-D1RT`'s replacement greps for two sentences the work created. Run against `57fdb8c`, the last green commit: both greps exit 1.
- `PL-C4RS`'s names the sentence the correction must add, so it is a specification rather than a bet on a phrase surviving. It exits 1 today on the `grep` half with `doc_check` passing.

**The two took opposite dispositions from one defect** — one closed because its work had landed, one rewritten because its work had not. That is the evidence that the fault is the sentinel's shape rather than any item's state, and `PL-Y1W6` was a third instance before either. `PL-KND7` carries the measurement; `PL-879R` (`needs-decision`) is where the general rule is decided.

## Also here

`bin/docket record` wrote the five `pr` numbers the base was owed, riding this branch rather than a commit composed for it: `PL-554Q` #639, `PL-7SVX` #638, `PL-83LS` and `PL-R7XK` #640, `PL-MQH0` #637.

## Checks

`make check` exits 0. `bin/docket check --verify` on the merged tree: **0 errors**, 165 commands in 206s. CI green on every head this branch has had, most recently `898e82b`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Degshdqi4ySxQrfpVXepS2
