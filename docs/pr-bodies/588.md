---
pr: 588
commit: ae8fc7bc28e1b69948253209d552e766a69627db
merged: 2026-09-15
items: PL-005, PL-25KS, PL-2K1R, PL-2QMK, PL-3355, PL-GMM7, PL-JFYT, PL-Q4VH, PL-TG60, PL-THXF, PL-YCWZ, PL-YVHK
subject: PL-25KS, PL-2K1R, PL-3355, PL-TG60, PL-005, PL-YVHK, PL-YCWZ, PL-2QMK, PL-Q4VH, PL-THXF, PL-GMM7, PL-JFYT: port the dashboard to PySide6 (#588)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit ae8fc7bc landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

## What this does

Ports the dashboard from Flet to PySide6 (queue item PL-25KS, Required scope item 2 of the Qt port), closes ten items that either rode the port as carried fixes or waited only for the Flet chart to leave, and drops one (PL-JFYT) that named a Flet-only defect the port removes.

**Shape.** Four modules where there was one 4 246-line file:

- `app/dashboard_frame.py` states every string and every per-tick claim of the dashboard with no toolkit loaded (status word, notice precedence, the seven readouts and their glosses, the four setting controls and the integer-slider mapping, transport enablement, the accounting panel, the control-change list, the chart captions, the new-case question). 75 tests, no display.
- `app/qt_widgets.py` holds the leaf widgets that decide nothing: `MetricPanel`, `ReadoutRow`, `ParameterSlider`, `NoticeLabel`, `NewCaseDialog`, `inert_splitter`, `FlowLayout`, `initial_window_geometry`.
- `app/run_view.py` is `RunView`: one run's widgets, controller, handlers, refresh and halt, with `_apply_agent_color_scheme` the single writer of agent colour.
- `app/simulation_view.py` is `SimulationView`: what runs share, in nested inert splitters, driving `qt_chart` per render tick.

The Flet dashboard, its chart-series module and their two test files are deleted; `flet` and `flet_charts` are permitted in no module under `src/`. `spikes/`, the Flet dependencies and the theme's visual pass stay with PL-7SVX, PL-3SQT and PL-L9RD.

**Parity.** Every user-visible string, hedge, status word, notice, caption, legend label and precedence rule of the Flet build is reproduced verbatim (the reader inventories of the old build were diffed both ways against the new tree and a shown dashboard's `interface_strings()`), identical except for the enumerated carried fixes below. The sliders are integer-valued at the display resolution, so the value applied to the model is exactly the value printed beside the control.

**Carried fixes and riders closed here:** PL-2K1R (the interpretation line "Model outputs — not measurements." beside the readouts), PL-3355 (no readout value can wrap from its unit: each column reserves its widest value), PL-TG60 (the accounting panel prints one decimal of a litre, on a one-SD measurement of the exhaust total recorded in `docs/MODEL.md`), PL-005 (a sized, centred window), PL-YVHK item 5 (README says the hover exists), PL-YCWZ with PL-2QMK (`tests/integration/test_qt_rendering.py` renders the whole interface headless and writes a screenshot), PL-Q4VH and PL-THXF (fixed by construction in the Qt chart; closed as the Flet chart left), PL-GMM7 (the educational disclaimer's words are asserted, in the frame and on a shown dashboard).

**The checks learned the Qt spelling.** `tools/agent_identity_check.py` reads `setEnabled`/`setDisabled`/`setVisible`/`setHidden` beside the attribute form and counts method calls inside the writer; `tools/contrast_check.py`'s citations name the symbols that draw each colour now; `tools/import_boundary_check.py` confines Flet everywhere.

## How it was verified

- `make check` green: 2 900 tests, strict mypy, ruff, the four `app/` checks, `doc_check`, `docket check`.
- Every closed item's `verify:` command run in full and passing; PL-7SVX's re-pointed at the spike directory since its old form now passes before its work.
- Headless renders at 1366×768 and 1600×1000 inspected (four and seven readout columns; every string in place; the control mark on both plots; the accounting panel at one decimal).
- An adversarial review pass over the diff (presentation-safety parity, Qt correctness and cost, the checks and documentation); every confirmed finding fixed in the last commits.

## What this leaves

PL-L9RD (the theme and the visual pass), PL-3SQT (the dependency swap), PL-7SVX (`spikes/`), PL-C92D. PL-16ZC and PL-8PSW, which waited on the chart port, are `ready`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01QePkZej8QnuSXbqvcSUk8H
