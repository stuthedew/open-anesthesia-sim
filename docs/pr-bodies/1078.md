---
pr: 1078
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGN75r6q3PWxdWvG6YjG9Tnp)_

Before: the apparatus asks "which files did this change touch" at 19 call sites, and its readers split git's answer four ways: on whitespace (`vcs.filed_with_work`, so `src/a b.py` came back as the two paths `src/a` and `b.py`), with `str.splitlines` (eleven readers, which also break a path at U+2028, U+2029 or U+0085), on `\n` alone, and on NUL. Without `-z`, git also quoted a path holding a tab, newline, `"` or `\`, and `ls-files` quoted an untracked non-ASCII name that the diff beside it printed as written. `tools/doc_check.py` named a failed read `git -c` and let a path it could not decode escape as a traceback.

After: every read `vcs.changed_path_args` builds asks git for `-z`, the one form in which git quotes no path, and every reader takes its paths from the NUL-ended fields: `vcs.listed_paths` for a `--name-only` or `ls-files` listing, and the `--raw`, `--numstat` and `log` readers from their own NUL records. A path is named as written, whatever it holds, by every reader.

This is step 3 of PL-PVW2's split (one answer per repeated predicate), and it closes the step's four members: PL-NK1L, PL-PQ0R, PL-Y2L6 and PL-0T5X.

How: `changed_path_args` gains `-z` and drops `-c core.quotePath=false`, which `-z` makes moot. `vcs.untracked_path_args` builds the untracked read the same way. The readers in `vcs.py`, `claims.py`, `arming.py`, `verify.py` and `tools/doc_check.py` read NUL fields; the holdings log and `claims.work_under_record` drop the one `\n` git opens each commit's paths with (measured on git 2.43.0). `doc_check._git_output` names a failed read by `vcs.subcommand_of` and answers an undecodable path as `GitUnanswered`, as `vcs._run_git` already did. The test fakes that serve these reads now print git's `-z` shapes.

Tests: `test_a_path_listing_is_read_one_way` commits nine names (a space, U+2028, U+2029, U+0085, a non-ASCII letter, a tab, `"`, `\` and a newline) and reads them through `diff`, `show`, the `--raw` landing split, the `--numstat` tip comparison and `verify.changed_paths`; each member has its own test beside it.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01APavEpm9bMJTSCWHhuBt7u

---
_Generated by [Claude Code](https://claude.ai/code/session_01APavEpm9bMJTSCWHhuBt7u)_
