---
pr: 1080
recorded: 2026-09-26
---

This drops PL-YKXQ, the finding that a fresh container's local `main` had diverged into pre-rewrite history. It had been triaged `ready` for a session-start check, which is not built. The brief's own 2026-09-19 note set the condition: a second clean container would justify dropping it. This container is that one.

## What was measured, 2026-09-26

- `git reflog main` has one entry: `branch: Created from refs/remotes/origin/main` at 03:48:07 UTC. The session's branch was cut from a fresh fetch at 05:08:29.
- The clone was current when it was made. Its tip, `49aa759d`, merged at 03:45:00, and the next merge, `9bd852c0`, landed at 03:51:43.
- `git rev-list --count origin/main..main` returned 0, and `main..origin/main` returned 12. Local `main` is a clean ancestor, as it was on 2026-09-19.
- In a scratch worktree, `git checkout main` printed `Your branch is behind 'origin/main' by 13 commits, and can be fast-forwarded.`
- `vcs.default_base` tries `DEFAULT_BRANCHES = ("origin/main", "origin/master", "main", "master")` in that order. So no docket read uses the local branch when `origin/main` resolves.

## Why that settles it

A container is cloned before its session starts, so its local `main` is `origin/main` as of the clone. It falls behind by whatever merges while the container waits, and git reports that on checkout. It can diverge only if `main`'s history is rewritten during that wait.

The 2026-09-06 container's `main` sat at `9a10124` ("Merge pull request 102") while the remote was merging #399. That clone predated the one `git filter-repo` rewrite (PL-SHG5).

So the check would guard a condition that recurs only if `main` is rewritten a second time. `CLAUDE.md` builds new tooling only where the work recurs, so the check is not built. **Reopen** on a second rewrite of `main`, or on any container where `git rev-list --count origin/main..main` is not 0.

Candidate 1 is not added either: a line in `docs/worker.md` against `git checkout main`. The `docket` skill already sends a session to `git log origin/main` for what has merged, and git reports the lag at checkout.

## Checks

- `bin/docket verify --self PL-YKXQ`: `ACCEPT`, and `make check` passes. The item's `verify:` command is printed but not run, because a dropped item built nothing.
- `make docket`: 0 errors. Its 1 advisory was there before this branch.
- `bin/docket arm`: `arm`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013dCYPvDob8JNZWRFFoAw9H

---
_Generated by [Claude Code](https://claude.ai/code/session_013dCYPvDob8JNZWRFFoAw9H)_
