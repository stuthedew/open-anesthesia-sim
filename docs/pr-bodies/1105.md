---
pr: 1105
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGDo4ewced4tH9fWpttCtwEW)_

Before: five spellings of "where is a fenced block" disagreed across docket and `tools/doc_check.py`. doc_check read `PL-6SRZ`'s wrapped triple-backtick code span as an opening fence that nothing closed, so `make check` never read that open brief below it: a line citation past the end of `tools/doc_check.py` appended to the brief passed (reproduced at `dae71d76`). The same line in an instruction file, in `docs/MODEL.md` or in any markdown file would have hidden every date, provenance marker or math delimiter below it from the checks that read those.

After: one module, `subprojects/docket/src/docket/fences.py`, answers where a fenced block is by CommonMark's rules, and every reader in docket and doc_check takes its answer. Three or more backticks or tildes at any indentation open a block, a backtick opener's info string holds no backtick, and a bare run of the same character at least as long closes it. A fence nothing closes is not a fence: its lines are read as written rather than hiding the rest of the file, as decided at triage in the item.

How: `fences.py` holds `OPEN_RE` and `CLOSE_RE` (moved from `checks`), `Block`, `blocks`, `fenced_lines` and `without_fences`. The last blanks each fenced line to spaces and keeps the terminator `str.splitlines()` cut, so every offset and line number past it holds. `checks`'s brief sections and `instructions.parse` read through it. In doc_check, `FENCE_RE`, `INDENTED_FENCE_RE`, `ANY_FENCE_RE` and `_without_fences` are gone; `_fenced_blocks`, `check_prose_provenance`, `_without_code` and every `without_fences` caller use docket's reading, and `tools/possessive_section_check.py` reads `doc_check.without_fences`, re-exported for it. `_without_code` keeps its order, so an indented code block is still tested before a fence. `.claude/rules/citation-drift.md` names the new function.

Verification: `make check` passes locally, as part of `bin/docket verify --self PL-92MY`, which answers ACCEPT. `subprojects/docket/tests/test_fences.py` pins the reading in 11 cases. The six new regression tests in `tests/unit/test_doc_check.py` and `subprojects/docket/tests/test_instructions.py` all fail on `main`'s code and pass here. No other fence pattern is left in docket or `tools/`, apart from the finished one-off `subprojects/docket/tools/migrate_from_punch_list.py`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XQaiHBJS49JjBHVWHffEyn
