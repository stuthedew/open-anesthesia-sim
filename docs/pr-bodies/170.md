---
pr: 170
commit: 1c086dc823465c7da660651bc95242f013686d75
merged: 2026-09-01
items: PL-K6QR
subject: PL-K6QR record what this project is at the top of CLAUDE.md (#170)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 1c086dc8 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Adds a **What this project is** section to `CLAUDE.md`, between the routing paragraph and "Working with the project owner" — the project owner's words, unedited.

## Why it belongs resident

The rules that depend on this framing were already resident — "Two standards, deliberately unequal", "where the two compete for a session, the simulator wins", and the standing decision that workflow work outranks product work until the workflow is settled — but their premise was not. A session could apply all three correctly and still not know what the project is for, which shows up as misplaced effort rather than as a rule violation: "streamlined" read as a size target, scaffolding polished past sufficient, a release treated as something with a date attached.

It is framing rather than a rule, so none of `PL-H7XN`'s four routing dispositions fits it. There is no moment it fires and no file whose reading should trigger it — a session weighs the simulator against the workflow apparatus constantly and early, before it would have any occasion to open a skill, a path-scoped rule, or `docs/maintainer.md`. That is the resident case.

## On the growth advisory

This adds 9 resident lines (`CLAUDE.md` 381 → 390, total 512), so `check_resident_instructions` raises its advisory. That is the check working: it asks whoever added the rule to justify it or route it elsewhere, and the paragraph above is the answer. `make check` is green — the check is documented as "Reported, never thresholded" and `make check` does not fail on advisories. The advisory self-clears once this merges, because the baseline becomes the new `main`.

No threshold was added. The docstring records why one would be the wrong mechanism: *"a limit would be met by deleting a rule to reach a number, which is the one outcome the routing pass must not produce."*

## Checks

`make check` green: ruff format (329 files), ruff check, `mypy` (38 source files, no issues), 887 tests passed, `docket check` 0 errors, `doc_check` 0 errors.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018Zg7wRso5cPQcJjAN3EFii

---
_Generated by [Claude Code](https://claude.ai/code/session_018Zg7wRso5cPQcJjAN3EFii)_
