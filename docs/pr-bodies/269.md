---
pr: 269
commit: f666f02192eb3269b081671b4ac07f6866c57e78
merged: 2026-09-03
items: PL-9K7K, PL-P23D, PL-ZRSP
subject: PL-9K7K, PL-P23D: put PL-ZRSP's ordering in its front matter, and stop counting blocked work as a choice (#269)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit f666f021 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two items, the second found by fixing the first. No simulator change — `src/anesthesia_sim/` is untouched.

## `PL-9K7K` — the ranking contradicted a brief's safety note

`bin/docket next` ranked `PL-ZRSP` (plot the F_A/F_I ratio) first among the v0.4.0 items: `P1`, `S`, `ready`, in scope for the step the project is on. Its own **Safety notes.** said the opposite — *"The control-input timeline (`PL-DR1Z`) is what makes that visible, so land this after it"* — because F_A/F_I is the textbook wash-in curve only while inspired concentration is held constant, and a dial change mid-run reads as uptake to a learner who cannot see when the dial moved.

The ordering sat in prose the ranking cannot read, so every session asking what to do next was pointed at the one v0.4.0 item its own brief defers. It is now `status: blocked` / `blocked-by: PL-DR1Z`, with a paragraph saying the block is sequencing only — the shape `PL-VM40` already used for its own. Nothing in the brief changed meaning; the front matter now states what the prose already said, and it is one line to reverse if you would rather ship the trace early with the constant-F_I caveat carried by the label alone.

`PL-DR1Z` is raised `P2` → `P1`, because `docket check` refuses a `P1` that waits on a `P2` — correctly: whatever gates safety-classed work is that work's schedule.

## `PL-P23D` — which took the top band to thirteen, and nothing could be demoted

Twelve of the thirteen are pinned there by `safety`/`science`; the thirteenth was `PL-DR1Z`, a `feature, ux` item that arrived by dependency. So the size advisory would have fired in every session with no action available — the exact failure `CLAUDE.md` names, where an advisory nobody can act on trains a session to skim the output where a real one also appears.

The mis-measure is the count itself: `docket.toml` says the limit is about what a session "can choose between at a glance", and a blocked item is not a choice it can make. The size branch of `_check_band_shape` now counts startable items and names how many blocked ones it held back. Measured on this store: thirteen in the band, nine startable, four blocked — so the advisory correctly stops firing.

`docket triage` states the same rule back to a session and counted the band the other way, so it moved with the advisory ("holding 3 startable of the 12"). Two numbers for one rule is worse than either. `_top_band` itself is unchanged, so a band of only blocked items is still that band rather than falling through to the next one, and the two ratio advisories below the size branch are untouched.

The new test was watched failing against the unmodified checker before the fix went in.

## Also in this diff

`pr: 268` written onto `PL-B875` and `PL-C1KK` by `bin/docket record` — the first use of the bare form v0.3.3 shipped, discharging both advisories on a commit that was being made anyway.

## Verification

`make check` green: ruff format, ruff check, strict mypy, the `type: ignore` checker, 1211 tests under the 100% `core/` gate, `bin/docket check` (0 errors), `tools/doc_check.py` (0 errors), `tools/contrast_check.py` (0 errors).

Docs swept: `python3 tools/doc_check.py candidates --base origin/main` lists mentions of the word "checks" across `ROADMAP.md`, `docs/ARCHITECTURE.md`, `docs/MODEL.md`, `docs/worker.md` and `docs/WORKING_NOTES.md`, none of which describe this advisory. The one document that does — `subprojects/docket/README.md:1004`, "the tool will tell you the top band has grown past what anyone can choose between at a glance" — is still true, and truer than before.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01MshJxxtvhYZhuPSN3SA2DT

---
_Generated by [Claude Code](https://claude.ai/code/session_01MshJxxtvhYZhuPSN3SA2DT)_
