---
pr: 1090
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGLK58CdqjR2U1uncSWBJKom)_

Before: while another branch was cutting a release, the session-start digest said so, but `bin/docket status` still printed `Next version would be 0.5.12.` beneath it, and `bin/docket release --dry-run` with no version named stopped at `Name the version to cut it` without mentioning the cut, so the version it asked for was then refused. `status` also kept offering a release's tag step as work after the tag was pushed: `release-process`'s next item on `main` is PL-HZWB, "Tag v0.5.11 ...", although `refs/tags/v0.5.11` sits on v0.5.11's cut, `fe2046f7`.

After: `status`'s `Unreleased:` line says what the digest's `Releasable:` line says, in the same words - `A release is already being cut on claude/other-cut (v0.2.6); do not offer another until it merges.` in the tests - and an unnamed `release` prints the guard's parallel-cut warning in place of asking for a version. `status` marks a tag step whose tag the clone holds, `[TAGGED: v0.5.11 exists - close it]`, instead of offering it as work. PL-53Y6, PL-QHCW's last open member, is closed. PL-HZWB is left for whoever reads the mark to close.

How: `cli.cmd_status` reads the flight report first, since that is what fetches, then asks `cli._cuts`, the digest's read, wherever it would print an offer. `render._cut_elsewhere` is the digest's old inline sentence, now printed by both surfaces. The guard's read of the other refs and the release train moved unchanged into `cli._rival_cuts`, which the named cut and the unnamed path both call: a rival replaces the naming advice, and a read that could not answer keeps the advice and says so, since the named cut refuses on it. For the tag step I took the Done when's "reported done" route rather than a `verify:`: the replay reports an open item whose command passes as an error, and the tag is pushed outside any pull request, so a tag step carrying one would turn `main` red at every tag until somebody closed it. `release.asked_tag` reads the tag from the title all seven tag steps have carried (`Tag vX.Y.Z on the merge commit of ...`), and `cli._tagged` marks the row where `git tag --list` holds it. A step titled another way, or a tag not yet fetched, is offered as before, and whether the tag sits on its cut stays `tools/doc_check.py`'s gate. `release.md` now names that title. Five tests in `test_release.py` fail on `main` and pass here: `test_status_names_a_release_cut_in_flight` (the item's `verify:`), end-to-end `status` and unnamed-`release` reads against a second branch carrying `docs/releases/v0.2.6.md`, the tag mark on both rows `status` offers items from, and the title reading. Against the real queue with a local scratch cut, `status`, the digest and the unnamed dry run all named it; without it, the offer and `Name the version to cut it` came back unchanged. `subprojects/docket/README.md` names `status` and the unnamed `release` beside the digest and says what the tag mark reads; it waited for #1089, which also changed it, to merge. `bin/docket verify --self PL-53Y6` answers ACCEPT on `c5eea8c7`, `make check` included; the one commit after it records this body.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LAaJZ2TwWHnE3q938DrA6u
