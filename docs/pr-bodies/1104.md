---
pr: 1104
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGJ7SYmFAt4aJ1czHuGrXqZi)_

Before: a section mark after a code-spanned item id, such as `PL-MB2W` § "Design round, 2026-09-24", was read by no check. `check_citations` hands a mark after any code-spanned source to containment, and `check_quoted_sources` took only a `.md` document, so the seven such citations in the documents and docstrings could name a section their brief lacks and `make check` would still pass. Changing the one in `docs/resident-instructions.md` to words `PL-MB2W`'s brief does not hold left `python3 tools/doc_check.py check` at exit 0.

After: `check_quoted_sources` holds them to the item's file by containment, as it holds a document. A quotation the brief does not contain is an error naming the file, an id no file under `docs/items/` holds is an error, and an id two files carry is declined rather than guessed at. The same broken citation now fails with `quotes PL-MB2W as "A section no brief holds", which is not in docs/items/PL-MB2W-who-holds-an-item-is-derived-by-every-reader.md`. All seven resolve today, so nothing else in the tree changes.

How: a new pattern, `ITEM_SECTION_RE`, matches a code-spanned id followed by `§` and a quotation, over the same sources the document form is read in: the documents, the live briefs and the docstrings. The cited brief is read whether it is open or closed, since a citation of a closed brief still claims the words are there. Only the mark is read after an id. There `under "X"` names the heading an item is listed under elsewhere, which is what both such sites meant, and a quotation after a bare id is prose.

Verified: `make check` green locally (4,965 tests), and `bin/docket verify --self PL-QYN4` accepts. Two of the three new tests fail against the old `doc_check.py`; the third, which pins that `under` is not read, passes under both. The standing documents were swept for any statement of how such a citation is read, and none needed changing; only `doc_check.py`'s own comments did.

Filed while fixing it, not worked here:
- PL-RX0W: the possessive after an item id, as in `PL-CNJ1`'s "writes nothing", is read by nothing either. Of its eleven uses the one that misses is a faithful quotation across bold text, which the comparison does not fold, so reading it needs that changed first.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_013m7YLhsw4cweNpK2Zt5CrB

---
_Generated by [Claude Code](https://claude.ai/code/session_013m7YLhsw4cweNpK2Zt5CrB)_
