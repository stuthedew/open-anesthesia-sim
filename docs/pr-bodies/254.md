---
pr: 254
commit: 11e73c8c80aff694329354f96d9ecfec47aeeb1c
merged: 2026-09-02
items: PL-6194, PL-WTQ1, PL-X204
subject: PL-6194, PL-X204, PL-WTQ1: triage the three captures from the conservation-guard work (#254)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 11e73c8c landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Triage of the two items `PL-B7ZV` (#253) left behind, plus one found while doing it. Nothing under `src/` or `tests/` changes — this is queue work only. The queue is now at **0 untriaged, 0 errors**.

Both captures turned out to be wrong about their own facts, which is what the triage was worth.

## `PL-6194` — 40 redundant parentheses, not one · P3, `refactor`, `ready`

Filed as "a stray pair of parentheses" in one file. Measured during triage: **40 occurrences across seven files under `src/`**, plus six in `tests/`.

| File | Count |
| --- | --- |
| `app/controller.py` | 16 |
| `core/uptake_system.py` | 8 |
| `core/patient.py` | 8 |
| `app/simulation_view.py` | 5 |
| `core/agent_simulation_validation.py`, `core/circuit.py`, `core/parameters.py` | 1 each |

They date to `875ba08` (2026-08-22, the initial v0.1.0 build) and no convention anywhere records them as deliberate. The item is rewritten around the measurement rather than around the one line I happened to notice.

Two things checked so nobody has to check them again:

- **No linter catches it.** `ruff check --isolated --select ALL --preview` reports nothing for `f(alpha=(VALUE))` — `UP034` covers `print(("x"))`, not a keyword argument's value — and `ruff format --diff` is empty. Measured against a scratch file.
- **A `tools/` check for it was considered and rejected**, against `CLAUDE.md`'s own gate: it would fire once, on this sweep, then never again. The reasoning is recorded in the item so the question is not re-opened each time it surfaces.

## `PL-X204` — the decision is settled by the call sites · P2, `refactor`, `ready`

Filed as an open decision between two approaches. Reading the call sites settles it, and rules out both:

- `check_agent_accounting()` and `UptakeSystem.agent_simulation_validation` are read for display by `app/controller.py:222` and by **seventeen assertions** across `tests/unit`, `tests/reference` and `tests/integration`. The read-only half is load-bearing and must survive.
- `require_valid_agent_accounting()` has **one** production caller, `uptake_system.py:354`, which computes its argument one line earlier from the same validator with nothing mutating in between.

So the fix is to change the `require` half to take the stored amount rather than the result — it computes from `self`, raises on failure, returns the result on success, and `advance()` loses a line. A static method fixes the reading but leaves the mismatch permanently unreachable rather than impossible; comparing the result's totals against `self` pays three float comparisons per step to catch a caller error and conflates a programming mistake with a conservation failure. The item names all three and says why the first wins.

## `PL-WTQ1` — new, found writing `PL-6194`'s `verify:` · P3, `defect`, `ready`

`check_math_delimiters()` scans an item's YAML frontmatter, so a `verify:` command whose regex escapes a parenthesis is read as LaTeX and **hard-fails `make check`**. GitHub does not render frontmatter as prose, so there is nothing there to render wrongly. `PL-6194` currently works around it with `[(]`/`[)]`, which is a workaround living in the wrong place.

Filed `P3` rather than raised as compounding friction: it fails loudly, nobody is routing around it, and it can only reach a `verify:` command that escapes a parenthesis.

## On the `verify:` commands

Every one was run before being written down, and each exits 1 today for the right reason — the paired `pytest … && grep …` shape, never a bare `-k`.

`PL-6194`'s first draft ran `uv run pytest` whole. `docket check` reported that this would set a **92s floor on every `make check`** until the item closed, against a 1.1s median across the store. Narrowed to two targeted files, ~5s.

`make check` green: ruff format, ruff check, mypy, `ignore_check`, 1183 tests, `docket check` (0 errors, 0 untriaged), `doc_check` (0 errors), `contrast_check`. The one remaining advisory (`PL-011`, `PL-CC23` predating the `verify:` requirement) and the three contrast shortfalls are pre-existing and tracked.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01VkLpTJgAoFChi5YvkNgzfa

---
_Generated by [Claude Code](https://claude.ai/code/session_01VkLpTJgAoFChi5YvkNgzfa)_
