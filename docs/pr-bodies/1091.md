---
pr: 1091
recorded: 2026-09-26
---

<!-- ccr-projects-attribution: {"github_login":"stuthedew"} -->
_Requested by **Stuart** · [project thread](https://claude.ai/code/project/chan_016gyqLFSkoxNkMmpF4GveeG?thread=cmsg_016gyqLFSkoxNkMmpF4GveeGLK58CdqjR2U1uncSWBJKom)_

Before: #1015 and #1044 landed on `main` with empty squash bodies, so their reasoning existed only on GitHub, and `tools/pr_body_check.py` reported both as lost on every session start. `PL-WFFX`'s spent verdict still said none of the 47 squash merges since #918 lost its body, because sessions arm auto-merge with nothing set, and `docs/maintainer.md` told Stuart the same thing ("Auto-merge is safe when nothing sets its message"), although a session armed #1044 exactly that way.

After: `docs/pr-bodies/1015.md` and `docs/pr-bodies/1044.md` hold both bodies, fetched from GitHub today under the header PL-979D's build writes, and the lost-body line has nothing left to report. `PL-WFFX`'s verdict names #1015 and #1044 and hands the fact to `PL-979D`, which is spent because every body is now recorded in the tree before its merge. The maintainer note dates its 49 and names the two exceptions. PL-HZ0M, PL-979D's last open member, is closed.

How: `python3 tools/pr_body_check.py --recover` wrote the two files, and nothing in them was edited. `bin/docket set PL-WFFX --generator` rewrote the verdict, which stays `spent`: a second live head for one fact is what PL-HZ0M's generator check refused. PL-979D's `root-cause-of:` already listed PL-HZ0M, so that file is unchanged. The maintainer bullet was the one other place the docs sweep found the claim, and `docs/maintainer.md` joined the item's `touches` for it. Which step emptied #1044's body stays out of scope, as the item says. `bin/docket verify --self PL-HZ0M` answers ACCEPT on `12d7a68e`, `make check` included; the one commit after it records this pull request's number and body.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LAaJZ2TwWHnE3q938DrA6u

---
_Generated by [Claude Code](https://claude.ai/code/session_01LAaJZ2TwWHnE3q938DrA6u)_
