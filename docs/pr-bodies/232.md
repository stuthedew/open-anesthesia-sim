---
pr: 232
commit: 690ff48c27eca26a468081e913b4e8a72e664f5f
merged: 2026-09-02
items: PL-5WFS, PL-D1ST
subject: PL-5WFS, PL-D1ST: recover the capture #231's squash left behind, and record its pull request (#232)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 690ff48c landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Two small follow-ons to [#231](https://github.com/stuthedew/open-anesthesia-sim/pull/231), which merged 32 seconds after it was opened.

## PL-5WFS — recovered from the branch

The squash took #231's first two commits. The third, a capture pushed moments later, was left behind and existed only on `claude/0-4-0-release-overview-x39i9i` — `git branch -r --contains` named that branch and nothing else. This restarts the branch from the merged `main` and cherry-picks it back, per `CLAUDE.md`'s rule for a designated branch whose pull request has already merged: keep the unmerged commits, rebase them onto the new base.

The capture itself: inside the current step, `bin/docket next` ranks by priority band and by which feature is nearest finishing, and never reads a brief's body. So a stated prerequisite is invisible to it. v0.4.0 is the live instance — it offers PL-F52R (the MAC-awake band), PL-ZRSP (the F_A/F_I trace) and PL-DHV7 (MAC multiples as a display unit) in close to the reverse of the order those briefs state, while PL-WB0X, which goes first, is P2 and sits well down the list.

Filed untriaged rather than fixed, because the first question is whether one instance earns a mechanism at all. `status: blocked` with `blocked-by:` already exists and may be the entire fix with no code; the brief argues both sides and names `dropped` as an honest close.

## PL-D1ST — the pull request number

`docket check` reported it closed on `origin/main` with no `pr`, and recovered #231 from the merge commit. Written into the file so it carries the number too — the one-line follow-up the `docket` skill says to fold into the next branch rather than split into its own.

## Checks

`make check` green on this base: ruff format, ruff check, strict mypy (42 files), 1152 tests, `docket check` 0 errors, `doc_check` 0 errors, contrast check 0 errors. `bin/docket stranded` reports no item left on any branch ref this checkout holds.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_019TRvkWuYAx2D9xJVir1adE


---
_Generated by [Claude Code](https://claude.ai/code/session_019TRvkWuYAx2D9xJVir1adE)_
