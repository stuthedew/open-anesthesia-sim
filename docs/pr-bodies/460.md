---
pr: 460
commit: 8d46af84ed9449ede8786cd1c56b235c2120ce65
merged: 2026-09-07
items: PL-61WW
subject: PL-61WW: carry the running agent's identity in a control the theme cannot grey (#460)
---

<!-- Recovered by tools/pr_body_check.py. The squash commit 8d46af84 landed with an
empty message body; this is the pull request body it should have carried, verbatim.
PL-843V. -->

Closes `PL-61WW` (during a run, the agent name in the agent selector loses contrast against the agent colour) — Gate 1, P1, `safety`-classed, feature `presentation-safety`.

## The defect

`disabled` was the whole of what a run did to the agent selector. Flet/Material paints a disabled control's label in the theme's disabled-content grey, which overrides the `color=` and `text_style=` the agent scheme sets while leaving the saturated ISO 5360 fill behind it. Grey on purple, for the whole of every run — at the one moment every number beside it (percent, MAC multiple, alveolar and mixed-venous partial pressures) is agent-specific. `CLAUDE.md`'s presentation clause makes that a safety failure rather than a styling one: the correct number under an unreadable identity is the wrong-context failure it names.

## Which direction, and why not the other two

The item listed three candidates and chose none.

- **Keep the label at `scheme.foreground` when disabled** would have depended on Flet/Material honouring an override of its own disabled-content colour. The unit test would pin the attribute this project sets, not the colour the widget paints — a green test proving nothing about the screen, which is exactly the failure the brief describes `make check` already having.
- **Drop `disabled` for a read-only presentation** is a behaviour change rather than a presentation one: it would let a selection reach `_handle_agent_change` mid-run, and what happens to a running case when `set_agent` rebuilds around it is a design question this item did not ask.
- **Move the identity into a control that is never disabled** is what landed, and it is the one the item notes also answers the mode-awareness question.

## What changed

`_running_agent_display` is a static chip standing in the selector's place while a run is going. It carries `_running_agent_text` (the agent's name, bold) and `_running_agent_lock_text` ("Locked while running"), drawn in the agent's own `foreground` on its own `fill`, and bordered like `_agent_header_badge` — the border being what keeps the sevoflurane chip's shape against a page it is 1.27:1 against. `_refresh_view` swaps it with `_agent_dropdown` on `is_running`; the dropdown stays `disabled` as well as hidden, since an off-screen control must not be operable. Both take `AGENT_SELECTOR_WIDTH`, so the transport controls do not move when a run starts.

The caption says "running" rather than "this case" because that is what is true — the selector returns on Pause.

## The checker half, met differently from what the Done-when anticipated

The Done-when expected the disabled foreground to become an explicit constant in `app/theme.py` so `REQUIREMENTS` could reach it. Under this direction there is no disabled foreground to declare: the pair rendered during a run *is* `agent.foreground` on `agent.fill`, which the three agent entries already measure. Those three entries were rewritten instead — to name all three places the pair is drawn (`_subtitle_text`, `_agent_dropdown`, `_running_agent_display`) and to record why the entry had stopped describing what was on screen. `check_citations` resolves every symbol named, so the coverage claim is audited rather than asserted. Adding a constant for a colour nothing renders would have been a worse answer to the same requirement. The item records the substitution against its Done-when.

## The brief's side question

No other control disabled during a run carries information. `_start_button` is disabled while running, failed, or at the supported run length, and `_pause_button` while not running; both name an unavailable *action*, and neither is the only statement of the state it reflects — `_status_text` says which of the four states the run is in, in words.

## Verification

- The item's `verify:` command was run before the work: exit 1, with the file's 184 tests passing and the named test absent. It passes now.
- Six new tests in `tests/unit/test_simulation_view.py`, one of them parametrized over all three agents — every fill in `AGENT_COLOR_SCHEMES` is saturated, so this was never one palette's problem.
- `make check` green: 2242 tests, 100% coverage, contrast 22 of 23 with the one tracked `PL-W8DQ` shortfall unchanged, 0 documentation errors.

## Docs swept

`docs/MODEL.md`, `docs/ARCHITECTURE.md`, `.claude/rules/ui-color.md`, `README.md`, `ROADMAP.md` — all still read true. `app/theme.py`'s own contrast claim gets a note recording that it is about the pair *as rendered* and is not self-enforcing.

## Captured, not fixed here

- `PL-K8YM` — `docs/MODEL.md` does not record the chip beside the selector and header, nor "no identity-carrying control is ever disabled" among the bars this interface holds itself to above AA. Left for its own item because three sibling sessions are holding that file.
- `PL-97VB` — nothing stops a *future* identity-carrying control being disabled, which is the class this fixes one instance of. The decidable half looks like an AST check; which rule to enforce is a design decision.

## Gate

Gate 1 (frozen under v0.5.0, "the case you can branch"): 38 of 132 cleared, 94 open with this one closed.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Ab27bS5tiozpDgWygnuVSA

---
_Generated by [Claude Code](https://claude.ai/code/session_01Ab27bS5tiozpDgWygnuVSA)_
